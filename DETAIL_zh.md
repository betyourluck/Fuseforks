**日本語** | [English](DETAIL_en.md)

# Outcasts Fuseforks — 详情

[README](README.md) 介绍了“做什么应用，如何构建”。本文则介绍
**其内部是如何组装的** — 目录、并发模型、界面、
内置工具、LLM 连线层，以及各项运作论述。

设计决策本身以 [`data_contract.yaml`](data_contract.yaml)（领域契约）和
[`specs/`](specs)（规范）为准，本文则是这些内容的阅读入口。

---

## 目录结构

```text
Fuseforks/
├── Cargo.toml                       Cargo 工作空间（resolver 3 / edition 2024）
├── data_contract.yaml               领域名词台账（若修改类型请先修改此处）
├── failures.md                      遇到的陷阱台账（症状 → 真因 → 处方 → 归纳）
├── fuseforks_icon.png               应用图标的原始图像（通过以下步骤重新生成）
│
├── crates/
│   └── fuseforks-core/                  ★ 核心。完全不依赖 GUI 层
│       ├── src/
│       │   ├── lib.rs               公开 API 与依赖方向声明
│       │   ├── model.rs             领域名词（AgentId / AgentSpec / ModelTemplate …）
│       │   ├── error.rs             CoreError 以及传递给 UI 的 ErrorPayload
│       │   ├── event.rs             CoreEvent（通过 broadcast 推送状态变化）
│       │   ├── world.rs             注册簿。同步的纯数据结构（不持有锁）
│       │   ├── config_store.rs      SKILL.md / Memory.md / Construct.md / icon.webp / Ordinance.md 与 world.json 的输入输出
│       │   ├── orchestrator/        ★ 生命周期与消息分发（Tokio）
│       │   │   ├── mod.rs           类型·Shared·分发骨架（agent_loop / deliver）
│       │   │   ├── bootstrap.rs     启动与恢复（不自动启动）
│       │   │   ├── lifecycle.rs     村庄编排（Servant、职务、模板）
│       │   │   ├── runtime.rs       运行与入口（start / stop / 中断 / 发话）
│       │   │   ├── settings.rs      设置与资源的入口（上限、语言、称呼、MCP、条例）
│       │   │   ├── sessions.rs      会话切换（新建 / 重新打开 / 分支 / 摘要）
│       │   │   ├── schedules.rs     定时任务触发（ticker、前置判定、分发、后置判定）
│       │   │   ├── turn.rs          回合执行（步骤 1-8。**作为核心文件保留** — 见下方注释）
│       │   │   ├── delegation.rs    委托与转发（ask / plan / transfer 的提示与判定）
│       │   │   └── context.rs       加载进 Prompt 的上下文（广场日志·进出室）
│       │   ├── compute.rs           ★ CPU 密集型处理与 Tokio↔Rayon 的桥梁
│       │   ├── schedule.rs          定时任务类型与触发规则（纯函数。时间和时区均为参数）
│       │   ├── schedule_probe.rs    定时任务的前置/后置判定（纯函数。判定、附记、批准密钥、重新请求分支。Spec 28 / 46）
│       │   ├── process.rs           子进程的启动与等待（run 工具与前置判定共用）
│       │   ├── doc_index.rs         Markdown 标题索引（纯函数。PageIndex 的思路）
│       │   ├── room_log.rs          广场日志的纯机构（可见谓词 / ID 解析 / 显示 ID 扩展）
│       │   ├── attachment.rs        附件的验证·存放·GC（纯机构。类型通过 magic 判定）
│       │   ├── secret.rs            秘密的保管（OS 凭据存储 / 测试用的 in-memory）
│       │   ├── tool.rs              ★ AgentTool / ToolRegistry（MCP 的接入点）
│       │   ├── tool_calls.rs        工具调用的参数与输出环（仅限内存。不写入日志也不写入保存目标。[Spec 57](specs/57_tool-call-detail.md)）
│       │   ├── tools/memory.rs      内置工具: remember（追加到 Memory.md）
│       │   ├── tools/fs.rs          内置工具: grep / fd / diff（读取系。限定在工作目录内）
│       │   ├── tools/edit.rs        内置工具: sd / yq（修改系。preview 默认 + 必须 diff）
│       │   ├── tools/file.rs        内置工具: file（创建·移动·复制·回收站。新建仅在此处）
│       │   ├── tools/rag.rs         内置工具: rag（已声明资料目录的标题索引。只读）
│       │   ├── tools/blackboard.rs  内置工具: blackboard（黑板的便签。只能编写自己的便签）
│       │   └── llm/
│       │       ├── mod.rs           LlmBackend / BackendFactory / EchoBackend
│       │       ├── canonical.rs     提供商中立的类型
│       │   │   ├── wire.rs          提供商的原始 JSON 形式（唯一的真相）
│       │   │   ├── openai_compat.rs OpenAI 兼容 adapter（encode/decode 纯函数）
│       │   │   ├── anthropic.rs     Anthropic Messages API adapter
│       │   │   ├── gemini.rs        Gemini 原生 adapter（Google 搜索接地·URL context·思考阶段）
│       │   │   ├── xai_responses.rs xAI Responses adapter（Grok 的 Live Search）
│       │   │   ├── openai_responses.rs OpenAI Responses adapter（思考摘要·web 搜索）
│       │   │   ├── meta_responses.rs   Meta Responses adapter（web 搜索·4 种附件）
│       │   │   ├── perplexity_responses.rs Perplexity Responses adapter（4 种搜索·出处）
│       │   │   ├── responses_input.rs  2 个 Responses 共享的 input 列构建
│       │   │   ├── retry.rs         重试纯函数（分类·等待公式·Retry-After。Spec 52）
│       │   │   ├── client.rs        HTTP 核心（URL·标头·重试）
│       │   │   └── error.rs         LlmError（重试可行性的判断轴）
│       │   ├── tests/orchestrator.rs    集成测试（不需要网络）
│       │   └── tests/external_ask.rs    集成测试: 外部 LLM 的请求（Spec 25）
│
└── apps/
    └── gui-tauri/                   ★ 外壳。依赖 fuseforks-core
        ├── src-tauri/src/
        │   ├── lib.rs               窗口启动与 IPC 命令注册
        │   ├── state.rs             编排器组装 + 事件中继
        │   ├── commands.rs          IPC 命令（轻量转发层）
        │   ├── mcp_server.rs        接收外部 LLM 请求的大门（HTTP + 合同密钥。Spec 25）
        │   ├── jev_settings.rs      工具结果压缩的设定与评分器的装入（Spec 59）
        │   └── probe_approvals.rs   记录是否可在当前终端执行前置判定（Spec 28）
        └── src/
            ├── types.ts             Rust 类型的镜像（手动同步的契约）
            ├── lib/ipc.ts           带类型的 invoke 包装器
            ├── lib/attachment.ts    附件的纯函数（类型判定 / 缩放尺寸 / base64）
            ├── lib/carries.ts       哪个连线可以携带哪种类型（界面警告用的复制品）
            ├── lib/pathComplete.ts  输入框的 `@` 路径补全（触发检测 / 排序 / 确定）
            ├── lib/scheduleProbe.ts 前置判定的显示规则（纯函数。返回字典的键）
            ├── lib/scheduleDraft.ts 定时任务表单草稿 ⇄ 连线类型的往返（纯函数。编辑入口）
            ├── lib/contextUsage.ts  上下文使用率圆环的比例与颜色（纯函数。[Spec 49](specs/49_context-usage-ring.md)）
            ├── lib/agentGroups.ts   组的分类・可见集合・批量启动门・drop 确定（纯函数。[Spec 51](specs/51_agent-groups.md)）
            ├── lib/jevThreshold.ts  工具结果压缩的阈值与标签键（纯函数。值以 Rust 为准，由扫描测试比对）
            ├── workers/imageConvert.ts   图像 → WebP 转换的 WebWorker（不阻塞主线程）
            ├── assets/fonts/        内置字体（不向外部 CDN 获取）
            ├── locales/ja.json / en.json        UI 文案字典（通过测试保证键集合一致）
            ├── composables/useOrchestrator.ts   单一 Store
            ├── composables/useUiSettings.ts     本界面的设置（保存在终端中）
            ├── composables/useChatClear.ts      会话面板的显示清空（仅显示·按会话）
            ├── composables/useWaveClear.ts      工作情况标签页的显示清空（仅显示·仅已结束的波）
            ├── composables/useHiddenGroups.ts   隐藏组的集合（保存在终端中。[Spec 51](specs/51_agent-groups.md)）
            ├── composables/useToolCallDetails.ts 会话面板中打开的工具行内容（开闭与获取状态。不保存）
            ├── App.vue              3 分栏网格
            └── components/
                ├── AgentList.vue / AgentCard.vue      左: Agent 列表
                ├── TopologyMap.vue                    中上段: Servant 的羁绊
                ├── PlanWavePane.vue                   中下段: 工作情况标签页（plan 的执行痕迹）
                ├── BlackboardPane.vue / BottomPaneTabs.vue   中下段: 黑板标签页（共享工作便签的浏览与删除）
                ├── ChatPanel.vue / ChatInput.vue      右: 会话（气泡形式）
                ├── GroundingNote.vue                  发话附带的 Grounding 来源
                ├── AgentSettingsDialog.vue / MarkdownEditor.vue   模态框: 设置
                ├── ModelTemplateDialog.vue            模态框: 模型
                ├── BatchWorkDirDialog.vue             模态框: 批量切换工作目录（自列表页脚）
                ├── OrdinanceDialog.vue / McpDialog.vue / ScheduleDialog.vue   模态框: 条例 / MCP / 定时任务
                ├── SettingsDialog.vue / SessionDialog.vue    模态框: 系统设置 / 会话列表
                ├── FirstRunTour.vue                   初次启动引导（9 步。仅在空村庄出现。设置 ＞ 用户界面 ＞ 引导中可重新调用）
                ├── RoleDialog.vue                     模态框: 职务（Servant 的雏形）
                ├── GroupDialog.vue                    模态框: 创建·重命名·删除组（[Spec 51](specs/51_agent-groups.md)）
                ├── CommandApprovalDialog.vue          模态框: 命令批准（等待判断的 pending）
                ├── StatsView.vue                      全屏: 统计（将 3 分栏整体替换。Spec 39）
                ├── TitleBar.vue                       自定义标题栏（条例·职务·MCP·命令批准·定时任务·系统设置）
                ├── StatusBar.vue                      最下段: MCP 服务器监听・**跳过计划确认开关**・**统计入口**・日期与时间（与诊断日志格式相同）·版本号
                └── PaneSplitter.vue / ErrorBoundary.vue / ToastHost.vue / ConfirmHost.vue
```

> **`turn.rs` 作为核心文件被保留下来**（2026-08-11 使用者判断）。
> 分割时虽有 2,444 行，但内容已按步骤拆分为各个 `fn`
> （`handle_message` / `build_prompt` / `present_tools` / `run_turn` /
> `CallRunner` / `dispatch_outcome`），**认知负荷已通过函数分割大幅降低**。
> 若在此处继续分割，获得的只有行数的减少，失去的则是 `pub(super)` 的增多。
> **下次进行分割的依据不是行数** — 而是 `run_turn` 与 `CallRunner` 同时修改的次数超过 5 次，或者在代码审查时该文件内部的上下滚动变得频繁。
> 步骤以 `CLAUDE.md` 的“巨大文件分割的 6 条准则”为准。

## Crate 分离的保证

依赖是单向的。

```text
apps/gui-tauri  ──依存──▶  crates/fuseforks-core
```

`crates/fuseforks-core/Cargo.toml` 中不出现 `tauri`，便是这种分离的机械保证。
向 GUI 发送通知只需将 `CoreEvent` 传入 `broadcast` 通道，核心层并不知道接收者是 Tauri 还是测试代码。因此，**无需启动 GUI 即可验证所有路径**。

---

## 并行模型 — Rayon 与 Tokio 的分工

| 负责内容 | 运行时 | 理由 |
|---|---|---|
| Agent 执行·LLM 调用·分发 | **Tokio** | I/O 密集型。在等待期间不占用线程 |
| 日志的 Token 统计 | **Rayon** | CPU 密集型。利用 CPU 核心数进行并行计算 |

桥接由 `compute::spawn_rayon` 通过 `oneshot` 通道完成，双方互不阻塞。

> **切勿将 Agent 执行交由 Rayon 处理。** Rayon 的线程池大小固定为物理核心数，
> 若在其中等待网络，则 1 个 Agent 将占用 1 个线程，导致同时运行数被核心数封顶（8 核机器上第 9 个会卡住）。
> 满足“不阻塞 UI”的要求，通过这种分工方式也能完全达成。

---

## 画面结构

| 位置 | 内容 | 常驻理由 |
|---|---|---|
| 左 | Agent 列表（状态・运行时间・Token・启动）。头部是**创建侧**（模型注册・添加），**底部是统一处理侧**（批量切换工作目录）。在有组的村庄中**按标题分类**，标题旁排列着 眼睛（显示/隐藏）・铅笔（改名・删除）・▶/■・批量启动开关，从列表最下方的虚线区域添加组（[Spec 51](specs/51_agent-groups.md)。将卡片拖拽到其他分类中可改变所属） | 始终查看 |
| 中央上段 | Servant 的羁绊 | 始终查看 |
| 中央下段 | 标签页切换: **黑板**（浏览村庄共享的工作便签） / **工作情况**（plan 的执行痕迹。[Spec 08](specs/08_plan-wave-pane.md) 的波形面板） | 始终查看（可通过分隔条缩小至 80px 高度） |
| 右 | 会话（气泡形式）。输入框下方有**清除显示**按钮（**仅显示。会话保留**），其左侧是**上下文使用率圆环**（[Spec 49](specs/49_context-usage-ring.md)。所选个体**最近 1 次调用**的输入 ÷ 模板的**上下文长度**。75% 以上为黄，90% 以上为红。**在回合运行期间不动作，确定后 1 秒内跟进**。分母通过模型注册的“获取”按钮与单价一同获取[Spec 50](specs/50_context-length-fetch.md)。表格中没有的模型保持手动输入。即便如此出现超过 100% 的数字，说明模板的上下文长度小于实际窗口。重启后直到第一个回合才会出现） | 始终查看 |
| 最下段 | 状态栏（**MCP 服务监听**・**跳过计划确认开关**（[Spec 53](specs/53_unattended-plan-and-verifier.md)。开启期间发光并显示“计划：无确认”。本机会记住开关状态，下次启动时保持）・日期与时间・版本号） | 始终查看（33px 的带状区域） |
| 模态框 | Agent 设置 + 配置文件编辑（从卡片设置按钮打开） | **偶尔打开** |
| 模态框 | 模型模板管理（从 Agent 列表头部打开） | 偶尔打开 |
| 模态框 | 职务列表・添加・编辑・删除（从标题栏的“职务”打开。[Spec 14](specs/14_role-label.md)） | 偶尔打开 |
| 模态框 | 命令批准（从标题栏的“命令批准”打开。[Spec 20](specs/20_command-approval.md)） | 偶尔打开 |
| 模态框 | 定时任务列表・添加・**编辑**・删除・前置判定・后置判定与批准（从标题栏的“定时任务”打开。2 分栏: 左侧列表，右侧输入。2026-08-30） | 偶尔打开 |
| 模态框 | 系统设置（从标题栏的“系统设置”打开。[Spec 13](specs/13_settings-dialog.md)） | 偶尔打开 |
| 模态框 | 会话列表・分支・导出（从会话面板的“会话列表”打开。[Spec 12](specs/12_session-persistence.md)） | 偶尔打开 |
| **全屏** | **统计**（**通过最下段带状区域中时钟左侧的图表图标**与 3 分栏切换。[Spec 39](specs/39_stats-view.md)）— 查看本村支付的费用，按 会话 × **Servant × 模型** × 结束方式统计（**切换过模型的个体会拆分行** — 由于单价因模型而异，折叠后从外部计算金额时会出错）。**在缓存率悬停时显示输入明细**（缓存读取 / 写入 / 新建）（[Spec 40](specs/40_cache-write-accounting.md)。**不增加列** — 在 8 列的表中追加第 9 列会导致横向滚动常态化）。**在注册了单价的村庄中，顶部显示 `≈ $` 的 2 行**（[Spec 41](specs/41_model-pricing.md)。第一行为金额与单价时间点，第二行为覆盖率）。**覆盖率呈 2 轴**（体数与 Token 量），**仅单轴会失真** — 实测中出现过 `5/7 体・Token 99.9%`，若仅看体数则为“缺 3 成”，若仅看 Token 则会读作“已备齐”。**不使用模态框而是将 3 分栏整体替换**（3 分栏仅被隐藏，输入中的文段与选中项均保留）。标题栏和最下段保留，但在**查看期间标题栏的对话框入口被堵住**（窗口操作保留）。**“全部会话”显示按结账日划分的 1 个月内容**（[Spec 42](specs/42_stats-period.md)。结账日若为 25 则为“8 月份（7/26〜8/25）”，若为月末则为 1 日〜月末。通过 `◀ ▶` 可回溯前一月，`▶` 停在当前期间，`◀` 停在包含最早记录的月份。**点击“全部期间”则恢复为以往的村庄生涯累计**。按期间切割时，仅表中排列该期间内支付的会话） | 偶尔打开 |

不将设置放在常驻面板中，是因为**偶尔打开的内容会夺走总是需要查看的内容的面积**。

**按钮用名称指代，而非字形。** 本 README 曾有一段时间将按钮写作“从 ⚙”，
在将图标从 Emoji 替换为 SVG 的工作中，**只有描述被遗留了下来**。Emoji 依赖字体，
其字形随环境而变，且不继承 `currentColor`，因此无法跟随主题配色
（[Spec 13](specs/13_settings-dialog.md) rev3 D8 中规定“永久元素中不使用 Emoji”）。
此后，台账通过**按钮的标签与放置位置**来指代 — 即使外观改变，描述也不会腐化。

**界面上出现的称呼是“Servant”，领域词汇是“agent”**（2026-07-31）。
仅将 UI 文言向世界观靠拢，而型、字段、IPC 命令、事件名、
crate 名（`AgentId` / `AgentSpec` / `create_agent` / `fuseforks-core` …）以及本
README、`data_contract.yaml`、`failures.md` 的正文仍保留“agent”。
称呼随时可能改变，但若改变型名，则需要同时修改 Rust、TypeScript 和台账这 3 个系统 — **不要将易变之物与难变之物绑定在同一个词语中**。
规则以 [data_contract.yaml](data_contract.yaml) 的 `vocabulary` 为准。

**中央上段的“Servant 的羁绊”是关于谁能向谁说话的图**（2026-08-05 改名，
旧名“村庄地图”）。名称取自光荣特库摩《三國志13》的羁绊系统 —
与那个一样，**连线本身就是机能**。

---

## UI 同步规则

真相在核心侧，`useOrchestrator` 的 `state` 只是其投影。
为了防止投影偏移，仅设定了 2 项规则。

1. **核心通过 `CoreEvent::*` 进行广播。** 不由 GUI 去拉取。
2. **状态不持有双重来源。** 编辑中的草稿（例如表单输入中）保存在组件的本地 `ref` 中，一旦按下保存即发往核心，核心将其持久化后，通过事件返回并更新整体状态。不会出现“同时存在本地修改与后端状态”的状态。

## 附着（[Spec 23](specs/23_image-attachment.md) = 图片 / [Spec 36](specs/36_multimodal-attachments.md) = 音频、视频、PDF）

在输入栏中**粘贴**（Ctrl+V）或从左侧的剪辑图标中**选择**后，目标仆从便会查看并回答。拖放操作（Drag and Drop）并不在入口处提供 — 考虑到从桌面等地方拖入的意外性，以及从非标准应用拖入时无法捕获 MIME 类型的问题，采用通过剪贴板或对话框显式选择的方式。支持的格式为图片（PNG、JPEG、WebP、GIF）、音频（MP3、WAV、OGG、FLAC）、视频（MP4、WEBM、MOV）以及 PDF。文件上限为 **20 MB**。
- 若超过限制，前端会在发送前拦截并报错。
- 如果通过 API 直接绕过前端，核心层（Core）会在 `attachment.rs` 中进行检查并拒绝（返回 `AttachmentTooLarge` 错误）。
大小限制的依据并非“LLM 的上下文能装下多少”，而是**考虑了 base64 膨胀（4/3）以及 Prompt 留白后，能够安全通过各个 LLM 提供商代理服务器（Proxy）的上限（实测 20 MB 是边界）**。**不会针对不同的目标更改上限** — 如果同一个文件根据目标不同而时而通过时而失败，规则将无法从界面上读懂。

### 可搬运的组合

**哪种线缆能够承载哪种类型，由单一的谓词**（`Provider::carries`）来管理，并**只有发送入口会对其进行读取**。表格是通过实测确定的（不是根据 HTTP 200，而是根据模型是否真的“读了该文件并作出了回答”）：

| 提供商与协议 | 图片 | 音频 | 视频 | PDF | 理由 |
|---|---|---|---|---|---|
| Anthropic 官方 | 〇 | 〇 | 〇 | 〇 | 原生完整支持 |
| OpenAI 官方 | 〇 | ❌ | ❌ | 〇 | 不支持音视频（API 规格） |
| Gemini 互换 | 〇 | 〇 | 〇 | 〇 | Google 侧原生支持 |
| xAI (Grok) | 〇 | ❌ | ❌ | ❌ | 仅支持图片 |
| Perplexity | 〇 | ❌ | ❌ | ❌ | 仅支持图片 |
| xAI 响应 (Responses) | 〇 | ❌ | ❌ | ❌ | 仅支持图片 |
| Meta 响应 (Responses) | 〇 | 〇 | 〇 | 〇 | 4 种类型全支持 |

在不兼容的提供商处发送时，发送入口会在发送前进行拦截并报错（例如“该模型不支持音频”）。
后者的错误作为连接端的响应显示在界面上 — **如果在同一层级处理结构上不可能的事项与运行时的拒绝，将无法在界面上进行区分。**

### 只能传递 1 个回合

**附着的内容传递给模型，仅限于该次发话送达的这 1 个回合。** 从第 2 个回合开始，模型将无法看到它（**但界面上会保留**）。输入栏的注释中也写了同样的内容。
- 如果希望在后续回合中继续引用，发送方需要在文本中写明“请参考刚才的图片”。
- 如果通过 `ask` 或 `plan` 委派给其他仆从，**附着的内容不会被带过去**。
转接方只接收没有附着理由的纯文本 — **比起图片无法传递，更糟糕的是没有人知道为什么传不过去**，因此只保留了事实。

### 不使用的人不支付费用

**工具数量不会增加 1 个。** 附着不是工具，而是输入的一种类型，因此向模型提示的 Schema 和系统提示词都不会增加哪怕 1 个字。没有附着内容的发言，其线缆输出在此功能引入前后**按字节计算完全相同**（在**所有 7 条线缆**中测试均已固定）。
在一个从不使用附着的村庄里，发送的内容和支付的费用都不会发生改变。

### 无法传递的场所

| 路径 | 附着 | 采取的替代行为 |
|---|---|---|
| 委派（`ask` / `plan`） | ❌ | 只传递文本。发送方需在文本中进行说明 |
| 转发（`transfer`） | ❌ | 同上。防止未经许可的附件扩散 |

如果尝试在委派或转发中强行附加，系统会在入口处剥离附件，仅将文本及“（附件已被剥离）”的附言送达。
无法区分机制是在正常运作还是已经损坏。在实机中**不仅对诊断有效，对恢复也起到了作用** — 读过断开说明的发送方将图片内容转录为文本后进行了传递。

### 实体的存放位置与寿命

图片不会进入聊天日志，也不会进入聊天记录的保存位置（`sessions.redb`）。实体放置在 `{workspace}/attachments/{uuid}.{ext}` 中，发话只持有其引用。
- 如果应用重启，未被引用的文件将通过垃圾回收（GC）清理（启动时检查）。
消失的图片在聊天面板中会变成带有“已过保存期限被删除”边框的区域 — 如果什么都不显示，看起来就像压根没附着过一样。

### 各连接端的支持情况（实测 2026-08-06）

Anthropic 官方 / OpenAI 官方 / Gemini 互换 / xAI / さくら互換（Qwen3-VL）的 5 个系统向其投递 WebP 和 JPEG，**在全部 10 种组合中图片均被正确读取**。
在旧版中曾发生过“Gemini 能够读取，但 Claude 报错”的情况，因此通过测试进行了固定。

---

## 输入栏的路径补完（[Spec 24](specs/24_path-completion.md)）

在输入栏中输入 `@` 时，目标仆从的**工作文件夹文件**将作为候选项出现。
输入文件名的一部分进行筛选，选中后**其相对路径将被记入正文**。
**输入中只有路径，文件内容不会展开。** 仆从有需要时可以通过 `file` 工具进行读取 — 不读取则不支付任何 Token。

### 为什么不放入内容

**因为历史的生命周期不同。** 工具的结果**只存在于当前轮次**，但用户的发言会持续加载到滑动窗口 8 个往返的 Prompt 中。将内容放入发言中，会导致无用的文本在之后的每轮对话中被反复重新发送。如果保持路径形式，仆从通过 `fd` 或 `grep` 查找文件的周次将整体消失，**每增加一周所带来的整段 Prompt 重复发送**也会相应减少。

### 候选不包含的内容

扫描规则与 `fd` / `grep` 保持一致（不把同一实现拆分为两部分）：
- 以 `.` 开头的隐藏文件与文件夹（如 `.git`、`.env`）
- 二进制文件、图片、大文件（超过设定上限的文件）
- 位于工作文件夹外部的路径

此外 **`.gitignore` 并未被参考。** 上述清单仅仅是通过名称进行过滤，因此即使写在 `.gitignore` 中，只要不在该清单之外的文件夹也会出现在候选项中。

### `@` 不是文件专用的符号

将来如果增加对仆从的提及（来自使用者的同步广播），也将其加载到**相同的 `@` 中**。
目前虽然只能输出文件，但符号的用途并未限定于文件。

### Enter 的意义有 3 种

| 状况 | Enter |
|---|---|
| 输入中（候选项弹出时） | 选择候选项（不发送消息） |
| 输入中（候选项未弹出时） | 发送消息 |
| 表单 / 对话框中 | 提交 / 确认 |

---

## 会话的结束方式（2 层）

主要框架（OpenAI Agents SDK / AutoGen / LangGraph）都将对话的结束划分为**语义上的结束**和**机械上的上限**两个层级。不存在只有单层实现的情况。

### 层 1: 语义上的结束 — 模型决定

采用 OpenAI Agents SDK 的规则。**没有工具调用的文本输出即为最终输出**。
也就是说，如果模型在文本中回答了“完成了”，但同时调用了工具，则该轮次不会结束，而是会继续执行工具。只有在**完全没有工具调用**的情况下，才会被判定为会话结束。

### 并列委譲 — `plan`

“并行分发，等待全部完成，然后汇总”这一功能此前在工具箱中是不存在的。
其特征在于**“以相同的父任务为起点，将分支的子任务分发给多名仆从，并在所有任务结束后汇聚结果”**。
**并行的只是分发而非执行。** 各个智能体的收件箱只有 1 个，并且轮次是串行处理的，因此如果工作线程被其他工作占据，就需要等待相应的时间。

### 层 2: 機械的な上限 — 安全網

各发话带有 `hop`，当达到 `max_hops`（默认 8）时会中断连锁，并通过 `CoreEvent::HopLimitReached` 进行通知。这与 LangGraph 的 `recursion_limit` 相同，用于防止无限循环。
> 曾存在无法结束的手段。仅传达一句话的往返就消耗了约 1,200 个 Token。
> 详情与出处见 [failures.md](failures.md) #11。
### 工具执行循环

在 `turn.rs` 的循环（第 4 步到第 6 步）中，LLM 返回的工具调用（Tool Calls）将在内存中的 `tool_calls.rs` 环形缓冲区内执行。
不写入磁盘，也不计入会话日志（[Spec 57](specs/57_tool-call-detail.md)）。
- **重试的界限**：若工具返回错误，将把错误字符串作为 ToolMessage 原样返回给 LLM，由 LLM 判断是重试、修改参数还是放弃。核心层不擅自进行重试。
- **并行执行**：若 LLM 同时返回多个工具调用，则使用 Tokio 的 `join_all` 并行执行。由于 `grep`、`fd`、`diff` 仅为只读（Read），因此并行执行是安全的。至于 `sd` 和 `file`，虽然原则上按每个工具独立处理，但若对同一文件进行并发修改则会通过互斥锁（Mutex）进行串行化。

---

### 工具的理由（[Spec 27](specs/27_tool-call-reason.md)）

调用任何工具时，均必须附带 **“为什么调用该工具”的理由（Reason）**。
- 理由将作为一行文本显示在界面上的工具行上方。
- 若模型漏掉了理由，系统将视其为参数错误，并返回“缺少理由”的错误提示，令其重新调用。
- **其目的在于将思考过程可视化。** 若只看到“正在执行 grep”，人类将无法判断其意图；而通过附加“为了寻找配置文件的定义位置而执行 grep”，人类便能在中途拦截错误的方向。

---

### 工具行打开与合并（[Spec 57](specs/57_tool-call-detail.md)）

在会话面板中，工具调用的执行结果默认处于**折叠状态**。
- 点击可展开并查看输入与输出。
- 若在同一个回合中连续执行了多个相同的工具（例如连续 3 次 grep），则会自动折叠并合并为 1 行（例如显示为 `grep (3件)`），以防止长篇的工具日志将实质性的对话内容淹没。
### 工具结果的压缩（[Spec 59](specs/59_jev-tool-result-pruning.md)）

将 MCP 工具返回的长正文中、与本回合委托无关的段落，在交给仆从之前删去。
判定交由**只作判断的模型 Jev**（由 TypeSafe AI 提供，经 Cloudflare Workers AI 调用）。

- **预设为停用。** 需要使用者在「系统设定 ＞ 外部联动 ＞ Jev」填入自己的
  Cloudflare 帐号 ID 与 API 权杖；未设定的村庄，工具正文不会有 1 个位元组的变化。
- **对象由工具自行申报**（`AgentTool::prunable`，预设为否）。回传为真的只有
  **MCP 工具**；`file` 与 `run` 不在对象内（编辑的素材需要逐字原文）。
  **`rag` 已于 2026-09-22 依实机结果移出对象** —— 12,051 字只切成 2 个段落，
  两次都以「全部删去」倒回全文。`rag` 的输出以换行排列标题，**不会输出任何空行**，
  因此上述「以空行切分」在结构上无法生效。
- **正净减少未达 25% 时不压缩。** 分母为原正文，分子为**加入记号后的差**，
  而非删去的字数 —— 记号约 150 字，删得少的正文反而会变长。
- **保留的部分为逐字原文，只在段落的边界切开。** 删去之处会留下记号，
  并可用 `omitted` 以逐字原文重新读取（只在该回合内有效，仅存于内存）。
- **压缩只在结果返回的当下进行 1 次**，其后的回合不再变动 1 个位元组
  —— 提示词缓存的前缀比对是**位元组等同**，回合中途改动正文会使其后全部重新处理。
- **失败时原样通过全文。** 通信失败、超过 20 秒、回合被中断、或判定为全部删去时，
  都返回原本的正文。
- **阵列型以元素为单位删除**（[Spec 60](specs/60_json-array-pruning.md)）。顶层为物件、
  其直属子层有 JSON 表示达 1,000 字以上的阵列时，**把每个元素当作 1 个段落评分**，
  在原文上删去无关的元素。删去之事记于顶层的 `_pruned` 键；每个阵列各有自己的 `id`，
  `omitted` 的编号在该阵列内从 0 起算。全部元素都会被删的阵列不予变动（不产生空阵列）。
  顶层为阵列的 JSON、以及更深层的阵列不在对象内。
- **删除的强度有 4 段**（0.1 / 0.2 / 0.3 / 0.5，预设 0.2 由实测决定）。
  **错误删去的多半是委托中第 2 个以后的论点**，这一点无法靠阈值解决。

送往外部的内容以 [`PRIVACY.md`](PRIVACY.md) 的 4-4 为准。

### 命令执行（`run`）

`run` 允许在当前的工作文件夹中执行外部命令（例如 `cargo build` 或 `pytest`）。
- **默认执行超时时间为 60 秒**（可在系统设置中更改）。
- 输出被限制在 **16 KB**（超过部分将被截断）。
- **必须配合理由（reason）**：与 `grep` / `fd` 相同，必须说明“接下来为了确认什么”。
- **前判定（Spec 28 / 46）**：在实际执行命令之前，系统会检查该命令是否符合安全规则。如果属于高风险操作（例如修改系统设置或运行未知脚本），会触发前判定对话框（CommandApprovalDialog），等待用户或设定的条件批准。
- **环境隔离**：不会直接继承宿主机的全部环境变量，只传递必要的白名单变量。

### 见底索引（`rag`）

`rag` 是针对指定资料文件夹的** Markdown 见出し索引**工具（只读）。
- **目的**：当エージェント需要查阅大量说明文档或参考资料时，无需将整篇文件读入，而是通过 `rag` 获取文档结构和见出し索引，精准定位所需内容。
- **工作原理**：
  1. 读取指定文件夹下的所有 Markdown 文件（`.md`）。
  2. 提取所有一至三级标题（`#`、`##`、`###`）。
  3. 按照文件路径和标题层级生成结构化的索引树并返回给模型。
- **只读性质**：`rag` 纯粹是浏览和检索工具，不能修改任何文件。
- **使用规范**：在需要大范围调查外部或内置文档时，应优先使用 `rag` 缩小搜寻范围，再通过 `file` read 具体的段落，避免消耗过多的 Token。

### 提示的收窄（Token 成本是差异化轴）

为了在多次对话中控制 Token 消耗，系统对提示的范围进行了动态收窄：
- **历史滑动窗口**：聊天记录默认保留最近的若干个往返，超出部分被滚动淘汰，防止 Prompt 长度无限膨胀。
- **文件不随对话长期驻留**：通过工具读取的文件内容仅存在于当前轮次的上下文中，不会作为历史对话的一部分反复发送。
- **按需加载**：鼓励エージェント通过 `grep`、`fd`、`rag` 等工具按需寻找精确位置，而不是盲目要求全文阅读。

### 批量更改工作文件夹（[Spec 29](specs/29_batch-workdir.md)）

通过エージェント列表底部的集成フッター（Footer），可以一键切换或批量修改多个エージェント的工作文件夹路径。
- **应用场景**：当项目根目录发生变动，或者需要将多个サーヴァント同时切换到新的测试分支时使用。
- **规则**：批量更改会同时更新受影响エージェント的配置，确保所有子任务在统一的目录下执行。

### 写入系的安全性边界（`sd` / `yq`）

针对文件的修改类工具（`sd` 用于文本正则替换，`yq` 用于 YAML 结构化修改）：
- **默认预览（Preview）**：执行修改时，系统默认只计算并返回 diff 差分（预览），**不会直接写入文件**。
- **必须显式确认（apply: true）**：只有在确认 diff 内容无误后，再次调用并传入 `apply: true` 才会真正写入。
- **安全隔离**：操作范围严格限制在当前的工作文件夹（`D:\Github\Fuseforks`）之内，禁止越权访问或修改外部文件。

### 文件与文件夹操作（`file`）

`file` 是负责文件的全生命周期操作的唯一核心工具。
- **支持的操作（op）**：
  - `read`：读取文件全文。
  - `write`：创建新文件或覆写全文（**创建新文件的唯一途径**）。
  - `append`：在文件末尾追加内容。
  - `mkdir`：创建文件夹。
  - `move`：移动或重命名文件/文件夹。
  - `copy`：复制文件。
  - `remove`：将文件移至垃圾桶（安全删除，不彻底销毁）。
- **大文件长文本规范**：长文写作必须以 `write` 起步，再用 `append` 续写，避免单次输出超出上限。

### 收敛的前提: 履歴

为了确保多エージェント协作的最终收敛，系统的历史记录机制设计如下：
- **可追溯性**：所有的动作、工具调用、输入输出均被完整记录在案。
- **状态透明**：通过 `blackboard`、工作状态标签页（PlanWavePane）以及広場ログ，任何时刻都能清晰掌握各サーヴァント的进展。
- **无状态阻断**：当发生错误或超时（如 hop 超限、Token 预算超限、循环调用拒绝）时，系统会明确记录阻断原因并终止，防止死循环。
## LLM 线路层

采用 canonical ⇄ wire 的 adapter 分离架构。orchestrator 仅组合 canonical 类型，方言的差异全部由 adapter 承担。将新增提供商的工作收敛到 1 个 adapter 文件中。

### 提示词缓存

在 Anthropic 原生路径中，在 **2 处**设置边界 — **工具定义 + 系统提示词的末尾**以及**用户输入的前方**（最多 4 处断点，但实际使用 2 处以避免超出缓存上限）。
- 每次对话都会附加长篇累积的广场日志、System 提示词和工具定义，**如果不使用缓存，随着历史记录的增加，成本会呈二次方上升**。
- **缓存由后端判断。** 客户端仅发送带有缓存控制标记的 payload，是否命中缓存、是否计费由 Anthropic 的缓存机制决定。
- 价格和缓存TTL：写入时为通常价格的 1.25 倍，从缓存读取时为 0.1 倍（有效期 5 分钟，每次命中会刷新计时）。
- **缓存命中的表现（统计中）。** 如果未命中或过期，`cache_read` 为 0、`cache_write` 增加、`input` 增加。如果命中，`cache_read` 增加、`input` 仅包含未被缓存的部分。
- 如果切换了模型模板（例如从 Sonnet 换成 Haiku），**缓存会瞬间清零并重新开始计费**。这是由于不同模型间的缓存是不通用的。如果没有进行人为干预，设置未被修改的智能体不会无缘无故地转移到其他线路上。

### 通过 Google 搜索进行 grounding

在模型模板中勾选“Google Grounding”后，该模型将能够访问 Google 搜索。
- 伴随请求发送 `googleSearch` 工具，当模型判断需要时会自动执行搜索并引用结果。
- 在发言下方会显示带有图标的来历说明（GroundingNote）。若包含网页链接，点击后可在外部浏览器中打开。

### Gemini 的思考阶段与 URL context（[Spec 48](specs/48_gemini-thinking-level-and-url-context.md)）

**Gemini 现在也支持思考阶段（`effort`）了** —
- 在模型模板中可以配置思考量（`low` / `medium` / `high`）。当传入大语言模型时，会作为 `thinking_config` 发送。
- **URL context。** 如果在输入中包含了网页 URL，Gemini 能够直接读取该页面的内容。
- 工具响应（`toolResponse`）每轮都会输出 — 将以往无声丢弃的信息可视化。

### Grok 的 Live Search（[Spec 31](specs/31_grok-live-search.md)）

当在模型模板中将**协议设为“xAI 原生（Responses）”**时，可以使用 Grok 的网络搜索功能。
- 在模型模板中配置启用 `live_search`。
- 请求体会附加 `search_parameters`，响应中会返回搜索出的引用来源。

### OpenAI 的 Responses ワイヤ（[Spec 34](specs/34_openai-responses.md)）

当在模型模板中将**协议设为“OpenAI 原生（Responses）”**时，可以使用带有网络搜索和思考摘要功能的 Responses 路径。
- 能够处理带有推理摘要（`reasoning`）的输出。

### Perplexity 的ツール（[Spec 45](specs/45_perplexity-tools.md)）

当在模型模板中将**协议设为“Perplexity（Responses）”**时，可以使用 Perplexity 的 4 种搜索工具及引用来源。

### API 密钥的存放位置

API 密钥保存在 **OS 的凭据存储**中（Windows 凭据管理器 / macOS 钥匙串 / freedesktop Secret Service）。`ModelTemplate` 仅持有密钥的“存在与否”这一布尔状态，实际的字符串绝不写入配置文件（`world.json` 等），也绝不保存在日志中。
- 如果凭据存储不可用（或者在某些 Linux 运行环境中缺少 Secret Service 服务），则回退到内存保持，并在启动日志中输出警告。
- 密钥的更新通过模态对话框完成。输入后的密钥会直接写入 OS 凭据存储，配置管理中只更新其引用。

## 运营

### 重试等待（Spec 52）

LLM 出现 `429`（RateLimit）或 `5xx` 时，采用指数退避加抖动重试。初始等待 1 秒，每次翻倍，最高 60 秒，并附加 0%〜10% 的随机抖动（jitter）。当遇到带有 `Retry-After` 头的响应时，尊重该响应头。若等待时间超过 60 秒，则直接中断，绝不盲目顺从，并在响应正文中写明“提供商要求在 N 秒后重试”并停下。

### 首次启动

初次在一个空目录下启动时，会出现引导（FirstRunTour，共 9 步），指导用户完成基本配置。之后如果需要再次查看，可以通过菜单中的“设置 ＞ 用户インターフェース ＞ 案内”重新呼出。

### 工作空间

每个エージェント都有各自的工作目录。可以通过底栏或菜单进行一键切换，确保批量操作时不会错乱。文件读写、编译、测试等操作均严格受限在当前工作区内。

### 应用图标

应用图标源自 `fuseforks_icon.png`。如需重新生成，可按照既定脚本和规程对图标源文件进行处理。

### 诊断日志

应用会记录详细的诊断日志，格式统一规范。可以通过界面或后台日志文件进行排查，便于追踪错误、超时及调用链异常。

### 按エージェント配置的 MCP

每个エージェント可以独立配置专属的 Model Context Protocol (MCP) 服务端或工具，实现不同サーヴァント各司其职、各用其工具。

### 远程 MCP（Streamable HTTP）

支持通过 Streamable HTTP 方式连接远程 MCP 服务器，使エージェント能够调用外部托管的工具和数据源。

### 村庄条例

村庄条例（Ordinance）是整个村庄的共同约束规则，所有エージェント在执行任务时均需遵守。条例存放在专属文件中，可在对应的管理界面进行查阅和更新。

### 职务（角色）

职务（Role）是サーヴァント的雏形模板。通过预设的职务，可以快速创建具备特定职责、性格或工具集的新エージェント。可在标题栏的“役職”中进行增删改查（[Spec 14](specs/14_role-label.md)）。

### 群组（Group）

群组（Group）用于对エージェント进行分类管理、可視集合控制、一括启动以及批量 drop 确定（[Spec 51](specs/51_agent-groups.md)）。在エージェント列表的头部或折叠区可以对群组进行新建、改名和删除。

### 计划（定时 / 周期执行）

支持对エージェント设定定时或周期执行任务（Schedule）。通过专门的予定管理界面（标题栏的“予定”），可进行予定的一覧、追加、**編集**、削除、前判定、後判定及审批。

### 系统设置

系统的全局参数配置（如系统语言、默认模型、全局天井等）可通过标题栏的“システム設定”进行统一管理（[Spec 13](specs/13_settings-dialog.md)）。

### 接收外部 LLM 的请求（[Spec 25](specs/25_mcp-server.md)）

Fuseforks 提供了一个内建的 HTTP 服务器网关（配合合键认证），允许外部的 LLM 或客户端通过标准接口向本村のエージェント发送协作依頼，实现跨系统、跨实例的自动化联动。

## 有意未实现的部分

作为骨架只准备了连接点，未放入内容的红线部分。**由于没有伪装成正在运行**，因此可以直接作为下一个工作单元着手。

### 保留：来自用户的同播（向多个智能体一触即发）

曾实现过一次，但**从 UI 中移除了**（[failures.md](failures.md) #25）。由于同播会并行运行所有人的轮次，谁也不会看到其他人的答案就进行响应。试图掌控全局的个体只会陷入“已经有答案了”的无限循环中，因此在架构上是不成立的。

| 变量商店（Airflow 的 Variables/XCom 相当・利用者需求 2026-07-30） | 有 2 个替代方案正在运行：`Memory.md`（散文・按智能体・持久化）与汇总文本（波间传递） | 带有键值的结构化数值。**与“设置较少”的概念存在张力**，因此起票时将其作为审阅的主战场 |
| 会话持久化与会话管理（利用者需求 2026-07-30 / 2026-08-02） | **已实现**（[Spec 12](specs/12_session-persistence.md) 完成）。关闭并重新打开后之前的对话会恢复，智能体会根据之前的话进行回答。从对话面板的“对话列表”中重新打开・分支（返回发出该委托之前，文案会返回到输入框中）・导出・删除。“新聊天”不会丢弃，而是打开一个新对话。“摘要并继续”使后续的 prompt 保持轻量。 |

### 长期记忆方针（已决定・等待连接）

短期记忆（对话上下文）与长期记忆作为**不同的层**来处理。绝不混淆。
- **短期记忆**：对话的上下文（由上下文窗口、摘要、波间传递维护）。
- **长期记忆**：通过 `remember` 写入 `agents/{id}/Memory.md` 的散文。
- **不使用的方向**：将所有历史记录向量化后塞入 RAG 数据库的模式。原因在于它会夺走智能体“自己决定该记住什么”的认知，并让原因变得不透明。
