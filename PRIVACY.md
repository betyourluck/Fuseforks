# プライバシーポリシー — Outcasts Fuseforks

**最終更新: 2026-08-11**

English version: [PRIVACY_en.md](PRIVACY_en.md)

---

## 要旨

**Outcasts Fuseforks（以下「本アプリ」）の開発者は、利用者の情報を一切収集しません。**

- 本アプリは**開発者のサーバーを持ちません**。ただし**単価表の取得先だけは例外**で、
  既定では開発者が用意した GitHub 上の静的ファイルを指します（下記）
- **利用状況の計測・解析・クラッシュ報告を行いません**。そのための仕組みを組み込んでいません
- **アカウント登録はありません**
- **自動更新の確認を行いません**（起動時に外部へ問い合わせません）
- 作成したデータは**すべて利用者の端末内**に保存されます

本アプリが外部へ通信するのは、**利用者が自分で設定した接続先**と、
**単価表の取得先**の 2 つだけです。

**単価表の取得について**（モデルごとの料金を画面に入れるための機能）:

- **利用者が「取得」ボタンを押したときだけ**通信します。
  **起動時・画面を開いたとき・定期的な確認は行いません**
- 既定の取得先は**開発者が用意した GitHub 上の静的ファイル**です。
  **接続先はシステム設定の画面から変更・空にできます**。空にすると
  この通信は一切行われません（単価は手入力でも設定できます）
- 送るのは **HTTP GET のみ**で、**村の情報・会話・モデル名を含みません**。
  取得先には「このファイルが要求された」という事実（および通信元の IP アドレス）
  だけが、その配信元（GitHub）のログに残ります

---

## 1. 開発者が受け取る情報

**ありません。**

本アプリは利用者の端末上だけで動作します。開発者は、利用者が本アプリを
使用しているかどうかを含め、いかなる情報も受け取りません。

---

## 2. 端末に保存される情報

本アプリは以下を利用者の端末（アプリケーションデータ領域）に保存します。
これらが外部へ送信されることはありません。

### ワークスペース

| 保存先 | 内容 |
|---|---|
| `world.json` | エージェントの定義、モデル接続の設定（**認証情報を除く**）、役職、画面の配置 |
| `sessions.redb` | 会話の履歴 |
| `Ordinance.md` | 利用者が書いた共通の規則 |
| `schedules.json` | 時刻で発火する依頼の設定 |
| `mcp.json` | 接続する MCP サーバーの宣言 |
| `agents/<id>/` | エージェントごとの人格・記憶・アイコン・実行を許可したコマンドの一覧 |
| `user/icon.webp`、`external/icon.webp` | 利用者が設定したアイコン（設定した場合のみ） |
| `attachments/*.webp` | 会話に添付した画像（**30 日または合計 500MB で自動削除**） |
| `exports/*.jsonl` | 利用者が明示的に書き出した会話 |
| `village_id` | この設定一式を識別するための乱数（端末や利用者を識別するものではありません） |
| `fuseforks.log` | 診断ログ（下記 6 を参照） |

### 端末ごとの設定（ワークスペースの外）

| 保存先 | 内容 |
|---|---|
| `mcp_server.json` | 後述するローカル受付機能の有効/無効・ポート番号・アクセストークン |
| `probe_approvals.json` | 予定の前判定コマンドをこの端末で実行してよいかの記録 |

---

## 3. 認証情報（API キー）の扱い

**API キーは設定ファイルに保存しません。**

利用者が入力した API キーは、**OS の資格情報ストア**に保存されます。

- Windows: 資格情報マネージャー
- macOS: キーチェーン
- Linux: freedesktop Secret Service

保存に使うサービス名は `jp.outcasts.fuseforks` です。

本アプリの設定ファイル（`world.json` など）は、**キーそのものを保持できる欄を
持っていません**。設定ファイルは平文で保存されるため、秘密を書ける場所を
構造から取り除いてあります。

診断ログにも認証情報は書き出しません。

---

## 4. 外部へ送信される情報

本アプリは、**利用者が設定した接続先に対してのみ**通信します。
送信先・送信内容の決定権は利用者にあります。

### 4-1. AI モデルの提供事業者

利用者がモデルを登録すると、その提供事業者へ以下が送信されます。

- 利用者が入力した依頼文
- 会話の履歴（直近の一定量）
- 利用者が書いた規則・人格・記憶の文章
- エージェントが実行したツールの結果（ファイルの内容や検索結果を含みます）
- 会話に添付した画像
- 認証のための API キー

送信先は利用者が登録した接続先で、たとえば Anthropic、OpenAI、Google、xAI、
またはそれらと互換のある任意のサーバー（利用者が自分で立てたものを含む）です。

**送信された情報の取り扱いは、各提供事業者のプライバシーポリシーに従います。**
本アプリの開発者はこれらの事業者と利用者の間の通信を仲介せず、内容を保持しません。

### 4-2. 検索による裏付け（グラウンディング）

利用者が該当する機能を有効にした場合、モデルの提供事業者が代行して web 検索や
X（旧 Twitter）の検索を行います。**検索語は提供事業者へ送信されます。**
この機能は既定では無効で、モデルごとに利用者が有効にする必要があります。

### 4-3. MCP サーバー

利用者が MCP サーバーを宣言した場合、本アプリはそのサーバーへ接続し、
エージェントが呼び出したツールの引数を送信します。接続先とその挙動は
利用者が選んだサーバーに依存します。

リモートの MCP サーバー（`"type": "http"`）を宣言した場合、その接続先へ
**利用者が設定したヘッダーが毎回のリクエストとともに送信されます**
（アクセストークンを含む Authorization ヘッダーを含みます）。ヘッダーは
平文の `mcp.json` に保存されるため、ワークスペースを配布するとトークンも
一緒に配られる点に注意してください。

---

## 5. 端末上で実行される操作

本アプリのエージェントは、利用者が許可した範囲で端末上の操作を行います。

- **ファイルの読み書き**は、利用者がエージェントごとに指定した
  **作業フォルダの中に限られます**（およびその外を読み取り専用で参照するよう
  利用者が明示的に宣言したフォルダ）
- **ファイルの削除はごみ箱への移動のみ**で、完全削除の経路はありません
- **コマンドの実行**は、利用者がエージェントごとに書いた**許可一覧に
  一致するものだけ**が走ります。許可されていない要求は実行されず、
  承認待ちとして記録されるだけです。既定では 1 件も許可されていません

---

## 6. 診断ログ

`fuseforks.log` には、動作の観測記録（ターンの開始と終了、使用トークン数、
呼び出したツール名と結果の文字数、エラーなど）が記録されます。

**記録しないもの:**

- プロンプトの本文
- ツール結果の本文
- 認証情報

ログは利用者の端末内にのみ保存され、8MB を超えると 1 世代だけ退避されます。
外部へ送信されることはありません。

---

## 7. ローカル受付機能（MCP サーバー）

本アプリは、同じ端末上の他のプログラムから依頼を受け取る機能を持ちます。

- **既定では無効**です
- 有効にした場合も **`127.0.0.1`（自分の端末）にのみ**待ち受けます。
  外部のネットワークからは接続できません
- アクセストークンによる認証が**必須**です
- 有効になっている間は、画面下部のステータスバーに表示されます

この機能の設定（有効/無効・ポート・トークン）は**ワークスペースの外**に
保存されるため、設定一式を他人へ配布しても受付機能は有効になりません。

---

## 8. 利用者による管理

- **データの削除**: ワークスペースのフォルダを削除すると、会話・設定・添付画像を
  含むすべてのデータが消えます
- **認証情報の削除**: OS の資格情報ストアから該当の項目を削除するか、
  本アプリのモデル登録画面から削除できます
- **送信の停止**: モデルを登録しなければ、外部への送信は発生しません
- **設定の持ち出し**: 会話は利用者の操作で JSONL 形式へ書き出せます

---

## 9. 子どもの利用について

本アプリは子どもを対象としていません。開発者は情報を収集しないため、
子どもから情報を収集することもありません。

---

## 10. 本ポリシーの変更

本ポリシーを変更する場合は、このファイルを更新し、最終更新日を改めます。
変更履歴はリポジトリのコミット履歴から確認できます。

---

## 11. 連絡先

本アプリのプライバシーに関するお問い合わせは、以下からお願いします。

- GitHub Issues: https://github.com/betyourluck/Fuseforks/issues

---

## 補足: 開発者が情報を持たない理由

本アプリは、利用者が自分の API キーで自分の選んだモデルへ直接つなぐ道具です。
開発者はその経路に一切関与していません。これは方針であると同時に**構造**で、
本アプリには開発者へ情報を送るための接続先が実装されていません。
