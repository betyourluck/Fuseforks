# CLAUDE.md

正式名は **Outcasts Fuseforks**（2026-08-09 改名。旧 Outcasts Concordia →
[Spec 30](specs/30_rename.md)）。アプリ内表示・System の送り手名も
「Fuseforks」。狙いは**エンジニアのホビー + Outcasts への導線** —
核（fuseforks-core / 契約 / 発火規則）は業務品質、殻（村・キャラ）はホビーの体験、
両方を中途半端にやるのが唯一の失敗形（README「思想」の節が正）。

## 台帳の構成（真実の記録先）

**README.md と CLAUDE.md は用途が違う**（2026-07-31 利用者指示）。README.md は
**アプリの紹介ドキュメント**で、読み手は利用者・見に来た人。CLAUDE.md は
**私と開発者のためのメモ**で、読み手は作業する側。測定値・実装手順・次の一手の
優先順位は memo 側に書き、README には「何が出来て、どこに穴があるか」を
読み手の言葉で書く。迷ったら「これは読み物か、作業の材料か」で分ける。

- **入口**（何をするアプリか / ビルド / 技術スタック / ライセンス）:
  [README.md](README.md) — **入口として 160 行以内に保つ**（2026-08-09 の分割時は
  日英とも 148 行。実測 2026-08-10 で 156 / 155 行）。**上限で書くのは、正確な
  行数が編集のたびに腐るから** — 守りたいのは「新しく来た人が最初に探すものが
  奥に埋まっていない」ことであって、特定の数字ではない（#67 の処方を数へ適用）。
  **2026-08-29 に「インストール」節（winget / brew / Releases の表）を 3 言語へ
  足して 164 / 165 / 160 行** — 数行超えたが、この節こそ新しく来た人が最初に
  探すものなので、上限の意図の側に沿う（次に削るなら別の節から）
- **全体像・設計判断**: [DETAIL.md](DETAIL.md)（ディレクトリ・並行モデル・画面・
  同梱ツール・ワイヤ層・運用）
- ドメイン契約（実装より先にここが正）: [data_contract.yaml](data_contract.yaml)
- 踏んだ罠（症状 → 真因 → 処方 → 一般化）: [failures.md](failures.md)
- プライバシーポリシー（**外向け**。Microsoft Store の登録に使う）:
  [PRIVACY.md](PRIVACY.md) / [PRIVACY_en.md](PRIVACY_en.md) —
  **実装に接地させて書いてある**（updater 無し / 計測系の依存 0 件 /
  キーは OS の資格情報ストア / ログに本文と認証情報を書かない）。
  **機能を足したら「外部へ送るもの」が増えていないか数える**
- 仕様: `specs/NN_*.md`（起票 → 査読 → rev 改訂 → Phase 分割で main 直コミット）

機構・enum・フィールド・API を変えたら、その名前で全台帳を grep して
追従漏れを回収するまで完了としない。

**README と DETAIL の分割（2026-08-09 利用者判断）**: それまで README.md が
1,655 行あり、**`bun install` が 1,164 行目に、しかも見出し無しで置かれていた**
（`20ad607` が `## 開発` の 1 行だけを削り、コードブロック 4 つと配下の `###`
10 個が親を失っていた。日英とも同じ形）。**新しく見に来た人が最初に探すものが
一番奥にある**のが分割の理由で、冗長さそのものではない。

- **置き場はリポジトリ直下。`docs/` へは入れない** — README の相対リンクは
  **39 本が全部 `specs/` を指しており**、直下なら 1 本も壊れないが `docs/` では
  39 本すべてが壊れる
- **「思想」の節は README 側に残す** — この CLAUDE.md の冒頭が
  「README『思想』の節が正」と名指ししている。移すと台帳の参照先が動く
- **台帳は日英で 4 ファイルになった。** #51 (b) の「数えるのはファイル単位」が
  効く範囲が倍になっている — README を直したら DETAIL も、日を直したら英も数える

## 台帳の書き方

**台帳は grep されるための文章であって、読まれるための文章ではない。**
上の grep 規律が成立するのは、書く側が引く側の語彙で書いているときだけ。

- **具体名詞を 3 つ以上入れる**（それが検索語になる）
- **実際に起きた動作を述語にする**（「〜が落ちる」「〜を拒否する」）
- **比喩を使わない**
- **6 ヶ月後に自分がどんなクエリで引くかを想像し、その語を入れる**

```text
✗ 境界の防御は、開かれた列挙ではなく閉じた許容によってのみ成立する
✓ 外部が書いたデータの転送層では、除外リストは必ずもう一度落ちる
```

✗ の側は読めるが引けない。**教訓を書けと言われたときに最も滑り込みやすい形**が
これで、書いた本人には良く見えるのが厄介なところ。`failures.md` の一般化・
`data_contract.yaml` の invariant・Spec の Notes すべてに適用する。

**会話の文体と台帳の文体は分ける。** 対話では比喩も詩的な表現も使ってよいが、
記録には持ち込まない。混ぜると、どちらの要求も満たさない文章になる。

**書かない境界（2026-08-05 利用者判断）**: **変更で新しい名前・型・ワイヤ・
画面要素が 1 つも生まれないなら、台帳に書かない。**「この程度ならコードが仕様と
同じだ」（利用者の言葉）。実例は `RoleDialog.vue` の入力欄を `textarea` から既存の
`CodeEditor` へ差し替えたマージ — DETAIL のディレクトリ木にも画面構成表にも
`RoleDialog.vue` は既に載っており、**中の入力部品が何かは元から書かれていない**
（書くと部品を変えるたびに腐る）。

**対になる例が `StatusBar.vue`** — 同じく小さいが**新しいファイルと新しい画面要素が
生まれた**ので台帳へ書いた。**判断軸は変更の大きさではなく「名付けが起きたか」。**

この境界が要るのは、上の grep 規律（機構・enum・フィールド・API を変えたら全台帳を
grep する）が**単独では「すべて書く」側へ倒れる**ため。**両方を持って初めて、
台帳は引ける大きさに保たれる。**

**用語の境界（2026-08-01 利用者判断）**: 外に見える文言 — システムプロンプト・
UI ラベル・README — は「**グラウンディング**」、内部の台帳・コメント・設計語は
「接地」のまま。「接地」は内部用語すぎて外部に通じないため。英語 README は
元から Grounding なので、この統一で日英も揃った。外向け文言を書くときは
「接地」を使わない。

**同型の四例目（2026-08-08 利用者判断）**: **会話ペインとカードのツール名を
識別子から日本語の動作名へ**（`room_log` →「会話をたどる」/ `ask_agent_3` →
「ジェミーに尋ねる」）。起点は実機 —「`room_log` っていうのも会話をたどるっていう
くらいでいいかもしれない」。**型・IPC・ログは 1 つも変えていない**
（`fuseforks.log` は `name=room_log` のまま）。実装は `lib/toolLabel.ts` の
純関数 1 本 + 辞書 `tools.*` 14 鍵。

- **一番効くのは理由の出ないツール**（`ask` / `plan` / `room_log`）。Spec 27 で
  理由欄の対象外にしたので、**名前だけが唯一の情報**になる。`grep` は
  「意図: 〜」が意味を運ぶが、`room_log` は名前が運ぶしかない
- **外部（MCP）は訳さない。** 名付けたのは接続先で、訳語を当てると
  **何が走ったかについて嘘になる**。識別子のまま**等幅**で出すので、
  **書体そのものが出自を示す**（`wants_reason` を外部へ足さないのと同じ線引き）
- **識別子は `title` に残す。** 撮った画面から `fuseforks.log` の `name=` を
  引ける（ステータスバーの時計を `diag.rs` と同じ書式に固定したのと同じ規律）
- **文の枠を「{name}: {tool}」へ中立化した。** 旧い枠
  「{name} が {tool} を**実行**」に訳語を入れると「会話をたどる を実行」になり、
  英語は `"Zari ran read the room log"` で壊れる。**訳した語句を文の枠へ
  埋め込むと語順が言語をまたげない**（Spec 13 P3b が `${label}に失敗しました` を
  2 段へ割ったのと同じ形）
- **カードと会話ペインで解像度が違う。** `AgentCard` は自分 1 体しか知らないので
  `ask_agent_3` の相手を解決できず、**id のまま出すのが正直**
- **表の突き合わせを機械で留めた**（`toolLabel.test.ts` が Rust の
  `BUNDLED_TOOL_NAMES` を読む）。抜けると**自分たちのツールが「外部のもの」として
  出る** — 型検査にも lint にも掛からない。**ミューテーションで実測**（`grep` を
  表から抜くと 2 本が赤。うち 1 本は `expected 'external' to be 'known'` で
  症状そのものを出す）

**同型の三例目（2026-08-05 利用者判断）**: **中央上段の表示名を「村の地図」から
**「サーヴァントの絆」**（英語は **`Kizuna`**）へ改名した。** 由来はコーエーテクモ
『三國志13』の絆システム。**型・ファイル名・IPC は 1 つも変えていない** —
`TopologyEdge` / `topologyPositions` / `TopologyMap.vue` / `list_topology` は据え置き
（**波ペインの表示名を「作業状況」へ変えて契約を据え置いた前例と同じ**）。
英語では**固有名が `Kizuna`、普通名詞が `tie`** の使い分け（`8 servants / 7 ties`）。
追従させたのは UI 辞書 ja/en・README 日英・**画面を指すコメント**のみ。
`data_contract.yaml` / `specs/` / `failures.md` の `topology` 語はそのまま
（構造の名前であって画面の名前ではない）。**改名で腐ったのは「地図」を含む文で、
`topology` で grep しても 1 件も出ない** — 表示名の改名は**型名の grep 網の外**。

**同型の二例目（2026-08-03 利用者判断）**: **システム設定ダイアログの語彙は
Fuseforks のテイストから降りてシステム用語にする**。UI ラベルは
「トークン制限」「制限あり / 制限なし」、内部の台帳・コード・ログ・設計語は
**「天井 / ceiling」のまま**。起点は実機の指摘 —「あまりにも砕けすぎていて
逆にわかりづらい」。`CoreError::InvalidTokenBudget` の `#[error]` は利用者に
見えるので制限側で書く。**ページ本文の「村」「サーヴァント」は残す** —
それは世界の説明であって分類名ではない。境界は「分類・ラベルか、説明か」。

## ライトテーマ（2026-08-05 着地。Spec 不要と判断）

利用者要望 —「Kataribe 等の利用者スクショを見ると**ライトモードのほうが多数派**。
僕はダーク派だが一般はライトを好む」。**フロントだけで閉じるので Spec は切っていない**
（決めることが無い変更に査読の窓は要らない。`StatusBar.vue` と同じ判断）。

入口は**システム設定 > ユーザーインターフェース > テーマ**（「メッセージ表示/非表示」の上）。
**選んだ瞬間に反映**（保存ボタン無し）。**選ぶまで OS に追従し、保存もしない** —
言語の「初回に確定して再判定しない」と規律が逆で、理由は
**言語は `world.json` に住み System 行として会話ログへ焼き付く**こと。

機構: `@theme` はダークのままユーティリティを生やす側、ライトは
`:root[data-theme="light"]` の**素の CSS** で変数を上書き（**レイヤ外なので
`@layer theme` に必ず勝つ** — 特異度に頼らない。ビルド出力で `depth 0` を確認）。
`color-scheme` も切り替える（**OS が描く部品**が反対のテーマで残るため）。

**「配色は `style.css` の 1 箇所」の規律は実際に保たれていた** — 生の Tailwind
パレットは `bg-black/60` の 9 箇所だけで、それも `bg-scrim` トークンへ畳んだ。
ワードマークの生 RGB・死んだフォールバック 6 箇所・地図の `#3a4152` も全廃。
残る生の色は閉じるボタンの `#e53935` / `#fff` のみ（OS の作法）。

**役職バッジとアバターは L/C だけをテーマから引く**（`--role-l` / `--avatar-l`）。
バッジは**文字色**なので白地に L=0.74 では読めず、アバターは**背景**で上の文字が
`text-surface-0`（テーマで反転）だから。8 色ぶん L/C を書き並べるとテーマごとに
ずれる余地が生まれるので、参照にして**構造で揃える**。

`surface-0/1/2` の意味は**明るさではなく奥行き**（0 = 最も奥 / 2 = 最も手前）。
ダークは奥ほど暗く、**ライトは向きが逆**。数字を明るさと読むと手前と奥が入れ替わる。

**`theme.test.ts` を新設**（`@theme` の色が全部ライトにあるか / ライトが余計な色を
増やしていないか / L/C が両テーマにあるか / `color-scheme` が両方にあるか）。
**値の良し悪しは見ない** — 閾値を検査すると「通ったから読める」という誤った確信を作る。

**中央上段を「サーヴァントの絆」（英語 `Kizuna`）へ改名**（下記「台帳の書き方」の
用語境界・三例目）。あわせて**辺の既定色を accent にし、ホバーは色を変えず
発光させる**（旧: 既定が `--color-line` でホバーだけ accent = 常時見えるべきものを
操作したときだけ見せていた）。`failures.md` #65 はその改修で自分が入れた退行。

## カードから絆を引く（[Spec 21](specs/21_drag-to-connect.md)・2026-08-06。**Done**）

起点は利用者 —「編集ダイアログを開いて絆をつなげるのは面倒」。リストのカードを
絆の地図のノードへ drop すると絆が張られる（~~3 つ目~~ **2 つ目**の入口 —
下の訂正を参照。線は人が引く、は不変）。

- **機構は保留コミット** — `@update:model-value` は配列を保留するだけ、`@end` の
  素の `elementFromPoint` + `closest` で確定 or 破棄（**選択子は 2026-08-13 に
  `.vue-flow__node` から自前の `[data-kizuna-node]` へ変えた** — 描画ライブラリの
  クラス名を名指ししていたので、差し替えで黙って壊れた）。
  発火順 `update → end` は同梱 Sortable の `_onDrop` と playground 実走の両方で確定
- **破棄だけでは DOM が移動後の並びのまま残る** — ソース読み
  「vue-draggable-plus が emit 前に差し戻す」は実走で反証された。
  `insertBefore(children[oldIndex])` の手動差し戻しが要る（P0 実測 3）
- 追加規則は `lib/kizunaDrop.ts` の `tieAddition` に 1 実装
  （~~ハンドルドラッグと drop の 2 経路が共有~~ **実際は drop の 1 経路だけ**）。
  **接続済みは方向付き**（`A→B` があっても `B→A` は張れる = 両端矢へ）
- **訂正（2026-08-13）— 地図のハンドル引きは最初から存在しなかった。**
  `TopologyMap.vue` は `#node-default` を上書きしており、その中に `<Handle>` を
  1 つも置いていない。Vue Flow はスロットを与えると**ハンドルごと差し替える**ので、
  ハンドルは 1 度も描かれておらず、`@connect` は到達不能、
  `.vue-flow__handle` の CSS は死んでいた。「対照実験済み」と書いた自己接続の観察は
  **playground の既定ノード**でのもので、アプリの挙動ではない。
  **入口は元から 2 つ**（設定のチェック / カード drop）。
  画面の案内文・DETAIL 日英・この節・Spec 21 の**4 箇所が互いを裏付けていた**ので
  誰も実装を数えなかった。`failures.md` #53 の 2 例目で、**入口どころか描画要素ごと
  無かった**形（発覚は描画ライブラリの差し替え時。詳細は #102）
- **D2（ドラッグ中のノードハイライト）は保留** — fps 47.5〜52.9 で基準 60 未達
  （毎フレーム分身を display トグルする素朴実装での数字）。分身へ
  `pointer-events: none` を 1 回打つ最適化後に P2 冒頭で再計測。
  代替の copy カーソル（地図の矩形判定のみ）は P1 で入れた
- **査読の前提 2 点を実測で訂正して採用**した経緯は Spec の rev2 Notes 6 —
  矢印は既にある（`markerEnd` / `markerStart`）/ 逆向きの追加は両端矢へ変わる
- 罠: 合成されていない（非表示の）ページでは Vue Flow のノード初期化が rAF 待ちで
  止まり、全ノード `visibility: hidden` のまま `elementFromPoint` に当たらない
  （**Vue Flow 時代の観察**。`v-network-graph` で同じ形が出るかは未測定）
- **P4 実機確認は完了**（2026-08-06。カードから絆が張れることを利用者が確認）。
  **P2 と S4 は利用者判断で削除して Done**（同日）— D2 のハイライトは fps
  47.5〜52.9 で基準 60 未達のまま閉じた。発見性は copy カーソルと no-op パルスで
  足りており、一度 60fps を割った機構を最適化してまで入れるとフレーム予算を
  常時払い続ける。**「どこに落ちるか分からない」の報告が実測で出たら別途起票**
  （頻度を見てから決める）。ネイティブ DnD 不発の仮説は未実測のまま
  （機構はどちらでも成立。`force-fallback` コメントは断定形にしない形で維持）。
  vitest 142 → 151

## 絆の地図を v-network-graph へ差し替えた（2026-08-13 着地。Spec 不要と判断）

起点は利用者 —「vis-network 的なネットワーク図の表現に惹かれている。三國志 13 の
絆マップに近いもの」。**フロントだけで閉じる**（Rust は 1 行も変えていない）ので
Spec は切っていない。ブランチ `20260813_vng` で試作し、実機を見ながら 6 往復で詰めた。

- **`v-network-graph` を選んだ決め手は SVG であること。** ノードを Vue のスロットで
  描けるので**色が CSS から引ける** = 「配色は `style.css` の 1 箇所」の規律が
  破れない。**canvas（vis-network）だと描画コードが色の 2 箇所目になる**。
  なお **`vue3-vis-network` は npm に存在せず**、`vue-vis-network` は Vue 2 専用で
  2022 年から止まっている（vis-network を使うならラッパー無しで命令的に叩く）
- **`layouts.nodes` が `topologyPositions` と 1 対 1** なので、既存の IPC と
  `world.json` の欄がそのまま生きた。**依存は入れ替え**（新 3 本 ↔ 旧 Vue Flow が
  連れていた `@vueuse/core` + d3 系 4 本）
- **配置は人が置く。** 自動整列は入れていない — **規則的な配置は実機で 2 度却下**
  された（円環も「エゴ中心の放射」も「網ではなく図表に見える」）。
  埋めるのは**一度も置かれていない個体だけ**（`lib/kizunaSeed`。既存座標に触らない）
- **ノードは丸いアバター + 名前 + 状態の輪だけ。** 役職・モデル名・トークンは外し、
  **ホバー中だけ右上の半透明パネル**へ出す。**外した 3 つは左のカードに全部載って
  いる**ので、アプリのどこからも情報は失われていない
- **選択は 1.5 倍 + accent の太い輪 + バウンド。** 大きさは `configs` 側の半径で
  切り替える — **CSS の拡大だけだと辺の接続点が旧い半径のまま残る**
- **この差し替えで型の外の結合が 4 つ露出した**（`failures.md` #102）。
  同時に**「地図のハンドル引き」が最初から存在しなかった**ことも分かった —
  台帳 4 箇所が互いを裏付けていて、誰も実装を数えていなかった

## 失敗したターンの払いを台帳へ入れた（2026-08-16 着地。Spec 不要と判断）

起点は `failures.md` #103（出力上限で本文が空になったターンは、課金は起きているのに
カードの累計・予算・`turn:` 行のどれにも出ない）。**統計画面（次の一手）の前提**
なので先に塞いだ。**新しい型・IPC・画面要素は生まれない**（`LlmError::OutputTruncated`
に `usage` 欄が 1 つ増え、`turn:` 行が失敗でも出るようになっただけ）ので Spec は
切っていない。詳細は #103 の処方の側が正。ここには次に触る人が要る 3 点だけ:

- **形は otari（mozilla-ai の LLM ゲートウェイ）の `inflight` 登録簿と同じ** —
  「使用量の記録は過去しか語れない」ので、要求が始まった時点で登録し、
  **終わり方に関わらず精算する**。`turn.rs` の `TurnSpend` がターンの飛行中台帳で、
  `run_turn`（所有者）と `run_turn_inner`（実行ループ）に割った。
  **`?` で内側から抜けても台帳は外側に残る**のが分割の理由
- **「払ったか」はエラーごとに違うので、型に写した** — `LlmError::usage()` が
  `Some` を返すのは**プロバイダが 200 を返した後にこちらが `Err` へ変えた variant**
  だけ（今は `OutputTruncated`）。400 / DNS / タイムアウトは `None` のまま
  Drop の全額返金へ落ちる。**分からないものに見積もりを捏造しない**
- **`turn:` 行は 4 つの出口すべてに 1 本出る**（完走 / `stop=failed:{CODE}` /
  `turn interrupted` / `turn budget exhausted`）。**数えるのは `turn:` 行だけでよい**
  （`turn failed:` は理由の文面。失敗したターンは 2 行になる）。
  `data_contract` の `observability_rule` / DETAIL 日英に追従済み

**評価基盤についての利用者の見立て（同日）**: 「オーケストレーション自動化は
評価基盤を揃えないとオカルトになりやすい」「Fuseforks は評価基盤からいえば
まったくゼロ」。**私の見立て — 観測（`fuseforks.log` の計器 / `sessions.redb` /
`failures.md` #1〜#106 / `specs/tla/`）は在り、評価（課題 + 期待される結果 + 判定器）は
3 つとも無い**、が正確な位置。参考として挙がった 2 つは層が違う —
**otari = 「いくら払ったか」の層**（LLM ゲートウェイ。予算の事前強制 /
`/v1/usage` / `inflight`。評価機能は無い。**→ 2026-08-18 の実読で射程が広がった** —
予算の予約と reconcile / **ルーティング（kNN を含む）** / MCP ループ / web 検索
バックエンド / OTLP まで持つ。**「利用者が言った otari の機能が何を指すか未確認」
だった宿題はここで閉じた**。費用の型の詳細は Spec 40 の「先行実装の読み」が正）/ **promptfoo = 「払った価値があったか」の
層**（assertions + LLM-judge + 回帰）。**Fuseforks への持ち込みはチェリーピック**
（利用者の言葉）で、**正解が機械的に決まる軸を先に**（完遂 = `reply: to=` の述語 /
費用 = `turn:` 行 / 成果物の検証可能性 = `run` の exit / 契約の遵守 = TLA・テスト）、
**LLM 判定は最後**。#103 を先に塞いだのは費用の軸に穴があると 2 行目の数字が
嘘になるため。~~**未起票**（材料の段階）。~~ **費用の層だけ
[Spec 39](specs/39_stats-view.md)（統計画面）として起票した**（2026-08-16）。
価値の層（assertions / pass rate / LLM-judge）は Spec 39 の Notes 1 に材料の表を
置いたまま**未起票**。**次に摘むのは「完遂」**（2026-08-16 利用者 —「完遂は欲しいね。
依頼主に返さないというのは結構あるからね。でも起票は後日でいいや」）。
**新しい計器は要らない** — `reply: to=` が既に宛先を持つので、委譲で呼ばれた
ターンの `reply: to=user` を数えるだけで失敗が拾える（#96 で入れた計器がそのまま
評価の材料になる）。**品質の層（rubric・閾値・LLM-judge）は SNS へ意見を投げて
返答待ち**（同日。返答が無ければ機械で決まる軸から進める）。

## 配布経路を 3 つに広げた（2026-08-16〜17 着地。Spec 不要と判断）

起点は利用者 —「Microsoft Store の予約はしたが、一般層向けに出すにはマニュアルや
ヘルプが少なすぎる。ただ開発者が使う winget や homebrew では公開したい」。
**Spec を切らなかったのは、決めることが識別子と配布物の選択の 2 つしか無く、
どちらも利用者がその場で裁定したから**（コアも画面も 1 行も変えていない）。

**前提の確認から入った** — winget のコミュニティリポジトリと Microsoft Store の
一覧は**一方通行**で、winget-pkgs に登録しても Store の GUI には 1 件も出ない
（逆は `msstore` ソースで見える）。だから「Store には出さず winget だけ」は
構造として成立する。

### winget（`Outcasts.Fuseforks`。**2026-08-29 マージ済み**）

**PR https://github.com/microsoft/winget-pkgs/pull/418363 は 2026-08-29 にマージされた**
（提出 2026-08-17 → **12 日**。New-Package の中央値 15.2 日とほぼ一致）。
**自動検証 10 本すべて SUCCESS**（未署名インストーラへの Defender スキャンも通過）、
CLA 署名済み、最終ラベルは `Moderator-Approved` / `Publish-Pipeline-Succeeded`。
`winget search fuseforks` が `Outcasts.Fuseforks 0.1.8` を返すことを実機で確認済み。

**載ったのは提出時の `0.1.8` で、その後 v0.1.9〜v0.1.11 が出ている。**
**古い版は壊れずに止まる**（エラーが出ないので気づく契機が無い）ので、
下の「版を出すたびに 2 経路の更新が要る」が唯一の追従経路。

- **`Outcasts.Fuseforks` は実質永続の公開識別子**（改名には move PR が要り、
  既存利用者の `winget install` が壊れる）。利用者裁定。根拠は実測で、MSI の
  `Manufacturer` と Summary の `Author` がどちらも `outcasts`
  （Tauri が `jp.outcasts.fuseforks` から導出している）
- **提出したのは MSI のみ。** NSIS の `-setup.exe` は出していない。MSI は
  `ProductCode` / `UpgradeCode` を持つので winget の更新・削除の検出が構造で決まる。
  対する NSIS は ARP の表示名と発行者の照合に頼るが、**実測で exe の
  `CompanyName` が空**だった = 最も壊れやすい形
- **`ProductCode` は版ごとに変わり、`UpgradeCode` は不変**
  （0.1.8 は `{AFC5FBCB-F63D-4F2C-9A5F-58A6A67F5DE5}` /
  `{B2DDE00E-42D3-505A-B1C4-62EE9E4F5A6C}`）。手で書くなら**必ず新しい MSI から
  読み直す** — 古い値を写すと更新検出が壊れる
- **NSIS は現ユーザー単位に入る**（開発機の ARP が `ARP\User\X64\fuseforks`
  だったので確定）。MSI は `ALLUSERS=1` でマシン全体。**この 2 つは同じ端末に
  並んで入り、identifier が同じなので村のデータは共有する** —
  **ここで実際に事故が起きた**（2026-08-20。`failures.md` #112）。手元のビルドを
  合わせて **3 世代が 1 つの `world.json` を共有**し、**単価の欄を知らない旧版が
  それを黙って削って書き戻した**。`#[serde(default)]` は「欠けても読める」だけで
  「消さない」を保証しない。処方は起動行へ `version: app=… profile=…` を出したこと
  （どの世代が触ったかをログで読む）。**未知の欄を保持して書き戻す側は未起票**
- **MSI はインストール中に WebView2 を外部から落とす経路を持つ**
  （`DownloadAndInvokeBootstrapper` が `NOT(REMOVE OR INSTALLED_WEBVIEW2_VERSION)`
  の条件で `powershell.exe` を呼ぶ）。Win11 には既に入っているので通常は飛ぶ
- **サイレント時にアプリは自動起動しない** — `LaunchApplication` の条件が
  `AUTOLAUNCHAPP AND NOT Installed` で、それを立てるのは終了ダイアログの
  チェックボックスだけ。`/quiet` は UI シーケンスごと走らない
- **CI は 1 行も変えなくてよい。** winget は版ごとに URL と SHA256 で固定する側で、
  **版を含むファイル名だからこそ自動化できる**（Microsoft 自身の PowerToys も
  版入りファイル名）。次の版は
  `wingetcreate update Outcasts.Fuseforks --version <V> --urls <上の式> --submit`
- **版上げは New-Package と速さの桁が違う** — 0.1.12 の update PR は
  提出から **1 時間足らずでマージ**された（2026-08-29 08:26 UTC。
  `Moderator-Approved` → bot が自動マージ。0.1.8 の New-Package は 12 日）。
  ただし**マージとカタログ配信は別** — マージ直後の `winget search` はまだ
  旧版を返し、publish パイプラインの索引再構築を待つ。**実測: マージ 08:26 →
  カタログ配信 10:01 までの間（15 分刻みの監視で捕捉）= 配信ラグは 2 時間弱**
- **`winget upgrade` はロケール照合で落ちうる**（2026-08-29 に開発機で実測）。
  症状は「新しいパッケージ バージョンを使用できますが、システムまたは要件には
  適用されません」（`UPDATE_NOT_APPLICABLE`）。verbose ログの決定的な 1 行は
  `Installer [X64,wix,Machine,en-US] not applicable: Installer locale does not
  match ... Preferred Locales: [ja-JP]`（`IsInstalledLocale: 1`）—
  **マニフェストの `InstallerLocale: en-US` と、端末のインストール記録の
  ja-JP が突き合わされて弾かれる**。種別・スコープ・アーキテクチャは無関係。
  - **踏む条件は「MSI を winget の外で手動で入れた + 日本語環境」** —
    開発機の 0.1.8 は提出前の手動テストで入れたもので、HKLM に居るのに
    winget の追跡記録が無い。`winget install` で入れた利用者は en-US が
    記録されるので踏まないはず
  - **処方は `winget upgrade --id Outcasts.Fuseforks --locale en-US`**
    （実機で 0.1.8 → 0.1.12 が完走。`UpgradeCode` による差し替えも動いた）
  - **未検証の予測**: winget 経由で入った今、追跡記録は en-US のはずなので
    **0.1.13 からは素の `winget upgrade` で通る** — 次の版で確かめる
  - 途中で **MSI 1618（別のインストールが進行中）** と **1602（UAC を
    承認せず = キャンセル扱い）** も踏んだ。どちらもロケールとは別の層で、
    再試行と UAC の承認で解ける
  - **恒久処方は「次の提出でマニフェストから `InstallerLocale: en-US` を
    消す」**（2026-08-29 利用者裁定 —「あまり winget でインストールしている
    人はまだいなさそうだしロケール消していい」。per-locale MSI 案は
    提示したうえで採らなかった）。根拠は winget-cli の
    `ManifestComparator.cpp`（`LocaleComparator::IsApplicable`）の実読 —
    **インストール記録由来の照合（preference 枝）には空ロケールの素通しが
    ある**（「そうしないとカタログ全体のアップグレードが壊れる」とコメントに
    明記）ので、宣言が無ければ今回の形は構造的に起きない
  - **残余を 1 つ受け入れている**: `--locale` を一度使った端末は利用意図が
    記録されて**要求**に昇格し、要求の枝には空ロケールの素通しが**無い** —
    つまりこの開発機（今日 `--locale en-US` で入れた）は 0.1.13 でも
    `--locale en-US` が要る可能性が高い。一般の利用者は使っていないので
    当たらない、が裁定の根拠
  - **提出の手順が 1 段増える**: `wingetcreate update` は前の版の
    マニフェストを引き写すので、`--submit` せず一度ローカルへ生成 →
    `installer.yaml` の `InstallerLocale: en-US` の行を削除 →
    `wingetcreate submit`。**wingetcreate は cwd に `manifests/` を
    生成する**（リポジトリ直下で実行すると未追跡フォルダが生える。
    `.gitignore` 済み。正本は winget-pkgs 側）
- **update の実走で確定した 3 点**（2026-08-29 の 0.1.12 =
  https://github.com/microsoft/winget-pkgs/pull/426022）:
  (a) **手元のマニフェストは要らない** — update は公式リポジトリの既存
  マニフェストを取り、版・URL・SHA256 だけ差し替える
  (b) **罠: フォーク `betyourluck/winget-pkgs` が本家より古いと
  「フォークされたリポジトリをアップストリーム コミットと同期できませんでした」で
  失敗する。** 処方は提出前に
  `gh repo sync betyourluck/winget-pkgs --source microsoft/winget-pkgs --branch master`
  （フォークは提出のたびに本家へ置いていかれるので、**次の版でも必ず踏む**）
  (c) **device 認証コードはコンソール直描画で、出力捕獲に乗らない** —
  `wingetcreate` の提出は人がコンソールで実行する
- **検証は Windows Sandbox**（winget-pkgs の `Tools/SandboxTest.ps1`）。
  **罠: マップ先のパスが `winget-pkgs` を含まないと警告の既定が `Inquire` になり、
  非対話シェルが応答待ちで固まる。** `-WarningAction Continue` を明示する

### macOS の署名と公証（`v0.1.9` から）

**`v0.1.9` が署名 + 公証された最初のリリース。** 起点は実測 — それまでの
`.app` に `_CodeSignature/` が無く、Gatekeeper がダウンロード後の起動を拒む状態だった。

- **秘密は 5 つ**（`APPLE_CERTIFICATE` / `APPLE_CERTIFICATE_PASSWORD` /
  `APPLE_ID` / `APPLE_PASSWORD` / `APPLE_TEAM_ID`）。**Team ID は `6XU323VJZN`**
- **環境変数の名前の一次資料は Tauri CLI の
  `crates/tauri-cli/ENVIRONMENT_VARIABLES.md`。** tauri-action の README には無い。
  `APPLE_SIGNING_IDENTITY` は置いていない — 渡すと証明書と一致するかの検証にしか
  使われず、省略すると証明書から推論される（`macos/sign.rs` で確認）
- **証明書の発行で既定が罠だった。** Apple の画面は
  `Profile Type: ● Previous Sub-CA` を既定で選んでおり、**そのまま進むと
  2027-02-01 に失効する証明書**が出る（同じ画面の説明文にそう書いてある）。
  **G2 Sub-CA を選ぶ。** 選べたことは**発行された証明書の期限 2031-08-18** で
  裏が取れる
- **CSR に入れたメールは証明書に残らない**（実測）。Apple は CSR から公開鍵だけを
  取り、subject は全部アカウント側から組み直す —
  `UID=6XU323VJZN, CN=Developer ID Application: kosei takahashi (6XU323VJZN),
  OU=6XU323VJZN, O=kosei takahashi, C=JP`
- **署名した配布物には実名が恒久的に載る。** 個人登録なので CN が個人名になり、
  `codesign -dvv` で誰でも読める。法人登録（D-U-N-S が要る）以外に回避策は無い。
  **利用者が承知したうえでの判断**（2026-08-17）
- **Windows での p12 の作り方**（Mac 無しで通る）: openssl で鍵と CSR →
  Apple へ CSR → `.cer` → **中間証明書 G2**
  （`https://www.apple.com/certificateauthority/DeveloperIDG2CA.cer`）と束ねて
  `openssl pkcs12 -export -legacy` → `openssl base64 -A`。
  **`-legacy` は macOS の `security import` 互換のため**（実機で通ることを確認済み）、
  **`-A` は改行なし**（折り返した base64 は `$GITHUB_ENV` の 1 行形式に載らない）
- **`MSYS_NO_PATHCONV=1` が要る場面が 2 つ** — openssl の `-subj` と
  `git show <ref>:<path>`。どちらも Git Bash がコロン付き引数を
  Windows のパスへ変換して壊す
- **`openssl rand -hex` は末尾に改行を付ける。** そのまま `gh secret set` へ流すと
  パスワードが 1 文字ずれて CI の証明書取り込みが落ちる
- **`.github/workflows/verify-notary.yml`（手動）を新設した。** 公証の資格情報だけを
  `xcrun notarytool history` で 1 分で検証する。**初回で元を取った** — 20 分の
  ビルドを回さずに「`APPLE_PASSWORD` が 15 文字 = App 固有パスワードではない
  （正しくはハイフン込み 19 文字）」を特定できた。
  **値を 1 文字も出さず長さだけで判定している** — `failures.md` #71
  （計器がモデルの出力経由で秘密をログへ書いた）の対になる形
- **私が開けた穴が 1 つ。** 署名の門 `HAS_APPLE_SIGNING` が**必要な秘密 5 つのうち
  2 つしか数えていなかった**。空文字で落ちる形を防ぐために作った門が、
  `APPLE_ID` を入れた瞬間に真になり `APPLE_PASSWORD` が空のまま発火する状態だった。
  **一般化: 「必要な入力が揃っているか」を検査する門を書いたら、その入力を実際に
  数える。依存は片方向とは限らない**（Tauri の文書は「`APPLE_ID` を指定するなら
  `APPLE_PASSWORD` と `APPLE_TEAM_ID` も要る」と明記していた）
- **YAML の罠**: `run:` を単一行で書くと、値の中の `Apple signing: DISABLED` の
  **コロン+空白**を YAML が入れ子のマッピングと読んで落ちる。**ブロックスカラー
  （`run: |`）内の同じ文字列は無害** — 同じ文字列が置かれた文脈だけで有効と
  無効に分かれる。目視では見えないので**パーサに通すまで信用しない**（#107 の同型）

### Homebrew tap（`betyourluck/homebrew-tap`）

**`brew install --cask betyourluck/tap/fuseforks` が通ることを実機で確認済み。**

- **完全修飾名は省略できない。** `brew tap` してから短い名前で入れようとすると
  `Refusing to load cask ... from untrusted tap` で拒否される。Homebrew は
  **tap することと、そこのコードを読み込む信頼を与えることを分けている**
  （`brew trust --cask <完全修飾>` を先に打てば短形も使える）。
  **世間の README に溢れている `brew tap X && brew install Y` は非公式 tap では
  通らない** — `Taps.md` にその段落があるのに、私は慣れた形を書いて実機で落ちた
- **リポジトリ名は `homebrew-` で始まれば後ろは自由。** `tap` にしたのは
  **索引を中身の 1 つの名前で呼ぶと、2 つ目が入った瞬間に名前が嘘になる**ため
  （完全修飾が `betyourluck/fuseforks/fuseforks` と重なるのも避けた）。
  cask は `Casks/fuseforks.rb`
- **`auto_updates true` は付けない。** 参考にした cask は付けているが、
  **Fuseforks に updater は無い**（`PRIVACY.md` が正）。付けると Homebrew は
  「アプリが自力で更新する」と読んで `brew upgrade` の対象から外し、
  **利用者が新版を受け取れなくなる**
- **`depends_on macos:` は書かない** — 実行時の OS 下限を測っていない。
  winget で `MinimumOSVersion` を書かなかったのと同じ判断
- **`zap` は村を消す**（`~/Library/Application Support/jp.outcasts.fuseforks` に
  `world.json` / `sessions.redb` / workspace が全部入っている）。通常の
  `uninstall` では消えないことを cask と README の両方に書いた
- **`.github/workflows/verify-cask.yml`（手動）を新設した。** この tap を保守して
  いる端末は Windows で `brew` を 1 度も実行できない。**`spctl` が本命** —
  署名の構造物が入っていることと Gatekeeper が実際に受け入れることは別の主張で、
  前者はファイルを見れば分かるが後者は macOS に訊くしかない。
  **`--no-quarantine` を使わない**（使うと判定を迂回して目的が消える）

**実測の結果**（`macos-latest` ランナー）:

```text
codesign -dvv   Authority=Developer ID Application: kosei takahashi (6XU323VJZN)
                Authority=Developer ID Certification Authority / Apple Root CA
                Notarization Ticket=stapled
stapler validate  The validate action worked!
spctl -a -vv      accepted / source=Notarized Developer ID
brew audit --cask --online --strict   指摘ゼロ
```

**3 つは主張の強さが違う** — `codesign` は署名の構造物、`stapler` は
チケットの綴じ込み（オフラインでも検証できる）、**`spctl` の `accepted` だけが
「Gatekeeper が実際に受け入れる」**を意味する。

### 公式 homebrew-cask は目標から降ろした（2026-08-17 利用者裁定）

**`Package-Acceptance-Policy.md` の門は 2 つあり、片方は時間で解けない**:

| 門 | 要求 | 実測（2026-08-17） |
|---|---|---|
| リポジトリの年齢 | 30 日 | 18 日（2026-08-29 に開く） |
| **注目度（本人提出）** | **225 stars** / 90 forks / 90 watchers | **1 star / 0 forks / 1 watcher** |

**他人が提出すれば 75 stars**に下がる。例外条項は「substantial, independently
verifiable public interest and **multiple requests for inclusion**」を材料に挙げる。

**降ろした理由は星が足りないことではなく、目的の裁定**（下の節）。**tap で完結**して
おり、`fuseforks` というトークンは homebrew-cask に存在しないので将来も使える。

### 版を出すたびに 2 経路の更新が要るようになった

**「リリースビルド」の節はタグを打つところで終わっている**が、そこで終わらなくなった:

1. **winget**: `wingetcreate update Outcasts.Fuseforks --version <V> --urls <MSI の URL> --submit`
2. **tap**: `Casks/fuseforks.rb` の `version` と `sha256` を新しい dmg から取り直す

**どちらも版ごとに URL と SHA256 で固定する形**なので、タグを打った後に忘れると
**配布経路だけが古い版を指し続ける**（壊れはしないが、新版が届かない）。

## ランディングページ（2026-08-27 作成。**2026-08-29 公開**）

**公開 URL は https://betyourluck.github.io/fuseforks.html**（`d5ea2b5` で
`docs/fuseforks.html` + `docs/images/` を追跡・push。200 と新コマンドの配信を
実測）。公開前に「申請中」の 2 箇所を消し、winget カードを
`install --id` / `upgrade --id` の 2 コマンド並記へ、**壊れたリンク 12 本**
（ヒーローの GitHub ボタン 2 / インストーラー直接取得 3 / フッター 7）を
実 URL へ直した。直接取得の 3 本は **`releases/latest`**（版固定にすると
版を出すたびに直す経路が 2 本から 5 本へ増え、このページは `git grep` の
網の外なので確実に腐る）。

**置き場が 2 つあり、リポジトリの中には無い。**

| | パス | 状態 |
|---|---|---|
| 作業用 | `RundingPage/fuseforks.html` | **`.gitignore` 済み**（`d754f35`）。この repo は追跡しない |
| 公開先 | `betyourluck.github.io/docs/fuseforks.html`（手元は `D:/Github/betyourluck.github.io/docs/`） | 利用者が上書き済み。**`docs/fuseforks.html` と `docs/images/` が未追跡（`??`）** = 「公開していない」の実体は git に入れていないこと |

- **`docs/` が Pages のルート**（`prices.json` が そこに居るのが根拠 — Spec 41 の
  単価表が `betyourluck.github.io/prices.json` で引ける）。だから URL は
  `betyourluck.github.io/fuseforks.html` で、**`index.html` へ改名しない**
  （別のページと相乗り）。フォルダ名の綴りは **`RundingPage`**（Landing ではない）
- **公開の条件は winget の PR が通ること**だった（2026-08-27 利用者判断）。ページが
  `winget install Outcasts.Fuseforks` を推す形なので、**未マージのまま出すと
  押した人の手元で必ず失敗する**。**PR #418363 は 2026-08-29 にマージされ、
  条件は満たされた** — ページ側で消すのは 2 箇所（`tag-pending` のバッジと
  「承認されるまでこのコマンドは使えません」の本文）
- **公開前に直す 9 件目が残っていた**（2026-08-29 発見）— 「インストーラーを直接取得」の
  **3 本とも `href="https://github.com"`**（GitHub のトップ）で、Releases へ行かない。
  **winget / brew を使わない人の導線が全部そこで切れる**。下の 8 件は本文の誤りだったが、
  これは**リンク先**なので本文をいくら読んでも出ない — **生成物のレビューでは
  `href` / `src` の実値を別に数える**（本文の正しさは行き先の正しさを含意しない）

**これで外向けの面が 3 つになった** — README（3 言語）/ Qiita の記事 /
ランディングページ。**後ろ 2 つはこの repo の grep 網の外**にあり、
ランディングページは**そもそも追跡されていない**。用語・機能・配布物を
変えたら、`git grep` では 1 件も出ないこの 2 つを別に数える。

### 実装と突き合わせて直した誤り（初版は生成物で、8 件が実装と食い違っていた）

- **プライバシーの文言が `PRIVACY.md` と矛盾していた**（最重い）。ページは
  「開発者へのデータ送信はゼロ」と書いていたが、`PRIVACY.md` は
  「単価表の取得先だけは例外」「取得先には要求の事実と**通信元の IP** が残る」と
  明記している。**外向けの 2 文書が食い違うと、読み手はどちらを信じるか
  決められない。** 処方は言えることだけに絞ること
  （「計測・解析・クラッシュ報告の仕組みを持ちません」）
- 版番号 `v1.0.0`（実際は `v0.1.11`）/ 「完全ローカル実行」（LLM へは通信する）
- **配布物の名前**: Windows は `.msi` と `-setup.exe` の 2 種、Linux は
  `.rpm` を含む 3 種（Release アセット 7 件を実際に数えた）
- プロバイダの列挙に **Meta と Perplexity** が無い（検索・接続先の 2 箇所）
- 機能一覧カード 12 枚のうち **1 枚だけ構造が違い**、アイコンと見出しが横並びで
  描かれていた（`.capability-header` の使用箇所が 1 つだけ）

### ロゴはアプリから逐語で写す

**3 箇所とも出どころが違う**ので、触るときは向こうが正:

- 三角の SVG → `TitleBar.vue`（`r="2.4"` の丸 3 つ + `path` 1 本
  `M10.6 7 6.4 16M13.4 7l4.2 9M7.4 18h9.2`・`stroke-width="1.8"`）
- `Outcasts` のドット字と紫の発光 → `style.css` の `--color-wordmark` /
  `--wordmark-glow-near` / `-far`（ダーク側）。フォントは DotGothic16 で、
  **アプリはローカル同梱・ページは Google Fonts** から読む
- 三角の色 → **`images/logo.webp` の画素を数えて `#27C9FF`**。
  **UI の accent（`oklch(0.72 0.14 232)`）とは別の色**で、ロゴのほうが明るい
  （L 0.781 対 0.72）。同じ青系なので目では気づかない

**踏んだバグ**: 色を入れても三角が白いままだった。CSS に
`.logo-icon svg { stroke: #ffffff }` が残っており、SVG の
`stroke="currentColor"` は**属性なので CSS に負ける**。グラデの角丸箱に
白い線を載せていた頃の名残で、箱を外した後は
**「currentColor と書いてあるのに効かない」**形になっていた。
**一般化: 属性で書いた SVG の `stroke` / `fill` は、CSS に同名の宣言が
残っていないか数える。属性は効いているように見えて効かない。**

## Fuseforks の目的の裁定（2026-08-17 利用者）

**ホビー / 学習の対象であり、普及を狙う道具ではない。**

起点は [AionUi](https://github.com/iOfficeAI/AionUi) の発見（**32,036 stars・
fork 3,282・2025-08-07 作成 = 1 年先行**）。**その Team Mode が `plan` と構造的に
同じ**ことを確認したうえでの裁定 —「Leader が subtask へ割って Teammate へ
**並列委譲**・各自が**別モデル**・結果を**束ねる**・**作業フォルダを共有**」の
4 点が一致する。利用者の言葉 —「**先行しているプレイヤーがいるのは、むしろ
良いことだと思う**」「AionUI からはワイヤーや機能などを勉強できればいい」。

**帰結**: 公式 homebrew-cask を降ろした。**星の数は普及の指標であって実装の質では
ない** — AionUi 本体の 32,036 は GUI と配布と提携が集めたもので、**Rust の核
`aionrs` は 117 stars**。核どうしなら 117 対 1 で、まだ「1 年先行した小規模実装」の
規模だ。

**設計の差は 3 つあり、どれも能力ではなく構造**:

1. **委譲の相手。** AionUi の Teammate は**外部の CLI エージェント**
   （Claude Code / Codex / Gemini CLI…）+ 自前の `aionrs`。各 CLI のハーネスを
   借りるので**ベンダー機能を実装しなくてよい**（利用者の観察。当たっている）。
   **ただし得られるのは「API が持つもの」ではなく「その CLI が露出したもの」**で、
   CLI 作者の製品判断を継承する
2. **ループの所有。** 粒度がプロセスなので、**自分が所有していないプロセスの連鎖に
   トークンの天井は掛けられない**。Fuseforks が自分で持っているのは
   ターンループ / 周回上限 / 因果ごとの CAS 予約つき天井 / 打ち切りの伝播 /
   プロンプトキャッシュの境界打ち / RepeatGuard / ツールの提示集合
3. **トポロジーの寿命。** AionUi は Leader→Teammate をタスクごとに組む。
   Fuseforks は**人が引いた線が村の恒久的な性質**

**学習が目的だと、この比較は裏返る** — Spec 31/34/36/37 が残したのは機能ではなく
観測だった（`carries` 表の 20 マス / Meta は添付を期限なしで保存 /
gemini-3.7-flash は 19,000 トークン未満でキャッシュを返さない / 文書と実装が
4 回続けて食い違った）。**CLI をラップすると製品は手に入るが、この知識は 1 つも
手に入らない。**

### AionUi から読む対象（`aionrs`。Apache-2.0・Rust）

- **`crates/aion-compact`** — **会話の要約ではなくツール出力の圧縮**で、
  **LLM を 1 回も呼ばない純粋なテキスト変換**。`CompactLevel::{Off,Safe,Full}` で
  `sanitize → fold_repeated_lines → compact_json`。`fold` は**似た行が 3 行以上
  続いたら先頭と末尾だけ残して `[... N identical lines]` に畳む**（似ている =
  共通接頭辞が短いほうの 50% 以上）。**実測で cargo のビルド出力が 69.8%
  （約 30% 減）**。Fuseforks の `run` / `grep` / `fd` にそのまま効く形で、
  まだ何も持っていない
  - **私は最初これを「段階的な会話圧縮」と説明したが誤りだった** —
    ファイル名（`fold.rs` / `level.rs`）から推測して書いた。**読まずに名前から
    中身を語らない**
  - **最初の測定 1.0% も交絡だった** — GitHub のログはタイムスタンプ接頭辞が
    全行を「似ている」に寄せる。**生のコマンド出力で測り直して 69.8%**
  - **向こうのコードにこの村が 2 度踏んだのと同じ罠がある** —
    `common_prefix_len` は**文字数**を返すのに `min_len` は `a.len()` = **バイト長**。
    ASCII では一致するので気づかないが、**日本語は比が 1/3 になり閾値 0.5 が
    実質 1.5 になって永久に畳まれない**。`failures.md` の
    「`MAX_*_CHARS` を `len()` と突き合わせる差分は日本語では枠の 1/3 で発火する」
    がそのまま当たる。**別プロジェクトで独立に同じ罠**
- **`crates/aion-providers`** — `openai_responses_projector.rs` が
  **Spec 34/37 と同じ問題（同一エンドポイントで要求の形が違う）への別解**。
  Fuseforks は adapter が関数ポインタを渡す形（`encode(messages, part_for)`）で
  着地した。`bedrock.rs` は Fuseforks に無い 7 本目のワイヤ
- ~~**`toon` は未読**~~ → **2026-08-18 に読了。この村には当たらない**
  （`crates/aion-compact/src/toon.rs`・3,876 バイト）。**同型のフラットな JSON 配列を
  `[N]{k1,k2}:` + 行 へ畳み、鍵名を行ごとではなく 1 回だけ書く**のが効き目。
  適用条件が厳しく、**全要素が同じ鍵数のオブジェクトで入れ子がゼロ**でないと
  `None` を返して**丸ごと諦める**（部分変換をしないのは良い設計）。
  **この村のツール出力に同型の JSON 配列は無い** — 上位 3 つは `file` / MCP fetch /
  `rag` で全部が散文とソース。**`fold_repeated_lines` が実効 0.5% だったのと
  同じ結論・同じ理由**（機構ではなく、扱う出力の形が決めている）
- **テストの置き方**: `#[cfg(test)] #[path = "x_test.rs"] mod x_test;` で
  **モジュール宣言は inline のまま中身を隣接ファイルへ出す**。
  Fuseforks は inline の `#[cfg(test)] mod tests`。**`orchestrator.rs` の分割で
  `#[cfg(test)]` ブロックが範囲ごと別ファイルへ運ばれた事故**（分割の落とし穴 2）は、
  隣接ファイル方式なら起きない

## コストの実測（2026-08-17。`fuseforks.log` 8 日ぶん）

**起点は利用者 —「ツール出力を削減する機構はこっちも欲しいね。コストと戦って
いるので」**（AionUi の `aion-compact` を読んだ流れ）。**測ったら、コストは
まったく別の場所にあった。** 以後この領域を触るときは、ここの数字から始める。

**母数**: `fuseforks.log` の 2026-08-09〜08-17。**394 ターン / ツール呼び出し
1,029 回 / prompt 合計 34,153,868 トークン**。

### 実効コストの内訳（重みは `budget.rs` の 未キャッシュ×1 / キャッシュ×0.1 / 出力×4）

| | 実効トークン | 割合 |
|---|---|---|
| **uncached 入力** | 8,363,890 | **56.1%** |
| **出力 ×4** | 3,954,328 | **26.5%** |
| cached 入力 ×0.1 | 2,578,998 | 17.3% |
| 合計 | 14,897,216 | |

**出力の 62% は思考だった**（`reasoning=` を持つ 329 ターンで output 824,191 のうち
508,402）。**思考だけで実効の 17.0%、本文は 10.6%** — **思考が答えより高い。**

### キャッシュ率を決めているのは時間ではなく量（仮説を 2 つとも実測で棄却した）

全体は **75.5%**。台帳が記録していた 99% は最良ケースの数字で、**95% 以上に
届いているのは 35 ターン（8.9%）だけ**。

**棄却 1 — 「0% は会話の頭」**（利用者の仮説）: **会話の頭は 8 ターンしかない**
（uncached の 2.3%）。再起動しても会話は復元されており、頭からやり直していない。

**棄却 2 — 「前ターンからの時間で TTL が切れる」**（私の仮説）: 相関しない。
**3 時間以上空いた 79 ターンが 73.7% で、1 分以内の 54 ターン（66.2%）より高い。**

**駆動しているのは prompt の大きさ**（プロバイダに最小サイズがある。#104 の
「gemini-3.7 は 19,000 トークン未満でキャッシュを返さない」と同じ族）:

```text
prompt        turns     prompt 合計   cache%
<5k              10          39,851    20.6%
5-20k           124       1,537,743    34.7%
20-60k          146       5,510,377    63.6%
60-150k          66       6,151,301    70.4%
150k+            48      20,914,596    83.3%
```

### コストは散らばっていない

- **48 ターン（12.2%）が prompt の 61.2% を消費**
- **`rounds 10+` の 20 ターン（5%）が prompt の 39%**（周が増えるたびに
  メッセージ列全体が再送される構造の実測値）

**「1 ターン 86,000 トークン」は平均であって実像ではない。** 中央値の
`rounds` は 1 で、少数の巨大ターンが平均を作っている。

### 巨大ターンの正体は検索（利用者の見立てが当たった）

利用者 —「ほとんどが調査で使っているので web_fetch や search だと思います。
とくに grok でのコストは search になります」。

**`xai search:` 行が `ticks`（xAI の `cost_in_usd_ticks`）を持っている**ので、
検索の費用は**ログから直接読める**:

| | 8 日間 |
|---|---|
| 検索が走ったターン | 110 |
| tool 呼び出し | 1,068 |
| **sources 合計** | **2,795**（1 回平均 25 件） |
| ticks 合計 | 140,210,104,000 |

**`ticks` の単位は 1e-9 USD と読むのが唯一妥当**（1e-6 だと 14 万ドルで桁が
合わない）。すると **8 日で $140.21 / 1 検索ターンあたり $1.27**。
**裏付け**: xAI の Live Search は概ね 1,000 sources あたり $25 で、
2,795 × $0.025 = **$69.9** = ticks 合計のちょうど半分。残り半分がトークンと
読むと合う（`cost_in_usd_ticks` は要求全体の費用）。**この単位の読みは推論で、
一次資料には当てていない。**

**二重に払っている** — 検索そのものの sources 課金と、**返った 25 件の本文が
プロンプトへ注入されて入力トークンになる**分。時間で突き合わせると:

```text
                 turns      prompt    平均prompt   cache%
検索なし           232  14,912,673      64,278     73.5%
検索あり(600s以内) 178  20,176,581     113,351     77.0%
```

**検索ありは平均 1.8 倍**。ターン数では 43.4% だが **prompt の 57.5%** を占める。

### 結論: ツール出力の畳み込みは作らない

AionUi の `fold_repeated_lines` をこの村のツール出力へ当てて実測した:

| 形 | 残る割合 |
|---|---|
| `fd` の一覧 | **11.9%** |
| `grep` の結果 | 61.0% |
| `run`（ビルドログ） | 69.8% |
| `file` read（Rust ソース） | 96.3% |
| **散文（README）** | **100.0%** |

**この村のツール出力は上位 3 つ（`file` 615,277 字 / MCP fetch 368,329 /
`rag` 203,324）が全部「散文とソース」で畳めない。** 畳める `fd` / `grep` /
`run` は合わせて 18.5% しかなく、**全体では実効 0.5% 前後**。

**同じ機構でも扱う出力の形が違えば価値が桁で変わる** — AionUi は CLI
エージェントの出力（ビルドログ・コマンド出力）を扱うので効く。

**私の見積もりの誤りを 1 つ記録する**: 最初にツール出力を「1 回だけ乗る」として
数えたが誤りで、**ターンの中では以後の全周で再送される**（正しい倍率は概ね
`1 + 0.1 × 残り周回`）。0.2% → 0.5% になったが結論は変わらない。

### 未測定で残したもの

**25 sources のうち実際に引用された件数。** `sources=` は返ってきた数であって
`citations` に載った数ではない。**差があるなら件数を絞っても失うものは無い**が、
絞ると Spec 31 の「出典の網羅性」（`no_inline_citations` を常送する判断）と
衝突する。**測ってから決める。**

## 位置取りと、しばらくは調査が主になる（2026-08-17 利用者）

**「オーケストレーションとコワークの中間」を位置取りにする。**

- **オーケストレーションを学ぶ先**: LangGraph / Dify
- **デスクトップのコワークを学ぶ先**: [AionUi](https://github.com/iOfficeAI/AionUi)
- **評価基盤**: promptfoo（価値の層）/ otari（費用の層）

**しばらくは機能追加より調査が主**（利用者）。上の「Fuseforks の目的の裁定」
（ホビー / 学習）と整合する — **先行実装から何を学ぶかが作業の中心になる。**

**評価基盤の 2 つは層が違う**（2026-08-16 に整理済み。「現在地」の節が正）—
otari =「いくら払ったか」/ promptfoo =「払った価値があったか」。**今日の実測で
費用の層は数字が揃った**ので、次は価値の層が空いている。

## 「未キャッシュ入力」の正体はキャッシュ書き込みだった（2026-08-18 実測）

**[Spec 40](specs/40_cache-write-accounting.md) P1 の計器で初めて測れた。**
母数は `claude-sonnet-5` の 13 ラウンド。**この領域を触るときはここから始める。**

```text
prompt 合計 525,818 = cached 455,850 (86.7%) + cache_write 69,942 (13.3%)
                      + 素の新規入力 26 (0.005%)
```

**「未キャッシュ」として扱っていた 69,968 トークンの 100.0% がキャッシュ書き込み**で、
**素の新規入力は 1 ラウンドあたり 2 トークン**だった。

- **これは構造。** `place_message_breakpoint` が**毎ターン**最後のブロックへ境界を
  打つので、**そのターンで増えた分は全部「書き込み」**になり、境界より後ろだけが
  素の新規として残る。**標本の偏りではないので、母数を増やしても向きは変わらない**
- **帰結: Anthropic の個体では入力の課金が実効モデルの 1.61 倍**
  （write ×2.0 + 新 ×1.0 + read ×0.1 = 185,495 対 未 ×1.0 + cached ×0.1 = 115,553）。
  **同じ天井が実質 1.6 倍ゆるい**
- **重みは変えない**（2026-08-18 利用者裁定）—「厳密にするにはかなり大変だし、
  そこに労力を割く必要もない」。**直すと既存の村の `tokenBudget` を人が下げ直す
  必要があり**、その移行コストが精度に見合わない。**金額が要るなら実効トークンを
  使わず、`Record::Turn` の生の数に外部の単価表を当てる**（Spec 40 の付録）
- **1h TTL は端から端まで効いていた** — `cache_write` と `cache_write_1h` が
  **完全一致**（69,942 = 69,942）。`EXTENDED_CACHE_BETA` が生きている証拠で、
  **欄を持たなければ今も言えなかった**
- **2026-08-17 の「コストの実測」8 日ぶんは、この分解を持たずに取った数字**
  （uncached 入力が実効の 56.1%）。**あの 56.1% の中身は、Anthropic の個体に
  限れば ほぼ全部が書き込み**
- **OpenAI 互換の口も同じ構造だった**（2026-08-18 実測。`gpt-5.6-terra` の 9 ラウンドで
  prompt 124,803 = cached 108,604 + write 16,172 + 素の新規 **27**。
  **未キャッシュの 99.8% が書き込み**）。倍率は 1.25× と報告されている（**一次資料は
  未確認**）ので、入力の課金は実効モデルの**約 1.15 倍**。Anthropic の 1.61 倍ほど
  ではないが 0 ではない。**`cache_write_1h` が 0 なのは OpenAI の挙動の観測ではなく、
  こちらの decode が 0 を焼いているから**（OpenAI 形の usage に TTL 別の欄が無い）—
  **この 0 は OpenAI の TTL について何も言っていない**

## `cargo fmt` を走らせない（2026-08-18 に踏んだ）

**このリポジトリは rustfmt clean ではない。`rustfmt.toml` も無い。**
Spec 40 P1 の作業中に `cargo fmt` を 1 回走らせたら、**80 ファイル・
3,616 挿入 / 1,084 削除**になった（自分の変更は 25 ファイル・419 挿入）。
整形されたのは**変更と無関係な既存コード**で、`settle_turn(...)` の呼び出しや
`.to_owned()` の折り返しまで動いた。

- **戻すのに、意図した変更ごと当て直す羽目になった**（テストの fixture は
  正規表現で機械的に埋めていたので当て直せたが、doc コメントは書き直し）
- **差分が読めなくなるのが害**（この村は「差分が移動だと読める形に保つ」を
  分割の 6 箇条で決めている。整形の混入はその逆）
- **処方: 触ったファイルだけ手で整形する。`cargo fmt` は走らせない。**
  整形したいなら、**それだけを主題にしたコミット**を別に立てる

## 先行実装の調査（2026-08-18。**コードを読んだ。README ではない**）

利用者指示で 4 実装を横断。**位置取り（オーケストレーションとコワークの中間）の
確認と、台帳に残っていた宿題 3 件の回収**が目的。

| | ★ | 言語 | 作成 | この村への当たり |
|---|---|---|---|---|
| [LangGraph](https://github.com/langchain-ai/langgraph) | 39,920 | Python | 2023-08 | **費用の歯止めがゼロ** |
| [Dify](https://github.com/langgenius/dify) | 152,774 | TS | 2023-04 | 層が違う（テナント課金） |
| [AionUi](https://github.com/iOfficeAI/AionUi) / `aionrs` | 32,080 / 118 | TS / Rust | 2025-08 | `toon` は当たらない（宿題を閉じた） |
| [otari](https://github.com/mozilla-ai/otari) | 393 | Python | 2026-04 | **直撃。費用の型を先に解いている** |

**otari の実読が最大の収穫で、詳細は [Spec 40](specs/40_cache-write-accounting.md) の
「先行実装の読み」の節が正。** ここには次に触る人が要る 3 点だけ:

- **`ModelPricing` が 5 レート（入力 / 出力 / cache read / cache write /
  **cache write 1h**）+ `pricing_tiers`** を持つ。**査読が挙げた 2 点（1h 専用の
  レート・段階課金）が、文書ではなく他実装のコードで裏付けられた**。段は限界税率
  ではなく**崖**（閾値を超えたら要求全体が新レート）
- **`cache_write_1h` は `cache_write` の部分集合**。この符号化を採って
  **Spec 40 rev3 で D6 を訂正した**（rev2 の 2 欄式は独立な数が 2 つなのに 3 つ
  持っていた）。**単価も同じ形で落ちる** — `1h` の単価が無ければ `write` へ、
  それも無ければ基本入力へ。**未設定は「無視」ではなく「1 段上へ落とす」**ので、
  表が不完全でも**合計トークンは失われない**
- **1 点だけこの村のほうが単純だった** — otari は
  `cache_tokens_in_prompt: bool` で OpenAI 形（内数）と Anthropic 形（加算バケツ）を
  持ち回るが、**この村は `decode` で `prompt` を合計へ正規化済みなのでフラグが要らない**。
  **畳んで失っていたのは `cache_creation` だけで、規約の正規化そのものは
  最初から正しかった** — #40 の穴の射程が、この読みで正確に決まった

**LangGraph に費用の歯止めは 1 つも無い**（実測 — `token_budget` / `cost_limit` が
**0 件**、`spend` の 8 件は**全部テストファイル**）。大域の停止条件は
`recursion_limit`（スーパーステップ数）だけで、あとはノード単位の `TimeoutPolicy` /
`RetryPolicy`。**`Interrupt` は human-in-the-loop の一時停止**（`Command` で再開）で、
**Spec 10 の打ち切りとは別概念** — 混同すると「あちらにもあるので要らない」と読める。

**「ループの所有が構造的な差」という 2026-08-17 の裁定に、独立した裏付けが 1 つ付いた** —
**オーケストレーションの参照実装が、費用の歯止めを持っていない。**

**4 実装 + この村で、因果ごとのトークン天井を持つのはこの村だけ**だった。
otari は利用者単位の予算（予約 + reconcile）、Dify はテナント quota、LangGraph は無し。
**ただし優劣ではなく位置の差** — otari は**横断するゲートウェイ**、この村は
**ループの所有者**。「予約して精算する」という同じ形を、otari は請求境界で、
この村は `TurnSpend` で実装している（Spec 38 が otari の `inflight` と同型なのは
偶然ではなく、**同じ問題に同じ形が要る**）。

**Dify は層が違う** — `quota` 372 件 / `billing` 428 件と厚いが、**テナント単位の
課金枠**であって実行中の因果ごとの天井ではない。SaaS の請求層。

## LangGraph の実読と突き合わせ（2026-08-19。**コードを読んだ。README ではない**）

起点は利用者 —「LangGraph の機能を簡単に落とし込む。動的ルーティングとか。本番運用まで
されている LangGraph には何があり、Fuseforks には何が欠けてるか」。`langchain-ai/langgraph`
`1e44bda`（2026-08-18）を scratchpad へ浅くクローンし、3 領域（グラフ / 実行・永続化 /
prebuilt・CLI・SDK）を読み手 3 本で読ませ、根拠は file:line で取った。**サーバーの実行本体
（`langgraph_api`）は OSS リポジトリに含まれず（Platform 側は別配布）、`docs/` の本文も
このリポジトリには無い（別管理）** — 運用 API は SDK の引数から読んだ。

### 「動的ルーティング」は 3 つあり、全部「閉じた辺の中でコードが選ぶ」

| LangGraph | 実体（file:line は `libs/langgraph/langgraph/`） | Fuseforks の対応物 |
|---|---|---|
| `add_conditional_edges` | ルータは**コード**。戻り値は `path_map` / `Literal` 型ヒント / ノード名で**閉じた集合**に写され、外れると `ValueError`（`graph/_branch.py:146,192`） | **絆（トポロジー）が閉じた辺の集合**。選ぶのは **LLM**（`ask_*` / `plan` / `transfer_to_*` は `connected_agents` にしか生えない） |
| `Send(node, arg)` | 実行時に**同じノードの N インスタンス**を per-instance 状態で撒く（`pregel/_algo.py:442-448`、入力は `packet.arg` そのもの `:1060`）= map。reduce は**チャネルのリデューサ**が行う | `plan` = 接続先へ per-task 本文で撒く波。**同じ個体に N 並列は不可**（受信箱 1 本・ターン直列）。**reduce は進行役の LLM が束ねる**（コードではない） |
| `Command(goto / update / resume)` | ノードが次を指す + 共有状態を更新 + 割り込みの再開（`types.py:799`）。`goto` も `branch:to:<既存ノード>` への書き込みでしかなく、**辺を広げない**（`graph/state.py:1749`、未知ノードは warning で捨てる `_algo.py:978`） | `transfer_to_*` = goto / `ask_*` = call して戻る。**`update`（共有状態への書き込み）に当たるものが無い** — 黒板（ファイル全文上書き）と広場ログと `Memory.md` が代わり |

**LLM の生の文字列が直接エッジを選ぶ経路は LangGraph に無い** — LLM が選ぶ場合は必ず
ノードの中で `Command(goto=...)` / `Send` に包まれ、`path_map` / 既存ノードの検証を通る
（利用者の査読で言い回しを正確にした。`tools_condition` は `"tools" | "__end__"` の 2 値で、
モデルが選ぶのは `ToolNode` の中のどのツールか）。
**Fuseforks はその逆で、LLM が次の個体を選ぶ**（辺は人が引く）。同じ「閉じた集合」でも
選ぶ主体が違う — **これが位置取り（オーケストレーションとコワークの中間）の差そのもの**で、
欠けではない。**Fuseforks で LLM を呼ばずに選ぶ経路は、スケジュールの probe（stdout 1 行目の
完全一致で配送を決める・Spec 28）の 1 例だけ。**

### 層ごとの「あって / 欠けて」

| 層 | LangGraph（実測） | Fuseforks | 判定 |
|---|---|---|---|
| 共有状態 | チャネル + リデューサ。**リデューサ無しの鍵に 2 ノードが同じ周で書くと `InvalidUpdateError`**（`channels/last_value.py:60`）。`add_messages` は id で置換する append | 黒板（`file write` = 全文上書き・後勝ち）。衝突は**条例「1 人 1 ファイル」= 文言**で避けている。**`file` の `append` は既にある**（Spec 09 rev2・2026-07-31・`tools/file.rs:274`。不在なら新規作成・上書きゲート無し・上限は追記後の大きさ）— **条例がそれを知らない**（「write は全文上書きなので 2 人で書くと片方が消える」とだけ書いてある） | **欠けは op ではなく 2 つ**: (a) 条例の文言が `append` を数えていない (b) `write` / `sd` の read-modify-write は**読んでから変わっていても黙って後勝ち**（`overwrite: true` は「存在するか」しか見ない。LangGraph の `InvalidUpdateError` に当たる検出が無い）。**同じ問題を `run.json` は既に構造で解いている**（read → 差分適用 → `write_atomic`・3 回リトライ。Spec 15 Notes 4） |
| 部分グラフ / 名前空間 | compiled graph をそのまま node に（`graph/state.py:667`）、`checkpoint_ns` で入れ子 | 無し（村は平坦。`hop` が深さ） | 欠け。要る利用が無い |
| 実行モデル | Pregel の superstep。周回境界で状態反映。**`recursion_limit` の既定は版で動く** — 配布物で確認: 1.0.0 = 25 / 1.1.0 = 10000 / 1.2.0 以降 = 10007（読んだ 1.2.11 開発版は `_internal/_config.py:32` で 10007。「25」は 1.0 系の値で文書に残っている）、`GraphRecursionError` | ターンループ。**周回境界で cancel / budget / RepeatGuard / hop を検査** = superstep 境界と同じ形。上限は `max_tool_iterations` 36 / `max_hops` | 同型 |
| ノード再試行 | `RetryPolicy`（0.5s ×2 ≤128s・3 回・Connection / 5xx のみ。`types.py:418`） | `chat_with_backoff`（200ms ×2 ≤5s・`max_retries`・429 / 5xx / 空応答）— **LLM 呼び出しだけ**。ツール実行の再試行は無く、**逆向きの `RepeatGuard`**（同じ失敗の 3 回目を止める）を持つ | 方針の差。**こちらは「再試行を増やす」より「繰り返しを止める」側** |
| ノード timeout | `TimeoutPolicy`（協調的） | `run` の `timeoutSecs` / `ask_timeout` / probe の timeout | 同等 |
| ノード結果キャッシュ | `CachePolicy`（入力の pickle ハッシュ・TTL・InMemory / Redis） | **意図的に無し**（2026-08-04: 検索結果キャッシュは RepeatGuard の判定を壊すと却下） | 採らない |
| 耐久性 / checkpoint | `durability` = `sync` / `async`（既定）/ `exit`（`types.py:89`）。**superstep ごと**に checkpoint | `sessions.redb` は message 投入時 + exchange ターン完了時 = **`exit` 相当の粒度（ターン単位）**。飛行中に落ちると半端（許容と凍結） | **欠け: 周回単位の再開**。落ちたら頼み直し（予定は次回発火） |
| タイムトラベル | `get_state_history` / `update_state(as_node)` / `checkpoint_id` からの fork（`pregel/main.py:1480,2515`） | `fork_session(at_seq)` + 分岐で依頼文を入力欄へ（Spec 12）。**状態の編集（`update_state`）は無い** | 半分。人が過去の答えを書き換えて再開する経路は無い（会話ログの改竄に当たる。黒板の削除だけ） |
| HITL | `interrupt()` → `GraphInterrupt` → `Command(resume=)`。**ノードを先頭から再実行**して N 番目の `interrupt()` に N 番目の resume を渡す（`types.py:955-975`）。`interrupt_before / after` | Spec 10 の打ち切り（resume 無し）/ `run.json` の pending 承認（人が依頼し直す）/ **人への質問は「答えて turn を終え、次の発話が次の turn」= 会話そのものが resume** | **欠けではなく形の差**。ターンより長生きする状態を持たない判断（対話シェル A 案の見送りと同じ） |
| ストリーミング | 7 モード（`types.py:122`）、`get_stream_writer` | **無し**（非ストリーミング。Spec 37 で `stream` 不要と実測） | 意図的 |
| 村横断メモリ | `BaseStore`（名前空間タプル・ベクタ検索・TTL・`PostgresStore` + pgvector） | `Memory.md`（個体）+ 黒板（作業）+ `rag`（宣言フォルダの見出し索引） | **欠け: 村横断の KV + ベクタ検索**。lindera / tantivy 451 依存を避けた判断が生きている。Memoria 連携は個体 session で別線 |
| 実行時コンテキスト | `Runtime`（`context` / `store` / `stream_writer` / `previous`） | `ToolContext`（`work_dir` / `rag_roots` / `cancel` / `language`） | 同型 |
| 費用の計測と天井 | **コアには無し**（再確認。`token_budget` / `budget` / `usage_metadata` = 0 件、`cost` 4 件は全部コメント）。計測は `usage_metadata` を読むコールバック側（LangSmith 等）に委譲するのが定石（利用者の査読） | 因果ごとの天井 / CAS 予約 / 実効トークン / 単価を**ループの中に**持つ | 置き場の差（所有しているものの違い） |
| prebuilt | `create_react_agent` は **`@deprecated`**（`langchain.agents.create_agent` へ。`chat_agent_executor.py:274`）。`ToolNode` は並列実行・`return_direct` / `remaining_steps` 警告 / `response_format` / pre・post hook / `wrap_tool_call` / `InjectedState` | ターンループが react loop そのもの。並列ツール 1 周に複数本 / 打ち切り文で次の手を書く（`remaining_steps` 相当）/ Structured Output は却下済み / 封筒と可変文脈の畳みが固定の pre hook / RepeatGuard・`decide`・理由が固定の `wrap_tool_call` | 同等。**supervisor / swarm / handoff は LangGraph 本体に無い**（別 repo） |
| 配布 / 運用面 | `langgraph.json`（`http` に **MCP エンドポイントのトグル**あり `schemas.py:471`）/ `langgraph dev`（閉源の `langgraph_api` が要る）/ `up` = redis + pgvector + api の compose / `deploy` = LangSmith へ | デスクトップ（winget / brew / MSI）。MCP サーバー（Spec 25） | 層が違う |
| サーバー API（SDK から読んだ。本体は閉源） | assistants（**版つき設定**）/ threads / runs（background・stream・wait・**`multitask_strategy` = reject・interrupt・rollback・enqueue**・`if_not_exists`・**`after_seconds`**・**`webhook`**・`on_disconnect`）/ crons / store / **資源 × 動作ごとの auth ハンドラ** / encryption plugin | `ask_fuseforks` 1 本（**同時 1 本 = `reject` 固定**・D7）/ schedules（cron + probe + sessionMode + summarizeAfter）/ セッション API / 合鍵 1 本 + loopback | **欠け: 完了 webhook・enqueue（待ち行列）・1 回きりの遅延実行（`after_seconds`）・背景 run + join・版つき雛形** |
| 可観測性 | LangSmith tracing（`langsmith_tracing` / API キー）。OTel / `/metrics` は 3 lib に無い | `fuseforks.log` の計器 + `sessions.redb` + 統計画面 | 方式の差 |

### 落とし込むなら（私の見立て。裁定は利用者）

1. **「動的ルーティング」は既にある** — 欠けているのは**ルータそのもの**ではなく
   **「LLM を呼ばずに選ぶ経路」**。LangGraph 流に足すなら、スケジュールの probe と同じ形
   （コマンドの stdout で宛先を決める）を**発話の配送**にも開く形になる。
   ただし**線は人が引く・選ぶのは LLM** がこの村の位置取りなので、足すなら「前判定」止まり。
   **→ 2026-08-22 に材料が出た**（下の「スイッチ」の節）。**動的ルーティングは採らない**
   裁定で、代わりに **LLM が書いた判定コードを走らせ、走るときに LLM は居ない**形へ動いた
2. **リデューサ** — 黒板の後勝ちを文言（条例）で避けているのが、LangGraph と比べて
   最も弱い所。~~**`file` に追記専用の op を足す**~~ → **数えたら既にあった**
   （2026-08-19 同日。`append` は Spec 09 rev2 から在る。**実装を読まずに名前から
   「無い」と書いた** — aion-compact を「段階的な会話圧縮」と説明した誤りと同じ形）。
   実際の穴は上の表の (a)(b)。**(a) は条例へ 1 行（コード 0）**、(b) は `write` に
   「読んだときから変わっていないか」の検査を足す形（`run.json` の前例あり）だが、
   **2 人で同じファイルを書く事故はまだ 1 度も観測していない**（2026-08-11 の黒板の
   事故は stale であって lost update ではない）ので、頻度を見てから。
   **→ 2026-08-29 に処方の形を確定**（リデューサもワークツリーも採らず CAS 検査
   1 本。「設計の材料 3 件」の節が正）
3. **運用 API の 3 つ**（完了 webhook / enqueue / `after_seconds`）は、Spec 25 の MCP サーバーと
   Spec 28 の予定に**それぞれ 1 欄足す**規模。需要が出てから
4. **採らない**: ノード結果キャッシュ（却下済み）/ ストリーミング（意図的）/ 周回単位の
   checkpoint（`exchange` の粒度を変える大工事で、得るのは「落ちたら頼み直し」の解消だけ）

## スイッチ（未起票・材料。2026-08-22）

起点は利用者 —「スイッチというのを作りたい。三叉でプログラムで値を判定して真だと
A に行き偽だと B に行く。真のほうだけつなげて偽に繋がっていないと単なる ON・OFF の
スイッチ」。**その日のうちに形が動いた**ので、動いた後の側を書く。**起票はしない**
（利用者 —「もっと考えてみる」）。

- **動的ルーティングは採らない**（利用者裁定）—「単なる LLM をファンクションに
  しているだけ」。**採るのは逆で、LLM に判定コードを書かせ、走るときに LLM は
  居ない**形。同じ入力に同じ分岐が返るので、**会話だけの連携が壊れる理由
  （毎回違う判断をする）が構造的に消える**。ルータが誰かで 3 つに割れる —
  LangGraph は**コード**（人が書く）/ この村の現状は **LLM**（閉じた集合から選ぶ）/
  この案は **LLM が書いたコード**。**→ 2026-08-30 に 4 つ目が増えた** —
  **出力の統計量**（semantic entropy。arXiv:2608.14707。「外部研究の受領
  （2026-08-30）」の節が正 — 分類は足す・機構は採らない）
- **「一度」コードに起こすことが、実装の値段を桁で分ける。** probe の端末承認は
  `SHA-256(canonical_json({args, command, cwd, villageId}))` で、**コマンド行が
  1 文字変われば承認が外れる**。**毎ターン書き直す設計ではハッシュ承認は原理的に
  効かない**（内容が毎回変わるものを事前に承認できない）。一度書いて人が承認し
  以後は固定なら、**probe の枠組みが 100% そのまま乗り、新しい承認機構が 0 個**
- **1:1 のスイッチは 2026-08-08 から動いている** — スケジュールの前判定
  （`ScheduleProbe { command, args, expect, timeout_secs, cwd }`）。`expect` は
  **stdout 1 行目（トリム後）の完全一致**で、一致すれば `to` へ配送・不一致なら
  何もしない（**exit code は判定に使わない**）。**三叉にする最小形はこの構造体へ
  「不一致のときの宛先」を 1 欄**で、新しいノード型も地図の変更も IPC の新設も
  要らない。**ただし掛かるのはスケジュールの発火だけ** — 配送は 5 経路ある
  （人の発話 / `ask_*` / `transfer_to_*` / `plan` の波 / スケジュール）
- **スイッチャーは絆マップの外に置く**（利用者判断）。**利用者ノードを地図に
  出さない裁定（2026-08-05）と同型** — どの辺にも掛かりうるものをノードにすると、
  辺を N 本増やして情報を 0 しか増やさない。外に置くと 3 つの問題が同時に消える:
  地図のノードが `AgentId` 前提（`topologyPositions` / `TopologyEdge` /
  `list_topology` / `layouts.nodes`）/ `connected_agents` が**ツールの提示集合**
  そのもの（`ask_agent_N` / `transfer_to_*` は接続先にしか生えない）/
  **提示集合と配送経路が初めて食い違う**（Spec 20 で同一性を確認して回帰を張った）
- **辺は色ではなく破線で示す。** 2026-08-05 に**辺の既定色を accent にし、ホバーは
  色を変えず発光させる**へ直しており（`failures.md` #65）、**色は既に「通常の辺」を
  意味している**。別色にすると色が 2 つの意味を運ぶ
- **on / off の 2 本の辺は述語の複製ではなく参照。** 辺が持つのは
  `switcher_id` + どちらの枝か だけで、**役職が `role_id` で名前を指すのと同じ形**
  （コピーと参照の分割）。スイッチャーを直せば両方の辺が追従する。**見栄えの解も
  参照から出る** — 片方をホバーすると両方が発光する形なら、三叉ノードが視覚的に
  やっていた「1 つの述語から 2 本出ている」をノードなしで表せる
- **未決 2 つ** — (a) **5 本の配送経路のどれに掛けるか** (b) **判定器が流れている
  本文を読むなら渡し方**（**→ 2026-08-27 の追記に第三案**）。argv も stdin も採れない（**閉じた許容は stdin に
  掛からない**。こちらの `shlex` と向こうの bash は別のパーサで、`ls -l; rm -rf /`
  は `cmd=ls` に見えるが向こうは 2 つ実行する = `run` が 3 rev かけて退けた線）。
  **本文を一時ファイルへ書きパスだけを渡す**形なら、文字列がシェルの解釈に一度も
  触れない（`@` 補完が中身を入れず参照だけ渡すのと同じ判断）
- **コードから村を制御する経路は今 1 本だけ** — 自分の MCP サーバー（Spec 25）。
  ただし **D7 で同時外部依頼は 1 本**に固定されており、スイッチャーがそこを通るなら
  この上限とぶつかる
- **LLM に判定させる版を入れるなら、計器は起票の前提。** 振り分け役のサーヴァントを
  1 体置く形との差は 3 つだけ（履歴を持たない / ターンを消費しない / 人格を持たない）で、
  **費用は掛かる**。`TurnSpend` に載せないと `failures.md` #103 を新しい経路で
  開け直す（払っているのにカードの累計・予算・`turn:` 行のどれにも出ない）

**元スレッド 2 本との突き合わせ（2026-08-27 追記。実読）**: 議論の元になった
Outcasts のスレッド 2 本を読んだ —
評価軸（https://outcasts.jp/thread/84f37b5a-d949-45fe-91ab-bdfb99acc613 「完走率より
人間の介入時間で測るべき」）と、判定器の設計
（https://outcasts.jp/thread/97b9a37a-c1b7-41a2-a63c-92d9702af13a 「自己生成テキストが
コンテキストを埋めるとどこから性能が落ちるか」）。**「スイッチ」「三叉」の語は
どちらにも無い**。スイッチへ効くのは後者の判定器の議論 3 点:

- **「不一致のときの宛先」が受けるのは `no_match` だけ。** probe の結末は
  `match / no_match / error / timeout / unapproved` の 5 値が既に分かれている
  （`schedule_probe.rs`）。三叉を「else の宛先」と書くと `error` / `timeout` まで
  偽側へ流れ、**検証機構が壊れたときだけ検証をすり抜ける**（eigent の採点器が
  3 回失敗で `quality_score=80` を捏造して合格へ倒す fail-open と同じ形）。
  **判定不能の 3 値は宛先を持たず「何もしない + 記録」のまま残す。** スレッドの
  「判定器が壊れたら合格側に倒さず `unknown` として別集計・判定カバレッジを併記」が
  この線の外部裏付け
- **未決 (b) の第三案 = 主張レコード。** 発話を「対象・属性・値・有効期間・出典・
  状態」のレコードに分解し、**判定器はレコード同士の関係だけを見る**（散文を
  読まない）。Spec 08 の凍結「分類は文言 parse でなく型で運ぶ」と同じ結論への
  独立収束で、`reply: to=` / `refusal=` の計器が既にこの形。ただし全面採用は
  発話へ構造化フィールドを要求する = ワイヤと条例の両方に触るので重い。
  **折衷は「本文を一時ファイルへ書き、判定器が読むのは先頭 1 行か特定の鍵だけ」** —
  probe が stdout 1 行目しか見ない規律を、入力側にも敷く
- **「判定器の精度とカバレッジを先に固定してから測る」の対応物はハッシュ承認。**
  probe 型のスイッチは判定が決定的（完全一致）なので精度の校正が要らず、
  **人が判定コードを 1 回見て承認し以後固定**、が校正に当たる。「承認機構 0 個で
  乗れる」の見立てに、評価設計の側から根拠が 1 つ足された

**未決 (a)（5 配送経路のどれに掛けるか）にスレッドは触れていない** — 利用者の
裁定待ちのまま。前者のスレッド（介入時間）はスイッチではなく**評価基盤の
「完遂」の軸**の隣で、利用者自身が「完全自律運営で統計を取って試してみます」と
返信している。後者の注入・訂正テスト（誤情報の保持時間 / 訂正反映までの遅延 /
旧事実の再出現率）は**価値の層**の材料で、ザリが「次の一区切りで測って共有」と
公開の場で応じている — 完遂の軸を数える作業と地続き。

## 設計の材料 3 件（2026-08-29 の議論。未起票）

利用者との設計議論 3 往復の記録。裁定が落ちたものと構想の段階のものを分けて書く。

### 黒板の並行書き込み: リデューサもワークツリーも採らず、write に CAS 検査 1 本（頻度ゲートの向こう側）

LangGraph 突き合わせの穴 (b)（`write` / `sd` の read-modify-write は読んでから
変わっていても黙って後勝ち）の**処方の形を確定した**。実装はしない —
lost update の観測は今もゼロ（2026-08-11 の黒板は stale であって lost update
ではない）。

- **リデューサは採らない** — 型付き共有状態が前提。黒板は Markdown の散文で、
  散文に健全なマージ意味論は append しか無く、それは既にある（Spec 09 rev2）。
  `InvalidUpdateError` は検知だけで解決せず、ターン建ての系では書き込み時 CAS と等価
- **git ワークツリー（Anthropic の方向）も採らない。** 構造的不一致 3 つ:
  (1) **work_dir は任意のフォルダでありリポジトリではない**（`D:\ManualeRAG` の
  実例。`git2` は唯一の C 依存として外した経緯・gix は依存が大きい）
  (2) **マージの所有者が居ない** — 束ねは進行役が**答え**を束ねる機構で
  **ファイル**を束ねない。日本語の散文は行マージと相性が最悪で、村の誰も
  構造的に解けない失敗形が生まれる (3) **寿命がターンより長い**（対話シェル
  A 案を見送った「所有者の再設計」と同じ）
- **「所有で分ける」は既に実装済み** — 付箋 1 人 1 ファイル / 指示書は依頼側のみ =
  ワークツリーと同じ形を git 無しでファイル粒度に敷いてある。欠けているのは
  規約が破れたときの網だけ（文言は保証ではない = #84）
- **観測が出たときの処方**: `file write` / `sd` に「読んだときから変わって
  いないか」の検査（読み時の指紋を渡し、変わっていたら**名指しで拒否して
  次の手を書く**）。前例は `run.json` の read → 差分適用 → `write_atomic`。
  黙った後勝ちを名前のある拒否へ変える — RepeatGuard・閉じた許容と同じ系譜で、
  マージの判断は LLM と人に残る

### Pull 型タスクキューは作らない（裁定）

起点は利用者 —「スケジュールで外部の GitHub / Jira / Backlog を監視し、
タスクがあがれば処理する。内部で持つ必要はない」。**同意して裁定。**

- **キューの 3 動詞（保持 / 占有 / 完了確認）は外部トラッカーが全部持っている**
  （open/closed・assignee とラベル・close）。内部に複製すると**真実が 2 つ**に
  なる（自動要約の裁定「状態が 2 箇所に住む」と同型）
- **既存部品で足りる**: probe = トークンゼロのポーリング / stdout 2 行目以降 =
  タスク本文の運搬 / **受信箱がそのまま村内バッファ** / アプリを閉じている間の
  耐久性は**外部トラッカーが無償で持つ**（デーモンでないことがむしろ外置きの根拠）
- **作らない代わりに数える罠 3 つ**: (1) **占有も外に置く** — 再処理の防止は
  probe コマンドのラベル除外 + 処理した個体が `run` でラベルを書き戻す。
  冪等性はトラッカーのラベル設計（条例 / 指示書の運用・コード 0）
  (2) **毒タスクのループは発火をまたぐ** — RepeatGuard はターン内の歯止め。
  1 発火の費用は `tokenBudget` が抑えるが**周期 × 予算で漏れ続ける**。処方は
  失敗ラベル → probe の除外。検出は完遂の軸（`reply: to=` / `refusal=`）
  (3) **宛先が停止中だと probe は走らず、ログも沈黙する**（Spec 28 の実測）

### core 単独実行の構想（GUI = 設計面 / コンテナ = 実行面。**構想の段階**）

利用者 —「**coreだけで回せられる様になりたい。GUI で配線を引いて設定ファイルを
作ったら Core と設定フォルダを docker コンテナにして回す**」。前段の議論 —
ヘッドレス実行とチェックポイント永続化の 2 欠けは「無人での耐久性」1 つに
畳まれ、それは**ライブラリの機能ではなく製品ティア**（LangGraph も運用層は
閉源の Platform 側）。方向は**「エンジンにならず、最良の一段になる」** —
耐久性とイベントは外部エンジン（compose / k8s / Actions）に所有させる。

**既にできている半分** — 村は最初からフォルダ / 合鍵は workspace の外
（「村を配っても扉は開かない」がそのまま「イメージに秘密を焼かずデプロイ時に
注入」になる）/ キーは keyring 参照で村に値が入らない / `fuseforks.log` は
ファイル計器でヘッドレスでも生きる。

**ギャップ 6 つ**（起票するならこの順で数える）:

1. **ホストバイナリ** — `mcp_server.rs` と `state.rs` の起動配線は **GUI 層に
   住んでいる**。`fuseforksd` へ持ち上げる工事（`fuseforks-core` 切り出しと
   orchestrator 分割の前例に乗る）
2. **`batch_start_invariant` と正面衝突** — ヘッドレスは自動起動しないと
   永遠に何も走らない。**伏線が既にある**: システム設定候補「起動時に一括起動」の
   「既定 OFF の明示オプトインなら不変条件の理由（開いただけで課金しない）は
   保てる」。コンテナのデプロイは「開いただけ」ではなく明示の意図。
   **この小 Spec が構想全体の先行条件**
3. **扉の bind 127.0.0.1 固定** — コンテナ内の loopback は外から届かない。
   bind を設定可能にし、境界を合鍵 + コンテナのネットワーク境界へ移す判断が要る
4. **`probe_approvals.json` の焼き込み** — 今は `{app_data_dir}` に居て GUI の
   IPC だけが書く。設計時に承認してデプロイ物へ含める形へ（ハッシュ承認は
   「人がこのコマンドを見て承認した」の証明で、どの端末かは本質ではない）
5. **workspace の排他所有** — redb は単一書き手で、**#112（3 世代が同じ
   `world.json` を触って欄が消えた）がこの構想の最悪形の予告**。GUI とコンテナが
   同じフォルダを同時に開く経路を構造で塞ぐ。示唆されるのは design
   （`world.json`）/ runtime（`sessions.redb`）の状態分離を bake の段で行うこと
6. **GUI 前提機能の design-time lint** — 計画の編集窓 ON の個体が居る村を
   ヘッドレスで回すと**波が黙って永遠に承認待ちする**。起動時に「この村は
   GUI が要る設定を含む」と名指しで拒否する（沈黙を作らない規律）

**スコープ外**: チェックポイント（コンテナ再起動 + スケジュールの次回発火 +
外部エンジンの再試行が粗い粒度の耐久性を持つ。「落ちたら頼み直し」が外部の
機構に頼み直させる形になる）。

**安く拾うもの 2 つ**: 呼ばれる側の 3 欄（完了 webhook / enqueue /
`after_seconds` — LangGraph 突き合わせで 1 欄ずつと見積もり済み。エンジンの
外注が現実になった時点で需要が立つ）/ **クラッシュ孤児の会計** — 飛行中に
プロセスごと落ちると**プロバイダには課金され `turn:` 行は 1 本も残らない**
（`TurnSpend` はメモリの登録簿）。ターン開始の意図を 1 行永続化し、次回起動で
「精算されなかったターンが N 本」と申告する。#103 の系譜の計器であって機構ではない。

## 外部研究の受領（2026-08-30。**未読 — 利用者の alphaXiv 要約と台帳を突き合わせた**）

利用者が alphaXiv で集めた 2026 年 7〜8 月のオーケストレーション研究 5 本。
**私は原文を 1 本も読んでいない**（2026-08-04 の受領と同じ扱い — 以下の主張は
要約に載っていたもので、独立に確かめていない）。突き合わせの結論:
**3 本は村が既に下した判断の外部裏付け、1 本は生きている裁定と衝突、
1 本だけ新しい道具を持つ**。

| arXiv | 主張（要約より） | 村の対応物 | 判定 |
|---|---|---|---|
| 2607.25656 OrchBench | 成否は数や並列度でなく**依存タスク間の情報転送**。計画だけの決定論シミュレーションが実行品質と r=0.816・コスト 1.3% | 分解の 2 原則 / 指示書 / `room_log` pull / 「単一に戻す判断」= Spec 25 の起点 | 裏付け + **新しい道具** |
| 2608.00685 When Does Orchestration Pay Off | Debate は CoT を上回らず・改善はモデル依存でトークン 2〜4 倍・**難しいほど効く傾向は無い** | ICPO（2604.27891）と同じ向き / VMAO の Replan を ×8.5 で退けた裁定 / 検証は 1 巡まで（条例） | 裏付け |
| 2608.14707 Semantic Uncertainty-Guided | 出力の意味的不確実性で再プロンプト・担当変更を**動的に発火** | **2026-08-22 の裁定「動的ルーティングは採らない」と衝突** | 衝突（下記） |
| 2608.22657 Physical Agentic AI | LLM は計画だけ・決定論ランタイムが毎アクションを検証（検証ゲートで誤ディスパッチ 23〜29% → 0%） | 閉じた許容の `run`（argv・シェル非経由）/ probe のハッシュ承認 / **Spec 43 の編集窓**（「人が見たデータがそのまま配送され、承認後の経路に LLM は居ない」— 同じ分離を先に書いていた）/ eigent fail-open の反面教師 | 裏付け |
| 2607.06906 Harness Effect | オーケストレーションは**単価を決めるランタイム層**。ハーネス設計だけでコスト 41% 減 | **チェックリスト 6 項目を村は 6/6 で実装済み**（下記） | 裏付け + 方法論の一致 |

- **Harness Effect の 6 項目と村の対応**: キャッシュを壊さない（#42/#45・実測
  99%）/ 履歴の制限（滑る窓 8 往復）/ ツール出力をコンテキスト外に
  （ツール結果は 1 ターン限り・添付 D1・`room_log` pull）/ 文脈隔離
  （個体ごとの履歴 + 広場ログは抜粋）/ ポーリングしない（`deliver_and_wait` は
  待ち・probe はトークンゼロ前判定）/ ループ制限（RepeatGuard・周回 36・hop・
  予算・Spec 44 の輪の拒否）。**「オーケストレーション層のコストを独立に測れ」
  という方法論は、2026-08-17 の 8 日実測と Spec 39〜41 が既にやったこと**
- **衝突の中身（2608.14707）**: semantic entropy は「LLM の判断」ではなく
  「出力の統計量」なので、**スイッチ節のルータ分類に 4 つ目が増える** —
  人が書くコード（LangGraph）/ LLM（この村の現状）/ LLM が書いたコード
  （スイッチ案）/ **出力の統計量**。ただし entropy の算出は同じ問いへ N 回
  答えさせるサンプリングが要り、**思考だけで実効の 17% を払っている実測の上に
  毎ターン N 倍を乗せる**。効果が示されたのも TruthfulQA / JailbreakBench =
  まだ建てていない価値の層。**分類は足す・機構は採らない**（裁定は不変）
- **新しい道具（2607.25656）**: 計画だけを決定論的に評価する形は、
  **`specs/tla/` がやったことのオーケストレーション版**（構造だけを、払わずに
  検証する）。Spec 43 の編集窓は同じ問いへの**人間版の答え**（機械の採点の
  代わりに人が計画を見る）。評価基盤の「LLM 判定は最後、機械で決まる軸を先に」
  という向きが、計画の層でも成立する外部証拠。**作る話ではない**（ホビー /
  学習の裁定・評価基盤は完遂の軸が先）— 読むならこれを 1 本目に
- **潮流の一文「必要なときだけ協調させ、情報の流れと実行権限を明示的に
  管理する」は 2026-08-17 の位置取り裁定とほぼ同文** — 村は流行を追わずに
  独立で同じ場所へ着地していたことが、日付つきで台帳から示せる

### Prime Agent と Governed Shared Memory を実読した（2026-08-30。**alphaXiv の構造化レポートで読んだ — 生の全文ではない**）

**Prime Agent（arXiv:2608.23552・Prime Intellect）**: 同じ Opus 5 が
ハーネスの差だけで ARC-AGI-3 を **30% → 95.5%**（人間 95.4% と同等）。
質を上げる機構を 3 梃子に分解し、**行き先を 3 つに分けた**（2026-08-30 利用者
「spec 化しましょう」）:

1. **ターンごとの終了条件テスト（autonomous mode = 予算 + 決定論テスト +
   通るまで続行）→ [Spec 46](specs/46_acceptance-probe.md) として起票**。
   村は部品を既に持っていた — 予算 = `tokenBudget` / 周期発火 = スケジュール /
   決定論テスト = probe。**欠けは「通るまで続ける」の輪だけ**
2. **Refinement（経験を手順書へ自動昇格）→ 条例の運用で開始（コード 0）**。
   **Factorio の 7 日走行で、エージェントが RCON の資源湧かしエクスプロイトを
   発見し、refinement がそれを有用なスキルとして保存した** — 自動昇格の危険
   として村が予測した失敗（2026-08-20「既定 OFF・手動ボタン」裁定）が
   **他人の実機で起きた**。手動ゲートの外部証拠。ボタンの機構化は頻度を見てから
   （黒板と同じ道 — 条例で運用開始し、機構は要るときに）
3. **永続 REPL（L2 — ターンをまたぐ IPython・値はコンテキスト外に住む）→
   採らない**。out-of-loop 実験（Claude Code 比 6 倍）で質に効くのは本当だが、
   自由な Python 実行は**閉じた許容の対極**（`run` が 3 rev かけて退けた
   汎用インタプリタ）で、寿命がターンより長い。村の代替は `run` + ファイル

ほか 2 点: **daemon 所有のセッション（クライアントと独立に生き、人は
attach/detach）は core 単独実行の構想（2026-08-29）の参照実装そのもの** —
`fuseforksd` を起票する日の実読第 1 候補。資源会計が root + 子孫セッションで
集計されるのは村の因果ごとの予算と同じ形。

**Governed Shared Memory（arXiv:2606.24535・Caura.ai + BGU）**: fleet-memory の
失敗モード 4 分類（unauthorized leakage / stale propagation / contradiction
persistence / provenance collapse）を黒板に当てると — 漏洩は work_dir の
canonicalize + 前方一致で構造的に閉じ、**stale は 2026-08-11 の黒板の事故
そのもの**、矛盾は機構なし（束ね / 検証役が散文で解く）、**来歴は 1 人 1
ファイル = ファイル名が来歴で最初から解けている**。

- **持ち帰る罠**: 「**重複排除が矛盾検出を飢えさせる**」— 同期の近似重複
  ゲートが、矛盾する書き込み（意味的に似ている）を非同期の矛盾検出器へ届く前に
  弾いた（400 中 206 が 409 で拒否・検出率 49%）。一般化:
  **同期の門が後段の検出器の入力を食う — write-path の 2 つのゲートは処理順が
  意味を持つ**
- **村の設計の裏付け 2 つ**: (a) スコープ強制が GET-by-id と検索の 2 経路で
  割れていた（検索の leak 率 0.439）— **可視性の述語が経路ごとに 2 実装に
  割れると片方が漏れる** = Spec 22 の「可視述語 1 実装共有」が先回りした穴
  (b) メモリをサービスにする値段 — strong write で p50 1.8 秒 / p99 19 秒。
  **村の「ファイル + 所有」は同じ保証の大半をゼロ円で買っている**

### 2608.25277 の「調整メッセージが 40〜60%」を実測で検証した（2026-08-30。**成立しない — 村では 0.5%**）

**この 1 本だけは原文を読んだ**（alphaXiv の全文）うえで、`fuseforks.log` の
2026-08-09〜08-30・**709 ターン / Σtotal 73,097,455 トークン**で測った。

| | 実測 |
|---|---|
| 個体間メッセージ（受信 212 通 + 個体宛返信 124 通 + 束ね 22 本） | **358,209 字** |
| 対 Σtotal（1.0 tok/字。0.7〜1.3 で感度） | **0.49%**（0.34〜0.64%） |
| ターン内再送を掛けた増幅版（chars × rounds） | 0.53% |
| 委譲で始まったターンの総支出（粗い上界） | 37.6%（187/709 ターン） |

- **論文の 40〜60% は会話中心の MAS（AutoGen / CAMEL 系 — 会話そのものが
  仕事の媒体）の数字**で、この村には桁 2 つ移らない。理由は構造 — 村の支出は
  システムプロンプト・ツール schema・ツール出力（検索の注入）・履歴の再送が
  占め、**個体間の文章は設計で既に絞り出されている**（指示書 = パスの参照渡し /
  ツール結果は履歴外 / 黒板 pull / cached 78.1%）
- **帰結: 委譲文の圧縮（typed graph の 2〜3×）は村では約 0.3% しか救わない。
  採らない。** 封筒は無料で、高いのは中の仕事（委譲起点のターンが支出の 37.6%）
- **移るのは品質側の発見** — 「マルチエージェント失敗の **76% は個体間の
  ミスアライメント**（順序の取り違え・前提の脱落・曖昧な指示のループ）」。
  これは**分解の 2 原則（自己完結・最終アクション保護・動詞で終わるな）の
  外部裏付け**で、条例の側が既に同じ穴を塞ぎに行っている。
  「schema だけ渡して解釈の案内が無いと効果ゼロ」（graph-aware executor
  prompt が必須）も、受け手側の案内が機構の半分だという同じ形
- 測定の限界: 字→トークンは概算（0.7〜1.3 で感度を取った）/ 滑る窓による
  後続ターンへの残存は数えていない（大半 cached なので ×10 でも 5% 止まり）/
  封筒の定型（【送り手:…】）は chars= の外

## eigent の実読と突き合わせ（2026-08-24。**コードを読んだ。README ではない**）

起点は利用者 —「eigent を調査したい。Fuseforks は単発エージェントのワークフローに
弱い。ワークフロー処理で参考にできる所があれば取り入れたい」。`D:\Github\eigent`
（Electron + React 107K 行 / Python backend 36K 行）と、土台の
`camel-ai==0.2.91a5` の wheel から `camel/societies/workforce/`（13K 行）を
scratchpad へ展開し、読み手 4 本（eigent 3 + upstream camel 1）で読んだ。
根拠の file:line は会話ログが正。

- **「Claude Code を参考」は半分だけ** — repo 全体で `Claude Code` への言及
  **0 件**（利用者の grep でも同じ）。**Skills 機構だけが SKILL.md 形式のほぼ丸移植**
  （frontmatter + scripts/references/assets の三分類・skill-creator 同型・探索パスに
  `.agents/skills`）。ハーネス本体は **CAMEL Workforce の薄いラッパー**
  （`chat_service.py` 2,901 行 + `utils/workforce.py` 1,106 行が上書き層で、
  差し込むのは SSE 通知・人間の編集窓・タイムアウト）
- **完遂の保証はどちらも無い** — 4 ヶ月前のレビュー症状（「生成しました→無い」
  「別のエージェントのせい」）は構造として温存。品質検査は LLM 採点で、**採点器が
  3 回失敗すると `quality_score=80` を捏造して合格へ倒す fail-open**
  （eigent 側 `workforce.py:166-173`）。**Task 初期値 FAILED の fail-closed 思想と
  同一 repo 内で自己矛盾**しており、これがレビュー症状の再現条件
- **コスト機構に取るものは無い** — トークン予算・金額とも repo に存在せず、歯止めは
  時間（step 1800s / サブタスク 3600s）と 200K 字の自動圧縮だけ。
  **5 実装目（LangGraph / Dify / AionUi / otari / eigent）でも、因果ごとの
  トークン天井 + CAS 予約を持つのはこの村だけ**

### 取り込み 4 点（2026-08-24 利用者裁定。安い順）

1. **分解の 2 原則を条例へ（コード 0・最優先）** — camel の分解プロンプトから:
   **サブタスクの自己完結**（「最初のタスク」「上記の〜」の相対参照を禁ずる。
   実行者はその 1 行しか知らない前提で書く）/ **最終アクション保護**（送信・提出・
   ファイル作成・返信という最終段を分解で落とすことを禁ずる）/ **動詞で終わるな**
   （「分析する」「調べる」で終えず、出力の形式・内容・出力先まで指定）。
   **条例差分は利用者が用意する**。最終アクション保護は「依頼主に返さない」の分解版
2. **完遂の安い機械検証は計器から** — camel は「DONE と言われても信じない」を
   2 段で持つ（LLM 不要のヒューリスティックが先・LLM 採点が後。
   `validate_task_content` は 10 フレーズ + 先頭語の照合を worker 内と主ループの
   2 箇所に置く）。`reply:` 行へ `refusal=` を 1 欄足す（下の材料が正）。
   **Spec 27 で却下した「`Error:` で ok を倒す」はツール結果への適用**で、
   こちらは依頼主への返信本文 — 偽陽性の構造が違う。機構ではなく計器（#47）
3. **人間の編集窓 — [Spec 43](specs/43_plan-edit-window.md) rev2 承認 →
   P0〜P1 完了**（2026-08-24。査読 7 点 → 採用 5 / 前提を訂正して採用 2。
   凍結 9 本を `data_contract` の `plan_edit_window` へ。P1 = コア実装 +
   結合 7 本、ミューテーション 2 回で赤確認。**停止の精緻化** — 進行役の停止は
   波ごと畳み、凍結 9 の破棄は完了競合・受信箱飽和の残余の網。
   **P2 = IPC 2 本 + トグル + 波ペインの編集パネル + 辞書 ja/en も完了**
   （型検査が投影の写し忘れ 4 箇所を捕まえた。vitest 437）。
   **P3 台帳も完了**（`a543e60`。README 日英 + DETAIL 日英へ「計画の確認」）。
   **P4 実機で 7 件すべて観測して Done**（2026-08-24〜25。記録は Spec の
   「P4 実機記録」が正）。**検収 7 の文面を rev3 で書き換えた** — 旧文面が
   期待した `plan bundle discarded: reason=agent_stopped` は、P1 で書いた
   精緻化（停止は波ごと畳む）により**到達しない**。**腐っていたのは検収項目
   だけで、実装・`data_contract`・この CLAUDE.md の 3 つは揃っていた** —
   凍結を精緻化したときに、その凍結を根拠にしている検収項目を数えなかった。
   **一般化: 凍結を精緻化したら、その凍結を当てにしている検収項目が到達可能な
   ままかを数える**（#51 (b) / Spec 26 rev4 と同じ形の 2 例目））。
   eigent は camel の一体だった `process_task` を
   **二分割してまで**、分解結果を人が編集・削除・追加してから走らせる窓を
   開けた（`eigent_make_sub_tasks` / `eigent_start`）。`plan` を
   「提示 → 編集 → 実行」の 2 フェーズへ。**「線は人が引く」の計画への延長**で、
   Spec 10 の打ち切りより手前で効くぶん止めるコストが安い。**骨格は二相**
   （ターンの中で人を待たない — 提示でターンが正常に終わり、承認が新しい
   因果の根になる。束ねは `Endpoint::System` の配送で進行役の新ターンへ）
4. **手順書の器（未起票材料・自動化しない）** — camel の
   `workflow_memory_manager`: 役割別フォルダの Markdown 手順書、**失敗と回避策を
   成功手順と同じファイルに残す欄**（`failure_and_recovery_strategies`）、
   create / update の 2 モードで無限増殖しない、検索でなく「題名を見せて番号で
   選ばせ本文を system message へ注入」。**ただし eigent 自身が繋いでいない**
   （`enable_workflow_memory` 既定 False・True を渡す呼び出し 0 件）— 成功ランを
   手順書へ自動昇格させるループは向こうにも無い。入れるなら自動要約と同じ
   「**既定 OFF・手動の『この依頼の手順を残す』ボタン**」形が整合

### 計器 `refusal=` の材料（取り込み 2 の定義。**同日に実装済み** — 起票を待たず数字を貯める側を利用者が採った）

**実装（2026-08-24）**: `crates/fuseforks-core/src/refusal.rs`（純関数
`is_refusal_form` + 語彙表の定数）と `turn.rs` の `reply:` 行への 1 欄。
単体 6 本 + 結合 1 本（`reply_destination_log.rs` に欄の実在検査）。
**ミューテーション 2 回で赤を確認**（判定を常に false → 正例 5 本が赤・負例は緑 /
窓の上限を外す → 窓のテストだけが赤）。core 599 + 結合 全緑・clippy 警告ゼロ。
**判定は前方一致ではなく「先頭 80 字の窓に語彙が現れるか」** — 利用者の定義案は
`startswith` と書いていたが、語彙表の「かねます」（文中に現れる形）が前方一致では
原理的に届かないので、窓 × 部分一致へ解釈を寄せた（窓が `Error:` 型の本文中
偽陽性を防ぐ側を担う）。含意で畳んだ語彙 2 つ（「大変申し訳ありませんが」→
「申し訳ありませんが」/「致しかねます」→「かねます」）は載せていない —
載せると決して判定に効かない行になる。data_contract の該当節も追従済み。

- **置き場は既存の `reply:` 行に 1 欄**
  （`reply: agent=… to=… hop=… chars=… refusal=yes|no`）。**配送は 1 ミリも
  変えない** — Spec 08 の凍結「分類は文言 parse でなく型で運ぶ」が縛るのは
  配送と分類で、観測だけの欄は縛らない
- **判定は返信本文の先頭 80 字と閉じた語彙表の照合**（ja / en の両方。例:
  「申し訳…できません」「できかねます」/ "i cannot" "unable to complete"）。
  語彙表はコードの定数で、設定にしない（増やすときはコミットが記録になる）
- **名前は refusal（断り形）であって failure（未完遂）ではない** — 「できません」が
  正答である依頼は実在する（不可能なことを不可能と報告するのは完遂）。
  数字の解釈は人の側に残し、**計器の名前に判定を埋め込まない**
- 読み方: 完遂の軸は 2 本になる — (a) 委譲ターンの `reply: to=user`
  （宛先の取り違え・#96 の計器がそのまま） (b) `refusal=yes`（断り形の返信）。
  どちらも `fuseforks.log` の grep だけで読める

### 採らない

- **LLM が引く依存 DAG** — coordinator が JSON で依存を書き**無検証**
  （循環検出は人が組む PIPELINE モードにしか無い）。スイッチの裁定（2026-08-22
  「LLM の判断を配送構造にしない」）と同じ理由
- **worker 並列の無上限**（docstring が "without artificial limits" と明言。
  プール上限 10 も auto_scale の or 条件で骨抜き）/ **`ask_human` の無限待ち**
  （人が答えないとサブタスクが 3600s タイムアウトまで固まる）/
  **再割当で同じ worker が返るとタスク本文へ苦情文を追記する**ハック
  （タスク本文がノイズで汚染され続ける）

### その他の観察（機構は作らない。次に触るとき要る）

- **単一キュー合流** — HTTP / ワーカー内部イベント / human ask / タイムアウトが
  全部同じ `asyncio.Queue` に `Action*` 型で積まれ 1 本のループが捌く。
  pause 中の割り込み質問が同じ機構で自然に書けている
- **サブタスクの状態機械** — waiting（割当済み）→ running（依存充足）→
  done / failed の 3 段 + **リトライ中はフロントに通知しない**（枯渇後に初めて
  失敗を見せる）。「勝手に直っている作業で不安にさせない」
- **終了判定を LLM の外へ** — 「pending が空 かつ in-flight = 0」のカウンタ帳簿 +
  返却待ち 600 秒 timeout。宛先選択が LLM の型では「誰も次を呼ばない = 終了」に
  なりがちで、完遂と放置が区別できない — の対策
- **eigent の再起動再開は文脈だけ** — `save_snapshot` は取るが**復元呼び出しが
  repo に 0 件**。「話は覚えているが作業はやり直し」= この村の「落ちたら頼み直し」
  と同じ穴

### 反面教師（一般化 3 つ）

- **評価器の失敗経路を「合格」に倒すと、検証機構が壊れたときだけ検証をすり抜ける。**
  採点不能は不合格側へ倒す（fail-closed）。#82 / #84 と同族
- **読み出しだけ実装して書き込み元が無い永続層は、「常に空を読む」が正常動作になる**
  （eigent の facts.json / summary.md / tool_events.jsonl の 3 本 — 読み手は
  実装済み・書き手の呼び出し 0 件）。**write を先に通す**
- **`.format()` を通るプロンプトは、書いた文字列とモデルが見る文字列が違う** —
  スキルのトリガ記法が `{{pdf}}`（意図）/ `{pdf}`（format 後にモデルが見る実物）/
  `#pdf`（UI の実装）の三重不一致。**モデルが実際に見る文字列で検証する**
  （#93「自分の型を見て相手のワイヤを見なかった」の同族）

## Perplexity を繋いだ（2026-08-19。コード 0・設定だけ）

起点は実機 — `ppl-deepseek-v4-flash-0731`（`baseUrl = https://api.perplexity.ai/v1`・provider 未指定）
が **404 本文なし**で落ちた。probe 5 本と文書（`docs.perplexity.ai/llms.txt` 起点）で確定:

- **`/v1/chat/completions` は存在しない**（404・本文なし = アプリと同じ）。互換の口は
  **`/router/v1/chat/completions`**（Router API。接地なし）で、**有料クレジットが要る**
  （403 `restricted_api_key`）。接地つきは **`/v1/agent`** で、**`/v1/responses` が OpenAI
  Responses 互換の別名**
- **繋ぎ方**: provider を **`OpenAI Responses`** に明示、`baseUrl` は
  `https://api.perplexity.ai/v1` のまま、モデルは `perplexity/deepseek-v4-flash-0731`
  （Agent API の有効 id。文書で確認）。**アプリの Responses ワイヤが送る欄そのまま**
  （`store` / `reasoning.summary+context` / `include` / 関数ツールと `web_search` の混在）で
  200 を実測。`22:33:59` から `backend=openai-responses` の `stop=-` を観測
- **→ 2026-08-27 に 7 本目のワイヤ `Provider::PerplexityResponses` を切った**
  （[Spec 45](specs/45_perplexity-tools.md)。P0〜P3 完了）。固有スキル 4 本
  （web / 金融 / 人物検索 / URL 取得）+ `*_results` 4 種を
  `GroundingEngine::Perplexity` へ写す — **下の (a) の宿題はこれで回収**
  （「参照元は返ってきていません」が実 URL の一覧になる）。(b) の
  `usage.cost` は `pplx tools:` 行の `tool_cost_usd=` でログに出る
  （統計への写しは D10 で範囲外のまま）。**相乗り構成はそのまま動く**
  （`ALSO_SERVES_RESPONSES` — 免除の表が互換の口 / Responses の口の 2 枚に
  なった）。finance は `max_steps: 5` を対で送らないと **200 のまま黙って
  空振りする**（probe 実測）。**P4 で Done**（2026-08-27。4 ツール各単独 ON で
  発火を利用者が観測）。**運用の観察: 4 本すべて ON にするとツール選択が
  モデル任せでバラける**（金融の質問でも web から拾う等）。機構は作らない —
  単独 ON = 用途別テンプレートが処方（トグルを 4 本に割った理由がここで効く）。
  **続報（2026-08-30）: stranded 警告の同名衝突を直した** — Perplexity の
  テンプレートに **OpenAI Responses 時代の `openaiWebSearch: true` の残骸**が
  「web 検索 … プロトコルを OpenAI ネイティブにしてください」と出続けた
  （利用者の指摘。従うと固有スキル 4 本を失う助言）。辞書のラベルが
  `openaiWebSearch` / `perplexityWebSearch` とも文字どおり「web 検索」で
  区別が付かなかった。処方 3 つ: (a) stranded 行のラベルへ持ち主のワイヤ名を
  併記 (b) **「オフにする」ボタンを新設**（それまで残骸を消す UI が無く、
  プロトコル往復か `world.json` 手編集しかなかった） (c) `providerSkills.ts` の
  「UI からは作れない」というコメントは**嘘だった**ので訂正（ON のまま
  プロトコルを切り替えて保存すると UI から作れる — 保存は draft の全欄を
  そのまま送る）。stranded の 10 ブロックは `strandedRows` の v-for 1 本へ畳んだ
- **固有の差 2 つ**（次に触るとき要る）:
  (a) **出典は `annotations` ではなく `search_results` という output item**
  （`{queries, results[{url,title,snippet,date,source,id,last_updated}]}`。probe で 15 件、
  `annotations` は 0）。decode は未知の種別を捨てて数える（`dropped content blocks:
  kinds=search_results`）ので落ちないが、**接地の注記は「参照元は返ってきていません」になり、
  本文の URL だけが残る**。写すなら `GroundingEngine` に 1 値（Spec 31 の形）
  (b) **`usage.cost`（USD）が返る**（検索 1 回 $0.0025 + 入出力 ≈ $0.003 / ターン。
  `tool_calls_details.search_web.invocation` も）。**単価表を当てずに実額が取れる唯一の口**。
  統計へ入れるなら Spec 41 の横に 1 欄
- `reasoning_tokens` は 0、思考の要約も返らない。`cached_tokens` も 0（2 ターンとも）
- **鍵の環境変数名は文書では `PERPLEXITY_API_KEY`**。この端末は `PPLX_API_KEY`（ユーザー
  環境変数）に置き、テンプレートは keyring。probe はその鍵で通した（値はログに出していない）

## 単価の手入力が保存されない実バグを直した（2026-08-19。`failures.md` #111）

起点は利用者 —「Perplexity は価格表が取れないのはいいのだが、入力と出力とキャッシュに
価格を手で入れても更新されなかった」。**真因は `type="number"` の `v-model` が `.number`
修飾子無しでも数値を渡すこと**（`@vue/runtime-dom` の `castToNumber = number ||
vnode.props.type === "number"` を実物で確認）。文字列前提の setter（`raw.trim()`）が
`TypeError` で死に、空欄（文字列のまま届く）だけが通っていた — だから「取得」は効き
手入力だけが効かなかった。**同じ経路で 2 つ目**: Spec 41 D1「手入力ならその日で
`pricing_as_of` は常に埋まる」が実装されておらず「単価の時点は未記録」と出ていた。

- 処方は純関数 `lib/priceInput.ts` の 2 本（`parsePriceInput(string | number)` /
  `todayIsoDate(now)`）。**初版 setter の再現（数値で `TypeError`）を赤として単体に残した**
- **利用者が実機で確認**（同日。単価を入れて保存 → 統計に `≈ $0.01`・被覆率 `1/1 体・
  トークン 100.0%` が出た）。**Perplexity は `usage.cost` も返す口**なので、次に
  この推定と実額を突き合わせられる（上の「Perplexity を繋いだ」の節）
- **一般化**（#111）: `computed<string>` の型注釈は setter の実引数を保証しない /
  「A を使わないから B が来る」は A 以外の経路を数えていない / 契約に「常に」と書いた欄は
  埋める経路を 1 つずつ数える

## 自動要約はオプション・既定 OFF（2026-08-19 利用者裁定。未起票）

起点は [SAIVerse](https://github.com/maha0525/SAIVerse) の「あらすじのあらすじ」
（`sai_memory/arasuji/generator.py`: 20 メッセージで L1・10 件で上位へ畳む再帰階層・
`SUMMARY_MAX_CHARS` 1200）を読んだ流れ。利用者の見立て —「SAIVerse は Kataribe のように
キャラクターとの会話を楽しむものだから省略で削られても影響はないが、Fuseforks は業務の
ワークフローもできる実用アプリなので自動省略はどうか」→ 裁定「**自動要約させるかは
オプション機能にするのが妥当。既定では効かせなくてよい。今は手動要約でユーザーが
必要と思ったときだけ要約させる機能がある**」。

- **Spec 12 の凍結「自動では要約しない（人が押したときだけ）」は既定としてそのまま。**
  足すなら**オプトイン**で、村の既定の挙動を動かさない
- **核（ワークフローの状態）に要約で状態を運ばせない** — 状態は会話の外のファイル
  （付箋 / 指示書）に置き、会話が長いときの処方は要約ではなく**参照**（依頼文のパス /
  `room_log` の ID で全文 pull）。要約を核に入れると状態が 2 箇所に住む
- **省略の代償はキャラ会話と業務で形が違う** — 前者は「前の話を忘れた」、後者は
  「答えられないことに気づけない」（#82 の形。黙って筋の通った誤答）
- 入れるなら**殻**（個体の付き合いの筋・`Memory.md` の 1 節）向けで、件数で周期を固定し
  （費用が予測できる）、長さの上限と「入れた個体の `prompt` が何トークン増えたか」の
  計器を同時に持つ。要約の長さはモデルで桁が違う実測（opus 1,163 字 / flash-lite 234 字）
- **Fuseforks も滑る窓 8 往復で会話を丸ごと落としている**（省略していないのではなく、
  薄く残す代わりに切って残さない）。業務で困らないのは残すべきものをファイルへ逃がす
  運用が効いている間だけ — 付箋 / 指示書の条例改訂が実機で効いているかが先

## 波の fan-out はプロバイダのレート制限を人数ぶん速く食う（2026-08-25 実測）

起点は Spec 43 P4 の検収 6。**Fuseforks の欠陥ではない**（利用者が無料枠の鍵を
差し替えて解消）が、**機構では見えない天井が 1 つあることの実測**なので残す。

`plan` の波で 5 体へ撒いたところ、**ロボットくん3号（`agent_4`）と5号
（`agent_7`）が同時に 429** で落ちた。本文が原因を名指ししている:

```text
Quota exceeded for metric: generate_content_free_tier_requests,
limit: 15, model: gemini-3.5-flash-lite
Please retry in 56.493566409s
```

**2 体とも同じ `gemini-3.5-flash-lite`** で、14 秒間に 8 周 + 11 周 =
**19 リクエスト**。無料枠は 15 requests/分。

- **村の予算（`tokenBudget`）は効いていた**（同じ走行で `budget stop:` が 4 本）。
  **プロバイダ側の RPM はこの村のどの機構も見ていない** — 天井はトークン建てで、
  リクエスト数の次元を持たない。**波は個体数ぶん速く RPM を食う**ので、
  同じ無料枠モデルを複数個体へ割り当てると波で必ず当たる
- **`chat_with_backoff` の天井 5 秒は、プロバイダが明示する待ち時間より短い** —
  実装は `Duration::from_millis(200 * 2^n).min(Duration::from_secs(5))`
  （`llm/client.rs:470`）で、サーバーが `retryDelay: "56s"` と**言っているのに
  読んでいない**。この 429 は再試行しても**構造的に必ず失敗する**
- **機構は作らない**（2026-08-25）。`retryDelay` を読んで待っても
  **RPM 15 は 2 体 × 10 周には足りない**ので症状は消えず、遅くなるだけ。
  直す価値は「失敗が減ること」ではなく「サーバーが教えた値を無視しないこと」の
  側にあり、**それは頻度を見てから決める**（#47 の規律。**今回は運用で解消した
  ので頻度は 1**）
- **鍵を差し替えた再走行が対照になった**（同日 03:12）。**`stop=failed` が
  0 件**で、同じ 2 体が 13 周・14 周を完走。**429 は鍵の枠の問題であって、
  周回数でも波の形でもない**ことがこれで確定した

**同じ走行で、過去の処方が効いていることが 3 つ読めた**（負の対照として価値がある）:

- **`fatal=false` で個体が落ちていない** — 10/10 稼働のまま
- **失敗したターンにも `turn:` 行が出て払いが記録されている**
  （`stop=failed:LLM_API prompt=59302 total=59713`）= `failures.md` #103 の処方
- **束ねが 2 つの失敗形を書き分けた** — 5 体中 5 体とも答えが無い最悪の入力で、
  244 字の束ねが「トークン予算の上限で打ち切られました」×3 と
  「が返りませんでした」×2 に分けた（#44 の規律が最悪ケースで成立）

## 現在地（2026-08-30 更新）

**この 2 日（08-29〜30）で配布が一周閉じ、調査が Spec 46 を生んだ。**

- **v0.1.12 = `c79e957` を全 4 経路で配信**（Release publish / **winget マージ** —
  0.1.8 の初回 PR #418363 が 08-29 に 12 日でマージ → 0.1.12 の update PR
  #426022 は**提出から 1 時間足らず**でマージ・カタログ配信ラグ 2 時間弱 /
  Homebrew tap / **ランディングページ公開** = betyourluck.github.io/fuseforks.html）。
  `winget upgrade` の実測で**ロケール照合の罠**を踏んだ（`InstallerLocale:
  en-US` 対 端末の ja-JP 記録。処方 `--locale en-US`・恒久処方は次の提出で
  マニフェストから宣言を消す = 利用者裁定。「配布経路を 3 つに広げた > winget」
  の節が正）
- **stranded 警告の同名衝突を修正**（ワイヤ名の併記 + 「オフにする」ボタン。
  「Perplexity を繋いだ」の続報が正）
- **調査 → Spec 化の一周**: alphaXiv 5 本の受領突き合わせ →
  **2608.25277 の「調整メッセージ 40〜60%」を実測で反証**（村では 0.5% —
  封筒は無料、高いのは中の仕事）→ **Prime Agent + Governed Shared Memory を
  実読** → 3 梃子の行き先（Spec 46 / 条例運用 / 採らない）。
  「外部研究の受領（2026-08-30）」の節が正
- **[Spec 46](specs/46_acceptance-probe.md)（予定の後判定）を起票 → rev2 →
  P0〜P3 完了**（同日）。**P4 実機検収 6 件は利用者が着手中** —
  実機は D:\Github\betyourluck.github.io の HTML 書き換えを後判定つきの
  予定で依頼する形。**判定は `fuseforks.log` の `acceptance:` 行だけで読む**
  （`schedule=… attempt=n/m outcome=match|no_match|error|timeout|unapproved`）
- **未貼りの条例草案が 1 つ** — 手順書の節（Prime Agent 梃子 2 の運用開始。
  草案は 2026-08-30 の会話で渡した。条例は利用者の資産なので貼るのは利用者）

以下は 2026-08-25 時点の記録。

## 現在地（2026-08-25 更新）

**`v0.1.11` = `acb80a6` を公開した**（3 OS 緑・アセット 7 件。タグ履歴の節が正）。
**この日に Spec 43 と Spec 44 を 2 本とも Done にした** — どちらも実機検収を
利用者が全件踏み、残っている実機確認は無い。

- **[Spec 43](specs/43_plan-edit-window.md) Done** — plan の編集窓（提示 →
  人が編集 → 実行の二相）。検収 7 件観測
- **[Spec 44](specs/44_ask-cycle-detection.md) Done** — 委譲の輪の構造検出と
  待ち時間の設定化。検収 5 件観測（4b だけ結合テストへ降ろした）
- **`failures.md` #113** — 有界ループの上限を回数で切ったら速さ依存になった

### この日いちばん重い観察: 検収項目が 2 本続けて到達不能だった

**Spec 43 の検収 7** と **Spec 44 の検収 4b** が、どちらも**実装では到達しない
操作**を書いていた。しかも**実装・`data_contract`・CLAUDE.md の 3 つは揃っていた** —
腐ったのは検収項目だけ。機序も同じで、**P1 で「実在範囲が実装で確定した」と
書いたのに、その範囲に依存する検収項目を数え直さなかった**。

**一般化: 凍結を精緻化したり実在範囲を狭めたりしたら、その凍結を当てにしている
検収項目が到達可能なままかを同じコミットで数える。** 実装記録は「何が起きるか」を
狭め、検収項目は「何を試すか」を指す — 前者を狭めた瞬間に後者が到達不能になりうる
（#51 (b) の「腐るのはその機能の節ではなく隣の節」の、凍結 → 検収という向き）。

### 個体が構造の規則を先取りしていた（Spec 44 P4 の副産物）

S2（相互委譲の輪）を実機で踏むのに **3 回空振りした**。真因は偶然ではなく、
**ルナの `Memory.md` に「依頼元にはタスクを振らず、調査・検証の委譲先から
除外する。指揮系統の混乱を避ける」が書かれていた**こと（`remember` で
**2026-08-15 02:43** = **#106 を踏んだ当日**・**P1 の 9 日前**）。
**振る舞いの規則が構造の規則を 9 日先取りしていた。**

- **`remember` は全期間で 3 回しか呼ばれていない**（2026-08-09 / 08-14 / 08-15）。
  そのうちの 1 つがこれで、**個体は滅多に書かないが、書くときは効く所を書く**
- **機構が不要という意味ではない** — #106 は実際に起きた。守っているのは
  **事故で起きる輪**で、それが希少であることの実測
- 4 回目に依頼文で「ask で聞き合うテストがしたい」と書いて**この規則を明示的に
  上書きして初めて発火した**

### 委譲を使っていないように見える理由（条例の事務量の実測）

利用者の観察「ルナはぜんぜん委譲を使わない」を数えたら、**今日 8 回委譲していた**
（ミュゼ 4 / イクス 2 / ザリ 2）。ただし **`file` 42 回 / `fd` 8 回**で、
**委譲 1 回の前に 4〜5 回のファイル操作**が挟まる（付箋の確認 → 着手記録 →
指示書の作成 → 検証基準の記録）。**2026-08-19 の条例（付箋と指示書を分けた）が
要求している手順そのもの**で、会話ペインの行数をその事務が埋めている。
**重いと感じたら削る判断ができる形で数字が出た**（例: 「着手時に 1 回」を
「依頼を出すときだけ」へ狭める）。

### sqz を試用中（2026-08-25 現在）

ツール出力の圧縮。**MCP ツール（`sqz_read_file` 等）は使っておらず、効いているのは
Bash の PreToolUse フックだけ**。実測の削減率はログの grep で 9〜36%、Rust の
`sed` で 38〜60%、doc コメントが厚い箇所で **65〜83%**。

- **圧縮された出力は編集の材料に使えない** — `sed` の出力に**元ファイルに無い
  空行**が入り、それを写した `Edit` が「文字列が見つからない」で失敗した。
  中間を `[N low-information segments omitted]` で畳むので、**逐語が要る用
  （編集の材料 / 契約の写し / golden）では `Read` で生読みする**
- **`SQZ_NO_DEDUP=1` は dedup を切るだけで圧縮は残る**。生が要るなら `Read`
- 生成物 4 件（`AGENTS.md` / `.clinerules` / `opencode.json` /
  `sqz-claude-guidance.md`）は**端末ローカルの絶対パスを含む**ので `.gitignore` 済み

## 現在地（2026-08-19 更新）

**[Spec 42](specs/42_stats-period.md)（統計の「全会話」を月の集計にする）を起票 → rev2 承認 →
P0〜P5 まで同日に着地 = Done**（P5 は利用者が実機で表示を確認）。 起点は利用者 —「全会話は期間を
決めないと無限に増えていくね」→「システム設定で月間の集計日を決めてもらって月の集計に」。
**重かったのは表の行数ではなく合計** — `totals` / `by_agent` / `≈ $` が村の生涯累計で、
使い込むほど「先月いくら」が読めない数字に育つ。次に触る人が要る 4 点:

- **締め日は `1..=28 | "eom"`（既定 月末）で端末に保存**（`localStorage` の
  `statsClosingDay`）。29〜31 を選ばせないのは、2 月に無い日を選ばせると「その月は
  どうなるか」の規則がもう 1 つ要るため（丸め案は 2 月にだけ黙って 28 日締めになる）。
  **締め日 1 は月末と同義ではない**（7/2〜8/1 と 8/1〜8/31）— 取り違えは説明文ではなく
  **設定ページ直下の「今の期間」のライブ表示**で防ぐ（統計画面と同じ純関数 1 実装）
- **ワイヤは `All { period?: {sinceMs, untilMs} }` だけ。`Session` に期間は無い**
  （送れる形が無いほうが強い）。**境界の計算はフロント `lib/statsPeriod.ts`、コアは
  2 数だけ受けて時計もタイムゾーンも読まない**。`period` 省略 = 全期間 = 旧挙動で、
  旧クライアントの `{kind:"all"}` は `#[serde(default)]` でそこへ落ちる
- **`aggregate` の計算順は `oldest_ms`（全 turn の最小・`all` のみ）→ フィルタ →
  `recorded_since`**。逆にすると `◀` が 1 つ前で止まる。**期間ありの表は `turns > 0` の
  会話だけ**で、そのとき表の合計 = `totals`
- **画面のナビの状態は締め月 `{year, month}`**。`period` から締め日を逆算しない
  （`since = 1 日` は月末締めとも 1 日締めとも読めて不定）。全期間 ON の間はナビ非表示
  （無効化ではない — 灰色の月名は「今その月を見ている」と読める）

**査読 8 点 → 採用 7 / 訂正して採用 1**。訂正したのは「1 日 = 月末と同義と説明する」案で、
同義ではないので構造（ライブ表示）で受けた。**実装が止まる矛盾は 0 だったが、rev1 では
「書いていないこと」を実装者が埋める形になっていた箇所が 5 つあった**（`oldestMs` の
定義域と計算順 / `previous` のシグネチャ / `totals` の但し書き / 全期間 ON のナビ /
ラベルの右端）。ミューテーションはコア 2 回・フロント 2 回で赤を確認。

以下は 2026-08-18 時点の記録。

## 現在地（2026-08-18 更新）

**この日に Spec を 2 本 起票して 2 本とも Done にした**（40 / 41）。**3 つの変更が
1 本の線で繋がっている** — どれも「**畳んだせいで情報が消えていた**」同じ形:

```text
Spec 39 rev4  by_agent を (個体, モデル) へ割った   ← 無いと単価を当てる相手が無い
Spec 40       cache_write を数えた                  ← 無いと金額が 1.52 倍ずれる
Spec 41       単価を登録して ≈ $ を出した
```

- **[Spec 39](specs/39_stats-view.md) rev4** — `by_agent` の鍵を `agentId` 単独から
  `(agentId, model)` へ。**モデルを切り替えた個体の全ターンが最後のモデルに
  畳まれていた**
- **[Spec 40](specs/40_cache-write-accounting.md) Done** — キャッシュ書き込みを
  6 ワイヤで数える。**「未キャッシュ」の実体はほぼ全部が書き込み**
  （Anthropic 100.0% / OpenAI 互換 99.8%）で、**Anthropic の個体では入力の課金が
  実効モデルの 1.61 倍**。**予算の重みは変えない**（利用者裁定）
- **[Spec 41](specs/41_model-pricing.md) Done** — モデルごとの単価と `≈ $`。
  **Spec 39 D5「通貨に換算しない」を覆した**（3 理由のうち 2 つが 3 日で消えた）。
  **この村で 1 本目の非 LLM 外向き通信**で、`PRIVACY.md` 日英に節を足した
- **先行実装の調査**（LangGraph / Dify / AionUi / otari）と、**単価表の公開**
  （`betyourluck.github.io/prices.json`。MIT の LiteLLM 由来 + Perplexity は手写し）

**次の一手**: 評価基盤の**完遂**の軸（`reply: to=` を数えるだけ。計器は要らない）/
単価表の overlay（**埋める範囲は見込みより狭い** — さくら系も公開表に載っていた）/
winget の PR は**人の承認待ちで手番なし**（中央値 15.2 日）。

以下は 2026-08-16 時点の記録。

## 現在地（2026-08-16 更新）

**マイルストーン = [Spec 39](specs/39_stats-view.md)（統計画面）が Done で、
`v0.1.8` = `95ad2fe` を公開済み**（2026-08-16。**起票から Done まで 1 日**・
実機の検収 6 件すべて観測。下のタグ履歴が正）。この版には
**[Spec 38](specs/38_budget-reserve.md)（予算の CAS 予約）**と
**失敗したターンの払いの清算**（`failures.md` #103）も入っている。

**評価基盤は「費用の層」だけが立った。** 次に摘むのは**完遂**（利用者が
「依頼主に返さないというのは結構ある」と言った軸）で、**新しい計器は要らない** —
`reply: to=` を数えるだけ。**起票は後日**（2026-08-16 利用者判断）。
品質の層（rubric・閾値・LLM-judge）は SNS へ意見を投げて返答待ち。

以下は 2026-08-13 時点の記録。

**マイルストーン = [Spec 37](specs/37_meta-responses.md)（Meta の Responses ワイヤ）は
P0〜P5 が着地して Done**（2026-08-13。**実機の検収 5 件すべて観測**）。
**`v0.1.7` = `d149645` で公開済み**（当時の記録）。

**この版で外向けの記事が 1 本出た** —
[Qiita「デスクトップで AI エージェントの村を建てる」](https://qiita.com/outcast_zari/items/be4463c69dd1fd2a0448)
（ザリ・ロブステル名義。2026-08-13）。**マニュアルであると同時に、この村で最初の
外部向け説明**なので、**次に画面や用語を変えたらここも腐る** — 記事が触れているのは
3 ペインの構成 / 条例 / モデル登録と固有スキル / 役職（**自分の役職を知らない**）/
サーヴァント追加（MCP・同梱ツール・RAG）/ **カードの drop で絆を引く** /
一括起動 / Alt+↑↓ / 黒板 / MPL-2.0。**台帳の grep 網の外にある**ので、
**用語を変えたときは記事も数える**。**2026-08-27 に 3 つ目が増えた** —
ランディングページ（下の節）。あちらは `.gitignore` 済みのうえ別リポジトリに住むので、
`git grep` では 1 件も出ない。

**`specs/tla/` を新設した**（2026-08-14。TLA+ の bug-model・出自は
[MASC](https://github.com/jeong-sik/masc) の `specs/bug-models/` の型）。
**1 つの `.tla` に安全版と壊れた版を書き、`.cfg` 2 本を対で回す** —
壊れた版が赤くなることを確かめて初めて「モデルが判別している」と言える
（「一発で緑になったテストは機構を壊して赤を確かめる」の形式版）。
**違反しなくなったら直すのは `.cfg` ではなく `.tla`** — 期待値を緩めて
通した形にしない。回すには java + `extra/tla2tools.jar`（gitignore 済み。
この開発機の JVM は Android Studio 同梱の JBR。**TLC は cwd に `states/` を
掘るので scratchpad へ写して回す** — リポジトリ直下で回さない）。

- `SessionAppendAtomic` — `session_store.rs` の `append` が RECORDS と
  SESSIONS(meta) を **1 トランザクション**で書くことを留める（割った版は
  2 遷移で torn を観測）
- `BudgetOvershootBound` — **`turn.rs` の「オーバーシュート 1 呼び出し分」を
  反証した**（`try_reserve` は load のみ = 予約しないので、波が N 体へ撒くと
  残額 1 でも N 体が通る。**上限は「同時に飛ぶ本数 × 1 呼び出しの実費」**）。
  台帳 4 箇所（`budget.rs` doc / `turn.rs` 注釈 / `data_contract` の
  `token_budget` / この Spec 11 の節）を同日に訂正。処方の判断は
  [Spec 38](specs/38_budget-reserve.md)

**`gemini-3.7-flash` は 19,000 トークン未満のプロンプトでキャッシュを返さない**
（2026-08-14 実測。`failures.md` #104）。同一個体・同一会話の A/B で
**18,305 が ✗ / 4 秒後の 19,035 が ✓**、`gemini-3.6-flash` は同じ帯で普通に効く。
`cached` は**約 8,000 の塊**で返り、**3.7 は 1 ブロックを 1 度も返さない**（最小 2 ブロック）。
**両者は同額なので、19K を超えない仕事に 3.7 を当てると入力が 1.63 倍**
（実測 $0.4593 対 $0.7500 /1M）。**会話は 3.6、19K を超え続ける仕事だけ 3.7。**
分けられたのは `cache:` 行が `model=` と `round=` を持つからで、**`turn:` の
`prompt` はラウンドの合計・~~モデル名も無い~~**ので帯も機種も見えない
（**→ 2026-08-16 に [Spec 39](specs/39_stats-view.md) P1 で `turn:` 行の末尾へ
`model=` を足した**。`prompt` がラウンドの合計であることは変わらないので、帯は今も
`cache:` 行でしか読めない）。

**この節に一度書いた「未 push が N コミット」は捨てた** — P4 で書いた「18」は翌日に偽になり、
**その後 2 本積むと再び真に戻る**という形で腐っていた。**総数は書く行為そのものが腐らせる**ので、
一度は基点（`origin/main` の sha）の指定へ置き換え、push しきったこの行で消えた。
**#67 の処方「総数をやめて列挙へ」の、列挙できない量への当て方は「基点を指す」。**

**6 本目のワイヤ `Provider::MetaResponses`**（`api.meta.ai/v1/responses`）。
起点は利用者 —「**データを学習に使われるという条件で破格の安さ**。秘匿情報を渡さない
ように注意すればかなり使える。さらに Gemini のように web_search が API では使える」。

**probe 24 発を先に撃ってから設計した。文書は 4 回続けて実装と食い違っている**
（Spec 23 の WebP / Spec 31 の legacy 410 / Spec 36 の xAI PDF / 今回 2 件）。

- **`stream` は不要**（提示された curl の `"stream": true` と
  `Accept: text/event-stream` は**どちらも省いて 200**）。**1 発目で規模が決まった** —
  必須なら SSE をクライアント核へ入れる話で、再試行・打ち切り・トークン計上が
  全部その上に乗るので桁が変わっていた
- **citations が返る。しかも xAI より強い** — `start_index` / `end_index` が
  **0 ではなく実区間**（xAI は 77/77 が全部 0 = メッセージ単位）
- **4 種別すべてを運ぶ**（Gemini に次ぐ 2 本目）。**動画は予測を覆した** —
  payload 無しの `input_video` が「`video_url` か `file_id` が要る」と**名指しで 400**
  を返したので撃てた。OpenAI Responses からの類推では ✗ と書いていた
- **`tool_choice` は `"auto"` のみ**（Kataribe で見た制約の再演）。
  型から欄ごと外した — **送れる形が無いほうが強い**（#77 と同じ）
- **受理集合を列挙するかは層で違う**（**P5 で前半を実測訂正**）。
  **型は列挙しない** — 誤ったツール型も content part も「どの型にも一致しない」だけ
  （OpenAI / Anthropic は全列挙、xAI は untagged enum の 422）。
  **スカラの enum 欄は全列挙する** — `reasoning.effort: max` に
  `` expected one of `none`,`minimal`,`low`,`medium`,`high`,`xhigh` `` が返り、
  **Spec 34 の手筋がそのまま効いた**。だから**未測定の「型」は送らない**が正しい射程で、
  欄の値は 1 つ撃てば教わる。**旧記述は型の層の観察を欄全般へ広げていた**（#91 と同族）
- **`effort` は `max` だけ受理されない。** 梯子は
  `none / minimal / low / medium / high / xhigh` で **`max` の段が無い**。
  `meta_effort` が `max` → `xhigh` へ写す — **丸めではなく同義への写像**
  （`xhigh` が天井なので意図をそのまま満たす）。`openai_compat` の
  `xhigh / max → high` は本当に段を落としており、**落とす先はプロバイダの天井で決まる**。
  写像が無いと 400 が `fatal=true` で**個体ごと落ちる**（実機で踏んだ）
- **`/v1/files` に添付が期限なしで保存される**（`expires_at: null`。P0b の最重要発見）。
  **互換の口で送った画像も残る**ので Responses に限らない。削除口は在り実際に消えるが、
  **一覧は最大 20 秒遅れる**（真実は個別 GET 側 — 1 回目の観測だけで
  「削除は効かない」と結論しかけた）

**P5 の実機が残したもの**（検収の詳細は Spec の側が正）:

- **検索は走るが出典は返らないほうが多かった** — `meta search:` 4 回のうち
  **`sources=0` が 3 回**、`sources=2` が 1 回。**「citations が xAI より強い」は
  *返るときの* 話**として読むこと。画面は `sources=0` を
  「参照元は返ってきていません（本文に URL があっても、出典としては確認できていません）」
  と正直に出しており、**Spec 05 の「検索した事実と出典が返らない事実を分ける」規律が
  6 本目でそのまま効いている**
- **検索した回だけ入力が桁で膨らむ** — `prompt` は検索なしが 10,624〜14,989 に対し
  検索した回が **65,014 / 103,660**。OFF の個体は 12,228〜12,233 で、
  **「跳ねない」は 5 倍の差として読める**
- **思考が本文の席を食う量は effort で単調に増える**（枠 1,024 固定で実測）—
  `high` が `reasoning` 721〜726 で本文 22〜23 字、**`xhigh` が 929 で 21 字**。
  出力は 3 回とも **1,024 きっかり**で `Finish::Length` に到達していた
- **本文が空になる経路はこのワイヤに無い** — `incomplete_details.reason` を持つので
  **理由が判り**、`LLM_OUTPUT_TRUNCATED`（`fatal=false`）として名前が付く。
  **#72 の空本文は理由が判らないときの受け皿**で、判る経路では到達しない
- **`agent reaped:` を初めて実機で見た**（`max` の 400 で個体が落ちた後の再起動）。
  下の「失敗した個体を ON だけで戻せるようにした」の節が
  「この行はまだ実機で見ていない」と書いていたのを、ここで回収した
- ~~**未確認で残す 1 件** — **失敗したターンは `turn:` 行を出さない**ので、
  払った出力と入力が**ログのどこにも出ない**。予算に計上されているかは未確認。~~
  **→ #103 として確定し、2026-08-16 に塞いだ**（予算にも計上されていなかった。
  下の「失敗したターンの払い」の節が正）

**自分が Spec 36 で作った罠が、6 値目を足した瞬間に出た** — `Provider::carries` を
`match (self, kind)` の**種別側ワイルドカード**で書いていたので、新 variant が
`audio=false` / `video=false` を黙って受け取る形だった（実測は全 ✓）。しかも doc には
「網羅で書くのは 6 値目を足す人へ問いを出すため」と書いてあり、**その主張自体が嘘**。
provider ごとに 1 腕の配列リテラルへ直したら、**今回コンパイラが実際に指した**。
**一般化: 「コンパイラが指す」と doc に書いたら、variant を 1 つ足して実際に落ちるか
確かめる**（網羅性の主張は、網羅していない書き方でも同じ文章で書ける）。

**TS 側は union と `Record` で網羅性が割れる** — `Provider` に 6 値目を足して落ちたのは
`carries.ts` の `Record<Provider,…>` **だけ**で、`providerSkills.ts` は黙って通った。
**手で数えたのは 5 箇所**（`DEFAULT_BASE_URL` / `ALSO_SERVES_COMPAT` / 判定と戻り値 /
`anyOffered` / 選択肢）。**`anyOffered` が最も静か** — 足さなくても型は通り、
トグルは描かれるのに**見出しの区切りだけが消える**。


**マイルストーン = [Spec 36](specs/36_multimodal-attachments.md)（多モーダルの添付）は
P0〜P4 + P6 が着地。残るのは P5 の一部**（この節と `data_contract` は書いた）。
**未 push が 12 コミット**（`v0.1.4` = `5fc4655` から）。

**この Spec の骨格は「carries 表」**（`Provider::carries`）。**どのワイヤがどの種別を
運べるかを 1 実装の述語で持ち、送信の入口だけが読む**。表は **P0 の probe 18 発で
全 20 マス（4 種別 × 5 ワイヤ）を観測**して決めた（HTTP 200 ではなく内容照合 —
音声は合言葉・動画は色の遷移・PDF は本文の固有名詞）。

| ワイヤ | 画像 | 音声 | 動画 | PDF |
|---|---|---|---|---|
| OpenAI 互換 | ✓ | ✓ | ✗ | ✓ |
| Anthropic | ✓ | ✗ | ✗ | ✓ |
| **Gemini ネイティブ** | ✓ | **✓** | **✓** | ✓ |
| xAI Responses | ✓ | ✗ | ✗ | **✓** |
| OpenAI Responses | ✓ | ✗ | ✗ | ✓ |

**覆した凍結が 1 つ**（`attachment_contract` 凍結 7）— **Gemini ネイティブに
`inlineData` を生やした**。Spec 23 の根拠は「画像は互換で運べるからネイティブに
要らない」だったが、**音声と動画を運ぶのはネイティブだけ**なので前提が種別の側から
消えた。**取り消し線つきでその場に覆しを書いた**（#56）。

**回収した宿題が 1 つ** — Spec 34 D8 が残した「ネイティブを選ぶと画像が黙って
落ちる」3 例（gemini / xai / openai の Responses）。**画像の行が全 ✓ になった**。

**この Spec で残した観察のうち、次に触るとき要るもの**:

- **xAI の PDF は公式文書に記述が無いのに通った**（文書と実装の乖離 2 例目。
  1 例目は Spec 23 の WebP）。**文書を根拠に ✗ を凍結しなくてよかった**
- **受理集合の列挙という手筋には適用範囲がある**（Spec 34）— OpenAI と Anthropic は
  誤った型 1 つで全列挙を返すが、**xAI は untagged enum の 422 で何も列挙しない**
- **「ワイヤが運べるか」と「モデルが受理するか」は別の層。** 互換の口に
  `input_audio` は実在するのに `gpt-5.6-luna` は拒む。前者は入口が断り、後者は 400
- **添付のコストは `prompt` 単独で読まない。** 9.3MB の PDF を 3 周のターンで送ると
  `prompt` は 520,323 に膨らむが、**未キャッシュは 173,564 = ちょうど 1 周ぶん**。
  **ターンの中の再送はキャッシュが吸う**（D1 が保証するのはターンをまたぐ側）
- **実効トークンで 1 ターン 21〜23%**（天井 100 万）。**D4 の門はサイズで切っていて
  トークンでは切っていない**ので、10MB いっぱいの PDF は天井の 1/4 を使う
- **Gemini の動画は公称の 1/3 以下**（10 秒 720p で音声との差が 1,085。公称は
  260〜300 tok/秒）。`mediaResolution` 依存で、**720p でも公称に届かない**
- **同じファイルを 2 回貼ると 2 回保存される**（重複排除しない）。D11 の帰結として
  正しいが、10MB 級を数回で 500MB の非常弁に近づく

**P4 で退行を 1 件入れて、利用者の質問（「Anthropic では jpg は開けない？」）で
見つかった** — **JPEG も PNG も GIF も貼れなくなっていた**。真因は
**同じ形の述語を層をまたいで写したこと**で、コアの `detect_format` が見るのは
**変換後**（画像は常に WebP）、フロントの `detectKind` が見るのは**利用者の生ファイル**。
**入力の集合が違うのに形だけ写した。** 処方は `routeAttachment`（既定を画像へ倒し、
magic は「画像でないものを外す」ためだけに使う）。
**一般化: 述語を層をまたいで写すときは、形ではなく入力の集合を数える。**

## 現在地（2026-08-12 更新）

**`v0.1.4` = `5fc4655` を打った**（2026-08-12。3 OS 緑・アセット 7 件。
`v0.1.3` から 18 コミット = **Spec 35 Done + 改名 `fuseforks-core` + `run` の
stdin 修正**）。**1 度目は落ちた** — CI の en ランナーで日本語 assert の 6 本が赤
（`failures.md` #101。テストが言語をホストの OS ロケールから暗黙に受けていた。
3 OS 全部 en だったので片肺公開は起きず、タグだけ消して打ち直した）。
**`v0.1.3` = `be1ed3d`**（2026-08-12。3 OS 緑・アセット 7 件）。
**`v0.1.2` は破棄** — **Release だけ削除し、タグは残す**（利用者判断 2026-08-12。
**タグは履歴、Release は配布物**。破棄したのは配布であって、その版が存在した事実ではない）。
`v0.1.1` = `b417dde` から **78 コミット**。リリースノートは利用者へ渡した文面が正で、
**思考の要約を先頭に置いた**（コミット数では Spec 34 が最大だが、
**画面が変わるのは要約のほう**で、Responses はその入口）。

**2026-08-12 に着地したもの**:

- **[Spec 34](specs/34_openai-responses.md) Done** — **5 本目のワイヤ
  `Provider::OpenAiResponses`**。起票から Done まで 2 日、査読 2 系統 19 点、
  probe 16 発、検収 9 項目中 6 項目を観測。詳細は Spec の側が正
- **黒板の削除を画面から**（一括は消しゴム + 確認、個別は日時の右のごみ箱）。
  **契約の凍結「GUI からの書き込み経路は作らない」は覆していない** —
  あれが守るのは「誰かの名前で内容が書かれる」ことで、削除はその形を取らない。
  **消し先は OS のごみ箱**なので、個別には確認を付けていない
- **`run` の実バグ 2 件**（どちらも実機でしか出なかった）:
  - **`PASSED_ENV` に `PROGRAMDATA` が無く、Windows OpenSSH が起動できなかった。**
    `-V`（版を出すだけ）ですら**出力ゼロで exit 255**。13 候補を 1 つずつ足して
    通ったのはこれだけ。**値は何でもよい**（空文字でも通るので `ssh_config` を
    読むからではない）。**規則は確定・機序は未確定**
  - **打ち切りとタイムアウトで `kill` を 1 度も呼んでいなかった**（`failures.md` #98）。
    `group_spawn` で殺せる形に作ってあったのに**殺す 1 行が無かった**。
    実機で ssh が打ち切り後 5 分 `Established` のまま生きていた。
    **Spec 15 の実機確認 (8)(9) が 8 日ぶりに閉じた**
- **`failures.md` #98 / #99。** #99 は**この日 2 回踏んだ形** —
  **計器は在ったのに、書いた側も読む側も「無い」と言った**

**マイルストーン 6 = [Spec 35](specs/35_bilingual-core.md) は Done**
（2026-08-12。**起票から Done まで 1 日**。査読 rev2 → P0 契約 3 箇所 → P1 枠組み →
P2 ツール 9 本 → P3 合成・封筒・System 行 → P4 台帳 → P5 実機）。
**golden 2 系（プロンプトのリテラル / ツール spec の fixture）が全 Phase を通して
緑のまま** = 日本語の村はバイト等価。**P5 は対照つき** — 同じ英語コアで、
新規の英語村（Bob）は英語で完走し、日本語の村（ルナ）は日本語で返った。
偏りの操縦はコアが 1 票を投じる機構で、票の重さは村の質量次第
（英語村を運用するなら条例・SKILL も英語で書く — 利用者の資産の側）。
**次は多モーダル**（音声・動画・PDF）で、**対話シェルを `run` から使う**（下記）は
その後ろ。

**`agent-core` → `fuseforks-core` に改名した**（2026-08-12。`bf67b36`）。理由は 2 つで
**強いのは後者** — AWS Bedrock AgentCore とのブランド衝突（Concordia と同じ形）と、
**crates.io の `agent-core` が既に取得済み**（v0.6.0・CLI 引数パーサの補助クレート）で
**単体で publish する道が最初から塞がっていた**こと。`fuseforks-core` / `fuseforks` は空き。
**Spec は切っていない** — Concordia の改名が Spec になったのは repo の外へ出る
副作用が 5 点あったからで、今回は数えるとゼロ（利用者に見える文字列にもワイヤにも
出ず、外に見えるのは公開リポジトリのディレクトリ名だけ）。射程 54 ファイル / 300 行。
**踏んだのは `.ts` の 6 ファイルが Rust のソースを相対パスで読んでいること** —
`defaultEnabledTools` / `toolLabel` / `groundingEngineKeys` / `presenceNotice` が
「Rust の表と TS の表が一致するか」を機械で留めている網で、**`cargo` は 1 つも
文句を言わないので Rust だけ通して満足すると落ちる。**

### 対話シェルの検討（2026-08-12 に実測。**起票前の材料**）

利用者要望。`ssh outcasts <コマンド>` は通るが対話セッションは返らない。
**着手前に測った 3 点**（`outcasts` = `outcasts.jp` へ実接続）:

- **ぶら下がりは `run` 側のバグで、2 行で直った**（同日着地）。
  `Stdio::null()` を **`Stdio::piped()` + 握った stdin の即 drop** に変えた。
  **Windows OpenSSH は NUL から EOF を受け取らない** — リモートの bash が
  stdin を待ち続ける。実コード経路の実測は **60 秒でタイムアウト → 1.04 秒 exit 0**。
  **`-T` は効かない**（420 秒でも返らず、素の 107 秒より悪化）。
  **tty の問題ではない** — MOTD は `pam_motd` が tty の有無に関係なく出すので、
  出ていることは pty の確保を意味しない。**Git Bash の ssh**（MSYS2）は
  `Pseudo-terminal will not be allocated…` を出して元から即返るので、
  **どちらの ssh で測ったかを書かずに「返る/返らない」と書かない**
  （`PROGRAMDATA` が要ったのと同じ二分岐）
- **合成テストでは判別できなかった。** `sort` に stdin を読ませる形は
  **`drop` を外しても 0.14 秒で緑**（普通のプログラムは `Stdio::null()` でも
  EOF を受け取る）。**判別するのは実機の Windows OpenSSH だけ**なので、
  回帰は `#[ignore]` の `runs_ssh_without_hanging_on_stdin` 1 本で留めた。
  **一発で緑になったテストは、機構を壊して赤を確かめる**まで信用しない
- **一発コマンドは通る** — `ssh outcasts 'echo ok; whoami; pwd'` が exit 0。
  **だから「`ssh -T` が通るか」では分岐しない**（測る前は分岐条件のつもりだった）
- **状態は「呼び出しをまたぐと」持ち越されない** — `cd /tmp` の**次の呼び出し**で
  `pwd` = `/home/ubuntu`。**ただし 1 回の呼び出しの中では持ち越される** —
  stdin へ複数行を流すとリモートは **1 つの bash が読み続ける**ので、
  `cd /tmp` → `pwd` = `/tmp`、`export FOO=bar` → `$FOO` = `bar`（413 ms・exit 0）。
  **B'' は状態を持つ。** 持てないのは**途中の出力を見て次の手を決めること**だけ

**分岐条件は「状態が要るか」ではない**（上の実測で覆った）:

```text
中間出力を見て次の手を決める必要があるか?
├─ No  → B''（1 呼び出し = 1 シェル。cd / export は効く）
└─ Yes → tty が要るか?
         ├─ No  → A-piped
         └─ Yes → A-pty（sudo のパスワード / vim / top）
```

**サーヴァントは自前のループ（36 周）を持つので、呼び出しをまたいだ分岐はできる**
（毎回シェルが新品になるだけ）。**A が要るのは分岐と状態の持続が同時に要るときだけ。**

**設計の芯は「閉じた許容が stdin には掛からない」**こと。`decide` が見ているのは
**構造として分かれた argv** で、シェルが存在しないことが安全の根拠
（Spec 15 が 3 rev かけて退けた線）。stdin へ流した 1 行は**向こうの bash が解釈する**。

- **`stdin: Option<String>` を足して 1 行ずつ `shlex` で割って `decide` に掛ける案は
  通らない。** こちらの `shlex` と向こうの bash は別のパーサで、
  `ls -l; rm -rf /` は `shlex` では `cmd=ls` に見えるが向こうは 2 つ実行する。
  **`resolve_in_work_dir` が文字列検査を採らなかった理由がそのまま当たる**
- **通る形は「解析ではなく構成」** — `stdin` を `Vec<Vec<String>>`（argv の列）で
  受け、`decide` を各 argv に掛け、**こちらが引用して 1 行に組み立てる**。
  モデルはメタ文字を持ち込む席を構造的に持たない
- **引用規則は「相手が誰か」で決まるので、汎用の `run` 機能にはできない。**
  `shell_escape::unix::escape` が正しいのは**相手が POSIX シェルのとき**だけで、
  `python -` / `patch` / Windows のプログラムが stdin を読む登録では誤る。
  **`run.json` の登録側に「この stdin は bash が読む」を宣言させ、
  宣言の無い登録では `stdin_argv` を受け付けない**形が要る
  （`api_key_env` と同じで、**説明文ではなく構造で持つ**）
- **セッションを持つ案（pty + `SessionManager`）は所有者の再設計を伴う** —
  予算（Spec 11）・打ち切り（Spec 10）・hop は全部ターン建てで、
  セッションはターンより長生きする。**Realtime API を見送ったのと同じ構造**。
  さらに**拒否した行を skip すると、シェルの状態とモデルの認識がずれる**
  （`cd` が落ちた後の相対操作が別の場所で走る）
- **出力の固定枠 12,000 字（stderr 4,000 / stdout 8,000）は RepeatGuard のため**で、
  対話では逆に効く — **プロンプトだけの応答は毎回バイト等価なので 3 回目で止まる**
- **依存は着手前に数える** — `portable-pty` / `vt100` が純 Rust かを先に見る
  （`git2` を「唯一の C 依存」として外した前例）
- **順序は C → B'' → A。** C = 相手を非対話にする（`ssh -T` / `-tt` /
  work_dir のスクリプト。**コード変更ゼロ**）/ B'' = 上の argv 列 stdin /
  A = pty セッション。**A は Claude Code 自身にできないことを作る側**
  （私の shell も stdin が null で、対話に見えるのは 1 発ずつの繰り返し）なので、
  **A へ進むなら「Claude Code に合わせる」の外へ出る判断を明示する**
- **A を起票する前に、C で足りない実例を 1 つ捕まえる** — 無いまま作ると
  `allow_in_session` に何を書くべきかが決まらない

### 英語化（マイルストーン 6）の起票前の実測（2026-08-12）

**形は「加算」で確定**（利用者裁定）。日本語を**バイト等価で据え置き**、英語を足して
`world.json` の `language` で**どちらか一方だけ**を送る。

```rust
match shared.language().await {
    Language::Ja => "現在の状態は「今の顔ぶれ」が正です。",
    Language::En => "The current roster is authoritative.",
}
```

- **凍結は破れない。** `data_contract.yaml` の「システムプロンプト・ツール説明は
  訳さない」は**置換を禁じている**のであって、日本語が 1 バイトも変わらない加算は
  Spec 08 の「束ねの文言は 1 字も変えない」とも衝突しない。
  **`stable_len` 6,463・キャッシュ率 99%・束ねの挙動の実測が全部そのまま生きる**
- **入力キャッシュは倍にならない** — 言語ごとに別のプレフィックスができるだけで、
  1 つの村では 1 つしか使わない。しかも言語は初回に確定して再判定しないので、
  割れるのは切り替えた 1 回だけ
- **対象外 3 つ**: MCP のツール説明（既存の凍結「名付けたのは接続先」）/
  村の中身（`Ordinance.md` / `Construct.md` / `SKILL.md` は**利用者の資産**）/
  台帳（`data_contract` / `CLAUDE.md` / `failures.md` / `specs` は書いた言語のまま）
- **混ざるのは想定内**（利用者裁定）—「英語 UI + 日本語 SKILL で日本語の質問に
  たまに英語が出る」は仕様として受け入れる。**偏りを完全に制御しようとすると
  村の中身まで面倒を見ることになり、そこは利用者の資産**

**配線は片方だけ足りない**:

| | 状態 |
|---|---|
| システムプロンプト | `build_prompt(shared, …)` が `Shared::language()` を引ける。**済み** |
| ツールの提示 | **`AgentTool::description(&self)` が文脈を取らない** → `ToolContext` へ言語を足して `spec_for(ctx)` 経由。**既にある逃げ道の四例目**（`rag_roots` / `cancel` / `work_dir` と同じ） |

**モデルへ届く面の実測（字数。「ja」は仮名と漢字だけを数えた実訳出量）**:

| コアが書く分 | 全体 | うち ja |
|---|---|---|
| システムプロンプトの枠組み（村を空にして `compose_system_prompt`） | 174（接地なし）/ 470（接地あり） | 141 / 344 |
| 同梱ツール 7 本の提示（説明 1,562 + schema 3,458） | 5,020 | **1,646** |
| 合成ツール（接続 1 体 / 3 体 / 9 体） | 988 / 2,689 / 5,901 | 556 / 1,172 / **2,306** |

- **schema の大半は訳す対象ではない** — `"type"` / `"properties"` は元から英語。
  だから**全体 5,020 字のうち日本語は 1,646 字**
- **合成ツールは接続数でほぼ線形に伸びる**（`ask_agent_N` と `transfer_to_*` が
  人数ぶん生える）。**コアの取り分は固定ではなく、村が大きいほど増える**
- **手順の文（`protocol_note`）は毎ターン乗り、385〜433 ja 字**
- **書く量と乗る量は別。** `ask_agent_N` は 1 つのテンプレートから N 個生えるので、
  **著述量は ~2,600〜2,900 ja 字**（+ `remember` / `run`）。**乗る量は村の人数次第**

**村との比**: ザリの `Construct` 4,869 + `Memory` 3,175 + `SKILL` 2,556 +
`Ordinance` 1,452 = **12,052 字**。9 体接続でもコアは ja 約 4,300 字なので、
**既存の日本語の村では英語化しても偏りは動かない（そしてそれが正しい）**。
**効くのは新規の英語村** — `Construct.md` が空で `SKILL.md` を英語で書いた利用者に
とっては、コアの数千字が日本語で混ざるかどうかが偏りを決める。

**測り方の注意**: `turn.rs` の日本語リテラルは 4,493 字あるが、**プロンプトに乗るのは
174 字**。残りは実行時のメッセージとエラー文で、**リテラルの総量をプロンプトの量と
読むと 25 倍外す**（私が一度そう見積もった）。**乗る面を測るには実際に組み立てる。**

**probe は捨てた**（Spec 34 と同じ扱い）。`tests/prompt_share_probe.rs` と
`delegation.rs` の一時テストは測ってから撤去。**数字が台帳に残れば probe は要らない。**

以下は 2026-08-11 時点の記録。

## 現在地（2026-08-11 更新）

**タグは `v0.1.1` = `b417dde` のまま。** その後 origin へ 28 コミット積んである
（**未タグ**。次に打つときは Thinking の 2 本と転送の切り替えが入る）。

**2026-08-11 に着地したもの**:

- **[Spec 32](specs/32_thinking-reception.md) Done** — 思考トークンを数える。
  **4 社すべてで `reasoning=` が出る**（`turn:` 行と `Usage`）
- **[Spec 33](specs/33_thinking-summary.md) Done** — 思考の要約を受け取って
  画面へ出す。**ネイティブ 3 ワイヤ**（xAI / Anthropic / Gemini）。
  **忠実度は社ごとに桁で違う**（xAI・Anthropic は 1 割未満、**Gemini は 74〜84%**）
- **転送を個体ごとに切れるようにした**（下記の節）。実機で
  「委譲したのに答えが利用者へ返る」が直り、**進行役が `rounds=2/36` で
  訊いてまとめる**形が観測できた
- **プライバシーポリシーを日英で追加**（Microsoft Store の登録用）
- **計器を 2 つ**（`handoff:` / `reply:`）。**転送はそれまでどのログ行にも
  出ていなかった**

**[Spec 34](specs/34_openai-responses.md) は P0〜P4 まで着地**（2026-08-11〜12。
**5 本目のワイヤ `Provider::OpenAiResponses`**）。残るのは P5 の実機で、
**検収 9 項目のうち 1a・1b・2 は観測済み**。その後が多モーダル。

**Spec 34 が残した構造と教訓は 5 つ**:

- **共有の単位は「測った側」と「決めた側」で別々に判断する** — 応答の型は
  11/11 発の serde 実測で共有（`Responses*` へ改名）、**要求のトップレベルは分ける**
  （`include` で必ず割れる）。「同じエンドポイントだから同じ型」は送る側で成立しない
- **`temperature` は欄ごと持たない**（400 実測）。モデル名の送り分けより、
  **送れる形が無いほうが強い**
- **受理集合は、誤った値を 1 つ送ると 400 が全部列挙する** — `minimal` を撃ったら
  `'none','low','medium','high','xhigh','max'` が返り、canonical の 5 値が
  全部通ると 1 発で分かった。正しい値を 1 つずつ試すより安い
- **`turn:` 行へ `backend=` を足した** — ワイヤを増やしたのに、**固有機能を
  使わない限りログが 1 行も変わらなかった**。ワイヤ固有の計器は
  「その機能を使ったとき」の証拠にしかならない
- **頻度ゲートの規律は、頻度が未知の事象にしか当たらない**（#47 / #72）。
  100% 起きる構造の穴へ「頻度を見てから」と書くのは、先送りに規律の名前を
  借りているだけ。Spec 34 では**同じ誤りを 2 回**した（画像の脱落 / 切り替えの案内）

以下は 2026-08-10 時点の記録。

**`v0.1.1` = `b417dde` を打った**（public 済み）。この版の中身は
**[Spec 31](specs/31_grok-live-search.md) Done = Grok の Live Search** で、
**起票から Done まで 1 日**・実機の検収 9 項目すべて観測。

**この Spec が残した構造は 3 つ**（次に各社機能を足すとき、ここに乗る）:

- **4 本目のワイヤ `Provider::XaiResponses`**（`/v1/responses`）。
  **OpenAI の thinking 復活と web 検索も同じ 1 本で解ける**（回収先が 2 社 4 機能）
- **モデル登録の「固有スキル」カテゴリ** — 利用者裁定で**恒久の器**になった
  （「今後はそれぞれのモデルの固有スキルを書くようにしたい」）。
  **アクティブ（トグル）とパッシブ（バッジ）の 2 形**を持ち、
  **載せる基準は「このアプリがその機構を実際に使っているか」**
  （サーバー側の自動最適化は載せない — 働きでないものを働きとして見せない）
- **`Grounding.engine`** の閉じた列挙。**Spec 05 で空だった `sources` が、
  初めて埋まる側で使われた**（Grok は実 URL を返す = Gemini と逆）

**この Spec で私は 3 回外している。どれも「1 サンプルから規則を作った」形**:
Live Search の分類（遠隔のツール → 実際はワイヤ）/ status URL の投稿者
（1 件の実ハンドルから一般化 → 実測は 77/77 が匿名形式）/ **集合へ要素を足す
影響**（否定側だけ数えて肯定側で踏んだ = #91）。

**v0.2.0 の残りは Spec 34（OpenAI の Responses ワイヤ）と多モーダル。** Thinking は
**[Spec 32](specs/32_thinking-reception.md)（思考トークンを数える）→
[Spec 33](specs/33_thinking-summary.md)（要約の受け取りと表示）→
Spec 34（OpenAI の Responses ワイヤ）**の 3 本に割れ、**32 と 33 は Done**
（2026-08-10〜11。**4 社すべてで思考トークンが取れ、ネイティブ 3 ワイヤで
要約の本文まで画面に出る**）。

**Spec 33 で分かった 3 社の非対称**（次に触るとき要る）:

| 社 | 送信側 | 既定で本文が | 忠実度（思考のうち返る割合） |
|---|---|---|---|
| **xAI** | 変更なし | **来る** | 2〜8% |
| **Anthropic** | `thinking.display: "summarized"`（**5 世代のみ**。旧世代は `adaptive` が 400 で、しかも既定で思考しない） | 来ない | 6% |
| **Gemini** | `thinkingConfig.includeThoughts`（ガード不要） | 来ない | **74〜84%** |

**Gemini は実質、思考そのものを返している** — 他 2 社と桁が違う。
**`kinds=reasoning:N` は item 数であって要約を持つ item の数ではない**
（2 周で 32 item のターンから捕獲されたのは 2 件）。

~~3 社まとめて。捨てている `decode` の行が 1 箇所なので最も安い。~~
**取り消し（2026-08-10。Spec 32 の起票時にコードで数えた）** — 実測は
**4 社 4 形**で、1 箇所ではない:

| 社 | 現状 | P1 の重さ |
|---|---|---|
| **Gemini** | **既に読んでいる**（`thoughts_token_count` を `completion` へ足し込み。`data_contract` に「この数え方でのみ `totalTokenCount` と一致する」と凍結済み） | **最も安い** |
| **xAI** | decode が数えるだけ（`XaiOutputItem` に payload の欄が無い） | 中 |
| **Anthropic** | ~~`budget_tokens` を送っていないので、そもそも返っていない~~ **取り消し（P4）** — `usage.output_tokens_details.thinking_tokens` が**実在し、何も送らなくても付く**（claude-sonnet-5 は**既定で思考する**）。P1 が「欄が無い」と判定したのは**自分の型を見て相手のワイヤを見なかった**から（`failures.md` #93） | **数はもう取れている**。本文の要求は Spec 33 |
| **OpenAI 互換** | `/v1/chat/completions` に**来ない** | ~~4 本目のワイヤそのもの~~ → **Spec 34 で着地**（`Provider::OpenAiResponses`。`reasoning.summary: "detailed"` で本文が返る — `auto` は 0 字） |

**数え落とした機序**: 「**捨てている**行」で数えたので、**足し込んでいる** Gemini が
網の外に落ちた。しかも Gemini の欄名は `thoughts_token_count` で、
`Thinking` / `reasoning` の grep では 1 件も出ない。**同じ概念に社ごとの呼び名が
あるときは、名前ではなく概念の側から数える**（#62 と同型）。

**Spec 32 の probe 8 発（2026-08-10・grok-4.5）で確定したこと**:
**`reasoning` は `completion` の内数**（8/8 で差 3〜5 = 本文のトークン数）/
**出力の 99% 以上が思考で本文は 4〜8 字**（払ったものの 99% がどの画面にも出ていない）/
xAI の `reasoning` item は `summary` を持ち、**`signature` も `encrypted_content` も
無い = 往復の要求が無い** / **`reasoning: {"summary":"auto"}` は効かない**
（短形と長形の分布が既定 2:2・auto 2:2 で一致。1 対 1 で止めていれば
「auto を送ると痩せる」という逆の実装をしていた）。

以下は 2026-08-08 時点の記録。

**v0.0.8 = `47b1cb7` を打った。利用者の言葉「これでほぼ v0.1.0」**（v0.1.0 で
public、v0.2.0 で公表）。**公開までのマイルストーンで v0.1.0 に要るもののうち、
残るのは Spec 27 の実機 2 件だけ**（検収 3・6。どちらも正常系ではないので
狙わないと出ず、利用者判断で保留中）。7・8 は同日に着地、2+4 は Spec 28 で Done、
3・5 は着地済み、9 は優先度低、6 は v0.2.0。

**この日に着地したもの**（34 コミット）: **Spec 28 Done**（実機 8 件全件）/
**Spec 29 は起票から Done まで 1 日**（作業フォルダの一括切り替え + 履歴）/
マイルストーン 8（Alt + ↑↓ で一覧の選択）/ 会話ペインを静かにする
（入退室の非表示 + 表示クリア）/ 絆ペインのリサイズ後の自動フィット。
**`failures.md` は #89 まで**（#88 承認の掃除漏れ / #89 入退室の通知の射程）。

**この日の 2 件のバグはどちらも「関数は正しい / テストは緑 / 射程だけが違う」形**
だった — #88 は**呼ばれていなかった**（`retain_for` の呼び出し元が 1 つ）、
#89 は**広く呼ばれすぎていた**（`from == System` だけを見て宛先を見ていない）。
どちらもコンパイラの外側で、**#89 に至っては Spec 07 から 9 日間在り、
気づく契機はテストではなくモデルの答えの違和感**だった。

以下は 2026-08-05 時点の記録（Spec 18 以前の経緯）。

**Spec 01〜17 のうち、実装が残っているものは無い。** 2026-08-05 に Spec 16 / 17 が
P0〜P3 まで着地し、**リポジトリは公開できる状態になった**（MPL-2.0・リリース
workflow・版番号表示）。**Spec 18 は rev3 承認 → P0〜P4 まで同日完了**
（コミット `703ffd9` / `40ea4ff` / `747af23` / `55dfc03` / `bbe6083`。
P4 は D12 どおり単独コミット = revert 単位が撤去に一致）。
**P5 も完了**（`91d740e`。README 日英 — rag の節新設 / 同梱ツール表 9 本 /
旧 RAG の記述回収 / 役職 4 欄 / **「null = 全提示」の旧記述が Spec 15 以降
誤りのまま残っていたのを同時に回収**）。**残: 実機確認 2 件**
（specs/ と blackboard/ の差 = 「効く条件」の検証 / 宣言外の拒否から宣言済みへの
切り替え）。**実機確認 1 件目は観測済み**（2026-08-05 16:49、D13 の再検証と同時）—
ザリが `D:\ManualeRAG` の宣言だけで `rag` を 4 回呼び、出力 6,576 → 1,164 →
1,079 → 365 字と絞り込んで完走（`rounds=4/36 stop=-`・キャッシュ 92%・利用者が
「いけました」）。**ただし `tool:` 行は `args_chars` しか書かないので、どの `op` を
使ったかはログから読めない**（`grep include:` と同じ計器の穴。実機確認 2 件目
= specs/ と blackboard/ の比較をログで検証したくなったら、`rag op:` の 1 行を
`grep include:` と同じ形で足す。今は足していない — 1 件目は本文の絞り込みが
数字で読めたので不要だった）。

**2026-08-05 に入ったもの（14 コミット）**:

| | 内容 |
|---|---|
| `9f1caf0` | `shell.json` → **`run.json`** 改名 + `command.json` の取り残し 16 箇所 |
| `3aed3b1` | **Spec 17** batch preview（`sd` に `paths`） |
| `000f881` | **Spec 16** grep の `include` / `context` |
| `16a9160` | `grep include:` の計器（D1 の賭けを実機で読めるように） |
| `fcd6079` | ブランチ `20260805_01` の合流（役職ダイアログを CodeEditor へ） |
| `fb82dca` | 黒板のフォルダ名を **`blackboard/`** へ |
| `2702e55` | 作業フォルダ欄の「参照…」ボタン |
| `f9bed59` | ステータスバーへ **版番号** |
| `96e0478` | **リリースビルドの workflow**（`v*.*` タグで 3 OS） |
| `1f87e1d` | **MPL-2.0 へ relicense** |
| `c7695e6` / `c291db6` | **Spec 18 起票**（+ Goal 差し替え） |

**タグは利用者が打つ**（`v*.*` の push だけで 3 OS のリリースビルドが走る）。
タグの履歴（**「現在」を 1 つ書くと次のタグで腐るので、列挙が正** — #67 の処方）:

- `v0.0.1` / `v0.0.2`（`96e0478`）/ `v0.0.3`（Spec 20 Done）
- `v0.0.4` = `3703e63`（Spec 23 Done）— **依存 +9 crate（`image` ほか・純 Rust）後の
  最初のリリースビルドで、3 OS 緑を実測済み**
- `v0.0.5` = `2a0e47a`（Spec 24 Done + Spec 25 rev3 + build.yml 修正。2026-08-07）—
  **打った日に GitHub 側の障害**（イベント配送の遅延 + Releases API の一時 403
  「Resource not accessible by integration」）で 2 度失敗し、障害回復後の
  打ち直しで 3 OS 緑。**「タグの消して同名再作成」は発火しないことがある** —
  確実に再発火させるには新しいタグオブジェクト（annotate し直し）で push する。
  この際 v0.0.2〜0.0.4 の release と artifact は整理で削除済み（installer は
  タグからリビルドで再現可能）
- `v0.0.6` = `5698d4a`（Spec 26 P0〜P3。2026-08-07）— **3 OS 緑**（Actions で確認）
- `v0.0.7` = `5191a11`（Spec 25 P5 / Spec 26 P4 の全件観測 + Spec 27 P0〜P3 +
  ツール名の動作名化。2026-08-08）
- `v0.0.8` = `47b1cb7`（**Spec 28 Done + Spec 29 起票から Done まで** +
  マイルストーン 8 着地 + 会話ペインを静かにする + 絆ペインの自動フィット。
  2026-08-08。**34 コミット**）— **利用者の言葉「これでほぼ v0.1.0」**
- **`v0.1.0` = `a516f78`（2026-08-09）— このタグでリポジトリを public にした。**
  内容は **Spec 30 Done**（改名 `Concordia` → `Fuseforks`。検収 5 まで観測）+
  **README を入口へ縮めて詳細を `DETAIL.md` へ分割**（1,655 → 148 行）+
  `images/`（ロゴとスクリーンショット）。**残るマイルストーンは公表（v0.2.0）**
- **`v0.1.1` = `b417dde`（2026-08-10。18 コミット）— [Spec 31](specs/31_grok-live-search.md)
  Done**（Grok の Live Search。**4 本目のワイヤ `/v1/responses`** + モデル登録の
  「固有スキル」+ 来歴の表示）。**`failures.md` #90 / #91 を派生**
- **`v0.1.2`** — **破棄（利用者判断）。タグは origin に残し、Release だけ削除した。**
  **打ち直しのときはここを見る** — タグを消しても Release は残り、
  Release を消してもタグは残る。**2 つは別々に片付ける**
- **`v0.1.3` = `be1ed3d`（2026-08-12。78 コミット）— Spec 34 Done（5 本目のワイヤ
  `Provider::OpenAiResponses`）+ 黒板の削除を画面から + `run` の実バグ 2 件**
  （`PROGRAMDATA` / 打ち切りとタイムアウトの `kill`）。**1 度目は落ちた** —
  `be1ed3d` の 1 つ前 `e935d2d` で打ったら **macOS と Ubuntu だけが赤**
  （`failures.md` #100。付箋名の区切り検査が `Path::file_name()` に寄りかかり、
  Unix では `\` が区切りではないので素通りした）。
  **そのとき Windows だけが完走して Release が public になった** —
  `build.yml` は `fail-fast: false` の matrix で**各ジョブが独立に
  `releaseDraft: false` の `tauri-action` を呼ぶ**ので、**1 OS 通れば公開される**。
  タグと Release を消して打ち直した。~~**片肺公開を塞ぐかは未決**~~
  → **2026-08-13 に決着。`releaseDraft: true` へ変えた**（`v0.1.5` で 2 回目を
  踏んだため。**同じ罠を 2 回踏んだら処方を注意から構造へ移す** = #67 の規律）。
  **以後、Release は draft で作られ、人が publish する** — 3 OS 分のアセットが
  揃ったことを見てから出せる。**効くのは次のタグから**
- **`v0.1.4` = `5fc4655`（2026-08-12。18 コミット）— Spec 35 Done（モデルへ届く面の
  二言語化）+ `agent-core` → `fuseforks-core` 改名 + `run` の stdin 修正**
  （Windows OpenSSH の 107 秒ぶら下がり解消）。**1 度目（`04a39ae`）は
  3 OS 全部が同じ 6 テストで赤**（`failures.md` #101 — テストの入力にホストの
  OS ロケールが混ざっていた。開発機 ja / ランナー en）。**全部落ちたので
  片肺公開は起きていない**（偶然の防御。ランナーが 1 つでも ja なら #100 の再演
  だった）。リリースノートは日英併記にした（この版の主題が英語対応のため）
- **`v0.1.5` = `06489ce`（2026-08-13。23 コミット）— [Spec 36](specs/36_multimodal-attachments.md)
  Done（多モーダルの添付）+ [Spec 37](specs/37_meta-responses.md) Done（6 本目のワイヤ
  `Provider::MetaResponses`）**。3 OS 緑・アセット 7 件。
  **1 度目の ubuntu はランナーの DNS で落ちた** — `index.crates.io` を引けず
  **依存解決の前で終了**（`cargo test` の step だが**テストは 1 本も走っていない**。
  ログの見出しだけ読むとテストの失敗に見える）。再走で緑。
  **その待ち時間に片肺公開が起きた** — mac と Windows が先に完走し、
  **アセット 4 件で public になっていた**。ubuntu の完走で 7 件に揃って自己修復したが、
  **これが 3 回目**（v0.1.3 / v0.1.5 で 2 回、#101 の全 OS 赤で 1 回免れた）。
  **`releaseDraft: true` はこの走行を見て入れた**（`bfa22ed`。**効くのは次のタグから**）。
  リリースノートは v0.1.4 と同じ日英併記で、**「Meta へ貼ったファイルは期限なしで
  保存される」を独立した節にした** — 機能の説明に混ぜると読み飛ばされるが、
  これは機能ではなく**利用者が負う条件**のため
- **`v0.1.6`** — **Release は破棄（タグは残す）。** `v0.1.7` に置き換えられた。
  破棄の作法は `v0.1.2` と同じ — **タグは履歴、Release は配布物**
- **`v0.1.7` = `d149645`（2026-08-13。17 コミット）— 絆の地図を `v-network-graph`
  へ差し替え + 失敗ターンの払いを計器へ**（`failures.md` #102 / #103）。
  3 OS 緑・アセット 7 件。
  **`releaseDraft: true` が入れた翌日に実際に効いた** — macOS だけが
  **アップロードの最終段**で落ち（`Error creating policy`。ビルドもテストも完走
  していて、dmg も `.app` も生成済み）、**Linux + Windows の 5 件が乗った draft の
  まま止まった**。旧設定なら **5/7 で public になっていた**。失敗ジョブだけ再走 →
  7 件で publish。
  **片肺の危機は 3 回とも別の OS が生き残り、原因も毎回違う**（テストの assert /
  ランナーの DNS / アップロードのポリシー）。**原因を 1 つずつ潰しても被害の形は
  同じ**という、draft 化を決めたときの判断がそのまま裏付けられた。
  リリースノートは `v0.1.6` を置き換える版として 1 本にまとめ、**「失敗したターンの
  払いは予算に計上されないが課金はされる」を独立した節にした**（v0.1.5 の Meta の
  ファイル保存と同じ扱い — **利用者が負う条件**は機能の説明に混ぜない）

- **`v0.1.8` = `95ad2fe`（2026-08-16。23 コミット）— [Spec 38](specs/38_budget-reserve.md)
  Done（予算の CAS 予約）+ **失敗したターンの払いの清算**（#103）+
  [Spec 39](specs/39_stats-view.md) Done（統計画面）**。`specs/tla/` の新設と
  `failures.md` #104〜#107 もこの版。
  **リリースノートの主題は統計画面**（コミット数では Spec 38 が最大だが、
  **画面が変わるのは統計のほう** — v0.1.3 で「Responses は入口、画面が変わるのは
  要約」と判断したのと同じ形）。**利用者が負う条件は 3 つ**を独立した節にした:
  統計の記録はこの版から / 天井付近では波の一部が欠ける（Spec 38 の代償・
  再依頼しない）/ **統計の「全会話」は会話のタイトル = 依頼文の先頭 30 字を
  一覧で出す**（スクショを共有するときに写る）。
  **v0.1.7 のノートに書いた条件が 1 つ解消した**（失敗したターンの払いが
  予算に計上されない）— **条件は足すだけでなく、消えたことも書く**

- **`v0.1.12` = `c79e957`（2026-08-29。15 コミット）— Spec 45（Perplexity の
  7 本目のワイヤ + 固有スキル 4 本）+ 起動の二度押し封じ + 絆の破線の両端条件 +
  #113 のフレーク畳み**。3 OS 緑・アセット 7 件・publish 済み（06:58 UTC）。
  **リリースノートの主題は Perplexity の固有スキル**（画面が変わるのはトグル 4 本）。
  「利用者が負う条件」は 2 つ — Perplexity の検索は向こう側でドル建て課金
  （予算の天井はトークン建てでドルを数えない）/ 4 スキル全 ON はツール選択が
  バラけるので用途別テンプレートを推奨。**winget マージ（PR #418363・同日）と
  同じ日** — ノートへ winget / brew のインストール節も入れた。
  タグは利用者が打ち、Release publish・tap 0.1.12（`5f4b8db`・digest から
  sha256）はこちら。**winget の 0.1.12 提出は利用者の手元**
  （wingetcreate の GitHub device 認証コードがこちらのコンソールから読めない —
  出力捕獲に乗らない形で表示される）
- **`v0.1.11` = `acb80a6`（2026-08-25。58 コミット）— Spec 41 / 42 / 43 / 44 の
  4 本と Spec 39 rev4 / Spec 40**。3 OS 緑・アセット 7 件。
  **`v0.1.10` の Release は draft のまま削除した**（利用者判断）ので、
  **リリースノートは v0.1.9 → v0.1.11 の全部を 1 本で覆っている** —
  Spec 40（キャッシュ書き込み）と Spec 39 rev4（`by_agent` を (個体, モデル) へ）は
  v0.1.10 の内容だが、この版のノートに入れた。**「利用者が負う条件」は 5 つ**で、
  重いのは **MCP で外から使う村は `.mcp.json` へ `timeout` 併記が要る**こと
  （既定が 600 秒になり、クライアント側の既定より長くなった）。
  **1 度目は macOS と Windows が赤**（`failures.md` #113 — `budget.rs` の
  有界ループの上限が**回数**で切られており、実時間で数ミリ秒しかなかった。
  Ubuntu だけ通ったのは**その回のランナーが速かったから**で OS 依存ではない）。
  **コードを 1 行も変えずに再走して 3 OS 緑**になり、フレークだと裏付いた。
  **`releaseDraft: true` が 4 回目に効いた** — 初回は Ubuntu の 3/7 アセットで
  止まっており、旧設定なら片肺公開になっていた。
  **フレークは同日に別コミット `efe3b7d` で畳んだ**（上限を実時間へ。
  実装は無改変・タグには含まれない）。
  Homebrew tap は `66838ac` で 0.1.11 へ（**sha256 は Release API の `digest`
  から取れる** — dmg を落とさなくてよい）。**winget は据え置き** —
  `Outcasts.Fuseforks` の PR #418363 が未承認のままなので、出しても同じ列に
  2 本並ぶだけで早くならない
- **`v0.1.10` = `fc813b3`（2026-08-18。14 コミット）— 配布経路の整備と台帳**。
  **この版に Spec 40 / 41 は入っていない**（タグの後に着地したので、単価と金額は
  **次のタグから**）。**Release は draft のまま削除した**（2026-08-25。
  中身は v0.1.11 のノートが覆っている。**タグは残す** — `v0.1.2` / `v0.1.6` と
  同じ作法で、タグは履歴・Release は配布物）
- **`v0.1.9` = `38575fd`（2026-08-17。5 コミット）— macOS の署名と公証**。
  **アプリのコードは 1 行も変わっていない**（`crates/` と `apps/*/src` の diff が
  空。変わったのは `build.yml` / `README` 日英 / `CLAUDE.md` の 4 ファイルだけ）。
  **配布物の性質だけが変わった版**で、リリースノートの主題もそこに置いた。
  **利用者が負う条件は 2 つ** — macOS は Apple Silicon 専用（以前からそうだったが
  どこにも書いていなかった。この版から README に明記）/ **機能追加も不具合修正も
  ゼロなので Windows と Linux には更新する理由が無い**。
  **署名の射程は macOS だけで、Windows のインストーラは依然として未署名**である
  ことも書いた（書かないと全体に掛かると読まれる）。
  **1 度目の macOS は 401 で落ちた** — `APPLE_PASSWORD` に App 固有パスワードでは
  なくアカウントのパスワードを入れていた。**タグは打ち直していない** —
  `gh run rerun --failed` で macOS のジョブだけ再走させ、Windows / Ubuntu の
  success と既にある draft の 5 アセットをそのまま生かした。
  **`releaseDraft: true` がまた効いた**（macOS 欠けの 5/7 で public になる形を
  塞いだ。片肺公開の 4 回目になるところだった）

**リリースの installer は Release Assets が正で、artifact には残さない**
（`2a0e47a` で `Upload artifacts` step を撤去。Release Assets と同じものの
7 日限りの複製で、ストレージ枠を食うだけだった）。

| 残っている実機確認 | 何を見るか |
|---|---|
| **Spec 16（4 件）** | `include` で台帳の一致が消える / **`*.rs` を書いたモデルが次のターンで `\.rs$` へ直すか**（D1 の賭け・計器あり）/ `context` の行を取り違えない / 打ち切りで `include` を選ぶ |
| **Spec 17（3 件）** | 10 ファイルが **11 呼び出し**に収まる / 一致ゼロへ apply しに行かない / `paths` + `apply` の拒否から 1 本ずつへ切り替える |
| **Spec 22 P5（3 件）** | 切れた抜粋の行頭 ID で全文を取得して完遂（`room_log read: outcome=ok`）/ opt-out 個体に `room_log` が出ない / 存在しない ID が not_found で返りターンが続く |
| Spec 14 P5（1 件） | 顔ぶれの `［役職］`（**`fuseforks.log` で読む** — プロンプトへ入る唯一の経路なので画面には出ない） |
| 条例の束ねの検証 | **本命経路**（独立検証役への ask）。フォールバックは観測済み |
| RepeatGuard | 並列の周で 1 本だけ止まる。**観測待ちで作業は無い** |

**Spec 16 と 17 は 1 回の走行で両方確かめられる** — `grep` で対象を見つけて
`sd` の `paths` で束ねて preview する依頼が、そのまま両方の経路を通る。

**[Spec 19](specs/19_user-identity.md) 起票・rev1 承認 → P0 完了**（2026-08-05）—
利用者の**呼び名とアイコン**をシステム設定「全般 > ユーザー」から設定できるようにする。
**起票時の実測で「呼び名は 1 つではなく 2 つあった」** — サーヴァントが読む封筒
`【送り手: ユーザー】`（`orchestrator` の `attribute_sender`。**日本語固定で
言語に追従しない**）と、会話ペインの表示名 `chat.you`（ja「あなた」/ en「You」。
**言語に追従する**）。本 Spec は 2 つを 1 つの設定へ畳む。
**封筒は毎ターンの user ロール本文なので `stable_len` は動かない**（呼び名を
変えても全員のキャッシュは割れない）。**P1 完了**（`normalize_user_name` /
`InvalidUserName` / `DEFAULT_USER_LABEL` / `attribute_sender` の分岐。単体 5 + 結合 2）。
**P2 完了**（`{workspace}/user/icon.webp`。`validate_icon` / `write_icon_at` /
`read_icon_at` / `delete_icon_at` を切り出して**エージェントと利用者で 1 実装を共有**。
単体 2）。**P3 完了**（IPC 5 本 + `state.userName` / `state.userIcon` +
システム設定「全般 > ユーザー」+ `ChatPanel` の表示名とアイコン + 辞書 ja/en。
vitest 127 → 132）。**P4 完了**（README 日英 4 箇所 + `settings_contract` +
`failures.md` #67）。**P5 完了 = Spec 19 は Done**（2026-08-05。実機確認 5 件とも OK）。

**P5 で検収項目の書き方の誤りが 1 つ出た**（利用者の指摘）。項目 4 を
「アイコンを設定して**会話ペインとサーヴァントの絆の両方**を見る」と書いていたが、
**絆の地図に利用者のアイコンが出る経路は 1 本も無い** — ノードは
`TopologyMap.vue` の `[...state.agents]` からしか生えず、利用者はノードとして
存在せず、`userIcon` の参照はゼロ件。**利用者のアイコンが出るのは会話ペインだけ**
（`AgentList.vue` も `state.icons[agent.id]` なのでサーヴァント専用）。
**害は範囲が広すぎたことではなく、合格条件が存在しない項目を置いたこと** —
地図で何も変わらないのを見ても「正解なのか実装漏れなのか」を判断できない。
**一般化: 検収項目を書く前に、その画面がその値を引いているか数える。
確かめられない項目は書かない**（`failures.md` #68。Spec 14 D5 / Spec 17 P1 と
同じ規律の三例目）。

**そこから出た「では絆の地図に利用者のノードを出すか」は、利用者がその場で裁定 —
出さない**（2026-08-05。`user_identity_contract` 凍結 9）。理由は
「**みんなに話せるので意味ない。ユーザーはサーヴァントみんなと繋がっている**」。
実測でも `send_user_message` は接続を**一切検査しない**（`connected_agents` は
サーヴァント同士の委譲にしか効かない）ので、**利用者の辺は構造上かならず全結線**。
**地図の情報量は「繋がっていない組み合わせがある」ことから来る**ので、全員と
繋がるノードは辺を N 本増やして情報を 0 しか増やさない。利用者の到達範囲は
そもそも線で描かれていない = **トポロジーの模型の外に居る**（「線は人が引く」の裏返し）。
**凍結 5（顔ぶれに載せない）と結論は同じだが根拠が違う** — 顔ぶれは*誤読を誘発する*、
地図は*無情報*。**同じ結論に別々の根拠があるときは両方書く**（片方が将来変わっても
もう片方が残る）。

**P4 で `settings_contract` の「新規の設定は 2 つだけ（言語・線削除の確認）」が
既に嘘になっていた**のを回収した。テーマが 2026-08-05 に着地した時点で 3 つ目が
入っており、**その着地は同じブロックの置き場の一覧に `theme` を足していた** —
10 行下の数だけが古いまま残った。**#62 の一般化（数の記述は、増えた名前で grep
しても引っかからない）を書いた後の再発。** 処方を注意から構造へ移した
（`failures.md` #67）— **総数を書かず要素を列挙すれば、追記そのものが更新になる。**
**一般化: grep で守れない記述は、grep しなくても壊れない形へ書き換える。
同じ罠を 2 回踏んだら、処方を注意から構造へ移す合図。**

**P3 で「変更系 IPC の呼び方が 2 経路ある」を踏んだ。** `saveLanguage` は
**生の `ipc` + try/catch** で書かれているが、`orchestrator.*` の action は
`mutate` を通り**例外を飲んで `false` を返す**。`saveLanguage` の形を写して
`orchestrator.setUserName` を try/catch で包むと、**catch へ入らないまま
「保存済み」と表示される**。**成否は戻り値で見る。** 使い分けの基準は
**投影を持つか** — 呼び名は `state.userName` を会話ペインが読むので
`orchestrator` 経由、言語は反映先が `setLocale`（グローバルな i18n）で投影が
無いので生の `ipc` でよい。**これはテストではなく実機でしか出ない**
（コアは正しく拒否し、画面だけが嘘をつく）。

**P1 で「同じ事故の処方でも 3 点すべてが常に要るわけではない」が出た。**
`api_key_env` の処方は (1) 保存時に拒否 (2) 読み込み時に不正値を落とす
(3) 正規化で内容が変わったら即ファイルへ書き戻す、の 3 点だった。呼び名は
(1)(2) を採り **(3) を採らない** — (3) が要ったのは**残るのが秘密だった**からで、
呼び名がディスクに残る害は「起動のたびに WARN が出る」だけ。むしろ手で直す
手掛かりになる。**要否は「残るものが秘密か」で決まる。**

**[Spec 20](specs/20_command-approval.md) — Done**（起票・rev2 承認・P0〜P4 =
2026-08-05〜06）—
`run.json` の `pending` を**タイトルバーの「コマンド承認」**から承認・却下する。
起点は利用者 —「今は手で写すのだが、リスト化して承認か却下ボタンを押して消していきたい」。
**取り除くのは JSON を書き写す手間であって、1 件ずつ許容を決める手間ではない**
（後者は閉じた許容の本体。Spec 15 D10 は取り下げていない）。

**査読が実装すると割れる 4 点を出した** — `pattern(open)` の規則が一意に決まらず
**実装不能**だった / 「存在しない要求は何も変えない」と差分適用が両立しない /
**IPC が `pattern: String` だと「粒度は機械が決めない」が「GUI が何でも決められる」へ
反転する** / バッジの数え方が未定義。rev2 で 8 点すべて反映し、**1 点は理由を訂正して
採用した**（`Malformed` の迂回は成立しない — `pattern_matches` はパターン側の先頭も
`normalize_command` に通すので、パス区切り入りは何にも一致しない。検査を入れる理由は
**防御ではなく衛生**）。

**着手中に既存のバグを 2 件見つけて直した。どちらも「契約へ書く前に実測した」ことで出た**:

- **`*` 単独のパターンで panic**（`failures.md` #69）。`&parts[1..parts.len()-1]` が
  要素 1 個で `&parts[1..0]` になる。**列の先頭と末尾を別々に落とす式は要素 1 個で
  交差する。境界値のテストは 0 と 2 だけでは足りず、1 が本番**
- **壊れた `run.json` へ `pending` を積むと、人の `allow` / `deny` が両方消える**
  （`failures.md` #70）。読みは既定へ落として安全側（全部 pending）だが、
  **その既定を書き戻すのが破壊的**だった。**「安全側へ倒す」は読みの語彙で、
  書きでは意味が変わる** — 読みは*判断を厳しくする*ことだが、書きは*記録を捨てる*こと。
  **契約も同じ誤りを持っており**（「deny が黙って消えるので危険側」= 片方だけ消える
  前提）、実測に合わせて訂正した

**P4 の実機で指摘 2 件。どちらも設計の穴で、実装の誤りではない**（2026-08-05）:

- **「二重に登録される」は決着済みの残骸だった。** 畳み込みは正しく効いており、
  ログの決定的な行は `allow=5 のまま decision=Allowed / pending=1` —
  モデルが少し違う引数で呼び直して既存パターンに当たり、**10 秒前の pending が
  残った**。`pending` から要素が消えるのは「その 1 件を承認・却下」と「上限で
  押し出し」の 2 つだけで、**広いパターンで別の要求を承認しても、それが覆う
  pending は残る**。実機の `allow` に `curl -sI *` と
  `curl -sI https://www.yahoo.co.jp/` が並んだのがその帰結。処方は
  `prune_settled`（**残ってよいのは、いま判定して `Unknown` のものだけ**）。
  **判定は既存の `decide` をそのまま使う** —「一覧に出るべきか」と
  「実行してよいか」は**同じ問いの裏表**で、別の述語を置くと答えがずれる
- **notify が付かないのは P2 の判断の裏側だった。**「`refreshAll` に混ぜない」
  （全操作で全個体の `run.json` を読むのは払いすぎ）は正しかったが、**絞った結果
  バッジの存在理由が消えた** — 開かなくても気づくために置いたのに、再起動まで
  気づけない。**コアを 1 行も変えずに直った**（`toolInvoked` が `tool` 名を運び、
  `run` は判断待ちが増える唯一の経路）。**一般化: 「頻度を絞る」判断をしたら、
  その代わりに何が更新されなくなるかを数える。節約の対象と、その機能が成り立つ
  条件は別に確かめる**

**P4 の検収 5 件中 4 件を観測**（2026-08-06。`concordia.log`）。決定的なのは
**却下**の 2 行 — `04:50:46 command=python decision=Unknown allow=8 deny=2` の
106 秒後に `04:52:28 command=cargo ... deny=3` で、`run.json` の `deny` に
`"python -v"` が入っている（同区間で `allow` にも `"cargo -v"` が入り 8 → 9）。
**どちらも `pending=0` で終わっており**、`prune_settled` が押し出しではなく
判定で消していることも同時に読める。

**検収 5 は踏めなかった — 項目ではなく機構が誤っていた**（2026-08-06。
**起点は利用者の読み**「`allow` が空だと `run` を持っていると認識できない。
でも `pending` はできないとダメだろう。`run` にチェックがついているならね」）。
実測すると**3 つの機構が輪になって閉じて**いた:

```text
allow 空 → offers_anything() = false → spec_for が None → run は提示されない
        → 提示集合が実行フィルタでもある（orchestrator の executable）
        → 呼び出しが弾かれ call() に届かない → note_pending に届かない
        → pending は空 → resolve が NotFound（D9）→ allow に 1 件も足せない
```

**`allow` の 1 件目は人が `run.json` を手で書く以外に入る経路が無く**、
承認画面は「書き写す手間」を 1 件目だけ引き受けられていなかった。

**裁定: `run` にチェックがあれば `allow` が空でも提示する**（要求は積める・
実行はできない）。**fail closed は提示ではなく `decide` が守る。**
`offers_anything` は `allows_anything` へ改名（**提示の判定に使わない**ことを
名前で示す）。`allow` が空のときの説明文は「この道具では何も実行できない」と
言い切り、パターンの箇条書きを出さない — **提示する以上、いま何ができないかを
本文で言わないと、モデルは実行できるつもりで呼ぶ**。回帰 2 段
（`tools::run` の単体 + orchestrator の結合）で「親切心で 2 段目を戻さない」を留めた。

**これは実装の誤りではなく契約の誤りだった** — `command_tool_contract` の D10 は
**同じ文の中で矛盾していた**（「**全部 pending**」と「**提示されず**」は両立しない。
提示しなければ `note_pending` に届かないので、pending は永遠に空）。実装は
矛盾した契約の片側を正しく実装しており、**テストもその片側を凍結していた**
（`run_needs_both_the_opt_in_and_a_non_empty_allow_list`）。
**一般化: 契約の 1 つの箇条書きの中に「機構 A の結果」と「機構 B の条件」が
同居していたら、両立するかを実装で数える。** grep は矛盾を教えない —
どちらの語も同じ行にあるので、**引くと必ず 1 件ヒットして正しく見える**
（`failures.md` #74）。

**書き直した検収 5' も同日に観測して Spec 20 は Done**（2026-08-06、`agent_8`）。
**決定的なのは `allow=0 decision=Unknown` の 1 行**が出たこと自体 — 提示され、
モデルが呼び、`call()` まで届いた証拠になる（修正前は実行フィルタで弾かれ、
**この行は原理的に存在し得ない**）。35 秒後に `allow=1 decision=Allowed`。
**「ある行が出た」ではなく「その行が存在しうること」が証拠になる形**で、
出力の内容ではなく**到達可能性**を読んでいる。

**未決として残っているもの**: (a) **Exhibit A のファイル冒頭注記**を入れるか
（MPL は推奨するが `LICENSE` 同梱で代替可。数百ファイルへ 3 行は doc コメントと
場所を争う） (b) **黒板の集計表が古い**（ザリ本人しか直せない）。
**ロックファイルの二重管理は 2026-08-05 に解消**（`package-lock.json` を削除して
`bun.lock` へ寄せた。下記「リリースビルド」の節）。

**公開の段取り（利用者 2026-08-05）**: **v0.1.0 で public、v0.2.0 で公表。**
**public は 2026-08-09 に着地**（`v0.1.0` = `a516f78`）。
**公表も済んだ**（2026-08-15 利用者申告 — Qiita と **note** の 2 媒体。
Qiita の URL は上の「現在地」にあるが、**note の URL は台帳に無い**）。
**つまり公開の 2 段はどちらも閉じている。** 残る関心は reddit への投稿
（利用者の言葉「reddit に書きたいというのもある」— 未着手）。

**履歴の秘密走査は 2026-08-09 に全文で完了し、実キーはゼロだった。**
初回（2026-08-05）は `*.json` / `*.toml` / `*.yaml` / `.env*` だけで
**Markdown と Rust が射程外**だったので、今回は**拡張子を絞らず 459 コミット・
244 追跡ファイルの追加行を全部**走査した（`sk-ant-` / `sk-` / `AIza` / `xai-` /
`xox?-` / `ghp_` / `gho_` / `github_pat_` / `AKIA` / `outcast_sk_` / PEM）。

- 当たったのは **7 行すべてが試験具か doc コメント**
  （`sk-ant-api03-EXAMPLE-NOT-A-REAL-KEY` ×3 / `sk-ant-api03-abcdef` /
  `sk-ant-SUPER-SECRET` / 説明文の `sk-ant-…` ×2）。出所は `world.rs` と
  `tests/orchestrator.rs` = **書式検査そのもののテスト**
- `AIza` / `ghp_` / `xox?-` に当たった各 3 行は、**この CLAUDE.md が走査
  パターンを列挙している文**。鍵ではない
- **村の実データは 1 度も追跡されていない**（`world.json` / `sessions.redb` /
  `*.log` / `village_id` / `probe_approvals.json` を履歴の追加ファイル全件で確認。
  唯一当たる `probe_approvals.rs` はソース）
- **計器の検定を先に取った**（`failures.md` #90 の処方）— 同じ走査で
  `Fuseforks` が 130 行・`sk-ant-` が 7 行出ることを見てから 0 を読んだ。
  **走査が空振りしていないことを確かめずに「該当ゼロ」と書かない**

**#1 の実キー漏洩はリポジトリではなく `%APPDATA%` の `world.json` で起きた**ので、
この結果と矛盾しない。**公開後の走査は取り返しがつかない** — 次に別の村を公開する
ときは、この順序（走査 → public）を守ること。

**[Spec 24](specs/24_path-completion.md) Done（2026-08-07。起票から 1 日）** —
入力欄で `@` からファイルパスを補完する。起点は利用者「Claude Code や Copilot の
`@` 補完のような機能を」。**効き目が大きいのは打つ手間ではなく、サーヴァントが
探す周回が消えること**（周が増えるたびにプロンプト全体が再送される）。

**Claude Code の `@` を実測した**（同日）— `@01_sd-yq-write-tools.md` まで打つと
本文は `specs/01_….md` へ正規化され、**中身が 279 行まるごと展開**され、
しかも `Read` **ツール呼び出しの形**で届く（ハーネスがターンの前に代わりに実行）。
**`use_tool` の回数は減っていない。減っているのは周回。** 絶対パスは転送層だけで、
人の読む面にもモデルの読む本文にも出ない。

**この村は展開しない**（D1）。**ツール結果は履歴に積まれないが、発話は積まれる** —
`file read` の結果は 1 ターン、発話は滑る窓 8 往復。**発話へ埋め込むと 1 万トークンの
ファイルが最大 8 回再送される**。**Spec 23 の D1 と同じ構造**だが、
**pull なら既存の `file read` の寿命が最初からそれを満たす**ので機構を作らずに済む。

**Spec を切ったのは規模ではなく、D1 と D2 が取り消しにくいから。**
**D2 = `@` の名前空間** — Slack / Discord では `@` は人で、README の「意図的に
未実装」に**利用者からの同報**が保留として載っている。実装するなら
`@ザリ @ジェミー` が最も自然な入口になるので、**ファイル専用として作るとその道が塞がる**。
**空いている名前空間の使い道は、埋める前にしか決められない。**

**査読が rev1 の明確な矛盾を 1 件出した** — D2 は「種別を持たせる」と書きながら
P0 の IPC に `kind` が無く、**目的と逆に将来 IPC を壊す形**になっていた。
**提示された 2 案のうち B（IPC は file 専用）を採り、査読が持っていなかった
根拠を足した** — **`state.agents` は `AgentSnapshot[]` をフロントに既に持っており、
将来の同報は IPC を 1 本も必要としない**。だから IPC へ `kind` を載せると
**`"file"` 以外が永久に入らない空の欄**になる（Spec 15 D7 で `scope` 欄を
今作らなかったのと同じ判断）。**合流はフロントで起きるので、拡張点も
フロントの候補型に置く。** ほか rev2 で反映した 2 点: **補完は安全境界を
1 つも増やさない**（挿入は `collect_files` 由来のみ・手で改竄しても `file read` の
`resolve_in_work_dir` が弾く。**囲いは読む側にあり、補完側に検査を足すと
同じ規律が 2 箇所に生える** — 「参照…」ボタンと同じ判断）/ **Enter の検査順序を
表で固定**（`isComposing` → 補完が開いている → 送信。**最優先が IME なのが
この村に固有**で、2 番目に置くと日本語でだけ壊れ、型検査にもテストにも掛からない）。

**P0〜P3 完了**（2026-08-06。rust 591・vitest 186・clippy 警告ゼロ）。実装で
出た判断のうち、次に触る人が要るもの:

- **P0**: 走査は `spawn_blocking` へ逃がす（20,000 件の同期 I/O でワーカーを
  塞ぐと**他のサーヴァントのターンが止まる**）/ **読めない作業フォルダは空の
  一覧でエラーにしない** — 壊れた work_dir は `resolve_in_work_dir` が既に
  名指しで報告するので、2 つ目の報告経路を作らない
- **P1**: **S5（`@` を含む普通の文章が通る）は「クエリに空白が入ったら閉じる」
  1 つの規則の帰結**で、専用の判定を書いていない。確定時に足す末尾の空白が
  **そのまま補完を閉じる**（閉じる処理を別に持たない）
- **P2**: **候補 0 件のときはキーを奪わない**（`popupCaptures` と `popupVisible`
  を分けた）— 一致しないクエリを打っている最中に Enter が飲まれると
  **送信できない理由が画面から読めなくなる** / 候補ボタンの
  `@mousedown.prevent` が無いと `blur` が先に発火して**クリックが当たらない**
- **見た目は実機の指摘で 3 回直した**（2026-08-06）— (1) 1 行を
  「アイコン + ファイル名（主）+ フォルダ（薄く）」へ。**D5 で「照合は
  basename 優先」と決めたのに表示が映していなかった** (2) 流し込みから
  `absolute` + `bottom-full` へ（開くたびに入力欄が下がり会話が縮んでいた）
  (3) 擦りガラス（`bg-surface-1/80` + `backdrop-blur-xl`）。**トークンに
  アルファを掛けるので `color-mix` へ展開され、ライトテーマの上書きに
  実行時で追従する** — 計算値をブラウザで実測して確認済み
  （dark `oklab(0.23 …/0.8)` / light `oklab(0.972 …/0.8)`）
- **P4 完了 = Done**（2026-08-07）。**決定的なのは検収 2** —
  `turn start: … chars=20` の直後が `tool: round=1 name=file` で、
  **`fd` も `grep` も 1 本も出ていない**。前日の同型のターン
  （`round=1 name=fd` → `round=2` で `file`×3）と**対で読める** = 探索の
  1 周が丸ごと消えた。`chars=20` も計算に合う（パス 19 字 + **確定時に足す
  末尾の空白 1**）。**見立てが 1 つ外れた** — 起票時に「`blackboard/` が候補を
  埋めるなら `SKIP_DIRS` へ足せる」と書いたが、**利用者が実際に使ったのは
  `@blackboard/ソネット.md`** だった。**邪魔だと思った候補が、最初の実用例だった**
- **隠しフォルダと `.gitignore`**（2026-08-06 利用者裁定）: **候補に出さない
  ままでよい**（速度。`SKIP_DIRS` が飛べば十分）。
  **なお隠し「ファイル」は候補に出る** — 除外は `is_dir()` の枝にしか無いので、
  `.gitignore` / `.servant_key` は並ぶ（`fd` / `grep` と同じ挙動で、
  補完だけの話ではない）。**この村の `fd` は
  `.gitignore` を読んでいない** — 落としているのは隠しフォルダと
  `SKIP_DIRS` の 6 つだけで、名前で落としているに過ぎない
  （`.gitignore` にある `blackboard/` は `fd` にも補完にも出る）。
  **`ignore` crate は Spec 18 で「同梱すると落ちる」と判断して外した経緯がある**

**[Spec 25](specs/25_mcp-server.md) 起票 → rev3 承認 → P0〜P5 完了 = Done（2026-08-07）** —
**Fuseforks を MCP サーバーにし、Claude Code や CLI からチャットを投げられるようにする。**
起点は利用者 —「**シングルショット業務ではオーケストレーションを頼むより Neo に
頼んだほうが正確だし速い。なので Neo の補完機能として Fuseforks を使えるようにしたい**」。
オーケストレーション + 複数パス + 検証が効く領域で**ワンポイントに使う**位置づけ。

**構造の制約が設計の大半を決める** — **村は 1 つのプロセスの中にしか居ない**
（`single_instance` / redb の単一書き手 / 稼働中の個体はメモリ）。だから
**MCP サーバーを別プロセスとして立てる形は採れず、扉は動いている GUI へ向かって
開くしかない**。採用は GUI 内の HTTP サーバー。

**オーケストレーション側は何も作らない** — `deliver_and_wait` が既にあり、
**外部の呼び出しは構造的に「サーヴァントが別のサーヴァントへ `ask` する」のと同じ**。

**依存は実測 +7 crate**（`chacha20` / `pastey` / `rand` / `rand_core` /
`ref-cast` / `ref-cast-impl` / `sse-stream`。291 → 298）。
**`tower` / `http` / `bytes` / `schemars` / `uuid` は既にツリーに居た。**

**利用者裁定 3 点**（起票時）: 窓口は**システム設定で決める**（ザリ固定でも
`agent_1` 固定でもない）/ 待ちは**素直に待つ**（`start` + `poll` にしない）/
**範囲はチャット 1 本のみ。村の状態は外からブラックボックス**（`roster` も出さない）。
**そこから 1 つ導かれる** — 村が見えないなら呼ぶ側は宛先を選べないので、
**ツールの引数は `message` だけ**になる。

**rev2 で D6 を反転した**（利用者裁定）— 外部発話は **`Endpoint::External { client }`**
を新設して入れる。**理由は表示ではなく入力**:「人間ユーザーからの命令でないと
分かれば、LLM も正確に対応できる」。**`【送り手: X】` はモデルが読むプロンプトの
一部**で、外部の LLM を利用者に見せかけると**その機構を自分で無効化する**。
**rev1 で私が推した「`Endpoint::User` + 印」は誤りだった** — D6 を
「後から人が読むための記録」の問題として立てていたため、**安いほうが正しく
見えていた**。**問いを取り違えると、値段の比較が答えを決めてしまう。**
`client` は `clientInfo.name` の自己申告なので **`normalize_user_name` と同じ
正規化が必須**（検査に落ちたら拒否ではなく既定ラベル `external` へ落とす —
呼び出し側が対話的に直せない値なので）。
**「攻撃者が書ける唯一の新しい経路」と書いたのは誤りだった** —
**2 つ目は `message` 本文そのもの**で、本文に封筒構文を書けば封筒が 2 つ並ぶ
（[Spec 26](specs/26_sender-envelope-integrity.md)・`failures.md` #84）。
**自由入力欄は 1 つとは限らない。**

**rev3 は査読 12 点の反映**（利用者査読 + 実装との突き合わせ査読。2026-08-07）。
重大だったのは 2 つ:

- **D7 新設 = 同時外部依頼は 1 本まで**（2 本目は busy で即拒否）。rev2 の
  「閉路は `max_hops` と予算が塞いでいる」は**誤りだった** — 外部入口は
  **新しい因果の根**で hop も予算も扉を通るたびに新品になり、既存の歯止めは
  **根の中でしか効かない**。村の `mcp.json` に自分自身を登録すると各周が
  天井 1M を持つ無限連鎖が組め、自己呼び出しはデッドロックの形を取る
  （窓口が飛行中ターンの中から自分の受信箱へ配送して待つ）。
  **上限 1 なら 2 周目が必ず busy に当たって切れ、デッドロックも即解ける** —
  閉路・併走の予算二重消費・デッドロックの 3 つを 1 つの機構で塞ぐ
- **トークンの置き場は第 3 の棚 `{app_data_dir}/mcp_server.json`**（workspace の
  **外**）。Spec 13 の 2 棚はどちらも使えない — `world.json` は村を配ると鍵も
  配られ、`localStorage` は検査する Rust から読めない。ON/OFF・ポートも同じ
  ファイル、**窓口だけ `world.json`**（村の内容物 — #52 の境界）。帰結として
  **「村を配っても扉は開かない」が構造で成立**。生成は「起動時」ではなく
  **サーバー ON の時点**（D5「OFF ではトークンも作らない」との矛盾解消）

数え直しが 2 つ — **rev2 の「網羅 match 5 箇所はコンパイラが全部指す」は
実測と食い違っていた**（`compute.rs:90` は `_ =>` のワイルドカードで指されない。
正しくは**指す 4 + 指さない 6** で、指さない側の挙動を表で凍結。#62 と同族 —
ワイルドカード match は新 variant を黙って吸う）/ **広場ログは依頼と返答を
分けて書く** — 依頼（from=External）は**編集ゼロで既に不可視**（可視述語は
`from` が `Agent` でない発話を落とす。手を入れずテストで凍結）、返答は
User 宛と同じ扱いで可視。ほか: `deliver_and_wait` は `from: &AgentId` に
`Endpoint::Agent` を焼き込んでいるので **`from` を `Endpoint` へ広げる**
（「そのまま再利用」は 1 行割れる）/ `ask_concordia` の名前を P0 凍結へ /
**クライアント側タイムアウト（`MCP_TIMEOUT` 系）の実測を P0 へ** — 既定が
`ask_timeout` より先に切れると S1 が主戦場（分単位）でだけ失敗する /
bind 127.0.0.1 固定 + Origin 検査（MCP Streamable HTTP 仕様の DNS rebinding 対策）。
**査読の前提 1 点は実測で訂正** — 「workspace 切り替え時のライフサイクル」は
実行時に存在しない（`state.rs:48` で起動時固定）。問いは「ON のまま窓口が
消える」形で P2 に定義した。

**P0 完了**（2026-08-07。実測 2 本 + `mcp_server_contract` 凍結）。
rmcp 2.2 server + axum の probe（scratchpad・使い捨て）を実物の Claude Code から
叩いた — (1) **streamable HTTP を受ける**（`--mcp-config` の `type:http` で
initialize → tools/list → tools/call 完走。**stdio シム案は死んだ**）
(2) **180 秒の呼び出しが既定設定のまま完走**（クライアント計 196 秒。
`ask_timeout` 既定 180 秒 ≤ 実測保持時間なので既定同士で S1 は切られない。
上げる村は `.mcp.json` の `timeout`（ms・サーバー単位）を併記。
**→ 2026-08-24 Spec 44 で既定が 600 秒になり、この「既定同士で切られない」は
崩れた** — MCP で外から使う村は `.mcp.json` の `timeout` 併記が実質必須）。
**副産物** — rmcp の `StreamableHttpServerConfig` は `allowed_hosts` 既定 =
loopback 3 種（DNS rebinding 対策）と `allowed_origins` を最初から持つ。
D1 の検査は bind 127.0.0.1 だけ自前で、残りは rmcp の機構に乗る。

**P1 完了**（2026-08-07。結合 12 本・rust 427 + 122・clippy 警告ゼロ）。
`Endpoint::External { client }` / `world::normalize_client_name` /
`world.json` の `reception` / `Orchestrator::ask_external` /
`deliver_and_wait` の `from` 一般化。**次に触る人が要る判断 4 点**:

- **封筒は名前だけでは足りなかった。** `【送り手: Claude Code】` は
  「そういう呼び名の人間」とも読める（**呼び名は利用者が自由に付けられる**）。
  D6 の目的は相手が人間でないと分かることなので
  **`【送り手: {client}（外部クライアント）】`** と種別を明示した。
  乗るのは外部依頼のターンだけで、全員の毎ターンに乗る固定費ではない。
  **ただし「種別を明示すればモデルが分かる」は保証ではなく傾向だった**
  （[Spec 26](specs/26_sender-envelope-integrity.md)。実機で同じ封筒に対し
  ミュゼは正しく判定し、**ザリは封筒を逐語で引用できるのに判断に使わないと
  明言して人間の依頼として扱った**）。処方は本文の無害化で、
  **規則をモデルに守らせるのではなく、守らなくても取り違えが起きない形**にした
- **「窓口が見つからない」と「窓口が未設定」を別のエラーで返す**
  （`AGENT_NOT_FOUND` / `EXTERNAL_RECEPTION_UNSET`）。設定し直すのと初めて
  設定するのでは人の次の手が違う。**だから削除時に `reception` を掃除しない** —
  掃除すると 2 つの状態が 1 つに畳まれて診断が消える
- **ワイルドカード 6 箇所は編集ゼロだった**（全部そのままで正しい挙動）。
  代わりに**テストで凍結**した — **編集しないものこそ、次に読む人が
  「対応漏れ」と読んで分岐を足しに来る**（`is_visible_in_room_log` に
  External の分岐を足すと、依頼が不可視である根拠のほうが壊れる）
- **ミューテーション 3 回で赤を確かめた**（#84 と同じ規律）。決定的だったのは
  (a) **D7 のゲートを外すと busy のテストが 180 秒ハングして失敗する** —
  `ask_timeout` ぶん居座るので、**ゲートが塞いでいるのは「拒否」ではなく
  「待ち」そのもの**だと赤で読める。(b) 正規化を外す → 名乗りの 2 本
  (c) `from` を `Agent` へ焼き戻す → 3 本。**3 つとも別のテストが落ちた**

**P2 完了**（2026-08-07。`apps/gui-tauri/src-tauri/src/mcp_server.rs` +
IPC 5 本・単体 6 + 結合 2・workspace 611 全緑・clippy 警告ゼロ）。
**依存は実測 +19 crate**（317 → 336。axum / rmcp-macros / schemars / rand /
chacha20 ほか。全部純 Rust）— **P0 の +7 は rmcp だけの数字**で、HTTP を載せる側と
派生マクロがその倍を持ってくる。次に触る人が要る判断 4 点:

- **`mcp_server.json` が読めないときは書き込みを拒む**（`schedules.json` と
  同じ規律）。既定値を書き戻すと合鍵とポートを消す = **#70 の再演**になる
- **状態は `enabled` と `listening` を別に持つ。** ポートが埋まっていると
  「ON なのに開いていない」が起こるので 1 つの真偽値に畳まない。
  **bind の失敗は保存の失敗ではない**ので、エラーにせず `lastError` で返す
  （設定は保存済みで、直すのはポート番号）
- **`allowed_origins` は自分で埋める必要があった** — rmcp の既定は**空 =
  Origin 検査が無効**。Host 検査（loopback 3 種）だけが既定で乗っている。
  **P0 で「rmcp の機構に乗る」と書いた 2 つのうち、実際に乗っていたのは 1 つ**
- **合鍵の検査は「実際に HTTP を話して」テストした。** 述語の単体テストは
  **層を張り忘れても緑のまま**なので、素通しの層へ差し替えるミューテーションで
  拒否側だけが赤になることを確かめた（通る側と対で見ないと、全部拒否する
  実装でも緑になる）
- **IPC 5 本は P2 で入れた** — 呼び出し元の無いまま置くと `dead_code` を
  `allow` で覆うことになり、**本当に死んだコードが隠れる**

**P3 完了**（2026-08-07。front 186 → 189・build 緑）。入口は**システム設定に
「外部連携」グループを新設**し、その下に「MCP サーバー」。判断 4 点:

- **型名の衝突を踏んで改名した。** `McpServerStatus` は**接続先の** MCP
  サーバー用に既にあった（Spec 02 の `listMcpServers`）— 英語ではどちらも
  "MCP server status" になる。**立てている側を `McpHostStatus`** とし IPC も
  `mcp_host_*` へ揃えた。**一括置換が client 側の型まで巻き込んで
  コンパイルが落ちたこと自体が、衝突が実在した証拠**。
  **一般化: 新しい型に名前を付けるときは、その概念の英語名で既存の型を
  grep する。** 日本語では「立てる側／繋ぎに行く側」で自然に分かれるのに、
  識別子は英語なので同じ名前へ落ちる
- **このページは押した時点で反映する**（保存ボタンを持たない）。天井・言語と
  規律が違うのは、**扉は「開いたか開かないか」が結果**で、押すまで分からない
  ものを溜めるとポートが埋まっていたことに気づくのが遅れるため
- **合鍵はそのまま画面に出す。** API キー（値を返す API が存在しない）とは
  扱いが逆で、これは**クライアントの設定へ貼るための値**。設定例の JSON も
  合鍵込みで組み立てて出す
- **会話ペインは「外部」バッジ**を送り手名の隣に置く。アイコンは持たせない —
  **利用者のアイコンを流用すると、外の道具が頼んだことが自分の依頼に見える**

**P3 でテストが「通った理由」を 1 つ間違えた**（`failures.md` 未起票・ここに記録）。
`sameEndpoint` が client まで比べることを確かめるテストで、**2 通の宛先を
同じにしていた**ため `foldsInto` の**宛先重複の判定**で先に落ちており、
送り手を比べていなくても緑だった。**ミューテーション（client の比較を外す）で
初めて分かった** — 宛先を変えたら正しく赤になった。
**一般化: 複数の条件が AND で並ぶ述語の 1 つを検査するテストは、
他の条件を意図的にすり抜ける入力で書く。** 同じ値で埋めると、どの条件が
効いて緑になったのか区別できない。

**D8 = 外部クライアントの呼び名とアイコン**（2026-08-07 利用者要望
「MCP 接続のユーザーのアイコンと名前を変えられるように」）。**D6 の延長で、
同じ問いの答えを引き継ぐ** — 呼び名は表示だけでなく**封筒**にも効き、
`【送り手: Neo（外部クライアント）】` になる（**種別の注記は残る**ので D6 の
性質は不変）。

- **設定は攻撃面を狭める側に効く。** `clientInfo.name` は攻撃者が書ける
  唯一の新しい経路（凍結 6）で、呼び名を設定した村では**その文字列が
  プロンプトへ届かなくなる**
- 呼び名は `world.json` の `external_name`、検証は `normalize_user_name` の
  **同じ述語**（封筒へ入る値、という一点で制約が決まる）。未設定なら
  **名乗りへ落ちる**
- アイコンは `{workspace}/external/icon.webp` で、**`user/` と分ける** —
  共用すると「外の道具が頼んだこと」と「自分の依頼」が同じ顔で並ぶ。
  Spec 19 が `write_icon_at` / `read_icon_at` / `delete_icon_at` を切り出して
  あったので、**述語は 1 実装のまま置き場だけ増えた**
- 解決は **`external_label` の 1 実装**を封筒と広場ログが共有する。2 箇所で
  組むと「画面では設定名・プロンプトでは名乗り」の食い違いが生まれる
- **`Endpoint::External` は据え置き** — 記録には名乗りが残る（誰が実際に
  呼んだかは監査の材料で、呼び名はその表示規則にすぎない）
- **起動テストが新しい IPC の未モックを 2 回とも捕まえた**（扉の状態・
  外部の呼び名とアイコン）。初期化が IPC を増やすと必ず赤くなる網で、
  「覆いが外れる前に当てておく」値を足すたびに効く

**P4 完了**（2026-08-07。README 日英 + `data_contract` の回収）。**数えたのは
ファイル単位**（#51 の日英版）。**回収した追従漏れ 3 件は、どれも Spec 25 の
節ではなく隣の節にあった**:

- **英語 README の画面構成表に版番号が無かった**（2026-08-05 の追従漏れ。
  日本語版にはある）。MCP の行を足すついでに回収
- **`icon_contract` の「アイコンは 2 種類ある」が 3 つ目で嘘になった** —
  総数をやめて列挙へ（#67 の処方）
- **`data_contract` の `Endpoint` の variant 一覧に `external` が無かった。**
  P0 で「ワイヤ形も凍結し直す」と書いた当の箇所で、`mcp_server_contract` の
  側だけ書いて `entities` 側を数えていなかった。**新しい variant は
  「その型を説明している場所」と「その型を使う契約」の 2 箇所にある**

**画面の目視は実機でしかできない** — vite の dev サーバー単体では
`window.__TAURI__` が無いので `invoke` が落ち、起動の覆いが外れない
（**コアが Tauri の Rust 側に居る**ことの帰結で、F5 を塞がない判断と同じ根）。
P5 の実機確認がその役を負う。

**P5 実機確認は 6 件とも観測して Done**（2026-08-07）。**検収の書き方で 2 件つまずいた** —
どちらも合格条件そのものの誤りで、実装は正しかった（`failures.md` #85）:

- **「OFF の村でポートが開いていない」は `39641` の行で数えると偽陰性になる** —
  OFF でも `TIME_WAIT` の残骸が数行残るので、**見るのは `LISTENING` の行**。
  **ON 側の対照を先に取る**（`TCP 127.0.0.1:39641 0.0.0.0:0 LISTENING <pid>`）。
  **`0.0.0.0` でないこと＝bind 固定も同じ 1 行で読める**
- **「2 本目が busy」は同一セッションで投げると判定にならない** — per-connection で
  止めている実装でも緑になる。**別セッションから投げて初めて「村ごとに 1 本」が読める**。
  D7 が塞ぐ 3 つ（閉路・併走の予算二重消費・デッドロック）はどれも村の単位で
  成立する必要があるので、ここが接続単位なら機構ごと無効

観測のうち、次に読む人が要るもの:

- **窓口未設定と停止中で文面が別物**（「窓口が設定されていません（システム設定で
  選んでください）」/「エージェント `agent_2` は稼働していません」）。**削除時に
  `reception` を掃除しない**判断が、実際に 2 つの答えを分けている。どちらも
  **116 / 117 ms で返り、`concordia.log` に 1 行も増えない** — ターンに入る前に
  切れているので**トークンを 1 つも払っていない**。**行が出なかったことが証拠**
- **busy は 6 ms**。A のターン 8.0 秒（20:38:00〜20:38:08）の 4 秒目に投げた B が
  即返る＝**キューに入っていない**。ゲートが塞いでいるのは拒否ではなく**待ち**
- **ログに残るのは名乗り**（`from=external:Neo-A`）。D8 で呼び名を設定しても
  `Endpoint::External` を据え置いた判断の実物
- **外部依頼が `fd` / `file` を普通に呼ぶ。** Notes 1 の「応じる範囲が広がっても
  **能力の境界は動かない**」の裏側で、送り手はツールの提示にも実行にも届いていない
- **未実装として据え置いた 1 点**: 停止中の文面が**内部 id `agent_2` を名乗る**。
  凍結「村の状態は外からブラックボックス」と方向が逆だが、**呼び出し側の次の手は
  id を知っても知らなくても同じ**（窓口を起動してもらう）。直すと外部向けと
  人向けで文面が分かれるので、**据え置きを利用者が裁定した**（2026-08-07）—
  「呼び出し側に見えればいい。使いたければ LLM が窓口を設定させてくださいと言う」。
  **同じ裁定で「拒否を村側のログに残す計器」も見送っている**（呼び出し側に
  理由が返っているので #72 の握り潰しには当たらない）

## 公開までのマイルストーン（2026-08-07 起票・2026-08-08 に 3 件追加）

**公開は 2 段**（v0.1.0 で public、v0.2.0 で公表）なので、項目もそこで割れる。
**総数を書かず列挙する**（#67 — 行を足すことが更新になる）。

**総数を書いていた初版は 1 日で腐った** — 見出しにも本文にも「6 件」と書いた翌日に
利用者が 3 件足し、**列挙の側は追記で済むのに数だけが 2 箇所で嘘になった**。
#67 の処方は増える側で正しく効き、**総数の併記がそれを打ち消していた**。

| | 内容 | 段 | 所見 |
|---|---|---|---|
| **1** | **同梱ツールの実行に理由を 1 行**（「プログラム修正前の確認のため」）。`run` も同じ扱い | **v0.1.0** | 利用者が「**他のユーザーは安心できるから必須**」。[Spec 27](specs/27_tool-call-reason.md) として起票 |
| **2** | **スケジュールがトリガーを呼ぶ**（利用者が書いた監視プログラムがキックすると実行） | v0.1.0 後 | **経路は Spec 25 で開通済み** — 外部から `ask_fuseforks` が呼べる。残るのは「本文と宛先を GUI 側に置く」ぶんで、`Recurrence` に時刻でない発火を 1 つ足す形 |
| ~~**3**~~ | **閉じる前の確認**（トレイ常駐は見送り） | **着地**（2026-08-08） | 下記「閉じる前の確認」の節が正 |
| **4** | **トリガー前に新規チャット / 後に要約**（dreaming 相当のチェックボックス） | v0.1.0 後 | **部品は両方すでにある**（Spec 12 の新規チャットと `summarize_session`）。配線だけ。**2 つは向きが逆**なので性質を書き分ける — 新規チャットは*捨てる*、**要約は残り、しかも落ちない固定費**（opus が 1 往復に 1,163 字を書いた実測がある） |
| ~~**5**~~ | **絆ペインの序列を左ペインの順序に** | **着地**（2026-08-08） | **数えたら足すものが 1 つも無かった** — `order` の欄も `reorder_agents` の経路も既にあり、ノード配置は既に左ペインに従っていた。残っていた依存は**双方向の辺の向き 1 箇所**だけで、決めていたのは id ではなく **`state.edges` に先に現れたほう**。`lib/kizunaEdges.ts` の `drawDirection` で `order` に正規化した。**新しい名前・型・ワイヤ・画面要素が 1 つも生まれない**ので Spec も台帳の節も無し（この行が記録）。**一方向は触らない** — `A→B` は事実そのもので、向きを入れ替えると矢印が逆を指す |
| **2 + 4** | **トリガーと前後処理** | **[Spec 28](specs/28_schedule-probe.md) として起票 → rev3 承認**（2026-08-08。Fable で仕様を練った）。骨格 = 発火時に probe コマンドを実行し **stdout 1 行目の完全一致でだけ配送**（トークンゼロの前判定・exit code は判定に使わない）+ 2 行目以降を依頼文へ付記 + `sessionMode`（continue / fresh）+ `summarizeAfter`（対象は `deliver_and_wait` で待って完了した個体のみ・handoff 先は対象外）。**外部トリガー API は対象外**（利用者判断 —「スケジュールでやったほうが楽」）。**査読 2 巡で最重要だったのは D10 の端末承認** — rev2 の内容ハッシュは workspace 差し替え攻撃が通り、査読の案 A（パス salt）も **workspace が `{app_data_dir}/workspace` 固定（`state.rs:56`）なので効かない**（差し替えは同じパスの中身の交換）。着地は **`{workspace}/village_id`（UUID v4・配布に載る）を salt に含めた `SHA-256(canonical_json({args, command, cwd, villageId}))`**、承認は `{app_data_dir}/probe_approvals.json`（ハッシュのみ・GUI の IPC だけが書く・保存のたび掃除）。残余（公開した村の id は漏れる = 標的攻撃は防がない）は保証と書かず明記。**P0〜P5 完了 = Done**（実機確認 8 件すべて観測。2026-08-08。**起票から Done まで 1 日**） |
| ~~**6**~~ | **fuseforks-core の指示も英語に** | **[Spec 35](specs/35_bilingual-core.md) P0〜P4 着地**（2026-08-12。残は P5 実機） | **旧記述の見立ては 3 つ覆った** — (a)「凍結を覆す変更」ではなく**加算**（日本語バイト等価 + 英語を足して `language` で選ぶ。golden が機械で証明）(b)「System 行は構造化が要る」は表示時に訳す場合の見積もりで、**記録時の言語で書けば構造化不要**（D6） (c) 実測は全部生きる（日本語の村は 1 バイトも変わらない）。**中国語は対象外のまま**（DeepSeek / Kimi / Qwen は OpenAI 互換。未実測 — 着手前にテンプレート登録だけで 1 往復通るかを測ると 1 項目消える、は今も有効） |
| ~~**7**~~ | **作業フォルダの一括切り替え**（サーヴァントにチェックボックスを付け、パスを 1 つ入れるとチェックした個体の `work_dir` をまとめて変える） | **Done**（2026-08-08） | **[Spec 29](specs/29_batch-workdir.md)。起票から Done まで 1 日**（査読 10 件 = 採用 7 / 訂正して採用 2 / 反証 1。**反証はタイムアウト** — `update_agent` はローカル IPC で `mutate()` 全呼び出しがタイムアウトを持たず、ここだけ足すと単体と規律が割れる。**訂正 2 件は査読の処方の側の誤り** — `snapshotToSpec` の欄名 `work_dir` はフロントの型に合わない（`workDir`）/ 「アプリの設定へ倒す」は一覧ヘッダの前例「モデル登録」を数えていない）。**起票時の実測で見立てが 2 つ覆った** — (a) この表の旧記述「これだけがコアに触る」は誤り。`update_agent` IPC が既にあり（稼働中でも受け付け・次の発話から反映）、**コアの変更はゼロ** (b)「決めどころは失敗の扱い」も半分消えた — **単体の保存が実在検査を掛けていない**（囲いはツール実行時の `canonicalize` + 前方一致）ので、一括だけ検査を足すと規律が割れる。残る決めどころはチェックの置き場（rev1 はダイアログ内の一時チェックを推し）と部分失敗の見せ方（通った個体だけ通し、名指しで報告）。**踏むと分かっている罠**: snapshot → spec の組み立てが既に 2 箇所に複製されており（`seed` / `setBatchStart`。「写さないと保存で既定へ戻る」）、3 箇所目を作る前に `snapshotToSpec` の 1 実装へ寄せる（D4）。**P4 実機 5 件すべて観測・指摘ゼロ**。**実装で 1 つ決まった** — 一括は **`mutate()` を通さない**（`finally` の `refreshAll()` で全状態の再同期が 8 回走り、失敗のたびにトーストが 8 枚出る。しかも `updateAgent` は `Promise<void>` で**成否を捨てている**）。`ipc.updateAgent` を直接呼んで個体ごとに `try/catch` し、最後に `refreshAll()`。**ただし「読み直しは最後に 1 回」は不正確だった**（同日訂正）— `update_agent` は 1 体ごとに `TopologyChanged` を出し、フロントはそのイベントで毎回取り直す。**`mutate` を避けて消えたのはトースト 8 枚と成否の握り潰しであって、再同期の回数ではない**（**一般化: 呼び出し側の再同期を止めても、コアがイベントを出していれば投影は動く。「何回取り直すか」は呼ぶ側だけを数えても分からない**）。その帰結で**適用中に行が 1 つずつ書き換わる**のが実機で見え、**イベントを止めるのではなく `busy` の間だけ一覧を進捗へ差し替えた**（再同期は投影の真実性を支える機構で、一括のために止める口を作ると「止め忘れると古い表示が残る」新しい失敗が生まれる）。閉じる導線も塞ぐ（閉じてもループは止まらず、結果を見られないまま副作用だけ進む）。**入口は一覧のフッター**（利用者裁定 — ヘッダの「モデル登録」「追加」は*作る側*、フッターは*既にいる個体をまとめて扱う側*。**アプリのステータスバーとは別物**）。**P4 の実機で要望が 1 つ出て同日に追加** = **最近使ったフォルダの履歴**（`fuseforks.workDirHistory.v1`・最大 8 件）。**積むのは適用が 1 体でも通ったときだけ**で、入力しただけ・参照…で選んだだけでは残らない。**いま各個体が向いているフォルダは混ぜない** — すぐ上の一覧に出ているので、履歴が埋めるのは「いまは誰も向いていないが、また戻る先」 |
| ~~**8**~~ | **ショートカットキー**（サーヴァント一覧の選択を移す） | **着地**（2026-08-08） | 下記「サーヴァント一覧の選択をキーで動かす」の節が正 |
| ~~**10**~~ | **改名**（`Concordia` → **`Fuseforks`**）。DeepMind に同名・同領域のプロジェクトがある | **Done**（2026-08-09） | **[Spec 30](specs/30_rename.md)。起票から着地まで 1 日。** 名前は `ClawVillage` → `Fuse Folks` → `Fuseforks` と動いた（`fuse` + `fork` = 撒いて束ねる = `plan` の機構そのもの）。**起票時の実測で CLAUDE.md の旧記述が 3 つ覆った** — 身元は 1 つではなく 4 つ（`SERVICE_NAME` / MCP の名乗り / `Endpoint::System` が漏れていた。実装中にさらに `localStorage` の 4 鍵が出て 5 つ）/ 269 箇所ではなく 84 ファイル・279 行・**289 出現**（`git grep -c` が返すのは一致行数）/ `bun.lock` の名前が既にずれていた。**D10 で裁定** = リポジトリと作業フォルダも **`Fuseforks`** へ（GitHub の rename・`git remote`・CI・この村自身の `work_dir` が同時に動くので repo の中の作業ではない。手順は Spec 30 D10） |
| **9** | **チャット入力欄の履歴**（未入力かつフォーカス中に上下で過去の依頼を呼び戻す） | **優先度低**（利用者裁定 2026-08-08） | **原本は `state.messages`・会話をまたがない**（同日裁定 —「違う履歴は使わないだろう」）。**IPC は 1 本も要らない**。**8 と同じキーを奪い合う**ので 2 つは 1 つの表として設計する。**「未入力のときだけ」が境界**で、1 文字でも入っていればカーソル移動が正。**優先度を下げたのは要望を出した本人の言葉** —「僕も Claude Code のチャット履歴機能を使ったことがなくって必要かどうかは微妙」。**要望者本人が確信を持てない機能**は、効いているか分からない機構の筆頭（#47 で L1/L2 を作らなかったのと同じ判断）。**しかも半分は既にある** — 会話一覧 → **分岐**が選んだ依頼の文面を入力欄へ差し戻す（Spec 12 P3）。上下キーが埋めるのは**「直前の数件を、会話を分けずに、キーだけで」の差分だけ**なので、着手時はそこまで射程を縮めて見積もる |

## 絆ペインのリサイズ後の自動フィット（2026-08-08 着地。Spec 不要と判断）

利用者要望 —「Fit の上に Auto Fit のトグルボタン」。**最初の読みを 1 度外した**：
「変化のたびに追従し続ける」と受け取り、ノードのドラッグとの干渉・Spec 21 の
drop の座標ずれ・「何を変化と数えるか」の 3 点を挙げて見送りを勧めた。
利用者の訂正 —「**ペインの幅と高さが変わった後に Fit を呼び出す機能が欲しかった
だけ**」で、**3 点とも当たらない**（ドラッグも drop もペインの大きさを変えない /
見るのはコンテナの箱だけ）。**要望を広く読むと、断る理由のほうが先に揃う。**

- **「追従し続ける」プロパティはどちらのライブラリにも無い**（Vue Flow 1.48.2 は
  初期化時 1 回の `fitViewOnInit` と命令的な `fitView()` だけ。
  **`v-network-graph` も同じで、あるのは `fitToContents()` と
  `autoPanAndZoomOnLoad`**）。**利用者の見立て「プロパティがあるはず」は外れ**だが、
  `ResizeObserver` 1 本で繋がるので自前で足した。**描画を差し替えても
  この機構はそのまま移った** — 呼ぶ関数の名前が変わっただけ
- **見るのはコンテナの箱だけ。** ノードの移動でも辺の増減でも発火させない —
  ドラッグ中に視点が動くと Spec 21 の `elementFromPoint` の座標と画面がずれる
- **120ms 落ち着かせてから 1 回。** 仕切りのドラッグ中は毎フレーム発火するので、
  そのたびに掛けると視点が追いかけ続ける（利用者の言葉も「変わった**後**に」）
- **既定 OFF のオプトイン**（`fuseforks.settings.v1` の `autoFitOnResize`）。
  既定を変えると既存の村の見え方が黙って変わる（`showPresenceNotices` と同じ判断）
- **ボタンは `Controls` の既定スロット = Fit の下**（利用者裁定。初版は
  `#top` で Fit の上に置いた）。**スロットの描画順は実測** —
  `top` → zoom → fitView → interactive → `default` なので、
  「下」は `#top` ではなく既定スロット。Controls のコメントは「操作ボタンを
  増やすと地図の面積を奪う」だが、これは**操作ではなく状態の切り替え**で、
  押した後は押さないもの
- **アイコンは利用者提供の SVG。ただし `fill` は `currentColor` へ直した** —
  原本は `#212121` 固定で、**ライトでもダークでも同じ黒になり `is-on` の
  accent も効かない**。絵文字を恒久要素に使わない理由（配色に追従しない）と
  同じ線引きで、**もらった図形もそのままでは置かない**

## サーヴァント一覧の選択をキーで動かす（2026-08-08 着地。Spec 不要と判断）

マイルストーン 8 の着地形。**フロントだけで閉じる**（コアの型も IPC もワイヤも
増えない）ので Spec は切っていない。`lib/agentNav.ts` の純関数 3 本 + 配線。

**鍵は Alt + ↑↓。** 起票時の要望は「Tab + 上下」だったが、**Tab を修飾キーにすると
アプリ全体のフォーカス移動が止まる**（Tab の `keydown` を `preventDefault` する
必要がある。押しっぱなしのリピートと `keyup` の取りこぼしも付く）。3 候補を出して
利用者が Alt を裁定した。**残り 2 つが落ちた理由が要点**:

- **Shift + ↑↓ は不可** — `textarea` で**行単位の範囲選択**。入力欄で最もよく使う
  編集操作を奪う
- **Ctrl + ↑↓ は不可寄り** — macOS で **Mission Control が OS 側で吸う**ので
  アプリまで届かない。**3 OS で配布するので、片方だけ効かない鍵は採らない**

**要件が「入力欄にフォーカスしていても効く」**（利用者 —「切り替えられないと
あまり意味がない」）なので**窓ごと聴く**形になり、そこから設計の本体は
**どこで効かせないか**に移る。

- **`isNavigableFocus` は閉じた許容**。通すのは `body` / `null` と
  **チャット入力欄（`data-chat-input`）だけ**。除外リストにしない理由は実在する —
  **`CodeEditor.vue`（CodeMirror）は既定の keymap で Alt+↑↓ に行の移動を持つ**
  （役職ダイアログとシステム設定で使う面）。許容側で書けば**ダイアログが 1 つ
  増えても既定で安全**。実機で「役職ダイアログの本文では行が移動する」を確認済み
- **修飾は排他**（`Ctrl+Alt+↑` / `Shift+Alt+↑` は拾わない）。
  **足りない条件で拾うより、余った条件で見送るほうが安全**な種類の判定
- **並び順は `inListOrder` の 1 実装を `AgentList.vue` と共有**。別々に整列すると
  **Alt+↓ が画面の並びと違う順で飛ぶ**。テストは `order` が id の並びと**わざと
  食い違う**一覧で書いてあるので、id 順に整列する実装では落ちる
- **端で巻き戻る**（入力欄のパス補完が `% count` で巻き戻るのと同じ作法）。
  未選択・一覧に無い id は「下で先頭・上で末尾」から入る
- **`scrollIntoView({ block: "nearest" })`** で追従する。9 体並ぶと一覧が
  スクロールするので、無いと選択だけ動いて見えない。**`nearest` なのは、
  クリックで選んだときも同じ watch を通るから** — 毎回中央へ寄せると画面が跳ねる

**素の ↑↓ は奪っていない** — Spec 24 のパス補完が使っている（実機で確認）。
**9（入力欄の履歴）が来たらここへ行が増える** — あちらは「未入力かつフォーカス中の
素の ↑↓」なので、同じ表の別の行になる。

## 改名（`Concordia` → `Fuseforks`。2026-08-09 着地）

**[Spec 30](specs/30_rename.md) が正。** 材料・決定・実測はすべてあちらにある —
名前の選定根拠（`fuse` + `fork` = `plan` の機構）/ 射程 84 ファイル・289 出現の
3 層分割 / **副作用が repo の外へ出る 5 点**（`identifier` / `SERVICE_NAME` /
MCP の名乗り / `Endpoint::System` / `localStorage` の 4 鍵）/ 台帳の置換境界
（**記録か、名前か**）/ D10（リポジトリ名も `Fuseforks` へ = 2026-08-09 裁定）。

**ここに残す 1 行だけ**: **過去の観測記録の `concordia.log` と
`[concordia]` 行は書き換えていない**。当時のログの実物を指す文字列なので、
grep して出てきても追従漏れではない。

## 各社固有機能を「サーヴァントの能力」として持たせる構想（2026-08-08 利用者）

**中国語対応の話から出た、より大きい構想** —「各社の固有機能みたいなのを
**固有スキルみたいな形でサーヴァントの能力のように使わせたい**」。
**OpenAI 互換はもちろん要るが、各社 Native のワイヤもいずれ要る。**
**ただし着手は公開後になるかもしれない**（利用者の見立て）。

**私の測定提案は的が外れていた。** 「DeepSeek か Qwen が互換で 1 往復通るか測れば
項目 6 から 1 つ消える」と書いたが、利用者の「ワイヤーができないと需要はない」は
**各社ネイティブのワイヤ**の意味だった。互換で通ることを測っても、答えるのは
**「使えるか」であって「固有スキルが使えるか」ではない**（#83 と同じ形 —
測っていない軸のほうが答えを決めていた）。

**「今は Google Search Ground くらいしか実装できていない」は少なく見積もりすぎ。**
実装を数えると**4 つが部分的に入っている**:

| 一覧の項目 | 現状 |
|---|---|
| Google Search Grounding | **実装済み**（Spec 05。`sources` が空という実測つき） |
| Anthropic **Prompt Caching** | **実装済み**（`place_message_breakpoint`。ターンをまたいで 99% を実測） |
| Thinking Mode | **半分**（社ごとに非対称。上の 4 社 4 形の表が正）— `reasoning_effort` で制御はするが、**返る thinking ブロックは捨てている**（`dropped content blocks: kinds=thinking` の計器つき）。**例外は Gemini で、思考トークンだけは既に読んで `completion` へ足し込んでいる**。[Spec 32](specs/32_thinking-reception.md) で数を分離する |
| Vision Input | **画像は実装済み**（Spec 23。互換層で 5 系統実測）。音声・動画は未 |
| xAI **Live Search**（web / X） | **実装済み**（[Spec 31](specs/31_grok-live-search.md)。4 本目のワイヤ `/v1/responses`。citations が実 URL で返る = Spec 05 と逆） |

**「半分」の中身はプロバイダで非対称だった**（2026-08-09 に実装で数え直し）。
OpenAI 互換は `reasoning_effort` を**送れる**（`openai_compat.rs:75`。要求できるが
受け取れない）のに対し、**Anthropic は思考の制御を 1 つも送っていない** —
decode が `Thinking` / `RedactedThinking` を `dropped` へ入れて捨てているだけ。
xAI の Reasoning Mode も同じ棚。

**ただし「送っていない = 思考していない」ではない**（Spec 32 P4 の実測）—
**claude-sonnet-5 は既定で思考する**。何も送らない要求で
**出力 2,048 トークン全部を思考に使い本文をゼロで終えた**応答を観測しており、
これが `failures.md` #72（出力トークンだけがあって本文が無いターン）の正体。
**送る側の現行形は `thinking: {type: "adaptive"}` + `output_config.effort`** で、
~~`budget_tokens`~~ は 400 で拒否される（サーバー自身が後継を名指しする）。
~~受け取り側は `decode` の捨てている行 1 箇所なので、3 社ぶんが 1 つの Spec に収まる形。~~
**取り消し（2026-08-10。Spec 32 P0）** — この数え直しは**「捨てている社」しか
数えていなかった**。**Gemini が 4 社目**で、`thoughts_token_count` を
**既に読んで `completion` へ足し込んでいる**（捨てていないので、
捨てている行を探す走査には 1 件も出ない）。**現在地の節の 4 社 4 形の表が正。**

**v0.2.0 の取り込み裁定（2026-08-09 利用者。13 件の一覧を三分した）**:

- **作らない（既にある 4 件）**: Search Grounding / Prompt Caching /
  Tool Use with MCP（Spec 25 で両方向）/ Vision Input（画像のみ）
- **順番は 受け取り（Thinking。~~3 社~~ → **4 社 3 Spec**へ割れた。Spec 32 → 33 → 34）
  → ~~Live Search~~ → 多モーダル（音声・動画・PDF）**。
  **Live Search は 2026-08-10 に着地した**（Spec 31。順番を飛ばしたのは利用者の
  優先指示による）。**残るのは Thinking の受け取りと多モーダル。**
  1 つ目が最も安く 3 社へ同時に効く。Live Search の 1 手目は実装ではなく
  **Grok へ 1 回投げて citations の有無を見ること**（Spec 05 の `sources` 空の前例。
  返らなければ Gemini への受け渡し物が無く 2 段検証が成立しない）。
  **→ 観測済み（2026-08-09。citations は返る = 判定 A。ただし legacy は 410 で
  死んでおり採用の前提は `/v1/responses` の 4 本目のワイヤ。詳細は
  「最優先は xAI の Live Search」の節の実測ブロックが正）**。
  多モーダルは Spec 23 D8（画像は互換経路のみ）を覆す変更で、
  **D1「渡るのは 1 ターンだけ」は据え置きが効く**（画像 1 枚 14,336〜38,272
  トークンの実測から、音声・動画は桁が上がる）
- **Structured Outputs は未実装**。`NoStructuredOutput` は名前が似ているだけ
  （強制ツール呼び出しが空振りしたときのエラー）。内部の構造化はツール呼び出しで
  足りており、効くのは利用者のワークフロー側
- **Gemini Context Caching は Anthropic と形が違う** — あちらは本文に境界を
  打つだけ、こちらは**事前に cache を作る API 呼び出しが要る**
- **見送り（ワイヤの欄ではない）**: Computer Use（実行ループごと要る +
  画面操作に許可リストは書けず `run` の閉じた許容と衝突）/ Code Interpreter
  （遠隔サンドボックスで同梱ツール層と競合）/ **Realtime API はこの村の単位と
  衝突する**（予算・打ち切り・キャッシュ・広場ログは全部「ターン」に載っており、
  E2E 音声ストリームはターンの外側）
- ~~**英語化（マイルストーン 6）との順番は機能が先**~~ — **2026-08-12 に利用者が
  逆へ裁定した**（後段の「例外なら逆」が発火した形で、規律を破ってはいない）。
  理由は市場 —「**日本で公開しても現在では需要がない。英語にしておけば世界を
  早くに相手にできる**」。**順番は 英語化 → 多モーダル。**
  なお「訳し直す」の心配は**加算なら当たらない** — 日本語をバイト等価で残して
  英語を足す形なので、多モーダルで増えるぶんは**そのとき 2 言語で書く**だけ

**一覧は性質で 3 つに割れ、コストが桁で違う。** Spec を切るときはここで分けること:

| 種別 | 例 | この村での意味 |
|---|---|---|
| **ワイヤの形が変わる** | Grounding / ネイティブ多モーダル / Structured Outputs / thinking ブロック / Citations / Search Domain Filter | **canonical 層が吸収できる。既に 2 つ通した実績がある** |
| **実質は遠隔のツール** | Code Interpreter / Computer Use | **同梱ツール層と競合する。** Computer Use は座標つき行動が返るので**実行ループごと要る** — ワイヤの欄ではない。**Live Search をここに置いたのは誤りで、Spec 31 で「ワイヤの形が変わる」側だと確定した**（サーバー側ツールだが、返るのは結果であって行動ではない） |
| **見えないコスト最適化** | Context Caching / Prompt Caching | 利用者には現れない。**Anthropic 側は既に入っている** |

**1 番目は素直に伸ばせる / 3 番目は既にやっている / 2 番目だけが別の獣。**
公開後に回すなら、**2 番目を切り離しておくと Spec が小さく保てる。**

### 最優先は xAI の Live Search（2026-08-08 利用者）— **Done（2026-08-10）**

**[Spec 31](specs/31_grok-live-search.md) が正。** 起票から Done まで 1 日で、
実機の検収 9 項目すべて観測。以下はこの節の起票時の記録で、**採否の判断は
「citations が返るか」だったところ、判定 A（返る）で決着した**。

理由は技術ではなく**流通** —「**ファイナンス系の需要を取れる。彼らは SNS で
拡散力があるから一般に Concordia を普及させる力になる**」。

**上の分類で Live Search を「実質は遠隔のツール」に置いたのは誤りの可能性が高い。**
xAI の Live Search は `/v1/chat/completions` の**リクエスト・パラメータ**で、
応答に citations が乗る形だと理解している = **「ワイヤの形が変わる」側**で、
**この村が既に 2 つ通した棚**。~~ただし一次資料に当てていない。着手時の最初の確認点。~~
**接続経路は既にある** — Spec 23 P0 で xAI に画像を通した実測が残っている
（公式文書が jpg/png のみと書いてあるのに WebP が通った）。

**↑ この理解は 2026-08-09 の実測で覆った。probe 4 発の観測が正**（使い捨て
script・実鍵・grok-4.5。予測を先に書いてから観測）:

- **legacy の `search_parameters` は HTTP 410 Gone** —
  `"Live search is deprecated. Please switch to the Agent Tools API"` と
  **サーバー自身が後継を名指しして断る**。「リクエスト・パラメータ」の理解は過去形
- **現行は `/v1/responses`（Responses API）のサーバー側ツール
  `{"type": "web_search"}` / `{"type": "x_search"}`**。つまり
  **採用の前提 = 4 本目のワイヤ**で、OpenAI の節の「思考の復活と web 検索が
  同じ 1 本で解ける」の**同じ 1 本に xAI も合流した**（回収先が 2 社 4 機能）
- **citations は返る（判定 A）。** 場所は応答トップの `citations` 欄ではなく
  **本文 `output_text` への `annotations`（`type: "url_citation"`）**。実物は
  TechCrunch の日付つき記事 URL。**ただし `start_index` / `end_index` が全部 0**
  = 主張単位ではなくメッセージ単位。rapidtables や YouTube も混ざるので
  **出典の選別はこちら側の仕事**
- **`x_search` は X の実 status URL + 反応の実数を返す**（いいね・リポスト・
  ブックマーク数が本文に乗る。inline の `[[1]](url)` 形式でも来る）。
  「実在するかしないかで検証できる」の受け渡し物が実物で確認できた
- **decode の罠 2 つ**: `x_search` の output 種別は **`custom_tool_call`**
  （`web_search` は `web_search_call` — 型名が揃っていない）/
  `output` は `reasoning` × N が interleave し **thinking トークンがこの経路では
  流れる**（web probe で reasoning 1,075 tokens）
- **コストの形**: 検索結果がプロンプトへ注入されるので入力が膨らむ
  （web probe = input 98,213 うち cached 62,720。`usage` に
  `cost_in_usd_ticks` / `server_side_tool_usage_details` が生える）。
  1 回目の x_search は 180 秒 timeout 内に ConnectionReset で落ち、
  **問いを小さくした再試行で通った** — 検索の重い問いは分単位を見込む

**採否を決めるのは「動くか」ではなく「citations が本当に返るか」。**
**Spec 05 の Google Search Grounding は、実装が完走したのに `sources` が空だった** —
検索したことは分かるが**何を根拠にしたかは返らない**。処方は「検索語と発表元だけを
言う告知」への縮小だった。**ファイナンスではその縮小形は商品にならない** —
出典のない「最新情報に基づく回答」は価値がゼロではなく**負**で、
**検証できない主張を検証できるかのような見た目で配る**ことになる。
**Grok が本当に citations を返すなら Gemini より適合が高く、返さないなら
Spec 05 と同じ縮小を食らう。** 測り方は Spec 05 が `sources` の空を見つけた手順が
そのまま使えるが、**パラメータを送る必要があるのでコード変更ゼロでは測れない**。

**Live Search が取るのは web だけではない**（2026-08-08 利用者）—
「**x.com の投稿から何がバズっているかなどを取得できる**」。

**反応数の扱いは 3 つに分かれる。2 つに分けた最初の書き方は誤読を生む** —
「エンゲージメント・データに触るな」と読めてしまい、**商品価値ごと殺す**:

| | 何を測るか | 判定 |
|---|---|---|
| **品質の指標** | **自分たちの出力**への反応 | **反指標**（`ai_design_north_star_mandate`） |
| **流通経路** | 誰に届くか | 正当 |
| **取得するデータ** | **世界で何が話題か** | **正当。しかも商品そのもの** |

ファイナンスでは「いま何に注目が集まっているか」は**市場についての事実**
（センチメント・噂の伝播速度・異常な注目量）。**自分のカルマを上げる話とは別。**

**これで設計の性質が 2 つ変わる。**

- **検証の問いが形を変える。** X の投稿が出典なら**実在するかしないかで検証できる**
  （一般の web citation より強い）。ただし**検証できるのは「そう投稿された」ことだけ**で、
  **内容が本当かは別**。ファイナンスでは「**言われている**」と「**本当である**」の差が
  損益そのものなので、**Spec 05 が「検索した事実」と「出典が返らない事実」を分けたのと
  同じ形の分離**が要る。器は既にある（`GroundingNote.vue`）
- **攻撃面が桁で広がる。X には誰でも投稿できる。** サーヴァントが話題の投稿を読むとは、
  **攻撃者が書いた本文がプロンプトへ入る経路が開く**こと。
  **Spec 26 の一般化 3 がそのまま当たる** —「攻撃者が書ける値を数えるときは、
  名前だけでなく本文を数える。**自由入力欄は 1 つとは限らない**」。
  あのとき数え落としたのは `message` 本文 1 つだったが、**今度の自由入力欄は世界中**。
  **機能を止める理由にはならない**（`MCP_DOCKER__fetch` で web の本文は既に入っている）が、
  **Spec の骨格が変わる** — 「取ってきて要約する」ではなく
  「**取ってきたものが本文として何を名乗れるか**」を先に決める形になる。
  **`defuse` は封筒の形しか潰さない**ので、そこは足りない

### オーケストレーションである意味が、ここで初めて機能として言える（2026-08-08 利用者）

「**X の投稿が本当かどうかのグラウンディングを Gemini にやらせたりで
オーケストレーションである意味が出てくる。そういうのをみんな欲しがっている**」。

**「言われている」を取るのは Grok、「本当である」を確かめるのは Gemini。**
**これは 1 社のモデルでは原理的にできない** — 各社の接地能力は**それぞれのワイヤに
縛られている**ので、Grok の中に Gemini の Google 検索接地は無い。
**モデルを増やせば良くなる、という話とは別物**で、
**能力の種類が違うものを繋ぐことにしか価値が無い**形。

**この村には既に同じ形の規律がある** — 条例の Verify 段
「**束ねに参加していないサーヴァントへ検証を頼む（自己採点しない）**」。
それが**ベンダーの層でそのまま効く**: **取ってきた個体に真偽を判定させない。**

**帰結: 出典の重要度が上がる。** Grok の citations は表示のためだけでなく、
**Gemini へ渡す受け渡し物**になる。返らなければ検証段への入力が無く、
**2 段目が成立しない** — 上の「採否を決めるのは citations が返るか」は、
表示の問題ではなく**構成の前提**だった。

**Spec を書くときの注意**: 「誰に届くか」（流通）と「何が良い答えか」（品質）を
**同じ節に混ぜない**。**ただし「何を取ってくるか」は品質の側**で、
反応数がデータとして入ることと、反応数を自分の成績にすることは別。

## orchestrator の分割（2026-08-11 着地。Spec 不要と判断）

起点は利用者 —「`orchestrator.rs` が大きすぎて大変だろうと思ったから分けたい」。
**目的は読みやすさではなく、私（作業する側）の作業のしやすさ。** そこで
「どう分けるか」を推測ではなく **git の変更履歴から数えた**。

### 実測（95 コミット）が境界を 1 つ動かした

| | |
|---|---|
| **55 / 95** | `handle_message` に入っている |
| 同時に変わるペア | `handle_message` + `HandoffTools` 9 / `run_plan` 9 / `ask_agent` 7 / `agent_loop` 6 / `deliver_and_wait` 5 |

**利用者の当初案は `turn.rs` / `delivery.rs` / `prompt.rs` の 3 分割**だったが、
実測ではその 3 つが**最も繋がっていた**（合計 27 回）。因果が輪になっている
（`ask` が `deliver_and_wait` を呼び、それが新しいターンを起こす）ため。
**裁定は `delegation.rs` へ 1 本化**（利用者採用）。今日の転送まわりの
3 コミット（#95 / #96 とその計器）は、この 1 ファイルにほぼ収まる。

### 関数分割を先にやった（順序を逆にした）

利用者の当初案は「ディレクトリ化が最優先」だったが、**実測が指していたのは
`handle_message` の 1,206 行**だった。ファイルを分けても関数は分かれない。

**決め手は「diff が読めない」こと。** 1,206 行の関数は git から見ると 1 つの塊で、
hunk context がほぼ全部 `@@ … @@ async fn handle_message` になる。
今日の 4 コミットも全部そうだった。段ごとの関数に割るとそこが直る。

```text
handle_message 1,206 → 196 行（段の並びだけ）
  build_prompt     223  段 4  モデルへ何を見せるか
  present_tools     92  段 5  何を選べるか
  run_turn         698  段 6-7 実行ループと統計
  dispatch_outcome 182  段 8  どこへ返すか
```

**`run_turn` の戻り値を段 8 の入力（`TurnProduct`）に一致させた** —
`Option` の `None` は「打ち切り・予算切れで後始末まで済んでいる」。

**`HandoffGates` を作った時点で、骨格側の再束縛が unused になった**
（コンパイラが教えた）。3 つのゲートは段 4・5・6 の**5 箇所**が読んでおり、
ずれると「出していないのに効く」か「出したのに効かない」になる（#95 / #96）。
**ゲートを渡すだけの関数になったことが、分割が効いた印。**

### 可視性は 1 つも緩まなかった

見積もりで「`Shared` の可視性が緩む」と書いたのは**誤り**。Rust では
private アイテムは**宣言したモジュールとその子孫**から見えるので、
`orchestrator::turn` からは親の private がそのまま見える。

要ったのは**逆向きだけ** — 親と兄弟から使うものに `pub(super)`。
`delegation.rs` の 16 個（`HandoffTools` のメソッド 11 + 型と関数 5）と
`turn.rs` の 3 本がそれで、**何が外から使われるかが use の 1 行に集まる**。

### 着地（10 ファイル・7,343 行）

```text
turn.rs 2,357 / mod.rs 1,340 / delegation.rs 883 / schedules.rs 638
settings.rs 489 / runtime.rs 392 / context.rs 342 / bootstrap.rs 337
lifecycle.rs 329 / sessions.rs 236
```

**`settings.rs` と `runtime.rs` は当初案に無い**（実物が連続していたので足した）。
逆に `lifecycle.rs` は当初案の「create/start/stop/update」から
**start/stop を外した** — 実物では create 系と start 系の間に設定が 480 行挟まる。

**各コミットで中身を 1 行も変えていない**（`use` の追加と `pub(super)` のみ）。
差分が「移動」だと読める形に保つため。**`tests/orchestrator.rs` は 8,026 行で
src より大きいが、この分割では 1 行も動かない** — 統合テストは公開 API 経由で、
src の内部構造から独立している（別の一手として残る）。

### 踏んだ落とし穴 3 つ

- **移動スクリプトが `impl Shared` の `persist` に当たり、範囲が逆転して
  ファイルを壊した**（30 エラー）。`git checkout` で戻し、探索を
  `impl Orchestrator` の範囲に限って再実行。**同じ名前のメソッドが 2 つの
  impl にあることを機械の側で数えていなかった**
- **`#[cfg(test)]` が `compose_room_log` と `truncate_chars` の間にあり**、
  `context.rs` の範囲へ丸ごと入った。中身は leak detector / `merge_tool_specs` /
  RepeatGuard で**全部 turn 系**だったので `mod.rs` へ戻し、`turn.rs` で回収した
- **clippy が赤のままコミットした** — `&&` で繋いだので `grep` の成功が
  そのまま `git commit` まで通した。**検査の結果を見てから繋ぐ**

### 巨大ファイル分割の 6 箇条（2026-08-11 利用者。**次も同じ手順で再現する**）

**この節は次に同じ規模をやるときの手順書。** 上の記録はその実例。

1. **計測してから切る。** 感覚で切らない。**ファイル分割は凝集度で切る** —
   `git log -p --unified=0` の hunk context を集計して「同時に変わる組み合わせ」を
   数える。今回はそれが当初案の境界を 1 つ動かした（同時変更 27 回）
2. **関数分割 → ファイル分割の順。これが最大の定石。**
   **ファイルを分けても `fn` は分かれない。** 1,206 行のまま別ファイルへ出しても、
   読む側は 2,000 行を 2 ファイルにまたがって読むだけになる
3. **振る舞い不変コミット。** 1 コミット 1 ファイル、**中身は 1 行も変えず
   `use` と `pub(super)` のみ**、各コミットで全スイート緑・clippy ゼロ。
   **`git bisect` が効き、レビューが `use` の差分だけになる**
4. **可視性は親子で閉じる。** Rust では private は宣言モジュールと**その子孫**から
   見えるので、**`pub(crate)` は禁止・`pub(super)` に集約**。
   今回の着地は `delegation.rs` の 16 個 + `turn.rs` の 3 本で、
   **その数が「何が外から使われるか」の全量**
5. **境界を型にする。** 引数のバケツリレーにせず、整合が要る組を型 1 つへ閉じる。
   `HandoffGates` を作った瞬間に骨格側の再束縛が unused になったのが効いた証拠で、
   **#95 / #96 のような事故がコンパイラで止まる形**になった
6. **割る理由がなくなったら止める**（2026-08-11 利用者。**6 箇条目**）。
   **割る根拠が計測から行数へすり替わっていないかを問う。**
   `turn.rs` は 2,444 行で据え置いた — 見かけの数字は大きいが、中身は
   段ごとの `fn` に割れており（`handle_message` 196 / `run_turn` 547 /
   `CallRunner` 199 + `RepeatGuard` 65 / `dispatch_outcome` 182 +
   `finish_*` 177）、**認知負荷は関数分割で既に下がっている**。
   ここで `turn/run.rs` + `turn/call.rs` へ割ると、**得るのは行数の減少だけ、
   失うのは `pub(super)` が増えること**で、1 箇条目に反する。

   **次に割る根拠は行数ではない** — (a) `run_turn` と `CallRunner` の
   同時変更が 5 回を超えた (b) レビューで `turn.rs` 内のスクロールが
   往復するようになった。その時が来たら上の構成で割る。

**残り 2 つへの方針も利用者が示した**（2026-08-11）:

- **`run_turn` 698 行の `for call in &calls` 300 行** — ここから先は「移動だけ」で
  無理。**`&mut` の持ち回りは型で断ち切る**（`struct RunTurn<'a> { repeat_guard:
  &'a mut RepeatGuard, plan_wave: &'a mut PlanWave }` + `fn on_call(&mut self)`）。
  `self.on_call()` にできれば `turn/run.rs` + `turn/call.rs` へ安全に割れる。
  **できないなら 2,357 行で止めるのも定石** — 責務が「ターンの実行」1 つなら
  核ファイルとして許容する
- **`tests/orchestrator.rs` 8,026 行は後回し。** **src の公開 API が固まる前に
  割ると `use` 地獄になる。** 切るときは `tests/orchestrator/` にして
  **src と同名**で `delegation.rs` / `schedule.rs` … と割り、`mod.rs` で束ねる。
  統合テストは公開 API 経由なので、それだけで緑を保てる

## 転送を個体ごとに切る（2026-08-11 着地。Spec 不要と判断）

起点は実機 —「**返答をユーザーに返してしまい、オーケストレーションにならない
ことが頻発する**」（利用者）。進行役が「報告してください」と書きながら
`transfer_to_*` を選び、ワーカーの答えが利用者へ流れていた。利用者は毎回
「ザリに返して」と口頭で直していた。

**Spec を切らなかったのは、決めることが 1 つ（既定の向きと語）しか無く、
利用者が即断したから。** 器も既にあった（`AgentSettingsDialog` のツール一覧）。

### 委譲と転送は**答えの行き先が逆**

| | 答えの行き先 |
|---|---|
| **`ask_*`（委譲）** | **依頼主へ戻る。** 戻り先は `reply_to` が決めており**モデルは触れない** |
| **`transfer_to_*`（転送）** | 会話ごと渡す。**利用者へ流れる** |

**「`ask` の戻り先をシステムで決めるべきでは」という提案は、既に実装済みだった。**
問題は戻り先ではなく**ツールの選択**で、**説明文では防げない** — `ask_*` の説明に
「答えを受け取るなら `ask_*`」と書いてあり、モデルはそれを読んだうえで転送を
選んでいる（#84 の一般化がそのまま当たる）。

### 着地形: `allowHandoff`（既定 true）

**転送だけ**が消え、**委譲と `plan` は残る**。1 つのフラグで 3 つとも消すと
「進行役が誰にも頼めない」に化ける。**揃えたものが 3 つ** — 提示集合と判定集合
（ずれると Spec 20 の形）/ 手順の文（提示していない道具を名指ししない）/
投影（`AgentSnapshot`。無いと保存のたびに既定へ戻る = Spec 14 P1）。

### 追加: 委譲で呼ばれたターンは、設定に関わらず転送を提示しない（2026-08-11）

**設定では足りなかった。** `allowHandoff` は**進行役**を想定していたが、
**ワーカー側にも同じ穴がある** — 実機でジェミー（`gemini-3.6-flash`）が
`ask` と `plan` の両方に答えず、**依頼主のザリ自身へ転送した**
（`handoff: agent=agent_3 to=agent` が 2 回）。

**症状は「1 つの依頼が 2 本に分裂する」形で出る。** 戻り口には定型文
（`body_chars=71`）が返って依頼主は「答えが無かった」と報告し、
中身は**別の因果**として 2 分 57 秒遅れて届き、余分に 2 ターン走る。
**画面では「スルーされた」の直後にその答えが並ぶ**（利用者の報告がこれ）。
詳細は `failures.md` #96。

- **判定は `reply_to` の有無ただ 1 つ**（`offer_transfer` に `!awaiting_reply`）。
  個体の設定とは独立の**構造で決まる規則**
- **`ask` と `plan` は落ちない** — ワーカーはさらに別の相手へ訊いて、
  その答えを自分の答えとして返せる。消えるのは「会話ごと渡して自分は答えない」
  1 つだけで、「自分では無理なので C へ」は `ask_C` で書ける
- **手順の文の末尾も分けた** — 委譲で呼ばれたターンに「結果が人間へ返ります」は
  嘘（実際は依頼主へ戻る）。**文言は保証ではない**（#84）ので、構造で塞いだ
  うえで**嘘を書かないためだけ**に分けている
- **ミューテーションでは輪が回り、利用者宛が 4 通になった** — 実機で見えた
  2 通はたまたま早く止まっただけ。**症状の上限は 2 ではない**
- **`PlanTaskState::HandedOff` は死んでいない** — 到達経路は
  **ツール非対応モデルの旧経路**（`use_tools = false`。終了マーカーが
  無ければ最初の相手へ渡す）として残る。既存テストをそこへ付け替えて凍結した
  （**バックエンドは 1 行も変えていない**。転送ツールが出ないので同じコードが
  別の腕へ落ちる）。**この旧経路はそれまでテストで 1 本も覆われていなかった**

### **この作業で私は 3 回外している。全部「読まずに書いた」形**

1. **`transfer_to_*` は `tool:` 行を出す前にループを抜ける**ので、`name=` の
   grep は構造的に 0 件しか返さない。それを「使われていない」と読んで、
   **表まで作って誤った診断を出した**（`failures.md` #94）。
   **肯定の対照は取っていたが、対照を取った家族が違った**
2. **`decide` の引数名が表す概念と、渡した値の概念が違った**（#95）。
   偽を渡すとツール非対応モデル向けの旧経路へ落ち、**空の本文が最初の接続先へ
   配送された**。実機で「チェックを外すと空になる」として出た
3. **テストが 2 を見逃した** — 提示側だけ検査し**判定側を守っていなかった**。
   Spec 27 P1 で同じことを書いた直後の再演

### 計器を 2 つ足した（**これが無いと診断できない**）

```text
handoff: agent=… to=… hop=…            転送そのもの
reply:   agent=… to=… hop=… chars=…    答えの宛先
```

**`reply` の `to=user` が「転送で来た依頼だった」の証拠**になる。
実機の 2 回とも、この 1 組が即座に原因を示した。
**往路（`turn start:` の `from=`）だけを持っていて復路が無かった**のが、
診断に何往復もかかった理由。

## 閉じる前の確認（2026-08-08 着地。Spec 不要と判断）

利用者要望 —「タスクトレイ常駐ではなく閉じるときに確認ダイアログを出せればいい。
ただシステム設定に出すか出さないかのチェックボックスが欲しい」。
**マイルストーン 3 の着地形**（トレイ常駐は見送り）。

**Spec を切らなかった判断**: 決めることが 1 つ（常に聞くか / 走っているときだけか）
しかなく、利用者が即断した。**器も既にある** — システム設定の
「メッセージ表示/非表示」は Spec 13 P2 で**節を足せば増える器**として作られ、
機構は「線の削除の確認」1 つのまま置いてあった。**2 件目はその器の想定内**。
`askConfirm`（Spec 12 P3）も保存先（`localStorage`）も既存のまま。

**ただし `settings_contract` は触った** — 置き場の鍵を列挙しているので
`confirmClose` が増える（#67 の処方が効いている形で、追記だけで済んだ）。

- **既定 ON。ON なら常に聞く**（利用者裁定）。「何か走っているときだけ」は冗長で、
  しかも**チェックボックスが「確認する」なのに聞かない場合があると壊れて見える**
- **小言にしないのは中身の側の仕事。** 閉じると止まるもの（飛行中のターン /
  MCP の扉 / 予定）を**そのときの状態から数えて出す**。判定は純関数
  `lib/closeConfirm.ts` で、訳語ではなく**辞書の鍵**を返す（`reasonDisplay` と同じ形）
- **「0 体です」とは書かない。** 失われないものを並べても判断の材料にならない。
  **予定の行だけは常に出す** — トレイに常駐しない以上、閉じれば必ず止まるのに、
  **その事実が他のどの画面にも出ていない**
- **`close()` ではなく `destroy()` を呼ぶ。** `preventDefault()` のあと `close()` だと
  **同じイベントが再発火して無限ループ**になる。capability へ
  **`core:window:allow-destroy` を 1 行足した**のはこのため
- **登録に失敗しても画面は使える**（確認が付かないだけ）。
  **閉じられなくなるほうが害が大きい**
- **既存テストが仕事をした。** `useUiSettings.test.ts` は保存形を**丸ごと**
  固定しており、鍵が 1 つ増えただけで赤くなった。**緩めずに期待値を直した** —
  あの網は「鍵が黙って増えると契約と実装が食い違う」を機械で留めている

## 送り手封筒の健全性（[Spec 26](specs/26_sender-envelope-integrity.md)・2026-08-07。**Done**）

**Spec 25 P5 の実機検証中に D6 の前提が崩れて派生した。** 検収 2（呼び名の設定が
封筒に効くか）を確かめる同じ走行で、**D6 の「モデルが封筒を読んで相手が人間で
ないと分かる」が保証ではなく傾向だった**ことが出た。

- **同じ封筒で結論が割れた** — ミュゼ（`muse-spark1.2-contributor`）は
  「MCP経由の外部エージェント」と正しく判定。ザリ（`claude-sonnet-5`）は
  **外部からの依頼を人間の利用者からの発話として扱った**
- **偽造は信じられなくても機能する。** 本文の冒頭へ `【送り手: ユーザー】` を
  書いた 1 通に対し、ザリは偽物を信じていない。**本物が変遷したという観察が、
  本物を捨てる根拠になった** — 攻撃者は嘘を信じさせる必要がなく、
  **本物を揺らがせるだけでよい**
- **倒れた先が定数だった。** ザリが拠り所にした「システムラベル」は
  チャット API の `role` で、**全 `Endpoint` で同じ `user`**。
  だから結論はかならず「人間の利用者」になる。**疑う姿勢は正しくても、
  帯域外の選択肢が無い系で帯域内を捨てると、判定の保留ではなく
  確信を持った誤りに着地する**
- **条例で教える処方は実測で効かなかった**（P1）。事実 3 文を入れて
  **同じ個体へ一字も違わない文面を投げる前後比較**を取ったが、
  **答えは一字も変わらなかった**。切り分けると**条例の当該節を見出しから
  太字の位置まで逐語で引用した** — 読めているうえで従っていない。
  **正解した 2 体のうちミュゼは条例が無い状態で正解している**ので、
  「条例あり + 正解」は効果を示さない。**寄与はどの観測でも測れていない**ため
  3 文を撤去し、条例に残したのは**外部エージェントの存在の事実 1 行だけ**
- **処方は本文の無害化**（`sender_envelope.rs` の `defuse` / `wrap`）。
  `【送り手: ユーザー】` は `【送り手（本文）: ユーザー】` として届く。
  **規則をモデルに守らせるのではなく、守らなくても取り違えが起きない形にした。**
  実機では受け取ったサーヴァントが 2 つを並べて書き写し、
  **本物を根拠に引用した** — 見分けるための規則を 1 つも必要としていない
- **冪等性は「1 回で寄せる」ではなく「寄せた形に再一致しない」で成立**
  （`【送り手（本文）:` は `送り手` の直後が `（` なので検出に当たらない）。
  **構造で成立しているので置換回数を数えなくてよい**
- **`normalize_user_name` の予約文字を 4 つへ広げた**（`【】［］`）。
  **既存テストが `【` を「通す」と明示的に主張していた** — 理由
  「閉じないので封筒の境界は壊れない」は不完全で、ラベルは
  `【送り手: {label}】` へ埋め込まれるので `【送り手: ユーザー` を名乗ると
  **書き出しが 2 回現れる**。しかも `normalize_client_name` が同じ述語なので、
  **呼び名を設定していない村ではこのラベルは外部クライアントが書いた
  `clientInfo.name` そのもの**
- **これは閉じた許容ではない**と契約へ明記した。混同されうる表記は Unicode
  全体に散っており、**列挙で塞ぐのは除外リスト**。**content 層に閉じた許容は
  作れない**（ワイヤに送り手の席が無いことの帰結）。潰すのは衝突の主要形で、
  混同されないことの保証ではない — 保証だと書くと `api_key_env` と同じ
  「説明文を構造と取り違える」誤りになる
- **査読 7 点で骨格が 1 つ変わった。** 指摘は D3 の文面についてだったが、
  引くと **rev1 の D5「モデルに読ませない」が達成不能**だった —
  送り手識別が content にしか置けない以上、**読ませないことは選べない**。
  選べるのは*読ませる量*と*破れたときの向き*だけ。rev3 でさらに
  **要求する読みを 0** にした（rev2 の「位置の事実 1 つなら守られる」も
  実測で否定された）
- **ミューテーション 2 回**で赤を確かめた。無害化を no-op にすると寄せる 7 本
  だけが赤で通常の発話 5 本は緑。**ただし冪等性テストは no-op でも緑**
  （何もしなければ冪等）なので、`DEFUSED` を再一致する形にする 2 回目で
  冪等性の 2 本が赤になることも見た
- 一般化は `failures.md` #84（**「モデルが X を読んで判断する」と書いた契約は、
  読んだうえで従わない個体を 1 体見つけた時点で落ちる。引用できることは、
  従うことの証拠にならない**）

**P4 実機確認は 3 件とも観測して Done**（2026-08-07）。**検収は 4 件から 3 件へ
減らした**（rev4・利用者裁定）— 削ったのは「条例の追記後、外部からの依頼に
サーヴァントが辞退せず応じる」で、**辞退した側の観測が 1 件も無く、条例を書く前から
応答している**ので変化の向きを測れない項目だった。**一般化: 機構を撤去したら、
その機構を当てにしていた検収項目が判定になっているかを数え直す** — 撤去は Tasks の
該当行だけを直させるが、**検収項目は別の節にあるので目に入らない**
（#51 (b) と同じ非対称。`failures.md` #85 の 3 例目）。

観測できたことのうち、次に読む人が要るもの:

- **2 体の結論が割れなかった。** 同じ 1 通を `muse-spark-1.2-contributor`（ミュゼ）と
  `claude-sonnet-5`（ザリ）へ投げ、**両方が `【送り手（本文）:` を書き写したうえで
  本物の封筒を送り手と判定した**。**原観測で割れたのがこの Spec の起点**なので、
  割れないことがそのまま効き目。**ザリは原観測で失敗した側の個体**で、今回は
  「本文中に埋め込まれた文字列（引用テキスト）」と自分で区別している
- **検収 2 が示していない範囲がある。** 貼り付けの 2 通は `from=user`＝本当に
  人間からなので、**原観測の失敗形（外部を人間と取り違える）は原理的に再現しない**。
  それを担うのは検収 1（`from=external` の 1 通）。**条件の違う 2 つの観測を
  「直った」の 1 語に畳まない**
- **計器の対照は同じログの中で閉じた** — `from=external count=1` の 1 通 /
  封筒構文の無い外部の 1 通で 0 行 / `from=user count=2` の 2 通 / 封筒構文の無い
  user の 4 通で 0 行。**origin ごとに出る通と出ない通が揃っている**ので、
  「user では出ない実装」も「常に出る実装」も同時に否定される
- **全角コロンが実機のワイヤを初めて通った。** `【送り手：X】` は単体テストにしか
  無かったが、1 通に半角と全角を両方入れて `count=2` を観測した。
  **日本語入力では全角のほうが自然に出るので、こちらが主経路になりうる**
- **モデルの「逐語で書き写す」は逐語の証拠として厳密ではない。** 2 体とも
  コロンの全角・半角を整えて書いた（届いたのは半角）。**区別したい文字が
  モデルの整形で潰れないかを、検収を書く前に数える**

**待ち行列（2026-08-05 更新）**: (1) **[Spec 17](specs/17_batch-sd-preview.md)
batch-sd** — **rev2 承認 → P0〜P3 完了。残るは実機確認 3 件のみ**
（`preview` 限定で通す判断は 2026-08-05 に利用者が下した） (2)
**[Spec 16](specs/16_grep-precision.md) grep の情報密度**（`include` / `context`）—
**rev2 承認 → P0〜P3 完了。残るは実機確認 4 件**。村からは 0 票で、起点は
改名作業での私の実測。**査読が曖昧 6 点を出し、添えられた参考実装には
既存の不変条件を破る誤りが 4 点あった**（`size_limit` が `pattern` の 10 倍 /
**文字数の枠をバイト長 `len()` で数えている** / `clip_line` の 240 字クリップが
抜けている / 窓の途中で `break` して文脈ブロックを半分だけ描く）。
**バイト長で文字数の枠を数える誤りは Spec 17 の査読でも出ており、2 回続けて同じ形。**
一般化: **`MAX_*_CHARS` という名前の定数を `len()` と突き合わせる差分は、
日本語では枠の 1/3 で発火する**（テストが ASCII だけだと通ってしまう）。
**着地後に計器を 1 つ足した**（2026-08-05）— 実機の `concordia.log` を調べたら
**D1 の賭けが観測できなかった**。`tool:` 行は `args_chars=88` のような**引数の
文字数しか書かず、中身はどの計器にも載っていない**ので、モデルが `*.rs` と
書いたかも、名指しの拒否を読んで `\.rs$` へ直したかも読めない。処方は
`grep include: agent=… pattern=… outcome=ok|glob|invalid` を
**`include` を使った呼び出しのときだけ** 1 行出すこと（省いた呼び出しでは増えない）。
**`run decision:` を足したのと同じ形**で、結合テスト 1 本
（`tests/grep_include_log.rs`）で留めた。**Spec は切っていない** —
既存の判断を見えるようにするだけで、決めることが無いため
(3) **[Spec 18](specs/18_bundled-doc-index.md) 見出し索引の同梱** — **rev3 承認 →
P0〜P4 完了（2026-08-05）。残は P5（README 日英）と実機確認 3 件**。
実装は同日のセッションで完走した（利用者が「次セッション」判断を覆して指示）。
着地の要点: `doc_index.rs`（純機構・単体 12 本）+ `tools/rag.rs`（単体 8 本・
2 段ゲート / 節経路つき search / 超過拒否 / 宣言外拒否）+ `ToolContext.rag_roots` +
定数 9/8 本 + D10（役職から除去・分類 2+3+6）+ 旧 RAG 撤去（P4 単独コミット）。
**実装で 1 つ穴が出た** — Spec の削除一覧は `rag.rs` の 3 型しか数えておらず、
**その型しか使っていない隣の機構**（`compute.rs` の `top_k_similar` /
`cosine_similarity` / `Scored`・`OrchestratorConfig.rag_top_k`）を数えていなかった。
撤去の grep は型名だけでなく**その型を運ぶ関数の呼び出し元**まで 1 段辿る。
**実機の初日に D7 が反証され、D13 で覆した**（2026-08-05 利用者裁定）:
ザリに `D:\ManualeRAG` を宣言したのに `rag` が出なかった。原因は 2 段ゲートの
1 段目 — **既存の村は全 8 体が enabledTools 明示配列**で、明示配列に新ツールは
自動で増えない（`file` の前例と同じ凍結）ため、「既定 ON」は既存の村では誰にも
効かない。裁定は**チェックそのものの廃止** — 宣言を書けるのは人だけなので
宣言がオプトインで、チェックは同じ意図の 2 つ目のスイッチだった。提示は
宣言のみ・`BUNDLED_TOOL_NAMES` は 8 本へ戻り（8 = 7 + 1）、`rag` は MCP 由来と
同じ「enabledTools の対象外」側に住む。回帰 2 段（コア結合 + 名前表テスト）で
「親切心で表へ戻すと同じ穴が開く」を留めた。**一般化: 「既定 ON で新設」は
既定に従っている個体にしか効かない。全個体が明示配列の村では死文になる —
新ツールの提示条件を設計するときは、実データの enabledTools が null か
明示配列かを先に数える。**Manuale（MCP サーバー・
1,685 行）の核を `AgentTool` 1 本として取り込み、**使われていない同梱 RAG
（`rag.rs`）を置き換える**。新規依存は **`pulldown-cmark` 1 本だけ** —
`rmcp` / `git2`（唯一の C 依存）/ `ignore` / `grep-*` は同梱すると全部落ちる
（`collect_files` と `run_grep` が既にある。行数と依存表は 2026-08-05 に
`D:\Github\Manuale` の実物で裏取り済み）。**最も重い決定は `rag_sources` を
「見出し索引を張るフォルダ」へ再定義し、work_dir の外を読み取り専用で許すこと** —
**アンケートで唯一裁定が落ちていたジェミーの「読み取り専用マウント」への回答**でもある。
**査読が指摘 7 点を出し、rev2 で全件反映した**（実装と突き合わせる方法 1 回のみ =
#57 の数え方で 1 票）。最も重かったのは指名していなかった箇所 —
**`rag_sources` は `AgentRoleDefaults` にも写っており**（`model.rs:286`）、
絶対パスへ意味を変えると **Spec 14 が `work_dir` を役職の雛形から排除した理由
（「端末ごとに違う絶対パス。村を配ると存在しないパスを指す」）をそのまま踏む**。
処方は役職の雛形から外し `role_contract` 凍結 2 を
**`11 = 対象外 2 + 入れる 3 + 入れない 6`** へ改訂すること（rev2 の D10）。
**一般化: 欄の意味を変えるときは、その欄が他のどの型に写っているかを先に数える** —
Spec 14 P1「`AgentSpec` に欄を足したら投影にも要るかを問う」の**逆向き**で、
足すときだけでなく意味を変えるときも同じ問いが要る。ほか
**`ToolContext` に `rag_sources` を運ぶ経路が無い**（`run` の手は `run.json` が
`ConfigStore` のファイルだから成立するもので、`World` の中にある欄には使えない。
Spec 14 P1 / Spec 15 P2 に続く**三例目**）/ **`## 参照資料` の push 注入を撤去しても
挙動は 1 ミリも変わらない**（索引が常に空なので一度も発火していない = rev1 は
機構としては正しく実効としては誤り。**テストでは落ちない**種類）/
`BUNDLED = DEFAULT ∪ {run}` の**等式に触れていなかった**（#62 と同型の穴が、
#62 を引用している節に開いていた）。**2 巡目の査読で 9 点、うち 7 点を反映して rev3**
（`tools/rag.rs` + `rag_tool_contract` へ名前を 3 層とも揃える / D9 を Notes から本文へ /
2 段ゲートを「空**または全滅**」へ / Phase 4 を単独コミットにして **revert の単位を
撤去に一致**させる = D12）。**2 点は実測で反証した** — `P4` は spec 群 235 箇所の
確立した略記で実在する / **8 → 9 は矛盾ではない**（`BUNDLED_TOOL_NAMES` は
`["diff","fd","file","grep","remember","run","sd","yq"]` で**旧 RAG はどちらの定数にも
入っていない**。`AgentTool` を 1 つも実装していない push 注入の機構だったため。
**ツールとして提示されていなかったからこそ push で差し込むしかなかった**のであり、
それは D6 が撤去しようとしている構造そのもの）。
**2 巡とも、仕様が変わった指摘は「査読で見てほしい」と指名していない箇所から出た** —
指名した D3 / D2 はそのまま承認され、動いたのは D10（`AgentRoleDefaults` への写し）と
名前の不一致。**Spec 14 P5「腐るのはその機能の節ではなく隣の節」の 2・3 例目。**
**同じ方法の査読は巡を重ねても 1 票**（#57）— 2 巡目で反証が 2 件出たのがその証拠で、
別の方法（実際にビルドする）で 1 度当てる価値が残っている
(4) **`yq` の `op: "check"`** — 衝突なし・未起票 (5) **広場ログの pull 化** —
**[Spec 22](specs/22_room-log-pull.md) rev2 承認 → P0〜P4 完了**（2026-08-06。
arXiv 2607.26637 を読んで起票 — 論点だった「目的を品質ではなくコストへ
絞れるか」は論文 RQ2 が正面から支持した。骨格: `room_log {id}` ツールを
orchestrator 合成側へ新設・可視述語 1 実装共有（`room_log.rs`）・ID は表示時に
衝突解消・読み口は `Shared.log` のみ。抜粋の切れた行に行頭 `[ID]` が付き、
次の手は `ask` から `room_log` へ。単体 13 + 結合 5。**残は P5 実機確認 3 件**）。
以前の候補
（システム設定への追加 3 件 / 黒板の機構化）はその後ろ。**MPL-2.0 への relicense は
2026-08-05 に完了**（下記「ライセンス」節）。

**村のアンケートの要望表は古くなっている**（2026-08-05）。`blackboard/ザリ・ロブステル.md`
に残っている集計は 2026-08-04 のもので、**そこで 2 票を集めた「grep の集計モード」は
その日のうちに `323e83a` で入っている**（`count_only: true` + ファイル別件数 +
打ち切り時の真の総数。テスト 3 本が留めている）。黒板は 1 人 1 ファイルなので
直せるのはザリ本人 — **次に依頼を出すときに「現況へ直せ」を添える**。
これは 2026-08-04 の教訓（残タスクは満たされても台帳へ自動では書き戻らない）の
**村側での再演**で、書き手が私ではなくサーヴァントになっただけ。

**アンケートで裁定が落ちている要望が 1 件ある**: ジェミーの
「`D:\ManualeRAG` 外の**読み取り専用**マウント」。採否がどの台帳にも無い
（`grep 'マウント' CLAUDE.md` が 0 件）。`work_dir` が 1 つしか無い以上これは
設計判断が要る要望で、**票が少ない側のものが 1 件だけ表から落ちた**形。

**Spec 15 に残る実機確認 5 件**（使いながら出る種類）: (6) Windows のコマンド名正規化 /
(7) パス付きの照合前拒否 / (8)(9) タイムアウトと打ち切りで孫まで kill / (10) 既定 OFF。
**`run decision:` のログ行を足したので、(6)(7) は普段の走行でも痕跡が残る。**

**(11)「`allow` が空なら非提示」は 2026-08-06 に撤回済み**（`specs/15:282` に
取り消し線つき。裁定は上の Spec 20 の節が正 — 提示はする・`decide` が拒む）。
**この数が 6 のまま残っていたのは、撤回を Spec 20 の節に書いて Spec 15 の数を
数え直さなかったから** — #51 (b)「腐るのはその機能の節ではなく隣の節」の再演で、
**機構を撤去したら、その機構を当てにしていた検収項目を数え直す**（Spec 26 rev4 で
同じことをしている）。

**2026-08-04 の教訓（2 回続けて出た）**: **残タスクとして書いた項目は、満たされた
ことを台帳へ自動では書き戻さない。** この日 Spec 04 / 08 / 11 と条例の 4 件が
同時に片付いたが、うち 3 件は**既に起きていたのに気づいていなかった** —
利用者が画面とログを見せたから分かった。**次にその領域を触るときは、台帳の
記述を信じず実測で確かめ直す。**

## 失敗ループのトークン燃焼への対策（**3 点すべて Done**・2026-08-04 完了）

failures.md #41 のトークン燃焼を **1 → 2 → 3 の順**で潰した。
1 だけが病（繰り返し失敗）を直し、2・3 は症状（燃焼）を抑える側。
**2026-08-04 に 3 点とも実機確認まで完了**（下記の各項を参照）。
以下は経緯の記録で、次の作業ではない。

1. **同一失敗の検出** — **完了**（2026-07-31）。ツール名 + 引数 + **結果本文**の
   完全一致が 2 回返ったら 3 回目は実行せず短い通知だけ返し、`ToolRepeatBlocked`
   を発行する（`orchestrator` の `RepeatGuard`。単体 7 本 + 結合 2 本）。
   判定材料が「エラー文言」でなく結果本文なのは、同梱ツールが失敗を `Err` では
   なく `Ok(<エラー文の本文>)` で返すため（failures.md #41 の一般化 3）。
   数えは **(ツール名 + 引数) ごとで隣接を要求しない** — 隣接判定にしていた
   初版は実機で 1 件も発火しなかった（モデルは 1 周に 2〜3 本を並列で呼ぶため
   数えが切れる。#41 の (c) と一般化 4）。**止めるのはその 1 本でループではない**。
   ループを切るのはその周のツールが全部止まったときだけ（一般化 5）。
   **実機で初発火した**（2026-08-01）— 61,891 字の `data_contract.yaml` を
   `file read` させたら止まった。ただしその周のツールがその 1 本だけだったので
   一般化 5 の打ち切り条件を満たし、ターンごと落ちて本文が空になった。
   **機構は設計通りで、真因は歯止めの先に道が無かったこと**（failures.md #44、
   `file read` の打ち切り文だけが次の手を書いていなかった。修正済み）。
   **残っていた実機確認（複数本を並列で呼ぶ周で 1 本だけ止まり、ターンは続く）は
   まだ観測できていない** — 発火が 1 回きりで、その周のツールがその 1 本だけ
   だったため。**ただし病そのものは再発していない**（8/3〜8/4 の走行で
   `tool blocked` はゼロ）。**観測を待つ性質の残タスクで、作業は無い**
2. **割り込み停止** — ターンループの周回境界で見る協調的な打ち切り。
   `stop_agent` は飛行中ターンの完了を待つ設計なので別機構が要る。
   plan の波の途中で切ったときの伝播が設計の主戦場。
   **Spec 10 として完遂（起票 → rev2 査読承認 → Phase 0〜4 → 実機確認まで
   2026-08-01〜02 の 2 日で Done）**。機構: `Envelope.cancel` の child_token
   連鎖・周回境界の検査・出口 2a/2b/2c・`run_plan` select 化・
   「■ 停止」「全ターン停止」UI・`interrupt_all`・`stop_agent` 高速化。
   コア結合 8 本 + フロント投影 3 本 + 加算的変更回帰。実機で「要求から
   0.0 秒」（波の join 待ちの即検知 = select 化の効き）と「依頼元の打ち切りに
   連鎖」と「打ち切り前に答え終わったセルは answered のまま」を観測。
   実装中の改善 1 つ: 打ち切りターンは sent_user_turn を送った形のまま積む =
   キャッシュはリセットされない —
   伝播の単位はエージェントでなくターンの因果（波の巻き添え範囲 =
   その波が配送した封筒だけ）。rev1 査読が矛盾 4 件（不変条件 1 の射程 /
   出口 3 点セットの経路別分割 / stop_agent 但し書き / 畳みの Reply 必須）と
   未定義 3 件（親トークンの畳み込み / run_plan join の select 化 /
   interrupt_all の冪等）を出し、rev2 で全反映。API は
   `interrupt_turn(agent_id)`、`PlanTaskState` へ `Interrupted` 追加、
   依存は tokio-util 直接化（間接依存として既に Cargo.lock に居るので
   コストゼロ。CancellationToken の tokio 本体昇格説は実ソースで否定確認）
3. **トークン予算** — 歯止めを 1 つコスト建てにする。Task Budgets は
   claude-sonnet-5 で使えるがモデルへの申告であって強制ではなく、
   Gemini 系には無い。村全体の天井はハーネス側に要る。
   **優先度が上がった**（2026-08-02）— 利用者の村の運用図（8 段フロー、
   人が関与するのは最初と最後だけ）の自律度は、自動の天井があって初めて安全。
   燃焼対策 3 点のうち 1（RepeatGuard）と 2（Spec 10）は Done、これが最後。
   **Spec 11 として同日に起票 → rev2 承認 → P0〜P4 の機械側まで完了**
   （2026-08-02）— 因果（ユーザー発話の宛先ごと / 予定の発火ごと）に
   実効トークン建ての天井、検査は周回境界、`budget_exhausted` を加算追加、
   既定 1M は新規 world.json のみ・既存は起動 WARN・設定 UI なし。
   実機確認 (1) 小天井で止まる = **観測済み**（2026-08-02。`ceiling=50000
   spent=50000`）。**(2) 既定天井で健全依頼が完走 = 観測済み**（2026-08-04）—
   天井 1,000,000 の村で 6 体へ撒く依頼が `stop=-` 完走し、`budget exhausted` は
   1 件も出ていない。**実効 110,157（素 166,085・7 ターン）で天井の 11.0%**、
   余裕 889,843。**Spec 11 は Done。**
   これで**燃焼対策 3 点（RepeatGuard / Spec 10 / Spec 11）は全て Done**

## 会話の永続化とセッション管理（Spec 12・rev2 承認 → P0〜P5 完了。**Done**）

**Spec 12 は rev2 承認 → P0 完了**（2026-08-02。契約は data_contract の
`session_store` + `reset_rule` の Spec 12 改訂節 + `sessionSwitched` の加算。
Spec 03 のヘッダにも改訂を記録）**→ P1 完了**（2026-08-03。
`crates/fuseforks-core/src/session_store.rs`。`SessionStore::open` /
`create_session` / `append` / `session_meta` / `list_sessions` /
`latest_session` / `records` / `tail_messages` / `restore_histories` /
`fork_session` / `delete_session` / `export_session`。単体 16 本・
workspace 全緑・clippy 新規警告ゼロ）**→ P2 完了**（2026-08-03。
書き込み点 2 種・起動時の復元・セッション API・`reset_conversation` の委譲。
結合 8 本）**→ P3 完了**（2026-08-03。IPC 7 本・`SessionDialog.vue`・
`sessionSwitched` の投影。フロント 2 本 + ワイヤ形の凍結 1 本）
**→ P4 完了**（2026-08-03。手動要約 `summarize_session` + 可変文脈への注入。
結合 3 本）。**→ P5 完了 = Done**（2026-08-08）。**P5 の実機確認は 2026-08-03 に
P3・P4 の記録として観測済み**（会話一覧・開き直し・新規チャット・分岐 / 要約が
作られ再起動をまたいで復元され `history_msgs` が 4 → 0 に落ちる）で、
**残っていた「要約して続ける」も、その後の運用で問題が出ていないことを利用者が
確認して閉じた**。起点は利用者の判断 —
「再起動して前回の続きから始められないと、道具として使えない」。
利用者が Claude Code / Copilot / Cursor / LangGraph / CrewAI を調査した
資料を提供し、Spec の参考節へ接地済み。

**設計の核は、この村には履歴が 2 層あること**（他ツールに無い事情）:
`Shared.log`（村の会話ログ・`AgentMessage`・リング 5,000）と
`AgentRecord.history`（エージェント個別のプロンプト履歴・`ChatMessage`・
滑る窓 8 往復）。**片方から他方を復元できない** — history には #45 の規律で
「送った文字列そのもの」（畳んだ可変文脈込み）が入り、log にその文字列は無い。
log には System 行や他人宛の発話があり、どの history にも入らない。
ゆえに**会話ログだけ保存して再開すると、画面は正しいのに全エージェントが
健忘症で始まり、その 2 つは画面上区別が付かない**。

**rev2 で保存先を redb 1 ファイルへ変更**（2026-08-02 査読）。rev1 の
`JSONL + index.json` は**真実が 2 つ**になっており、原子性の矛盾も fork の孤児も
その帰結だった。redb は機構を**減らす**（index.json・temp+rename の自前実装・
壊れた行の切り捨て・fork の 2 段階が全部消える）。ハイブリッド
（redb = 索引 / JSONL = 本文）は採らない。

`{workspace}/sessions.redb` にテーブル 2 つ（`sessions` / `records`、
records のキーは `(session_id, seq)`）。`Record` は **message / exchange /
summary の 3 種別**（**→ 2026-08-16 に [Spec 39](specs/39_stats-view.md) P0 で
`turn` を足して 4 種別。** ターン 1 本の使用量。履歴の入力にはならない）。
復元しないものは波・予算・MCP 接続・稼働状態（作業の寿命）。
索引は作らない・要約は手動のみ。**`export_session`（JSONL 書き出し）は P1 の
完了条件** — 読めない保存先を作るなら出口は機構の一部（この企画の診断は
grep に依存している）。

**P1 の実装で決めた 3 点**（2026-08-03）:

- **`fork` は seq を振り直さない**。振り直すと `summary.coversUpToSeq` の指す
  境界がずれ、複製した側だけ要約の覆う範囲が変わる
- **1 往復 → `ChatMessage` 対の変換を `world::exchange_pair` の 1 実装に統一した**。
  押し込む側（`push_exchange`）と読み戻す側で規律が分かれると、**復元した履歴
  だけが空メッセージを持ち、次のターンで 400 になる**（failures.md #29 の再演）
- **`restore_histories` は `RestoredHistories { histories, summaries }` を返す**。
  要約が履歴の席を持たないことを型で保つ（注入は可変文脈の畳みへ相乗り = #45）

**P2 の実装で決めた 3 点**（2026-08-03）:

- **`start_agent` が履歴をクリアするのをやめた**（既存挙動の変更としては最大）。
  旧い規律「起動は新しい会話の開始として扱う」は、会話を始め直す手段が他に
  無かった時期の代用だった。**起動時に自動起動しない契約と組み合わさると、
  再起動して開いた会話の履歴を、エージェントを起動した瞬間に捨てる** — S1
  （続きから始められる）が原理的に成立しない。始め直しは「新規チャット」が担う
- **飛行中ターンの拒否は「既存の会話を開くとき」だけ**。凍結済みの 2 節が
  衝突して見えた（`reset_rule` の案 A = 完了書き込みは許容 / 不変条件 11 =
  切り替えは idle のときだけ）。解は**着地先で分ける** — 新規チャットの着地先は
  新しい空の会話で既存の記録を汚さないので案 A が成立する。resume / fork /
  continue_latest / 開いている会話の delete は `SESSION_SWITCH_BLOCKED` で拒む。
  **自動で `interrupt_all` してから切り替えることはしない**（線は人が引く）
- **保存の失敗は WARN 1 行で続行**し、保存先が開けない起動も普通に通す
  （その村では会話が再起動で消え、`reset_conversation` は改訂前の挙動へ落ちる）

**P3 の実装で決めた 3 点**（2026-08-03）:

- **分岐は `ForkPoint` の一覧から地点を選ぶ 2 段階**。S4 は「ある地点までは
  同じで」が本体なので、会話まるごとの複製しかできない fork では満たさない。
  候補は**ユーザー発話だけ**（全レコードを並べると入退室の System 行や往復の
  記録まで候補に出て、「どこで切ったのか」が読めなくなる）。
  **切るのは選んだ依頼の「直前」で、依頼の文面は入力欄へ差し戻す**
  （実機の観察で直した。下記）
- **`conversationCleared` では通知を出さない**。この 1 本は新規チャット・
  開き直し・分岐のどれでも同じように飛ぶので、既存の「新規チャットを
  開始しました」は開き直しのときに嘘になる。通知は操作した側が出す
- **書き出し先は `{workspace}/exports/{session_id}.jsonl` に固定**。ファイル選択
  ダイアログのために plugin を足すより、置き場所を 1 つ決めてパスを通知に出す
  ほうが依存も操作も少ない

**P3 の実機確認**（2026-08-03、利用者）: 会話一覧・開き直し・新規チャット・
分岐すべて確認済み。**分岐で 1 件、設計の誤りが実機でだけ見えた**:

- 症状: 分岐先に**返事の付かない依頼が宙に浮いたまま残る**（`合言葉はブラボー`
  が画面に出ているのに、`合言葉は？` に `アルファです` と答える）
- 真因: 選んだ依頼**を含めて**複製していた。返事と `exchange` はその発話より
  後に書かれるので、`at_seq` の外へ落ちる。**これは契約の「半端なターン」の
  規律どおりで、機構は正しく動いていた** — 正しく動いた結果が使いにくかった形
- 処方（利用者の案）: **依頼は複製先に残さず、文面を入力欄へ差し戻して
  書き換えられる状態にする**。S4「そこから別の頼み方を試したい」がそのまま
  操作になる。差分は `ForkPoint`（`seq` → `at_seq` = seq − 1、`text` と `to` を追加）と
  `ChatInput.fill()` の公開だけで、`fork_session` の inclusive 契約は触っていない
- **一般化**: テストが全部通る機構でも、「その結果を人がどう使うか」は実機でしか
  出ない。単体・結合は `at_seq` の意味を固定しただけで、**その `at_seq` が
  使う人にとって正しい位置かは検証していなかった**（failures.md #49）

**この Spec で failures.md へ 3 件残した**（2026-08-03）: #48 復元の配線を入れたのに
起動ボタンが履歴を捨てていた（代用機構は本来の機構が来た時点で害へ反転する）/
#49 境界値の「意味の正しさ」と「位置の適切さ」は別で、後者は実機でしか出ない /
#50 ターンループの外へ出した LLM 呼び出しが、どの計器にも載っていなかった。

**P4 の実装で決めた 4 点**（2026-08-03）:

- **対象は稼働中のサーヴァントだけ**（2026-08-03 の実機指摘で修正。初版は停止中にも
  呼んでいた）。要約の目的は以後のプロンプトを短くすることで、停止中の個体には
  以後のターンが無い。それでも呼べば**参加していない個体のぶんまで押した人が
  トークンを払う**。履歴は停止しても消えないので、起動してから押せばそのとき
  要約される。飛ばした相手が居たら System 行に必ず書く（#44 の規律）
- **要約を作るのは本人**（そのエージェント自身のモデル）。別のモデルに書かせると
  要約の文体と本人の文体が食い違い、次のターンで「自分が書いた覚え書き」として
  読めなくなる。要約の呼び出しにツールは提示しない（呼びに行く個体が出て、
  1 回で終わる呼び出しがループになる）
- **境界は書く前に取る**（`SessionStore::last_seq`）。書いた後だと自分自身を覆う
  要約になり、`coversUpToSeq < 自身の seq` が破れる
- **畳むのは保存が済んでから**。空の要約・失敗した要約では畳まない —
  要約に失敗した代償が履歴の喪失になるのは最悪の交換
- **使ったトークンは統計へ積み、`summarize:` の 1 行を出す**（2026-08-03 の実機
  ログで塞いだ穴）。要約はターンループの外の LLM 呼び出しなので、積まないと
  **押した人が払った分がどの数字にも現れない**（カードの累計・村の集計・`turn:` 行の
  どれにも出ない）。実機の 1 回目はログに一切痕跡が無く、`history_msgs` が
  4 → 0 に落ちたことからしか実行を確認できなかった
- **要約の長さはモデルで桁が違う**（2026-08-03 実機、4 体同時）。同じ 1 往復に対し
  `claude-opus-5` が 1,163 字、`gemini-3.5-flash-lite` が 234 字。opus は畳んだ
  履歴より長い要約を書いた。**要約は滑る窓と違って落ちない**ので、膨らみは以後の
  全ターンに乗り続ける固定費になる。処方は計器のみ — `summarize:` 行へ
  `folded_chars` を足し、`summary_chars >= folded_chars` で WARN。
  **機構では止めない**（止めると「要約したのに畳まれない」が画面から読めない）
- **`/compact` のようなスラッシュコマンドは採らない**（利用者と相談して決定）。
  入力欄は「特定のサーヴァント 1 体への発話」の場で、要約は村全体の操作なので
  射程が誤解される。入力欄に構文解析を持ち込むと、取りこぼしはそのまま相手へ飛び、
  `/` で始まる正当な発話を飲む。GUI にはボタンの置き場所がある

**実測で決めた**（2026-08-02）: redb 4.1.0 = 2 依存 10 秒ビルド、
「後ろから 16 件」0.03 ms、20,000 件の全走査 11 ms、fork 55 ms。
候補比較は tantivy+lindera = **451 依存**（しかも zstd-sys で C が戻る）、
rusqlite bundled = 10 依存。**SQLite FTS5 の既定 unicode61 は日本語で 0 件**、
trigram なら全ヒット — 日本語検索に形態素解析は要らない、が lindera を
外した根拠。D1〜D9 は全決着、D10（redb のファイル形式更新時の移行）が残る。

## 計器を 2 つ足した（2026-08-06。**どちらも実機の 1 ターンから出た**）

利用者の報告「ザリが本文を返さない」を調べたら、**別々の穴が 2 つ**出た。

**1. 計器が利用者の秘密をログへ書いていた**（`failures.md` #71）。
`text tool call leaked:` が漏れた本文を先頭 160 字**そのまま** `head=` で出しており、
実機の `concordia.log` に `Authorization: Bearer outcast_sk_…` が平文で残った。
**計器は正しく動いていた** — 入力がモデルの書いた文章だったことが問題で、
そこには利用者が渡した秘密が入りうる。処方は本文を落とし、**どの目印で当たったか**
（`markers=close+param`）と字数だけにすること。**`api_key_env` と同型で方向が逆**
（あちらは入力から入る経路、こちらは出力から出る経路）。
**一般化: モデルの出力を記録する計器は、秘密の転送経路になる。**

**2. 出力 399 トークンがどの計器にも載らずに消えていた**（`failures.md` #72）。
`turn: rounds=1/36 stop=- prompt=22425 total=22824` で本文もツール呼び出しも無い。
`anthropic.rs` の `decode` が `text` / `tool_use` 以外を `Other => {}` で
**黙って捨てていた** — Anthropic でそれに当たるのは `thinking` /
`redacted_thinking` だけなので、**思考だけに使って本文を出さずに終えた**と読める。

**「本文が返りませんでした」の置き換え自体は正しい**（空の assistant を積むと
次のターンが 400 で落ちる = #29）。**欠けていたのは診断で、機構ではない。**
処方は #47 の L0 と同じ — `dropped content blocks: kinds=… output_tokens=…` を
1 行出し、空本文の文言へ**観測できた事実だけ**を足す（「出力 N トークンを使いましたが、
本文もツール呼び出しも返しませんでした」）。**画面で「thinking です」と断定しない** —
見たのはトークン数であって、ブロックの中身ではない。**機構は作らない**
（thinking の昇格・再試行は、2 回の観測で足すと効いているか分からない機構が増える。
#47 で L1 / L2 を作らなかったのと同じ判断）。**頻度を見てから決める。**

**一般化: 「未知の入力を握り潰して先へ進む」防御は、握り潰した事実を数えないと、
後から原因を追えない沈黙になる。** `_ => {}` を書くときは、そこを通った回数と種別を
出せるかを必ず問う。**受け皿が底なしだと、そこへ落ちたものは存在しなかったことになる。**

## 観測中（機構を作らず計器だけ置いてある）

**本文へ漏れたツール呼び出し**（2026-08-02、failures.md #47）。モデルが
ネイティブの `tool_use` ではなく本文へツール呼び出しの XML を書くことがあり、
ハーネスは「ツールを呼ばなかった = 最終出力」と読んで生の XML を配信する。
実機の初発は claude-opus-5 + MCP 27 本で **1 回だけ**（21:39）。同じ依頼を
再実行したら `MCP_DOCKER__fetch` はネイティブで成功したので、プロンプト起因では
なく一過性の揺らぎと読んでいる。

- **L0 計器**（Done）— `text tool call leaked:` を 1 行。検出は
  `</invoke>` / `<parameter name=` / `<invoke ` の 3 つ。**`<invoke` だけを
  探すと実機で一度も発火しない**（届く本文は先頭の `<invoke name="` が
  食われて `MCP_DOCKER__fetch">` から始まる。削ったのは API 側で、
  `anthropic.rs` の decode は XML を解釈しない）。単体 2 本 +
  **in-situ 1 本**（実際にターンを走らせログへ 1 行出るまで見る）
- **L3 提示外呼び出しの返却**（Done）— フィルタが捨てていた呼び出しを
  `tool_result` で返す。判定は RepeatGuard の後ろ。data_contract の
  `tool_execution` 不変条件に凍結
- **L1 自動修復 / L2 教示リトライ は作らない**（2026-08-02 利用者判断）。
  1 回しか観測できていない事象に機構を足すと、効いているか分からない機構が
  増える。計器があるので次に起きれば必ずログに残る — **頻度を見てから
  Spec 起票**。実装時の要点は data_contract の `leaked_tool_call` に書いてある
  （L1 の正規表現は開始タグが食われた形を主に据えること）

## 広場ログの見える範囲（2026-08-04。**Spec 22 で pull 経路が付いた**）

起点は利用者の報告 —「チャットの全文がエージェントへ渡っていない。途中で
途切れているとソネットが報告する」。調べた結果、**エージェントから見える会話は
4 層に分かれており、「1 つの文字数制限」ではなかった**:

| 層 | 渡るもの | 上限 |
|---|---|---|
| 自分宛のユーザー発話 | **全文** | 無し（`attribute_sender` は `content` を丸ごと渡す） |
| 自分の履歴 | 直近の往復 | **8 往復＝16 メッセージ**（`world.rs` の `max_turns * 2`） |
| 他エージェント間の発話（広場ログ） | 抜粋 + **`room_log` ツールで全文 pull**（Spec 22） | 抜粋は **12 件 × 各 200 字**・pull は 1 回 20,000 字まで |
| ユーザーが**別の**エージェントへ送った発話 | — | **見えない**（`is_visible_in_room_log`。**pull でも読めない** — 述語 1 実装） |

**1（着地・`data_contract` の `room_log` へ凍結）**: 抜粋であることを本文に書く。
切った行へ `（全 N 字）`、見出しへ「直近 N 件まで・各行は先頭 N 字の抜粋」。
切った行の次の手は当初「発言した相手へ `ask`」で、**Spec 22 が「行頭の ID を
`room_log` へ」に差し替えた**（`ask` は相手のターンを消費するうえ返るのは再話。
上限超とリング溢れのフォールバックとしてだけ残る）。**機構は設計どおりで、
欠けていたのは打ち切りの伝え方だった** — `…` だけではモデルが「省略」と
「言い終えた」を区別できない。**#55 で grep の件数を直したその日に、同じ形が
30 行離れた `compose_room_log` に残っていた**（`failures.md` #58。一般化は
**規律は「どの契約の節に書いたか」で射程が決まってしまう** — 打ち切りの規律は
`bundled_tools_contract` に書かれていて「ツールの話」として読まれる形になっていた）。

**2（`room_log_excerpt_chars` を上げる）と 3（ユーザー発話を広場ログに含める）は
課題。利用者は両方とも避けたい意向**（2026-08-04）:

- **2 を避けたい理由**: 上げると**毎ターン・全員に**乗る。200×12 = 2,400 字が、
  1,000 にすると 12,000 字 ≈ 6,000 トークン／ターン／個体。しかも
  **論文 1 の品質仮説（毎ターンの可変注入が品質を落としていないか）が未測定**の
  まま数字だけ動かすことになる
- **3 を避けたい理由**: 「宛先外のエージェントはメッセージがあったことすら
  知らないべき」（既存のユーザー指示・凍結済み）を崩す

**利用者が示した方向（まだ練られていない）**: **push で全部渡すのをやめ、
pull にする** — (a)「続きが読みたいものだけエージェントが判断して読める」/
(b) 渡すときに「〇〇という黒板のファイルを見ろ」と参照で連携する。
**これは黒板で既に採った原則と同型**（「push 注入はしない = pull = 読みたいときに
`file read`」）。今回の 1 の処方（「全文が要るなら相手へ `ask`」）も
**pull 型の最小形**なので、方向は既に踏み出している。

**練るべき論点は [Spec 22](specs/22_room-log-pull.md) で決着した**（2026-08-06
起票・rev2）: 原本は**新しい器を作らず `Shared.log` のまま**（置き場を決めるのは
器ではなく可視述語 — ファイルにすると `file read` で凍結が破れるか N 重書き込みに
なる）/ コストは push の固定費を動かさず pull の変動費を足す形で決着（`ask` の
相手ターン消費 + 再話が、ツール 1 呼び出し + 原文へ変わる）/ 「読まずに誤答」は
論文 RQ3（検索者の能力が正解率に直接効く）を根拠に**抜粋を絞らないことで担保**
（D6 — 縮小は `room_log read:` 計器で使用実態を観測してからの別判断）。
**課題 2 は動機ごと消え、課題 3 は不採用のまま述語ごと据え置き。**

## 付箋と指示書を分けた（2026-08-19。条例の改訂。コード 0）

起点は LangGraph の突き合わせで「黒板の後勝ちを条例の文言で避けている」と書いたこと →
数えたら **`file` の `append` は Spec 09 rev2 から在り、条例だけがそれを知らなかった**。
利用者の裁定 —「**append する付箋と、ファイルでの情報受け渡し用の指示書を明確に分ける**」。
条例は利用者が貼った（2026-08-19）。私が触ったのは `.gitignore`（`briefs/`）と DETAIL 日英。

| | 付箋 `blackboard/<表示名>.md` | 指示書 `briefs/<依頼名>.md` |
|---|---|---|
| 何か | 自分の作業ログ | 相手に渡す完結した文書（目的 / 入力 / 合格基準 / 出力先） |
| op | **append で育てる**。write は着手時の 1 回だけ（見出しだけにして古い内容を消す） | **write で書く**。直すのも書いた本人が `overwrite: true` で |
| 書き手 | 本人だけ | 依頼を出す側だけ。受け手は書かない |
| 読み方 | 着手時に 1 回 `fd` → 関係する付箋だけ | **依頼文に書かれたパスを read**（探さない） |

- **フォルダ名を `handoff/` にしなかった** — この村で handoff は転送（`transfer_to_*` /
  `handoff:` 行）の既存の語。同じ語を別物に当てると grep が嘘をつく
- **黒板タブに指示書は出ない**（`list_blackboard` は `blackboard/` 直下の平置きだけ）。
  付箋は板に貼るもの、指示書は机の上の文書、という分け方そのもの
- **残る穴は `write` の lost-update 検出**（読んでから変わっていても黙って後勝ち）。
  `run.json` は read → 差分適用 → `write_atomic` で解いている前例があるが、
  **2 人が同じファイルを write する事故は未観測**なので頻度を見てから

## 村の黒板（共有作業メモ）— 条例で運用開始（2026-08-02）、Spec 未起票

利用者の要望は「BBS ではなく、みんなが共有している記憶 = 作業メモ」。
正体は blackboard（共有の作業記憶）で、長期記憶（Memory.md / Memoria）とは
寿命が違う — 黒板は作業の寿命、Memory.md は個体の寿命。混ぜない。

### 黒板は work_dir の下にある（2026-08-11 に実機で 2 つ出た）

**work_dir が違う個体は、互いの黒板を 1 文字も読めない。** `resolve_in_work_dir`
（`tools/fs.rs`）が canonicalize 後の前方一致で弾く。**これは設計どおり** —
作業フォルダが違うのは違う作業をしているということで、`fd` / `grep` / `sd` と
同じ規律が黒板にも掛かっているだけ（利用者裁定 2026-08-11「それはそれで正しい」）。
例外は `rag` だが、宣言したフォルダしか読まないので黒板には届かない。

**ただし work_dir を移すと、その個体の付箋は届かない場所に残る。**
実機（2026-08-11）— 10 体中 9 体が `D:\Outcasts.jp`、ジェミーだけ
`D:\ManualeRAG`。`D:\Outcasts.jp\blackboard\ジェミー.md`（2026-08-09）が
取り残され、**本人は消すことも更新することもできないのに、他の 9 体の
「着手前に必ず読む」対象に入り続けていた**。検証役のミュゼが
「黒板は更新されず stale」と検出したのが発覚の経路。

- **古いだけなら本人が直せるが、これは直せない。** 出口は人が消すことだけ
  （2026-08-11 に利用者が全消し）。**Spec 29（作業フォルダの一括切り替え）の
  副作用**で、あの Spec のどの節にも書かれていない
- **移動した個体は「自分が村の黒板に何を書いたか」を確認できなくなる** —
  履歴は滑る窓 8 往復で落ちるので、**書いたことを忘れたまま書いたものだけが残る**
- **一般化: フォルダを基準にしたスコープは、フォルダを移しても中身が付いてこない。**
  移動が変えるのは「これから触る場所」だけで、「これまで書いたもの」は動かない

**条例に非対称があった**（同日に利用者判断でルール化）— 「仕事に着手する前に
黒板を読む」は**必ず**なのに「作業が終わったら消してよい」は**任意**で、
溜まる一方だった。処方は着手側の義務へ寄せること（「新しい仕事に着手したら、
自分の付箋の古い内容は消してから書く」）と、読む側に判定を明示すること
（「関係ないメモは束ねに入れない」）。**文言は保証ではない**（#84）ので、
効いたかは観測でしか分からない。

**実測のコスト**: 8 ファイル・約 30KB が「着手前に必ず読む」対象になっていた。
ミュゼは 1 ターンで 3 つ読んで**約 12,400 字**を払っている
（`file ok=true body_chars=2450 / 1363 / 8597`）。

**機構は作らず運用で始める**（「機構を足す前に既存の機構で書けるか確かめる」）:

- `file` ツール（Spec 09）+ 共通 work_dir で既に書ける。**条例へ 1 節書いた**
  （2026-08-02、`{workspace}/Ordinance.md` の「村の黒板」節）。採ったのは
  下の**付箋形** — 黒板は `blackboard/` フォルダ、着手前に fd + file read、
  書くのは自分の `blackboard/<表示名>.md` だけ、進行役だけが `blackboard/まとめ.md` を
  束ねる、仕事の答えは黒板でなく返信で返す。初案の「単一ファイル +
  書くのは進行役だけ」は付箋形で置き換えた（file write は全文上書きなので
  並行書き込みは後勝ちで消えるが、1 人 1 ファイルなら衝突が構造的に起きない）
- **push 注入はしない**（pull = 読みたいときに file read）。毎ターンの可変注入は
  論文 1 の品質仮説の疑いの筋で、広場ログに続く 2 本目を増やさない。
  実測で「読み忘れ」が出たら、広場ログと同型のコスト設計
  （オプトアウト・上限・System に積まず可変尾部 = #45 の規律）で機構化を検討
- 膨張したら要約が要る（Memoria の昇華と同型）。急がない
- **GUI に黒板タブを追加**（2026-08-02 利用者要望）— 中央下段をタブ化し
  「黒板 / 作業状況」を切り替え（波ペインの表示名を「作業状況」へ改名）。
  `blackboard.rs` + IPC `list_blackboard` + `BlackboardPane.vue` /
  `BottomPaneTabs.vue`。**読み取り専用**（書き込み IPC 不在 = 条例の
  「1 人 1 ファイル」を GUI が迂回しない）、pull のみ（表示中 10 秒の再読 +
  手動更新。push 注入なしの原則は GUI 側も同じ）。契約は
  data_contract の blackboard_contract
- **実機確認済み**（2026-08-02）— ザリが条例だけを頼りに
  `blackboard/ザリ・ロブステル.md` を書いた（依頼待ちの状態・タスク分解方針・
  実行上限のメモ。機構の追加指示なしで付箋形が成立）。GUI の黒板タブ表示も
  利用者が確認。work_dir がこのリポジトリを指す村では黒板がリポジトリ直下に
  生まれるため、`blackboard/` を .gitignore へ追加した（黒板は作業の寿命の共有記憶で
  あってリポジトリの内容物ではない。コミットすると公開リポジトリへ漏れる）

**利用者の運用図（8 段: 相談→方針→調査依頼→グラウンディング/MCP→
DAG 分業→実行→フィードバック→完了報告）**との対応: 1〜6 は既存部品
（進行役 / ask・plan / Spec 05・02 / 波）。7 のフィードバック = VMAO の
Verify 段は**条例へ書いた**（2026-08-02、「束ねの検証」節。コード変更ゼロ）:
配るとき合格基準を 1 行添える / 束ねに参加していないサーヴァントへ ask で
検証（自己採点しない）/ 見るのは 空・取り違え・矛盾・欠落 の 4 点 /
聞き直しは該当の相手へだけ・1 巡まで / 揃わなければ揃わない事実を添えて報告。
自動の再計画は引き続き入れない。**フォールバック経路は観測済み・本命経路は未観測。**

- **初回走行（2026-08-02 17:47）では何も観測できず** — ザリが 6 体全員へ撒いた
  ため「束ねに参加していないサーヴァント」が存在せず、条例のフォールバック
  （自分で 4 点確認）へ落ちた。**自己検査はログに映らない**
- **2 回目（2026-08-04 13:17）でフォールバックが「読める形」で出た** —
  同じく 6 体全員へ撒いたが、ザリが**報告の中で明示的に申告**した:
  「条例では束ねを報告する前に参加していない者に検証を頼むことになっている。
  今回は 6 名全員に配ってしまったため手空きの検証役が居なかった。よって
  4 点点検（空の答え / 取り違え / 矛盾 / 論点の欠落）を自分で実施し、その旨を
  集計と一緒に `blackboard/ザリ・ロブステル.md` に明記した。次に同種のアンケートを
  取るときは 1 名を検証役に空けておく」。**条例が読まれ、フォールバックが選ばれ、
  その選択が申告され、黒板へ記録され、次への反省まで出た** — 機構ゼロで
  ここまで動いている
- **残るのは本命経路（独立検証役への ask）の観測だけ。** 依頼文で 1 体を波から
  外すか、検証専任を 1 体足す（線は人が引く）。**ザリ自身が「次は 1 名空ける」と
  書いているので、次の同種の依頼で自然に出る可能性がある**

図は 2 枚（2026-08-02）。2 枚目（AI Crossing）は黒板が構図の中心で、
追加の読みが 3 つ:

- **並行書き込みは付箋の形で解ける** — 「1 人 1 ファイル（`blackboard/<名>.md`）を
  自分だけが書く。読みは全員がフォルダごと（fd + file read）。進行役が
  `blackboard/まとめ.md` へ束ねる」。上書きの衝突が構造的に起きず、機構ゼロのまま
- **承認ループ（Goal→Plan→Execute→Report→Approve→Repeat）は今の会話の
  形そのもの**。承認ゲートの機構は不要 — 要るのは報告の書式（条例）
- **村長 = Fuseforks は人格設計であって機構ではない**。進行役はトポロジーの
  帰結（plan は接続 2 体以上で生える）のまま。「進行役フラグ」は作らない —
  線は人が引く、の原則を維持

**この 3 つとは別に出ていたより大きい梃子（failures.md #42）は完了**（2026-07-31）。
`cache_control` が **system ブロックにしか打たれておらず**、ツールループが毎周
`messages` 全体を素の値段で送り直していた（1 ターン 2,052,314 トークンの入力のうち
1,826,109 が素の値段）。`anthropic.rs` の `place_message_breakpoint` で
**最後の発話の最後のブロック**へ境界を追加した（種別は問わない — `tool_result` に
打てないとツールループの周回だけ境界が抜ける）。単体 4 本追加、workspace 全緑。
見積もりは**同じ仕事が約 1/3 の値段**。2・3 が「燃焼を止める」側なのに対し、
これは**燃焼を止めずに安くする**側なので、仕事を減らさない。
**実機確認まで完了**（2026-08-01）— claude-sonnet-5 のザリで 8 ツール呼び出し
（`file` / `fd` / `grep`×5 / `file`）158.4K トークンの turn が
**入力の 87% をキャッシュ**。修正前の実測は 11% と 16%。安定プレフィックスは
6,463 トークン固定なので、5〜6 周で 87% には構造的に届かない = 履歴が載っている
（#42 の一般化 1 を逆向きに使った判定）。
**ただし 87% は「RAG 無し・村に他に誰も居ない」最良ケースの数字だった**
（failures.md #45）。可変な文脈を `Role::System` で積んでおり、adapter が位置に
関係なく先頭へ畳むため、他人が喋ると前方一致が切れていた。**2026-08-01 に 2 段で
修正** — (1) 可変文脈を最終 user 発話へ畳む (2) **送った文字列をそのまま履歴へ積む**
（(1) だけでは送信と保存が食い違い、cached が tools+system の 6,465 で頭打ちになった）。
修正後の実測は**ターンをまたいで 99%**（`cached(N)` が `prompt(N-1)` と 2 トークン差で
一致 = 前のターンのプロンプト全体が載っている）、ツールループ内で 84〜99%。

実装時に Anthropic の仕様へ接地して未決 2 点を潰した。**`tool_choice` の変更は
履歴層のキャッシュを落とす**（tools 層と system 層は保つ）ので、まとめ呼び出しの
1 周だけは今も書き込みになる。**境界の後方探索は 20 ブロックまで**で、1 周の追記が
超えるとその周だけ書き直しになる（実機の 2〜3 本並列は 1 周 7 ブロック前後）。
どちらも仕様上の限界なので打ち消す仕掛けは置かない（data_contract の
`prompt_cache.bounds`）。

## システム設定ダイアログ（Spec 13・**rev3 承認 → P0〜P5 完了。Done**）

**P5 の台帳側完了**（2026-08-03）— README 日英の回収。**射程が見積もりより広かった**:
起票時の「⚙ 8 箇所」に対し、実機のアイコンと突き合わせたら ⏰ / 📜 / 📡 / 📁 も同じ原因
（絵文字 → SVG の置換に記述が追従していない）で腐っていた。**▶ / ■ は正**
（`batchStart.ts` は今も文字グリフ）、「何ができるか」表の絵文字は README の装飾なので据え置き。
**置換先を絵文字にせず、ボタンのラベルと置き場所で指す形へ統一**し、その規律自体も
README 本文へ 1 段落書いた（次にアイコンを変えても台帳が腐らない）。
同時に**⚙ とは別原因の追従漏れ 3 件**を回収 — README に Spec 13 の節が 1 つも無かった
（`grep "システム設定" README.md` が 0 件）/ `tokenBudget` の説明が「world.json の 1 つだけ」の
まま（P1 で画面から触れる事実が抜けていた）/ ディレクトリ木と workspace 木に直近 2 Spec の
産物（`SettingsDialog.vue` / `SessionDialog.vue` / `ConfirmHost.vue` / `locales/` /
`useUiSettings.ts` / `sessions.redb` / `exports/`）が無い。**一般化は
`failures.md` #51 へ**（記号 1 文字の追従漏れは識別子と違ってコンパイラにも lint にも
引っかからない）。

**実機の指摘で分類を改稿**（2026-08-03、P4 の後）— 左メニューを
**保存先ではなく主題で切り直した**: 全般（言語）/ コスト管理（トークン制限）/
ユーザーインターフェース（メッセージ表示/非表示）。初版の「村の設定 /
この画面の設定」は保存先での分類で、**実装の都合がそのまま目録に出ていた**のが
「砕けすぎて分かりづらい」の真因。保存先は分類から降ろし、各ページ末尾の注記へ
（村に保存＝配ると付いて回る / 端末に保存）。「メッセージ表示/非表示」は節を
足せば増える器にしたが、**機構は今も線の削除 1 つだけ**。用語境界を 1 つ追加
（上の「同型の二例目」）。

**P4 完了**（2026-08-03）— コアのコード変更ゼロ。UI 側 `errors.{code}` 辞書 31 本 +
`formatError`（ja = message そのまま / 他言語 = 訳語 + 原文併記 / 未知 code =
原文へ）。表示 10 箇所を集約。**System 行は訳さないと決定**（会話ログの一部で
あり UI 文言ではない。表示時に訳すには kind+params の構造化 = ワイヤ +
session_store の加算が要り、やるなら別 Spec。利用者の異論があれば差し戻し可）。

**P3b 完了**（2026-08-03）— 残り全コンポーネント + useOrchestrator + index.html を
辞書化（並列 4 分担、辞書はフラグメント統合で競合ゼロ）。機構の変更 3 つ:
`STATUS_LABELS`/`WEEKDAY_LABELS` → `*_LABEL_KEYS`（辞書キーの表）、`batchLabel` は
`titleKey`+`titleParams` を返す（純関数は i18n を知らない）、`guard`/`mutate` の
`${label}に失敗しました` 連結を `opFailed` + `op.*` の 2 段へ（語順が言語を
またげない）。意図的な残り: 言語名ラベル・実ファイル名・console ログ・コア産
文字列（P4）。既知の残: toLocaleString("ja-JP") の日時整形は言語に追従しない。
検収は機械 2 段（テンプレート走査 + リテラル走査）で完了。

**P3a 完了**（2026-08-03）— 村民 5 体 + ジェミーの i18n 調査と突き合わせ、
「判断次第」3 点は凍結済み契約で決着（A-4 訳さない / rust-i18n 不採用 = 案 A /
タイトルはブランド名）。コア `world::Language`（生 String で受けて未知は
未確定へ・sys-locale で初回確定・**再起動で再判定しない**）+ IPC 2 本 +
vue-i18n 11 基盤（辞書 `locales/{ja,en}.json`・**鍵集合一致テスト**）+
言語設定ページ（保存 → 成功 → setLocale の順）+ 代表 3 ファイル差し替え
（TitleBar / App / SettingsDialog）。結合 2 本 = 確定保存・再判定なし /
**言語切り替え前後で system ブロック不変**（PromptProbeBackend）。
**P3b 残**: 残り 18 コンポーネント + useOrchestrator トースト + index.html
`<title>`（一覧は Spec の P3 設計記録）。将来言語 zh-Hans / de / fr は
辞書追加 + 契約の選択肢改訂で足せる形にしてある。

**P2 完了**（2026-08-03）— `useUiSettings.ts`（`fuseforks.settings.v1`、書き込みは
watch = 値が変わったときだけなので「触らず閉じたら不変」が構造で成立。boolean
以外は既定へ落とす）+ `TopologyMap.removeEdge` の先頭に `askConfirm`（既定 ON・
id 併記・双方向は「両方向とも切れます」）+ SettingsDialog「この画面の設定 >
線削除の確認」（即保存を明記）。vitest 4 本追加（63→67）。

**P1 完了**（2026-08-03）— コア `token_budget` / `set_token_budget`（persist 経路は
`upsert_template` と同じ、`Some(0)` は新設 `INVALID_TOKEN_BUDGET` で拒否）、
IPC 2 本、`SettingsDialog.vue`（2 ペイン + 左メニュー。**未実装ページは目録に
並べない**）、TitleBar に COG + 「システム設定」。結合 3 本（保存 → 依頼 →
新天井で打ち切り = 再起動なしで効く / なしへ戻すとキーごと消える / 0 拒否は
メモリもファイルも触らない）。⚙ 文言の回収は予定 3 箇所 + 実行時 4 箇所
（error.rs / client.rs ×2 / useOrchestrator.ts）— **COG を入れた時点で
「⚙ の画面」は間違った行き先を指すようになる**ため全て
「「モデルテンプレートを管理」の画面」へ統一。README 日英の ⚙ 8 箇所は P5 で回収。

**P0 完了**（2026-08-03）— `data_contract.yaml` の `settings_contract`
（ConfigFileKind 配下、`session_store` / `blackboard_contract` と同じ棚）。
凍結 5 点: 置き場の 2 分割・OrchestratorConfig を出さない線・言語の 3 層・
アイコン規約（SVG）・即時反映の定義。P0 で具体名を 2 つ確定 —
言語は `world.json` の `language`（"ja" | "en"）、画面設定は `localStorage` の
`fuseforks.settings.v1`（`confirmEdgeDelete`、既定 true。命名は
`fuseforks.layout.v1` に揃えた）。**data_contract.yaml は既存の `- **` 箇条書き
66 箇所が YAML alias と誤読される既知の件（別セッション task_62bfa37a）で
strict パーサでは元から通らない — settings_contract はリテラルブロックなので
無関係**（前半だけの切り出しパースで検証済み）。次は P1: 天井の読み書き
IPC 2 本 + `SettingsDialog.vue` の骨格（2 ペイン + 左メニュー + COG 入口）。

起点は利用者の言葉（2026-08-03）— 「**僕もなにが設定できるのか分かりやすい**」。
主目的は**既にある設定の可視化**。トークンの天井（Spec 11）は `world.json` の
`tokenBudget` として既に存在するのに、**手編集以外に触る方法が無く、天井の
無い村は起動のたびに WARN が出るのに直す場所が画面に無い**。

**新規の設定は 2 つ足す（言語・線削除の確認）**。rev1 は「S4 は設定の追加では
ない」と書いていたが虚偽で、査読で潰した。**`OrchestratorConfig` は出さない**
（履歴の往復数・hop 上限・広場ログの窓 ほか 12 個）— ここが「増やさない」の本体
（**→ 2026-08-24 Spec 44 で `ask_timeout` を World へ移して設定化。12 → 11 個。
`OrchestratorConfig` 自体は出ないままなので凍結の形式は不変**）。

- **入口はタイトルバーの「スケジュール」の右**、COG + 「システム設定」。画面は
  **2 ペイン**（左メニュー・右ページ、Kataribe と同型）。**左メニューが設定できる
  ものの目録そのもの**になるのが Goal への効き筋（利用者指示 2026-08-03）
- **アイコンは SVG。絵文字を恒久要素に使わない**（rev3 で機構へ）— 絵文字は
  フォント依存で字形と大きさが環境ごとに変わり、`currentColor` を継承しないので
  テーマの配色に追従しない。**システム設定は COG を使ってよい**（カードの設定は
  利用者が鉛筆の SVG へ変更済みで、歯車は空いた）
- **天井の表示は実効トークンの整数**（rev3 の D1）。素の値は参考でも出さない —
  打ち切り判定に使われるのは実効値で、素の値を並べると「まだ余裕があるのに
  切られた」という誤った読みを作る。入力の内訳が画面に無いので逆算もできない
- **線削除の確認は既定 ON**（rev3 の D6）。他 6 種の破壊的操作はすべて確認あり、
  線だけ無いのは整合性の破れ。OFF は 1 回切れば済むが、ON を知らずに失うと戻せない
- **言語は `world.json` に置く**（rev2 で `localStorage` から変更）。System 行は
  **会話ログに保存される**ので、言語は村の共有物に触る。ペイン幅と同じ棚に置けない
- **多言語化は 3 層** — (1) UI 文言（引用符付き 324 種 + 地の文 81 箇所）
  (2) コアの文言（`#[error]` 31 本 + System 行）(3) システムプロンプト・ツール説明。
  **(3) は訳さない**（訳すのは翻訳ではなくプロンプトの変更で、Spec 08 の
  「束ねの文言は 1 字も変えない」と衝突する）。コアは言語を知らない（案 A 固定）
- **地図の線の削除だけが、確認なしで消える唯一の破壊的操作**（実測で棚卸し済み。
  地図の破壊的ハンドラは `@edge-click` の 1 つだけ）。機構は Spec 12 P3 の
  `askConfirm` が既にある
- **査読の前提誤り 2 件をコードで反証**: (a)「天井の即時反映にホットリロード IPC が
  要る」— `world.json` は投影で、天井は**依頼ごと**にメモリの `World` から読まれる
  （`new_root_budget`）。IPC 1 本で次の依頼から効く (b)「×0.1 があるから `u64` に
  入らない」— 重みは**内部の milli 表現**に閉じており、天井は実効トークンの整数。
  `Some(0)` の正規化もコアに既にある

## フッターのステータスバー（2026-08-03 着地。Spec 不要と判断）

利用者要望 —「**管理ツールなのでスクショするときに時刻があったほうがいい**」。
`StatusBar.vue`（22px の帯・最下段）+ `lib/clock.ts`（純関数・単体 5 本）。
`App.vue` の**グリッドの外**（flex 列の末尾）に置く = 3 ペインの分割規則に
巻き込まない。契約面はゼロ（新しい型・enum・ワイヤ・IPC が 1 つも無い）ので
data_contract は触っていない。**Spec を切らなかったのはそのため** — 決める
ことが無い変更に査読の窓は要らない。

**時計は言語設定に追従させない。これは追従漏れではない。**
形式を `YYYY-MM-DD HH:MM:SS` に固定し ja / en で同一にしてある。理由は用途 —
`diag.rs:132` の `%Y-%m-%d %H:%M:%S%.3f` と揃えることで、**撮った画面の時刻から
`fuseforks.log` の該当行を目で引ける**。en のロケール整形
（`8/3/2026, 10:15:03 PM`）にすると、月日の順が読み手の国で変わる・12 時間制の
判別が要る・ログと突き合わせられない、が同時に起きる。

**Spec 13 P3b の「既知の残: `toLocaleString("ja-JP")` の日時整形は言語に追従
しない」とは別件。** あちらは追従させるべきなのに漏れている箇所、こちらは
**意図して固定している**箇所。同じ「言語に追従しない」でも処方が逆なので、
まとめて直そうとしないこと。

現在時刻は**引数で受け取る**（`formatClock(now: Date)`）— `schedule.rs:5` の
「内部で `Local::now()` を呼ばない」と同じ規律。内部で `new Date()` を読むと、
テストが壁時計に依存して特定の時刻でだけ落ちるものになる。

**左側は意図的に空けてある。** 「常に見えている細い帯」なので、常駐させる価値の
ある情報しか置かない。増やすときは**常に見る必要があるか**を毎回問う。

**左端へ MCP サーバーの待ち受け表示を足した**（2026-08-07 利用者要望 —
「**誰もいないのにつけっぱなしにしないように**」）。空けてあった左側を初めて
埋めたもので、規律（常に見る必要があるか）を満たす理由は
**扉が開いていることが他のどの画面にも出ない**こと — 設定を開くまで気づけず、
その間ずっと同じ端末の任意のプロセスが村へ依頼を投げられる。

- **開いているときだけ出す。** OFF のときに「OFF です」と出すと印が常時
  画面にあることになり、**印が付いていること自体の意味が消える**
- **`enabled` ではなく `listening` で出す**（ポートが埋まれば ON でも開かない）。
  見せたいのは「実際に受け付けている」ことで「そう設定してある」ことではない
- **状態は `state.mcpHost` の投影に置いた** — ステータスバーが読むので、
  設定ダイアログの生 `ipc` 呼び出しも `orchestrator` 経由へ寄せた
  （Spec 19 P3 の「使い分けの基準は投影を持つか」）。
  **`refreshAll` には混ぜない** — 状態が変わる点は起動・設定の適用・合鍵の
  作り直しの 3 つだけで、定期的に引き直す理由が無い
- `statusBar.test.ts` を新設（**判定が `listening` を見ているか**と
  **OFF の枝が文言を持たないか**の 2 点だけ。文言・色・位置は留めない）

**版番号を右端へ足した**（2026-08-05 利用者要望）。`Version: 0.0.0` の形。
**時刻だけでは「いつ」しか分からず「どのビルドの」が抜ける** — 不具合の報告を
受けたとき、その画面がどのコードのものか分からないと再現に取り掛かれない。
この 2 つは「常に見る必要があるか」を満たす。

**取り込みはビルド時**（`vite.config.ts` の `define` で `__APP_VERSION__`）。
`git describe --tags --abbrev=0` の直近タグから `v` を落とす。**Kataribe と
同じ方式**（`D:\Github\Kataribe\app\vite.config.ts`）で、タグを手で bump しなくても
追従する。**`tauri.conf.json` の `version` とは独立** — あちらは配布物に乗る版で、
CI がタグから書き換える側。

**タグが無いときは `0.0.0`。** Kataribe は空文字にして非表示にしているが、
こちらは出す — ステータスバーの目的が「撮った画面がどの状態かを示す」ことなので、
**「版が出ていない」と「版が 0.0.0 である」を画面で区別できるほうがよい**
（`0.0.0` は「まだタグを打っていない開発ビルド」の印として読める）。
**`tauri.conf.json` の `0.1.0` へ落とさない** — 打っていないリリースを名乗ることになる。

**「Version:」は訳さない。時計の形式を固定したのと同じ理由** — 版番号は語ではなく
識別子で、読み手の国で表記が変わると報告と突き合わせられなくなる。
`title` / `aria-label` は訳す（そちらは説明であって識別子ではない）。

## 作業フォルダの「参照…」ボタン（2026-08-05 着地。Spec 不要と判断）

利用者要望 — `AgentSettingsDialog` の作業フォルダ欄からネイティブのフォルダ選択
ダイアログを開く。**絶対パスを手で打たせると、区切り文字と綴りの取り違えが
そのまま「作業フォルダが存在しません」になり、直す手掛かりが画面に無い。**

**Spec 12 P3 の「ファイル選択ダイアログのために plugin を足さない」とは事情が違う。**
あちらは**保存先**の話で、`{workspace}/exports/` に固定すれば plugin が要らなく
なった（置き場所を 1 つ決めるほうが依存も操作も少ない）。作業フォルダは
**端末ごとに任意の絶対パス**で、固定できる既定が存在しない。**同じ判断を
覆したのではなく、固定できるかどうかで分かれる別の場合。**

**依存の実測**（`cargo tree` の一意クレートを前後で比較）: 増えたのは
**3 本だけ**（`tauri-plugin-dialog` / `rfd` / `tauri-plugin-fs`）。Tauri が既に
持っていたぶんが大半だった。**`tauri-plugin-fs` は入るが登録していない** —
`lib.rs` の `.plugin()` にも capability の `permissions` にも無いので、
**コマンドは到達不能**（依存として存在するだけ）。

**決めた 3 点**:

- **手打ちを残す。** 打てなくすると、村を配った先や別の端末で開いたときに
  パスを直す手段が消える。ボタンは**足す**のであって置き換えない
- **選ばれたパスは検査せずそのまま欄へ入れる。** 実在の強制は Rust 側の
  `canonicalize` + 前方一致が持っており、フロントで先回りすると
  **同じ規律が 2 箇所に生える**
- **`dialog:allow-open` に scope を書かない。** 返るのは人がネイティブの
  ダイアログで選んだパスだけで、**選択そのものが人の操作**。ここを絞ると
  設定できない村が出る（`opener:allow-open-path` を `$APPDATA/workspace` に
  絞ってあるのとは求められているものが逆）

## 画像の添付（[Spec 23](specs/23_image-attachment.md)・2026-08-06 **Done**）

起点は利用者 —「**おそらく僕はコストの面で使いたくない機能だが、無ければ
『どうして無いの？』と聞かれそうな機能だ。なので機能は入れておきたい**」。
**この Goal の形が全部の判断を決めている** — 目的は「画像を活用する」ではなく
**「目立つ欠落を埋める」と「使わない人に払わせない」の両立**。

- **使わない村の固定費はゼロ** — ツールを 1 本も増やさないので**モデルが読む面は
  1 字も伸びない**。**添付なしのシリアライズ結果はバイト等価**（P2 で凍結）。
  **Rust の型は増える** — この 2 つを同じ語で書くと実装時にどちらを守るか読めなくなる
  （rev3 の査読で切り分けた）
- **D1 は 1 ターン限り（利用者裁定 2026-08-06）**。`Record::Exchange.sent` が
  `String` であること・滑る窓 8 往復で **1 枚が 14,336〜38,272 トークン**に化けること・
  Goal がコストを避けていることの 3 つ。**足りなければ「もう一度添付する」という
  人の操作を挟む** — 自動で残すより因果が明確（利用者の言葉）
- **Spec 05 の「Gemini はネイティブ経路が要る」は当てはまらない** — あれは
  `google_search` という**ツール**の制約で、画像は content の話。
  **同じ「プロバイダ差」でも、道具か中身かで結論が変わる**（D8: 画像は互換経路のみ）
- **Spec 19 の画像機構を延長する。ただし `validate_icon` はパラメータ化しない** —
  あの doc は「2 種類のアイコンが同じ述語を通る。2 箇所に書くと片方の上限が
  古いまま通し続ける」と書いており、**`ICON_MAX_BYTES` が 1 つであること自体が
  不変条件**。共有するのは `is_webp` のマジックバイト判定だけ。
  **一般化: 「一般化して共有」と書くときは、その関数が doc で何を不変に保っているかを
  読む。共有の単位は関数ではなく述語**
- **P0 完了**（2026-08-06）— **実測 5 系統 × WebP / JPEG = 10 通り全部通過し、
  D3 の既定は WebP で確定**（Anthropic / OpenAI 公式 / Gemini 互換 / xAI /
  さくら互換 Qwen3-VL。ローカル互換のみ未実測）。**xAI は公式文書が jpg/png のみと
  明記しているのに WebP が通った** — 文書は将来の実装の予告でもあるので、
  JPEG フォールバックは保険として維持。`data_contract.yaml` へ
  `attachment_contract`（凍結 9 本）+ `prompt_cache` へ「画像は安定プレフィックスに
  入らない」を追記
- **P1 完了**（2026-08-06）— `attachment.rs` 新設（`validate_attachment` は検証と
  同時に寸法を返す / GC は `gc_plan` 純関数 + I/O の 2 層で**現在時刻は引数** /
  アニメーション判定は **VP8X フラグと ANIM チャンクの両方** — フラグだけだと
  細工ファイルが素通り / VP8X キャンバス寸法がビットストリームに勝つ）。
  `is_webp` を切り出して `validate_icon` と共有（上限は共有しない —
  凍結 4 の述語境界）。`CoreError::InvalidAttachment` は `InvalidIcon` と別変種。
  単体 16 本（fuseforks-core 401 → 417）
- **P2 完了**（2026-08-06）— canonical へ `ImageMediaType` / `ImageAttachment`
  （**data は base64 済み文字列** — adapter は純関数なので実体読みは P3 の責務）+
  `OaiContent` enum 化（**decode は無傷** — 応答側は `OaiResponseMessage` の別型）+
  `AnthropicRequestBlock::Image`（画像はテキストより前 / breakpoint に Image の腕 /
  `message_tokens` は Image を 0 = 少なめ側）+ **#29 の空発話フィルタに添付を追加**
  （画像だけの発話は空ではない）+ gemini は素通りをテストで凍結（D8）。
  **バイト等価は golden 文字列リテラルで固定**。単体 6 本（417 → 423）
- **P3 完了**（2026-08-06）— **保存は発話の記録より前**（検証に落ちたら発話ごと
  拒否。「画像なしで送信」は意図と黙って食い違う）/ **D1 は構造で成立**
  （展開が読むのは受信の参照だけ。履歴は `String` なので画像を持てない。
  GC 済みは本文で断る）/ **D6 は `note_dropped_attachment` 1 実装を
  handoff・ask・plan の 3 経路が共有** / **D3 の JPEG 再試行は
  `LlmBackend::chat` の最外周**（発火は OpenAiCompat × 400 × WebP 添付の
  3 条件 — 無関係な 400 で再課金しない）/ 起動時 GC + 計器 3 行
  （`attachment gc:` / `attachment missing:` / `attachment fallback:`）。
  **依存 +9 crate・全部純 Rust**（image / image-webp / zune-jpeg ほか。
  base64 は既にツリーに居た）。結合 3 + 単体 2（lib 425・結合 122）
- **P4 完了**（2026-08-06）— `ChatInput.vue`（貼り付け + 参照… + チップ + S4 注記。
  **2 枚目は拒否ではなく置き換え**）+ `workers/imageConvert.ts`（**convertToBlob の
  type は希望であって保証ではない** — `isWebpBytes` で確かめて正直に失敗）+
  **`read_attachment` IPC を追加**（P3 の読み口の漏れ。書く経路だけ作って
  読む経路が無かった）+ GC 済みはプレースホルダ（消えたことを消えたと言う）+
  辞書 ja/en（`INVALID_ATTACHMENT` 含む）。vitest 164 全緑（+7）・build 緑
- **P5 完了 + 実機で検収 1 を観測**（2026-08-06）— README 日英へ節と表と 2 つの木、
  `data_contract` へ実測と実機観測。**実機は `gpt-5.6-terra`（OpenAI 互換）で
  画像の説明が返り、JPEG フォールバックは発火しなかった** = WebP が互換層を
  そのまま通った裏取り（実額 30,255 tokens）。
  **英語 README のディレクトリ木に `tools/rag.rs` が抜けていたのを同時回収した** —
  **一般化: 日英 2 ファイルを「README 日英」という 1 行の Task 名で書くと、
  片方だけ直した状態が完了として記録される。数えるのはファイル単位**（#51 (b) の日英版）。
  **残: 実機確認 4 件**（2 ターン目に渡らない / 転送の断り書き / GC /
  **JPEG フォールバックの経路** — ここは結合テストが無いので実機だけが検証）
- **実機 2 ターン目のログで、検収項目の側の誤りが 1 つ出た**（2026-08-06。
  `failures.md` #80）。検収 2「2 ターン目の `prompt` が 1 ターン目より小さい」は
  **符号が逆**（履歴が毎ターン増えるので増えるのが正常。実測 9,146 → 9,741 → 10,234）
  かつ**差が読めない大きさ**（499×499 の画像 ≈ 255 視覚トークンに対し、
  1 ターンぶんの変動は 493 トークン）。**測りたい量がノイズより小さい。**
  書き直した検収 2' は**長辺 1568px 近くの大きな画像**で比べる。
  **一般化: 検収項目に数字を使うなら、期待する変化の向きと、その差が他の変動より
  大きいかを書く前に数える。** #68（存在しない量を見に行った）との違いは
  **量は存在するが小さすぎる**こと — 確かめ方が違うので両方残す。
  **転送は起きたが断り書きは出ていない。これも正しい** — その発話に添付が無く、
  D6 は「そのターンの受信に添付があるとき」だけ発火する。踏むには
  **1 通の中で「画像 + 転送の依頼」**を送る
- **画像のコストを実測した**（2026-08-06。1568×843 を claude-opus-5 へ）。
  `system_digest` 同一・`history_msgs` 両方 0・`cached` 両方 0 の周どうしで
  **`prompt` 20,881 → 23,437（+2,556、本文は 55 文字短い）**。28×28 パッチ式の
  予測 `56 × 31 = 1,736` が差の 2/3 を説明する。**残り約 800 は広場ログ** —
  `system_digest` は `Role::System` しか畳んでおらず、**広場ログ・入退室通知は
  #45 の規律で user 発話へ畳まれているので digest の外**（`failures.md` #81）。
  **一般化: 計器が守っている範囲は、その計器の名前より狭い。** 2 つの実行を
  「同じ条件」と言う前に、**固定されていない項を挙げる**。
  処方は計器の追加ではなく**読み方の変更** — 検収 2' は `prompt` の大小ではなく
  **`cached` が前ターンの `prompt` 付近にならないこと**で読む（キャッシュは
  バイト一致でしか効かないので、履歴の 1 通目がテキストだけなら
  最初のコンテンツブロックで前方一致が切れる）
- **検収 4（GC）合格**（2026-08-06 22:08）— 前半（期限内を消さない）は
  `removed=0` が 5 回。後半は **mtime を細工して踏んだ** — **実データに触れず、
  使い捨てを 2 枚置いて境界の両側を同時に見た**（31 日前 = 消えるべき /
  29 日前 = 残るべき）。**観測の前に予測を書き、`removed=1 remaining=5
  bytes=87380` が 1 文字も違わなかった**。**効いているのは「29 日前が残ったこと」** —
  単に古いものを消しているのではなく 30 日の境界で切っていることが同じ 1 行から
  読める。**数字を見てから解釈すると、どんな値でも辻褄が合う**ので予測を先に書く
  （#80 の逆側の規律）
- **検収 5（JPEG フォールバック）をテストで覆った**（2026-08-06。
  `tests/attachment_fallback.rs`）。**P3 の「HTTP 経路ごと踏む結合テストは
  書いていない（スタブが要る）」という判断が弱かったのを撤回した** —
  ループバックの最小 HTTP スタブはこのリポジトリの他のテスト（`run` はプロセスを
  起動し、`mcp_live` は MCP サーバーを立てる）と同じ水準で、書ける。
  **実機で踏めない経路をテスト無しで残すのは、エラー回復の経路が必要になった
  瞬間に壊れているという一番痛い形**（P0 で 5 系統すべてが WebP を受けたので、
  拒む接続先が実在しない。だからこそ機械の側で作る）。
  4 分岐 = JPEG で成功 / 両方拒否の文面 / 添付なしは再送しない /
  Anthropic では発火しない。依存は **dev-dependencies に tokio の `net` /
  `io-util` を足すだけ**で配布物は増えない。
  **一発で緑になったのでミューテーションを 2 回入れて赤を確かめた** —
  (a) フォールバック無効化 → 上 2 本 (b) provider 検査を外す → Anthropic の 1 本。
  **4 本が別々の仕事をしている**ことがこれで分かる。
  **一般化: エラー回復のテストが一発で通ったときは、通った理由が正しいか
  機構を壊して確かめる**（#80 の「合格条件が判定になっているか」の実行時版）
- **検収 3 と、4 の前半を観測**（2026-08-06）— 1 通で「画像 + ジェミーに送って」を
  送ると `turn start: agent=agent_3 hop=1 from=agent_8 chars=93` で、
  **93 = 断り書き 16 字 + 改行 2 + 本文 75 字**と計算に 1 文字も違わない
  （別ターンにした失敗例は `chars=28` で断り書き無し。**対で読める**）。
  GC は `attachment gc: removed=0 remaining=3 bytes=79072`。
  **設計の意図を超えたことが起きた** — 断り書きは**診断**のために置いたのに、
  **送る側がそれを読んで画像をテキストへ書き起こして渡した**
  （「画像内の文字は『MEMORIA MAP』です。黒〜濃紺の背景に…」）。受け取った側は
  その記述で構造化した記録を返している。**一般化: 歯止めの理由を伝えると、
  モデルは代替経路を自分で見つける。** #44 は「後で人が診断できるように」の規律
  だったが、**その場でモデルが回復するためにも効く** — 禁止だけを伝えると止まり、
  理由まで伝えると回り道を探す。**落ちたのは画素だけで、情報は落ちていない**
- **実機で設計の穴が 1 つ出て、その場で塞いだ**（2026-08-06 利用者裁定。
  `failures.md` #82）。画像を貼った**次の**ターンで「これをジェミーに見せて」と
  頼むと、転送先に**理由の書かれていない本文だけ**が届いた（断られたのではなく、
  画像を受け取っていない）。真因は 2 段で、**深いほうが本体** — (1) D6 の門は
  「**そのターンの受信に添付があるか**」で張られており、添付と転送依頼が別ターンだと
  鳴らない。**「貼る → 話す → 見せて」が最も自然な流れ**なので現実の使い方は
  ほぼ門の外 (2) **D1 が画像だけでなく「画像があったという事実」ごと消していた** —
  送る側は自分が書いた説明文は覚えているのに、画像という物があったことを知らず、
  「もう手元に無い」と言う材料が無かった。
  処方は可変文脈へ 1 行（**残すのは印であって画像ではないので D1 は不変**。
  #45 の規律でそのまま履歴へ載る。約 40 トークン = 画像本体の 2%）。
  **一般化: 履歴から何かを外したら、外したという事実まで一緒に外していないかを数える。**
  物を消すことと、物があったという記録を消すことは別の判断で、
  **後者は「答えられない」ではなく「答えられないことに気づけない」として現れる**
  （前者は画面に出るが、後者はもっともらしい答えになる）。
  **D6 とこの 1 行は 2 つで 1 組**として契約の両方に書いた。
  **同日中に実機で効きを確認**（検収 6・21:47）— 「さっきの画像をジェミーに送って」に
  「このターンでは画像データを保持していないため送れません。ジェミー宛てに
  貼り直してください」と回答し、**`turn start: agent=agent_3` が 1 本も出ていない**
  （転送を試みていない）。**修正前の同じ頼み方は `chars=28` の黙った転送**だったので、
  ログの 2 行が正確に対になる
- **検収 2' は観測して合格**（2026-08-06）— `prompt=23584 cached=18897
  history_msgs=2/16 system_digest=8f3fbdec58cd1cbb`。**`cached` 18,897 は
  1 ターン目の `prompt` 23,437 と 4,540 離れている** = 前方一致が system の直後で
  切れた = 履歴の 1 通目に画像が無い。**副産物で tools + system = 18,897 が実測でき、
  #81 の「説明できない約 800」が閉じた**（畳んだ発話は 19:05 が 1,984 / 21:02 が
  2,804 で、差 820 は広場ログの成長分）。
  **同時に Spec の記述が 1 つ実測で外れた** — Notes 4 の「2 ターン目以降は率が
  戻る」は誤りで、**下がるのは 2 ターン・戻るのは 3 ターン目から**（2 ターン目
  80.1% 対 画像なしの同型 95%）。**機序は D1 そのもの**で、これは
  **#45 の規律に対する意図的な例外**の代償（`failures.md` #45 へ追記した 2 例目・
  一般化 8）。**代償と証拠が同じ数字**になっている — `cached` が頭打ちになること
  自体が、画像を履歴へ残していない証拠。**機構は足さない**（直すには画像を
  履歴に残すしかなく、それは D1 の否定）

## 右クリックメニューの抑止（2026-08-06 着地。Spec 不要と判断）

利用者要望 —「Kataribe など他の vue+tauri+rust のアプリと同じように、配布ビルドでは
右クリックのポップアップを出さない。ただし dev は開発者ツールが要るので残す」。
`lib/contextMenu.ts` の `keepsNativeMenu`（純関数・単体 6 本）+ `main.ts` で
`if (!import.meta.env.DEV)` の下に 1 本だけリスナーを張る。契約面はゼロ
（新しい型・enum・ワイヤ・IPC が 1 つも無い）ので `data_contract` は触っていない。

**残す場所は「入力欄の上」と「テキストを選択しているとき」**（コピー・貼り付けの
導線がそこにしか無い）。**選択子に `[contenteditable]` が要るのは CodeMirror のため** —
`CodeEditor.vue`（役職ダイアログ / システム設定）の編集領域は `<textarea>` ではなく
`contenteditable` を持つ `div.cm-content` で、ここを外すと**役職の本文を貼り付け
られなくなる**。型検査にもテストにも掛からない種類の退行なので、選択子そのものを
テストで留めた。

**F5 / Ctrl+R は塞がない**（2026-08-06 利用者了承 —「今のところ F5 は使えても
問題はないようだ」）。**Kataribe との明確な差で、これは追従漏れではない。**
**「今のところ」なので凍結ではない** — 実機で再読み込みによる実害
（入力途中の依頼文が消えて困った等）が出たら、そのとき塞ぐ判断へ動かしてよい。

**2026-08-21 にその条件が発火し、1 箇所だけ例外を作った** — **エディタに
フォーカスがある間の `Ctrl+R` だけ飲む**（下の「エディタの鍵」の節）。予測は
「入力途中の依頼文が消える」だったが、実物は**条例と役職の本文**だった。
**窓ごと塞いだのではない**ので、この節の残りはそのまま生きている。

あちらが同時に塞ぐのは**再読み込みがゲームセッションの表示を吹き飛ばす**（コアが
画面側に居る）ため。**この村ではコアが Tauri の Rust 側に居り、WebView の再読み込みでは
落ちない** — `initialize()` が `refreshAll()` / `listMessages` / `list_plan_waves` で
投影を張り直す（**Spec 08 の実機確認「GUI を再読み込みしても波が残る」がまさにこれ**）。
稼働中のサーヴァントは走り続け、会話ログも戻る。**消えるのは画面側の状態だけ**
（入力途中の依頼文・開いているダイアログ・スクロール位置・選択中のサーヴァント。
ペイン幅とシステム設定は `localStorage` なので残る）。
しかも Spec 08 の実機確認は**再読み込みで投影が張り直ることを確かめる手順**なので、
塞ぐとその確認が配布ビルドでできなくなる。
**一般化: 同じ処方でも、コアがどちら側に居るかで要否が変わる。他アプリの実装を
写すときは、処方だけでなくその処方が要る理由がこちらでも成立するかを数える。**

**判定はビルド時に畳まれる**（実測 — 配布ビルドの `index-*.js` は
`window.addEventListener(\`contextmenu\`, …)` が**条件ごと消えて無条件**になっており、
dev では丸ごと消える）。`import.meta.env.DEV` は Vite の定数なので実行時コストはゼロ。

## エディタの鍵（2026-08-21 着地。Spec 不要と判断）

起点は利用者 —「条例とか設定ファイルの編集でつい `Ctrl+S` を押して保存したと
思って閉じちゃう」。**フロントだけで閉じる**（コアも IPC も 1 行も変えていない）
ので Spec は切っていない。`CodeEditor.vue` の keymap 2 本 +
`lib/editorPhrases.ts` + 4 面の配線。

- **`Ctrl+S` は合図だけを出し、押せるかは親が決める**（`CodeEditor` の `save`）。
  **条件は保存ボタンと 1 実装を共有する**（各面の `canSave`）— 2 箇所に書くと
  `Ctrl+S` だけが「保存中」「壊れた JSON」をすり抜ける形が生まれ、
  **型検査にも実行時にも掛からない**
- **鍵は `Mod-`**（CodeMirror の記法で mac は Cmd）。`Ctrl-` と書くと mac で
  効かない — Alt+↑↓ の「3 OS で片方だけ効かない鍵は採らない」がそのまま当たる
- **`Ctrl+R` はエディタ内でだけ飲む。** リロードは画面を作り直すので編集中の
  本文が消えるが、**dirty の確認はどの面でも「閉じる」経路にしかなく、リロードは
  そこを通らない**。JetBrains 系では `Ctrl+R` が Replace なので手癖で押される
  （**この村の置換は `Ctrl+F` のパネルの中にある**）。フォーカスがある間だけなので、
  Spec 08 の実機確認（再読み込みで投影が張り直る）は今までどおり通る。
  **実機で飲めることを確認済み**（2026-08-21 利用者）— **WebView2 はページ側の
  `preventDefault` を尊重する**。Chrome は `F5` / `Ctrl+R` を**予約鍵**として扱い
  ページからは止められないので、**同じだと決めつけずに測った**（外れていたら
  Rust 側で `AreBrowserAcceleratorKeysEnabled` を切る話になり、Windows 専用コードが
  入って 3 OS で挙動が割れていた）
- **検索パネルの文言は `EditorState.phrases` を compartment で差し替える。**
  **鍵はライブラリが `state.phrase()` へ渡す英語の原文そのもの**なので
  `locales/*.json` には置かない（あちらの鍵は `editor.save` のような識別子で、
  こちらはライブラリ側の文面が鍵）。英語は上書きせず空の表を返すので、
  **`ja` / `en` の鍵集合一致テストの外に居る**。代償は三言語目でここに腕が 1 つ増えること
- **機械の網は 2 本。** `editorSaveWiring.test.ts` が 4 面の配線とキーの書き方を
  走査で留め（ボタンの条件を直書きへ戻すと赤）、`editorPhrases.test.ts` が
  **ライブラリの dist を実際に走査して**原文と表を両方向で突き合わせる
  （更新で文面が増えても減っても赤）。**どちらもミューテーション 2 回で赤を確認**
- **`RoleDialog` に閉じるときの dirty 確認は足さない**（2026-08-21 利用者裁定）。
  他の 3 面は**開いた時点で唯一の実体を編集しており、閉じることが唯一の破棄経路**
  なので確認がそこに乗る。`RoleDialog` は**一覧 ⇄ 下書き**の二層で、`draft` は
  **捨てられる前提で設計されている**（`blank()` / `startEdit` の deep copy /
  独立した `cancel()`）。**同じ規律を写す対象ではない** —「4 面のうち 3 面にしか
  無いので追従漏れ」と読んだ私の初見が誤りだった。**足すなら置き場は「閉じる」では
  なく `draft` を差し替える 1 実装**（`startNew` / `startEdit` / `cancel` / 閉じるが
  通る点）で、そこ以外へ足すと**意図的な破棄である `cancel()` にまで確認が生える**。
  なお意図せず下書きを失う経路は閉じるより**一覧の別項目のクリック**のほうが近い
  （`startEdit` が `draft` を確認なしで上書きする）
- **`F5` は飲まない**（2026-08-22 利用者裁定 —「`Ctrl+R` ほど間違えない」）。
  **害は同じで、違うのは頻度**。`Ctrl+R` は JetBrains 系の Replace の手癖で押されるが、
  `F5` を押す動機はエディタの中に無い。**同じ害だから同じ処方、とは書かない**

## 失敗した個体を ON だけで戻せるようにした（2026-08-06 着地。Spec 不要と判断）

起点は利用者の報告 —「エラーが発生すると再起動するまで再稼働できない」。
真因・処方・一般化は `failures.md` #78。ここには**次に触る人が必要な 2 点**だけ:

- **`failed` は `idle` と同じ停止側で、`start_agent` で再稼働できる**
  （`data_contract` の `AgentStatus` note に凍結）。**ここを塞ぐと、画面の
  トグルは OFF に見えているのにアプリを再起動するまで戻せない状態になる**
- 失敗経路には `stop_agent` に当たる後始末が無い。畳むのは**次の
  `start_agent` の冒頭（reap）**で、`JoinHandle::is_finished` により
  「登録がある」と「走っている」を区別する。**`agent reaped:` の 1 行**が出る
  （**2026-08-13 に初観測** — Spec 37 P5 で `effort=max` の 400 が `fatal=true` で
  個体を降ろし、その次の `start_agent` で `agent reaped: had_error=true` が出た。
  **導入から 7 日、実機で失敗経路を踏むまで一度も出ていなかった**）

## OpenAI 互換のパラメータ差の吸収（2026-08-06 着地。Spec 不要と判断）

起点は利用者の実機報告 — **gpt-5.6-luna が 2 つの 400 で連続して落ちた**。
どちらも `openai_compat.rs` の純関数 1 本で吸収し、型・IPC・画面は 1 つも
変えていない（決めることが無いので Spec は切っていない）。

| 症状 | 処方 | 純関数 |
|---|---|---|
| `max_tokens` を `unsupported_parameter` で拒否（#76） | 上限欄を排他 2 本にして送り分け | `uses_max_completion_tokens` |
| `reasoning_effort` と function tools は併用不可（#77） | tools を送る周だけ `none` を明示 | `reasoning_effort`（引数を 1 つ追加） |

- **どちらも全面置き換えにしない。** 新欄 `max_completion_tokens` を知らない
  互換サーバ（llama.cpp / vLLM / gpt-oss 系）があり、思考の停止も
  「ツールを送る周」だけに掛かる。**`temperature`（明示時のみ送る）と
  `reasoning_effort`（モデル名で送り分け）に続く 3・4 例目**で、処方の形は既にあった
- **#77 の核は「送っていないパラメータの名前で拒否された」こと。**
  **省略は「無効」ではなく「サーバ既定を採る」** — 黙っている間、値を決めていたのは
  サーバだった。「送っていないから責任範囲の外」という読みが誤り
- **画面の思考段階は要求であって保証ではない。** gpt-5 系はツールを送る周で
  利用者の選択を `none` へ上書きする（プロバイダの制約で回避経路が無い）。
  丸め（xhigh / max → high）と同じ扱いで、`data_contract` の `Effort` note に層を
  分けて書いた — **canonical は「未指定なら送らない」、ワイヤは adapter が決める**
- **契約が既に嘘になっていたのを 1 件回収した**。`Effort` の note の
  「既定値を勝手に補わない」は、grok-4.3 へ `none` / o 系へ `low` を**以前から
  補っていた**ので、今回の変更が来る前から実装と食い違っていた
- **1 つ直すと次が出る形。** 400 はパラメータを 1 つずつしか教えないので、
  **1 件直した時点では「直った」と言えない** — 実機で 1 周通るまでが 1 件

**残っている代償と、その出口（2026-08-06 利用者判断で見送り）**:
`gpt-5.6-luna` はツールを持つ限り**思考なしで動く** — 推論モデルの推論を
殺したまま使っている。これは不具合ではなく、`/v1/chat/completions` が
併用を許さないことへの対処。**出口は 1 つだけで、`/v1/responses`
（Responses API）へ移ること** — 400 の本文自身がそう書いている。

**同じ出口が OpenAI の web 検索も開ける。** 一次資料で確認した（2026-08-06）—
一般の gpt-5 系の `web_search` は **Responses API 専用**で、
`/v1/chat/completions` で検索するには専用モデル（`gpt-5-search-api`）が要り、
旧 `gpt-4o-search-preview` 系は **2026-07-23 に停止済み**。
**Gemini グラウンディングと同型**（互換層が道具を拒むのでネイティブ経路が要る =
Spec 05 / Spec 23 Notes 5 の「道具か中身か」）。

**見送った理由**: 利用者が「今回はいらない、いずれ対応しよう」と判断
（2026-08-06）。**片方だけのために 4 本目のワイヤは割に合わない**が、
**思考の復活と web 検索が同じ 1 本で解ける**ので、起票するならこの 2 つを
1 つの Spec にまとめること。規模の実績値は Gemini ネイティブが
`gemini.rs` 715 行 + `wire.rs` の Gemini 型 17 個 + `client.rs` の分岐 +
`Provider` 追加 + UI + 契約。Responses API は `messages` ではなく `input` で
リクエストの形が根本的に違うので、同規模以上を見込む。
**着手前に測るべきこと**: `gpt-5-search-api` が関数呼び出しを受けるか
（受けるならテンプレート登録だけのコード変更ゼロの道が実在する。
旧 `gpt-4o-search-preview` は非対応だった）。

## リリースビルド（`.github/workflows/build.yml`・2026-08-05 着地）

**`v*.*` タグの push だけで走る。** 通常のコミットでは走らない — 3 OS 分の
Tauri ビルドは重く、毎コミットで回すと待ち時間と CI 時間の両方を食う。
Kataribe（`D:\Github\Kataribe\.github\workflows\build.yml`）を写し、
**この村の構造に合わせて 4 点を変えた**。

| Kataribe | この村 | 理由 |
|---|---|---|
| `setup-node` + `npm ci` | **`setup-bun` + `bun install --frozen-lockfile`** | `tauri.conf.json` の `beforeBuildCommand` が `bun run build` |
| `rust-cache` に workspace 2 つ | **1 つ（既定のルートのみ）** | ルート 1 ワークスペース（`members = apps/gui-tauri/src-tauri, crates/fuseforks-core`）で target も 1 つ |
| artifact は `app/src-tauri/target/…` | **`target/release/bundle/`** | 同上。**bundle はルートの target 配下に出る** |
| `node -e` で version 書き換え | **`bun -e`** | node は runner に最初から入っているが、**この workflow が明示的に用意しているのは bun だけ** |

**版番号の経路は 2 本あり、独立している**（Kataribe と同じ設計）:

- **配布物に乗る版** = `tauri.conf.json` の `version`。**CI がタグから書き換える**
  （`X.Y` なら `.0` を補って 3 要素 semver にする。Tauri が 3 要素を要求する）。
  **CI 内だけの書き換えでコミットはしない**
- **画面に出る版** = `__APP_VERSION__`。`vite.config.ts` が
  `git describe --tags --abbrev=0` から取る。**タグを打ったコミットをビルドするので
  両者は同じ値になる**

**フロントとコアのテストを Tauri ビルドの手前に置く**（`cargo test --workspace` →
`bun run test` → `tauri-action`）。壊れていればそこで落ち、**3 OS 分の重いビルドに
進まない**。

**ロックファイルは `bun.lock` 1 つ**（2026-08-05 に `package-lock.json` を削除して
寄せた。それまでは 2 つとも追跡していた）。

**寄せた理由は、二重管理が CI を静かに壊す形だったこと。**
**2026-08-05 に実際に食い違った** — `npm install` で `@tauri-apps/plugin-dialog` を
足したとき `package-lock.json` だけが更新され、`bun.lock` は古いままだった。
ビルドの入口は `bun` なので、**そのまま CI へ出していれば依存が入らず落ちていた**。
そのときは `bun install` で追いつかせたが、**気づいたのは偶然**で、次に同じことが
起きればタグを打って 3 OS 分のビルドが走ったあとに分かる。

**`npm` を使う経路は 1 つも無い** — `tauri.conf.json` の `beforeBuildCommand` は
`bun run build`、workflow が用意するのも bun だけ、README の手順も `bun install`。
**追跡ファイルだけが残っていた。**

**一般化: 入口が 1 つしか無いのにロックファイルが 2 つあると、使っていない側への
更新は入口側へ伝わらない。** 依存を足した本人の手元では動くので、壊れるのは CI か
別の端末。**ロックファイルは「どちらでも動く」ものではなく、どちらが正かを
1 つ決めるもの。**

### タグを打った後にもう 2 つ仕事がある（2026-08-17 追記）

**この節はタグの push で終わっていたが、配布経路が 3 つになったので終わらない。**
詳細は上の「配布経路を 3 つに広げた」の節が正。ここには**忘れると何が起きるか**だけ:

| | やること | 忘れると |
|---|---|---|
| **winget** | `wingetcreate update Outcasts.Fuseforks --version <V> --urls <MSI の URL> --submit` | `winget install` が古い版を入れ続ける |
| **Homebrew tap** | `betyourluck/homebrew-tap` の `Casks/fuseforks.rb` の `version` と `sha256` を新しい dmg から取り直す | `brew install --cask` が古い版を入れ続ける |

**どちらも版ごとに URL と SHA256 で固定する形**なので、**壊れずに古いまま止まる**のが
厄介なところ — エラーが出ないので気づく契機が無い。

**macOS の署名は 5 つの secret が揃っているときだけ発火する。** 揃っていなければ
署名せずに `Apple signing: DISABLED` の 1 行を出して進む（**黙って進まない**）。
資格情報が疑わしいときは `verify-notary.yml` を手動で回すと **1 分で真偽が出る**
（ビルドを回すと 20 分）。

## システム設定への追加候補（2026-08-03 利用者。小出しに来る前提）

Spec 13 の器（左メニュー = 目録 / メッセージ表示/非表示のページ）へ後から
足していく分。**まだ起票していない。** 出てきた順に置く:

- ~~**委譲の待ち時間 `ask_timeout`**~~ — **[Spec 44](specs/44_ask-cycle-detection.md)
  rev2 承認 → P0〜P1 完了**（2026-08-24。査読 7 点 → 採用 5 / 前提を実装で訂正して
  採用 2 — 転送はデッドロックを作れない（`HandedOff` の返信が配送より前）ので
  判定は `deliver_and_wait` 1 箇所で閉じる。凍結 6 本は `data_contract` の
  `ask_cycle_contract`。**P1 で `Envelope.waiting` + 判定 + `World` の時計
  （既定 600・`OrchestratorConfig` の欄は撤去）が着地** — 結合 4 + 単体 3、
  変異 2 回で赤確認。**「ask された個体の転送」はツール経路に存在せず
  旧経路（`use_tools=false`）だけ**、が実装で確定（P1 実装記録）。
  **P2 = IPC 2 本（`get_ask_timeout` / `set_ask_timeout`）+ システム設定 >
  コスト管理の「委譲の待ち時間」+ 辞書 ja/en も完了**（`02736d9`。保存告知は
  `askTimeoutNote` を新設 — 同じページに保存ボタンが 2 つ並ぶので `savedNote` を
  共有すると片方の保存で両方の枠に告知が出る）。**P3 台帳も完了**
  （README 日英の「委譲と合流」/ DETAIL 日英の層 2 へ「委譲の待ち時間と、
  循環の即時拒否」+ 設定表の行 + 「トポロジー上の循環は許可」の段落へ
  待ちの循環の例外を追記）。**P4 実機で 5 件すべて観測して Done**
  （2026-08-25。**検収 4 は 4a / 4b へ割った** — 4b の
  「A→ask→B→転送」は `turn.rs:968` の `!awaiting_reply`（#96 の門）で
  **ツール対応モデルでは到達不能**なので実機から降ろし、結合テスト
  `a_two_deep_chain_keeps_the_upstream_after_a_handoff` へ寄せた。
  記録は Spec の「P4 実機記録」が正）。**次に触る人が要る 3 点**:
  (a) **輪は狙わないと作れない** — 3 回空振りし、しかも**ルナの `Memory.md` に
  「依頼元にはタスクを振らず、調査・検証の委譲先から除外する」が書かれていた**
  （`remember` で 2026-08-15 = **#106 を踏んだ当日**・**P1 の 9 日前**）。
  **振る舞いの規則が構造の規則を先取りしていた**（機構が不要という意味ではない
  — #106 は実際に起きた。守っているのは事故で起きる輪）
  (b) **タイムアウトの検収はワーカー側を見ると読めない** — 凍結 6 で相手の
  ターンは止まらないので画面では走り続ける。**判定は依頼主の `tool:` 行だけ**
  （`body_chars` が定型文の長さか答えか）
  (c) **届かなかった答えの払いは `turn:` 行に残る**（実測 `prompt=198340` の
  4,491 字が会話に現れない）。**統計に出て会話に出ない量が実在する**
  **外の扉との相互作用を P0 で数えた** — 既定 600 で
  Spec 25 の「既定同士で S1 は切られない」が崩れ、`.mcp.json` の `timeout`
  併記が実質必須になる（設定画面の `mcpNote` と DETAIL 日英に明記）。
  利用者 —「かなり重大な欠陥だ。
  ちょっと重たい作業はまったくできない」。骨格 = 輪の検出を構造へ
  （`Envelope.waiting` の連鎖 + 配送時の即答拒否・転送は末尾を除く）、
  時計は保険へ格下げして既定 600 秒 + `world.json` + システム設定 >
  コスト管理。Spec 13 の「出さない」を 1 点名指しで覆す）。
  起票前の材料（180 秒固定の現状・利用者の向き・(c) 案の導出）は
  **Spec 44 の Goal / Design へ写して回収済み** — ここに残すと
  「現状はコード定数の 180 秒固定」が生きた事実として嘘になる

- ~~**テーマ**~~ — **2026-08-05 に着地**（上記「ライトテーマ」の節）。
  見込みどおり配色は 1 箇所に集まっており、受け皿はそのまま使えた
- ~~**ユーザー（呼び名とアイコン）**~~ — **[Spec 19](specs/19_user-identity.md) として
  起票・rev1 承認 → P0 完了**（2026-08-05）。**この器へ足した中で最初に Spec を
  切ったもの** — テーマがフロントで閉じたのに対し、`world.json` に欄が増え IPC が
  生え画面要素が生まれる。**「全般」の先頭**に入るので既定の表示ページが
  「全般 → 言語」から「全般 → ユーザー」へ動く
- **ツール実行ログを会話で非表示にする** — **「メッセージ表示/非表示」の 2 件目**。
  P2 で器を作ったのはこのため。ただし会話ペインのツール行は
  「**エージェントが何をしたかは会話ログに現れない**」を埋めるために置いた計器
  （`chatRows` の設計理由）なので、既定 ON は動かさない
- **起動時に一括起動 ON のサーヴァントを立ち上げる** — **既存の凍結された
  不変条件を変える**（`batch_start_invariant`: 「**自動起動ではない。**
  アプリを開いた時点では誰も走らない — 開いただけで課金が始まる作りにしない」。
  data_contract:583 / model.rs:201 / **DETAIL.md の「画面の構成 > 起動」** /
  failures.md #1701 に接地。**旧記述の `README.md:186` は改稿前から既に外れており**
  — その行は画面構成の表の 1 行で、不変条件の本文ではなかった —
  2026-08-09 の README / DETAIL 分割で回収した。**行番号で台帳を指すと、
  指した先が動いたことに気づけない**ので、以後は節の名前で指す）。
  **既定 OFF の明示的なオプトインなら不変条件の理由（開いただけで課金しない）は
  保てる** — 起票時はそこを軸に書くこと。既定 ON にすると理由ごと壊れる。
  **→ 2026-08-29 に重みが変わった** — core 単独実行の構想（「設計の材料 3 件」の
  節）の**先行条件**になった。ヘッドレスは自動起動しないと永遠に何も走らない

## 役職（Role）のラベル（**[Spec 14](specs/14_role-label.md) として起票・P0〜P5 完了**）

入口は**タイトルバーの「条例」の次くらい**。Role は 2 役を兼ねる:

1. **サーヴァントを作るときのテンプレート**（雛形としての Role）
2. **サーヴァント自身の判断基準** —「このサーヴァントはこういう役割なんだ」を
   本人が読む。つまり**プロンプト素材**になる

起票前に確かめる点が 3 つ（設計はまだしない）:

- **(2) はプロンプトへ入る** = 安定プレフィックスの一部になり、
  `ordinance_contract` の連結順（条例 → 自分について → … → 顔ぶれ → Memory）の
  どこへ挿すかで、キャッシュの安定境界が動く
- **他人の役職が見えるべきか**は Spec 06（顔ぶれ）の拡張になる。見せるなら
  「誰に何を頼めるか」の判断材料が増える一方、毎ターンの可変注入は増やさない
  規律（#45 / 論文 1 の品質仮説）と衝突しないか要確認
- **Role を「進行役フラグ」にしない。** 既存の判断は
  「進行役はトポロジーの帰結（plan は接続 2 体以上で生える）」「線は人が引く」。
  役職ラベルは**人格・語彙の設計**であって、配送や権限の機構ではない —
  ここが混ざると AdaptOrch を採らなかった根拠ごと崩れる

### **[Spec 14](specs/14_role-label.md)（2026-08-04 rev2 承認 → P0〜P5 完了 → 残は実機確認 1 件・未決ゼロ）**

**改名の追従と削除耐性は Done**（2026-08-08 利用者裁定 —「運用で何度も試した」）。
**参照方式の設計が狙っていたのはまさにこの 2 つ**（`role_id` で指すので改名が
全バッジへ波及する / 設定はコピーなので参照先が消えても spec は壊れない）で、
**日常の操作でしか踏めない形**でもある。残る 1 件は顔ぶれの `［役職］`。

**P5 の台帳側**: README 日英へ「役職」の節。**追従させた 3 箇所が本体** —
顔ぶれの書式が `agent_id（表示名）: 稼働中` のままだった（Spec 06 の節にあり、
Spec 14 を書いている間は一度も目に入らない場所）/ `world.json` の中身に 6 つ目の
役職が抜けていた / ディレクトリ木と画面の構成表。**機能を足したときに腐るのは、
その機能の節ではなく隣の節**（#51 の (b)「着地時にどの台帳へ書いたか数える」）。

**P4**: 顔ぶれの行は `agent_2（ジェミー）［調査役］: 稼働中`。**名前だけ**で説明は
載せない / 引けなければ `［...］` ごと省く。**`stable_len` 不変を結合で証明**
（役職を足しても安定プレフィックスは 1 字も動かない = 全員のキャッシュが割れない）。
System 行は**付与・改名・削除の 3 経路**で、判定は「表示名が変わったか」の 1 点。

**実機で穴が 1 つ出た**（2026-08-04、利用者指摘）: **契約は「既存の個体に後から
役職を付けられる」と書いていたのに、画面に入口が無かった**（選べるのは新規作成の
ときだけ）。**Spec 13 の起票理由と同じ形**（天井は存在するのに触る場所が無い）で、
「既にある設定の可視化」を主目的に Spec を 1 本立てた直後に隣の Spec で再発した。
処方は `AgentSettingsDialog` へ役職の選択（`role_id` だけ差し替え・設定は流し込まない）
+ dirty 判定にも追加。**一般化は `failures.md` #53** — 契約に「できる」と書いた
操作は入口があるか数える / 作成フォームへ何かを足したら編集フォームにも要るかを問う。

**役職の色を追加**（2026-08-04 利用者要望）。**置き場は `name` の隣（参照側）で
`defaults` には入れない** — 色は表示の属性で、変えたら既にいる全個体のバッジが
即座に追従するのが期待される挙動。`defaults` に入れると「色を変えたのに既存の
個体が変わらない」になる。**機構 3 の分割がそのまま新しい欄の置き場を決めた。**
**閉じた列挙**（8 色）で、色値は `style.css` の `--color-role-*` にしかない —
自由入力にすると**暗い背景に暗い色を選べて読めないバッジが作れる**ので、
明度と彩度を固定して色相だけ変える（`avatarHue` と同じ形）。
`roleLabel` は `roleBadge` へ広げた（名前と色を 1 回で返す。2 関数に分けると
「名前は出るが色が付かない」場所が生える）。**実機で 7 色が出ない事故**
（`failures.md` #54）— **Tailwind v4 の `@theme` は未使用の変数を落とす**ので、
実行時に組み立てる `var(--color-role-${色})` は全部消えていた。**teal だけ
残ったのはテストの文字列リテラルをスキャナが「使用」と読んだから** =
テストが自分の検証対象を 1 色だけ延命し、誤った確信まで与えていた。
定義は素の `:root` へ（`@theme` はユーティリティを生やす色の棚）。

**D6 決着（2026-08-04 利用者裁定）: サーヴァントは自分の役職名を知らない。
これは穴ではなく設計。** 理由は**ペルソナが役職名に引きずられる**こと — 人格は
`Construct.md` / `SKILL.md` の文章が担っており、そこへ label を差し込むと
モデルが語の含意へ寄る。**役職は人が読む飾りであり、雛形の名前**。
実機で観測してから決めた — バッジが「助役」のザリに訊くと「進行役」と答えた
（`SKILL.md` から答えており、ラベルは読んでいない）。**回帰テストで留めてある**
（顔ぶれに自分を含める / 「# あなたについて」へ役職を書く のどちらでも落ちる）—
**決定を文章だけで残すと、次に誰かが親切心で足す。**
これで 2 役の (2)「サーヴァント自身の判断基準」の担い手も確定した:
**`Construct.md` の本文であって、役職名ではない。**

**P3 の入口は実機で差し戻した**（2026-08-04）。一度システム設定の左メニューへ
入れたが、**タイトルバーの「条例」の右**が正。理由 —
**役職はシステム設定のカテゴリではなく村のカテゴリ**（利用者の言葉）。
私の起票時の理由付け（「`world.json` に住むから村の共有設定の棚」）は
**保存先で分類した**もので、**Spec 13 が rev で潰したのと同じ誤りを直後に
踏み直していた**。境界は**「村の内容物か、アプリの設定か」** — 条例と役職は
「この村がどういう村か」、言語とトークン制限は「アプリがどう振る舞うか」。
`world.json` に住むのは条例も言語も同じで、**保存先は分類の根拠にならない**
（`failures.md` #52。一般化は「直したばかりの誤りほど隣の機能で再発する。
台帳へ書くのは箇所ではなく判断基準」）。

**P3 で決めた 3 点**: CRUD は `RoleDialog.vue` へ切り出し
（1 ファイル 1 責務。置き場が変わっても判断は変えていない）/
**`RoleDefaults` の 5 欄すべてを画面に出す**（一部だけだと残りは手編集でしか
触れず、Spec 13 の目的と衝突する）/ 削除の確認は出すが **`danger` は立てない**
（役職を消してもサーヴァントは壊れないので、赤くするのは実害に見合わない脅かし）。

**D5（P2 で立てた未決）は消えた** — 作成フォームは**名前しか集めていなかった**ので、
上書きされて困る編集値が存在しない。**未決を立てるときは「その形が実際に
そうなっているか」を先に見る**（実装を読めば起票の時点で消えていた）。

**D2 も起票時の前提が誤りだった** — 地図のノードは「2 段で 3 段目は狭い」と
書いていたが、**実機には既にチップ行（状態 + トークン）があった**。そこの先頭へ置いた。

表示の解決は `lib/roleLabel.ts` の 1 実装（凍結 5 の「3 箇所 1 実装」をフロントでも
1 ファイルに閉じる）。**引けなければ `null` でバッジごと描かない。**

**P2 で決めた 3 点**: 流し込みは `create_agent` の中だけ（**作成の入口を 2 つに
しない** — 片方だけ流し込む実装がいつか生える）/ 役職が引けなくても作成は通す
（村を配った先で役職が欠けているだけで新規作成ができなくなるのは罰が重い）/
`Construct.md` の書き込み失敗で登録を巻き戻さない。**CoreEvent は増やさない**
（モデルテンプレートと同じで、変更後は UI が引き直す）。

**P3 へ送る未決（D5）**: `create_agent` は役職の意見で spec を上書きするので、
**作成フォームが役職を選ばせたうえで各欄も編集させると、編集した値が黙って
上書きされる**。処方は「フォームを役職の値で前埋めする」を推し
（**前埋めもコアの `apply_to` を通す** — 規律を 2 箇所に書かない）。

**P1 で契約を 2 点訂正した**（実装でしか出なかった。詳細は Spec の P1 実装記録）:

- **参照の存在検査は `model_template_id` だけ**。`rag_sources` には**検査する
  相手が居ない** — `RagIndex.by_source` は断片を索引した瞬間にキーが生える
  実行時の器で、宣言された登録簿が無い。作成時点の索引はほぼ必ず空なので、
  検査すると「調査役を作る → あとで資料を食わせる」順序を壊す。
  **境界は「宣言された登録簿か、実行時に育つ器か」**
- **`AgentSnapshot` にも `role_id` が要る**。設定ダイアログと一括起動トグルは
  **投影から `AgentSpec` を組み直して保存する**ので、投影に無い欄は
  **保存のたびに消える**。さらにバッジはカード＝投影から描くので、無いと S2 が
  原理的に成立しない。**`AgentSpec` に欄を足したら「投影にも要るか」を必ず問う**
  （`batchStart` に同じ注記が既にあった＝同型の三例目）

**型名は `AgentRole`**（`llm::Role` が既存。改名の代償は 68 箇所 vs 0 箇所で測った）。
**ドメインの語は「役職 / Role」のまま** — Rust の型名だけ接頭辞で分ける。

rev1 の査読が**実装したら落ちる矛盾を 10 件**出し、rev2 で全件反映して承認。
草案は 1 行へ畳んだ（内容は全て Spec 14 へ写した）。決着の要点だけここに置く。

**P0 で凍結するのは 7 点**: 置き場 / 雛形の欄 / コピー参照の分割 /
**発火点は新規作成ただ 1 つ** / 表示解決規則（3 箇所 1 実装）/
顔ぶれの役職名だけ / 実行時分岐に入らない。
**`AgentSpec` の分類表 12 行は要約せずそのまま契約へ写す** —
要約すると「箇条書き 4 本を 4 欄と数える」罠に戻る（rev1 が実際に踏んだ）。

**査読で最も効いたのは「バッジのドリフト」**（機構 8 を新設）。他の 9 件は
「書いたことが矛盾している」= 実装すれば落ちるが、これだけは**書いていないことが
嘘になっていた**。コピー方式を採った時点でバッジは嘘をつけるのに、S2 が
「誰が何の役かが一目で分かる」と約束していた。**落ちないのでテストでは出ない。**
処方は機構でなく言葉 —「**バッジは由来であって現在の中身を保証しない**」を
仕様と UI の両方に書き、S2/S3 の約束をその範囲へ縮めた。

- **主論点は二択ではなかった。** 「コピーか参照か」は**性質の違う 2 つを 1 つの
  問いにしていた**。分けると両方素直に決まる — **設定の中身はコピー**
  （後から役職を直しても既存の個体は変わらない。条例と同じ性質の層を 2 つにしない）、
  **役職の名前は参照**（`role_id` → 表示名。改名でバッジが全部追従する。
  変わりやすい表示名で指さない = #51 一般化 2、`AgentId` vs 表示名と同じ形）
- **「System Prompt に全部入りになる」は起きない**（利用者の懸念）。
  プロンプトへ入るのは**顔ぶれの行の役職名だけ**で、**役職の目録は一切入らない**
  （目録の読み手は人）。顔ぶれは既に `connected_agents` に絞られ、既に
  `stable_len` の外なので**キャッシュの安定境界は動かない**
- **説明はモデルに見せない**（利用者から任された判断）。**自分の役職は全文**
  （`Construct.md` = 安定部・キャッシュが効く）、**他人の役職は名前だけ**
  （可変部・毎ターン）。値段と価値が一致する非対称
- **雛形に線と場所は詰めない。** `connected_agents` を入れると役職を選んだ瞬間に
  線が引かれ「**線は人が引く**」が崩れる。`work_dir` は端末ごとに違う絶対パスで、
  村を配ると壊れた参照を配ることになる
- **セッションとの食い違いには何も作らない。** system プロンプトは毎ターン
  組み直される（`session_store` は保存しない）ので**現在の役職が必ず勝つ**。
  残るのは履歴の残り香だけ。役職変更の System 行は
  `compose_presence_notices` 経由で届くが**保証ではない**ので約束にしない

## コマンド実行（[Spec 15](specs/15_command-execution.md)・**rev4 → P0'〜P5 完了。実機で動作。残るは実機確認 6 件のみ**）

**2026-08-04 に利用者裁定で骨格が再変更された。** 登録制（登録名でしか呼べない）を
やめ、**エージェント別の allow / deny リスト**へ。置き場は
`agents/{id}/run.json` で **`mcp.json` と同じ棚**（`ConfigFileKind` に 1 つ足す）。
3 状態 = allow なら即実行 / **deny なら拒否して承認要求を積まない** /
どちらにも無ければ拒否して `pending` へ積む。

**これは 2026-07-31 の「閉じた許容・除外リストではない」判断を覆すもので、
覆したことを Spec に書いた上で覆している。** rev1 が拒否されたのは
**気づかずに**反転したから（#56）で、今回は判断を持っている本人が明示的に覆した。
**台帳の役目は判断を凍らせることではなく、覆すときに覆したと分かるようにすること。**

**シェル非経由は 1 文字も弱まらない** — エージェントは `command` + `args` 配列を
書き、**照合も argv 配列に対して行う**ので `&&` も `|` も `$(...)` も構造的に
存在しない。rev1〜rev3 でシェルを退けた根拠はそのまま生きている。

**戻ってきた宿題が 2 つ**（rev1 査読の CRITICAL 1 / MAJOR 6 が指摘し、登録制では
消えていたもの）: (a) **PATH 解決の曖昧性** — `allow: ["python"]` はその時の PATH の
python を走らせる。処方は「構造的に決まる」と言わず、**解決したフルパスを毎回
結果とログに出す** (b) **Windows のコマンド名正規化** — パス区切りを含む `command` は
照合前に拒否 / 小文字化して比較 / `PATHEXT` を剥がしてから比較。

**残せるもの / 捨てるものは Spec の Tasks に表で数えた**（#51 (b) の実装版）。
残る: `run.rs` の実行機構（プロセス起動・`env_clear`・出力の分配・木ごと kill）/
`write_atomic` / `AgentTool::spec_for` / `ToolContext.cancel` /
`DEFAULT_ENABLED_TOOLS` の分離 / `defaultEnabledTools.test.ts`。
捨てる: `CommandRegistry` 型 / `load_commands` 系 / IPC 5 本 /
`CommandRegistryPane.vue`（`mcp.json` と同じ編集経路へ寄せるので専用画面が要らない）。

**D8〜D10 は 2026-08-04 に利用者裁定で確定**（村の査読には出さない）:
**D8** `timeoutSecs` はファイルに 1 つ（per-pattern にすると対応表がもう 1 つ増え
「書いたとおりに効く」を壊す。将来は `timeoutOverrides` を足す余地だけ残す）/
**D9** `pending` は per-agent 20 件・`firstRequestedAtMs` の古い順に押し出し /
**D10** `run.json` を持たない個体は「全部 pending」= **これが fail closed**
（`allow` に一致しない呼び出しは 1 つも走らない。**初回の手間は欠点ではなく仕様**）。
**「提示されず」の半分は 2026-08-06 に撤回した**（下記「提示は 1 段」の節）。

**パターン構文は完全一致と末尾 `*` の 2 つだけ。** 中間ワイルドカードを持たないのは、
`*` が何個の引数に対応するかの規則が要り**書いた人の意図と照合結果がずれる場所が
増える**ため。**`"ruff"` は引数なしにしか一致せず、任意引数を許すなら `"ruff *"`** —
この差を UI と拒否文面の両方に書く（書かないと「ruff を許可したのに ruff check が
通らない」で必ず躓く。利用者の指摘）。**deny が allow に優先**し、
**モデルへは allow だけを列挙して deny は見せない**（禁止の一覧を毎ターン積むと
トークンを払って禁止の方法を教えることになる）。

**実機で動いた**（2026-08-04）— ザリが `curl` を実行し、`git log` が `pending` へ
積まれ、人が `allow` へ行を動かすと次のターンで通った。**11 件のうち (1)〜(5) 達成。**
`run.json` は `allow: ["git status *", "git log *"] / pending: []`。
**「1 つのファイルを開いて行を allow か deny へ動かすだけ」という設計が実機で成立した。**
**その手作業は [Spec 20](specs/20_command-approval.md) の承認画面が引き受けた**
（2026-08-05）— ただし**取り除いたのは書き写しであって、1 件ずつ許容を決める
手間ではない**（後者は閉じた許容の本体）。`run.json` の直接編集も残っている。
初走行で `canonicalize()` の `\?\` 前置がモデルの読む本文へ漏れているのが見え、
`display_path` で塞いだ（`failures.md` #60。**Spec 09 Notes 1 が 1 週間前に
「観測されたら」と予告していた条件が、別 Spec で満たされた**）。

**ファイル名は `run.json`（2026-08-05 に `command.json` → `shell.json` →
`run.json` の 3 つ目で確定）。** 設定する対象の名前をそのまま採る＝`mcp.json` と
同じ規則。`shell` を降ろした理由は**弁解の doc コメントを必要とする名前だった**こと —
`ConfigFileKind::Shell` の直上に「ファイル名は `shell.json` だが、実行はシェルを
介さない」と書いてあり、型を定義しているその場所で言い訳が要る名前は間違っている。
**互換読み込みは作らない**（旧名フォールバックは #48 の代用機構と同じ形で、
本来の名前が来た時点で害へ反転する）。既存の `shell.json` は読まれなくなり、
落ちる先は D10 の「全部 pending」。**起動 WARN も置かない** — 該当する村が
利用者の手元の 2 体しか無く、**一度も鳴らない機構は「何を入れるべきか」を
問い続ける空の欄**になるため（利用者判断 2026-08-05）。

**この改名で 2 件目の追従漏れが出た**（1 件目 = `command.json` の残り 16 箇所）:
**`ConfigFileKind::all()` が `[Self; 4]` のまま `Run` を含んでいなかった。**
doc は「GUI のタブ生成に使う」と書いていたが実際の呼び出し元は自分自身のテスト
1 本だけで、そのテストはファイル名 4 本を固定していた＝**`shell.json` も
`run.json` も、どのテストにも覆われていなかった。** `Shell` で grep しても
`[Self; 4]` は出てこない — **CLAUDE.md が既に書いている「数の記述は、増えた名前で
grep しても引っかからない」の実例が、書いた当人の直後の作業に残っていた**
（`failures.md` #62）。処方は 5 種へ広げ、テストの期待値に `run.json` を足し、
`all()` の doc を実態（テストのための一覧）へ直したこと。

**P0' 完了**（2026-08-04）— `command_tool_contract` を allow / deny / pending へ
全面改訂し、`ConfigFileKind` へ `run` を追加。**rev3 から撤回した凍結 4 点
（閉じた許容 / 除外リストを作らない / 実行ファイル名を書けない / `program` は絶対パスで
fail closed）を「撤回した」と明記した** — 書かないと次に読む人は生きている凍結だと
読む（#56 の逆向きの事故）。`ConfigFileKind` の note にも
**「run だけは人と機械の両方が書く」**を追記（`mcp.json` は人しか書かない）。

**Notes 4 が本 Spec で初めて出る形**: `pending` を `run.json` に置くと
**機械が書くファイルを人が編集する**ことになり、衝突しうる（人が編集中に
エージェントが `pending` を足す）。処方は書き込みを**読み直してから差分適用**に
すること。`mcp.json` は人しか書かないのでこの問題が無かった。

**P2 完了**（2026-08-04）— `tools/run.rs`、単体 7 本、workspace 463 本全緑。
依存に `command-group 5.0.1`（`unsafe_code = "forbid"` なので Job Object /
`setsid` を自前で書けない。`trash` と同じ判断）。

**契約が実装を見ずに書けていなかった点が 2 つ出た**（どちらも P0 の凍結時には
見えなかった）:

- **`description()` は個体を知らない。** 契約は「実行可能な登録だけ列挙」と凍結したが、
  `AgentTool::description(&self) -> String` は `ToolContext` を受け取らない。
  `enabled_tools` / `WORK_DIR_TOOL_NAMES` の除外は**名前の集合**で書かれているが、
  「登録が 1 件も実行可能でない」は**中身を見ないと決まらない除外**で名前では書けない。
  処方は **`AgentTool::spec_for(&self, ctx) -> Option<ToolSpec>`**（既定は `spec()`、
  `None` で非提示。加算的で他の 7 本は 1 行も変わらない）。
  **Spec 14 P1 の「`AgentSnapshot` にも `role_id` が要る」と同型** —
  **契約に欄や規則を足したら「それを運ぶ経路が既にあるか」を実装前に問う**
- **Spec 10 の不変条件 1 と衝突しかけた。** 「検査点は周回境界だけ」を守ると、
  打ち切りを押しても走っているコマンドは**最長 `timeoutSecs`（上限 1 時間）走り続ける** —
  実機で観測した「要求から 0.0 秒」がそこだけ破れる。処方は
  **`ToolContext.cancel: Option<CancellationToken>` を追加し、`run` だけが見る**。
  判断は「**葉で 1 箇所だけ見るのは、周回境界の検査を増やすことではない**」
  （ターンループの構造は変わらず、止められない待ちを 1 つ潰すだけ）

**P3 完了**（2026-08-04）— コア: `DEFAULT_ENABLED_TOOLS` 新設（`BUNDLED_TOOL_NAMES` は
7 → 8 本）/ `is_bundled_tool_presented` の `None` 分岐を既定集合へ /
`ToolRegistry::specs_for` / `execute_tool` へターンのトークンを渡す。
アプリ: `AppState` が登録簿・登録要求・`ConfigStore` を持ち起動時に実在検査 /
`RunTool` の登録 / IPC 5 本。結合 3 本、workspace 466 本全緑。
**P2 の宿題（`cancel: None`）は解消** — 打ち切りがタイムアウトを待たずに効く。

**P3 で決めた 4 点**: 登録 0 件でも `RunTool` は登録する（提示の判断は `spec_for` に
1 箇所だけ持たせる。`state.rs` で出し分けると**同じ規則が 2 箇所に生える**）/
`save_commands` は保存のたびに実在検査を掛け直す（パスを直したらその場で戻る）/
**`AppState` と `RunTool` が同じ `Arc<RwLock<CommandRegistry>>` を見る**ので
登録を足したら次のターンから載る（`ToolContext.work_dir` を「呼び出しの瞬間に
引く」のと同じ考え方で、登録時に固定しない）/ `resolve_command_program` は
登録画面の補助で実行時には使わない。

**テストで分かったこと**: `setup_with`（結合テストの土台）は**同梱ツールを 1 本も
登録しない** — 登録は GUI 側（`state.rs`）の仕事。既定集合の変更が他の同梱ツールへ
波及していないことを見るには、比較対象を明示的に足す必要があった。

**P4 完了**（2026-08-04）— `CommandRegistryPane.vue`（CRUD は 1 ファイル 1 責務で
切り出し。`RoleDialog.vue` の前例）+ システム設定へ 1 ページ + `AgentSettingsDialog`
へ `run` の行 + IPC 5 本 + 型 3 つ + 辞書 ja/en。vitest 112 → 116 本。

**実装で穴が 1 つ出た**: `AgentSettingsDialog` の `isToolChecked` は
`enabledTools: null` のとき**無条件に `true`** を返していた（「null は全部 ON」の
旧い前提のまま）。`run` を一覧へ足しただけだと**既定 OFF なのに画面ではチェックが
付いて見え**、コアと画面が食い違う。しかも**保存するまで気づけない**。
処方は Vue 側にも `DEFAULT_ENABLED_TOOLS` を置き、`isToolChecked` と `toggleTool` を
そこから引くこと。**この手動同期は `defaultEnabledTools.test.ts` で機械に留めた** —
Rust の `tools/mod.rs` と `.vue` を両方読み、2 つの表が一致することと
**`run` が既定集合の外に居ること**を検査する（名前の並びのずれはコンパイラにも
lint にも引っかからない = #51 と同じ性質。`roleColorTokens.test.ts` が
ビルド成果物を走査したのと同じ形）。

**P4 で決めた 3 点**: 警告リストはコードに直書きし**判定には一切使わない**
（D6 の「警告リストは除外リストではない」を画面でも守る）/ 引数は**1 行 1 引数**で
編集する（空白区切りにするとシェルの引用規則が要り、「シェルを介さない」設計と
衝突する）/ 登録要求から作る下書きは**名前だけ**（何を固定引数にして何を
`allowExtraArgs` に回すかは利用者の判断で、機械が決めると閉じた許容の粒度が
勝手に決まる）。

**P1 完了**（2026-08-04）— `crates/fuseforks-core/src/command.rs`（型 + 純機構）+
`config_store.rs` の I/O 4 本。単体 15 + store 8 本、workspace 456 本全緑。
`schedule.rs`（型 + 純機構）と `config_store.rs`（I/O）の分業を踏襲。
**実装で決めた 7 点は Spec の「P1 実装記録」**。要点だけ:
**`load_commands` は `Err` を返さず空 + `dropped` で返す**（閉じた許容なので
「空 = 何も実行できない」が安全側。読めない登録簿で起動を止めない）/
**`Unavailable` は 2 値**（`ProgramNotAbsolute` / `ProgramMissing` — 理由ごとに
次の手が違う）/ **`mark_availability` は実在判定の述語を注入**（`apply_to` の
`template_exists` と同じ形。無効印は毎回付け直すのでパスを直せば復活する）/
**`get` は無効な登録も返す**（「登録が無い」と「登録はあるが使えない」は別の答え）。
**併せて `write_atomic` を切り出した** — temp + rename が `save_world` /
`save_schedules` に個別に書かれており、今回の 2 本で**同じ 8 行が 4 箇所**に
なるところだった。既存 2 本も寄せた。

**P0 完了**（2026-08-04）— `data_contract.yaml` へ `command_tool_contract`
（`file_tool_contract` の隣）。不変条件 15 本 + 置き場 2 ファイル。あわせて
`enabled_tools_invariant` へ**破壊的変更**を記録した（`null` の意味を
「全同梱ツール」から「既定集合 `DEFAULT_ENABLED_TOOLS`」へ変え、
`BUNDLED_TOOL_NAMES = DEFAULT_ENABLED_TOOLS ∪ {run}`。着地は P3。移行は不要で、
`null` の解釈が変わるだけで**既存個体は `run` を得ない**のが移行の目的）。

**P0 で既存の追従漏れを 2 件回収した** — 契約を書く前に隣の記述を読んだので出た:
`tool_extension_point` が同梱ツールを**「4 本」**のままにしていた（Spec 01 の
`sd` / `yq` と Spec 09 の `file` が入っておらず、**同じファイルの
`enabled_tools_invariant` は 7 本**＝ 1 つの契約ファイルの中で本数が 2 つあった）/
`specs/02` に Spec 09 の続報が無かった（Spec 09 Phase 4 は「Spec 02・07 の
本数記述へ続報を追記」と書いたが、**実際に入ったのは 07 だけ**）。
**一般化: 数の記述は、増えた名前で grep しても引っかからない。**
`file` で全台帳を引いても「4 本」「6 本」は出てこない — #51 の grep 規律は
**名前**を対象にしており、**名前の個数を述べた文**は網の外。集合に要素を足したら
`\d 本` / `\d 個` / `seven` のような**数量表現でも引く**。

起点は利用者の要望（2026-08-04）—「エージェントに python とか Lint、curl を
使わせたい」。同梱ツールは読む系と書く系で閉じており、**書いたものを走らせて
確かめる経路が無い**。

**rev1 は査読で拒否された。最も重い指摘は「2026-07-31 の利用者判断を、説明なく
反転している」。** README の「意図的に未実装の部分」に既に書いてあった —
「**Hook に登録したコマンドだけを実行できる**形。任意コマンド + 危険なものを弾く
除外リスト、ではない — **閉じた許容**であることが設計の本体」。rev1 の
「コマンド名の許可リスト」は開いた許容でその正反対。**しかも rev1 の Notes 4
（作業フォルダ境界が実効的に消える）は、README の同じ行の後半（「囲いは登録の側に
置くしかない」）と同じ観察で、台帳はそこから逆の結論を既に出していた。**
一般化は `failures.md` #56（**未実装の機能ほど、判断だけが先に散文へ落ちている** —
実装があれば判断はコードと契約に刻まれるが、実装が無い機能の判断は
「意図的に未実装」の表にしか残らず、そこは作業中に開かない場所）。

**rev2 の骨格は登録制**。利用者が `{name, description, program(絶対パス), args,
allowExtraArgs, timeoutSecs, cwd}` を登録し、エージェントは**登録名でしか呼べない**。
利用者の言葉「許可されたコマンドは JSON でリスト化して次は承認無しで使える」は
登録制でそのまま満たせる。

- **中核は「シェルを介さない」**（rev1 から維持）。実行ファイル + 引数配列で起動し、
  `cmd /c` / `sh -c` を経由しない。シェルへ 1 本の文字列を渡すと照合は文字列の
  パースでしか効かず、**パースで守る境界は必ず抜けられる** —
  `resolve_in_work_dir` が「パス文字列の検査」でなく「canonicalize 後の前方一致」を
  採ったのと同じ判断。**前例が無い道である自覚を Spec に書いた**（Claude Code /
  Cursor / Aider はいずれもシェル経由）
- **`program` は登録時に解決した絶対パスで、実行時に `PATH` を引き直さない。**
  rev1 は「判定は構造的に決まる」と言いながら PATH 解決に委ねており、
  **どのバイナリが走るかがその時の PATH で変わる**自己否定だった（査読 CRITICAL 1）。
  副産物として **Windows のコマンド名正規化問題**（`python` / `python.exe` /
  `PATHEXT` / 大文字小文字）が**問題ごと消えた**
- **配布の分離は置き場ではなく絶対パスで成立する。** rev1 は
  「`world.json` に入れず端末側」と書いたが、`world.json` も `{workspace}` 直下
  （`config_store.rs` の `root()`）なので分離になっていなかった（査読の見落とし指摘）。
  正しくは、**別の端末へ workspace を丸ごと配っても記録された絶対パスが存在せず、
  起動時の検証で落ちる** = fail closed が構造で成立する
- **未登録は待たずに拒否し、登録要求へ積む。積む先は永続化**（rev1 は in-memory で、
  非同期承認の根拠を保存層が否定していた）。**「初回は必ず失敗する」ことを認め、
  再試行の設計を明記した** — 自動リトライはしない / 予定発火は次回発火で通る
  （それが無人タスクの再試行機構）/ 人の依頼は人が出し直す
- **`run` は安全機構を増やさない。囲いは登録ただ 1 つ。** 作業フォルダも
  ごみ箱限定も環境変数の遮断も**境界ではない**、を契約と UI に書く。特に
  **`env_clear` の位置づけを訂正した** — rev1 の「渡さないことでしか塞げない」は
  塞げる範囲の過大表示で、**ファイル経路（`~/.aws/credentials` 等）は塞がらない**。
  残すのは事故の確率を下げるためであって秘密保護の境界ではない（査読 CRITICAL 2）
- **`run` を `WORK_DIR_TOOL_NAMES` に入れない**（査読 CRITICAL 3）。あの集合は
  「作業フォルダがあるときだけ提示し、かつその中しか触らない」ツールの集合で、
  境界を破る `run` を入れると集合の不変条件が壊れる。提示条件は別に書く
- **既定 OFF は破壊的変更として Phase 0 で凍結する**（査読 MAJOR 5）。
  `enabledTools: null` の意味を「全同梱ツール」から「既定集合」へ変え、
  `DEFAULT_ENABLED_TOOLS` を新設。rev1 は Notes に書いただけで Tasks にも
  検収項目にも無かった
- **プロセス木ごと kill する**（査読の指摘）。`tokio::process::Child::kill` は
  対象 PID にしか効かず**孫が残る**ので、rev1 の実機確認は実装方針では達成不能だった。
  Windows は Job Object、Unix は `setsid` + `kill(-pgid)`。依存を 1 本足す
  （`command-group`）— `trash` と同じ判断で、安全側の挙動を自前の unsafe で書かない
- **タイムアウトは登録ごと**（既定 60 / 上限 3600）。rev1 の 60 秒固定は
  `cargo build` / `pytest` を最初から締め出し、**Goal と正面から矛盾していた**
- **出力の分配を決定的にする** — `MAX_OUTPUT_CHARS`（12,000）のうち
  **stderr へ先に 4,000 字、残りを stdout**。割合ではなく固定字数にするのは、
  RepeatGuard が結果本文の完全一致で数えるため（打ち切り方が一致率に直結する）

**RepeatGuard の効きが落ちる**のも記録しておく — 判定は結果本文の一致だが、
`date` / `git log` / `curl` は同じ引数で違う出力を返す。**病理は「同じコマンドを
叩き続ける」形で現れうるのに歯止めは効かない。** 機構は足さない（何が異常かを
決められない）。実測で出たら Spec を切る。

**再査読で骨格は承認され、未決はゼロになった**（2026-08-04）。D6 =
`allowExtraArgs` を**持つ**（持たないと `ruff check <path>` /
`cargo test -- --nocapture` のように**対象が実行時に決まるコマンドが全滅**し
Goal を果たせない。機構で止めると*汎用インタプリタの一覧*という**除外リスト**を
作ることになり閉じた許容と衝突する）。**「警告リストは除外リストではない」を契約へ
明記する** — 除外リストは「載っていないものは通す」が、警告は「載っているものに
注意を促す」だけで許容の判定に一切関与しない。D7 = 個体ごとには**絞らない**
（必要になったら `commands.json` へ `scope: [agentId]` を足せる。今その欄を
作らないのは、空の欄は「何を入れるべきか」を利用者に問い続けるため）。

**rev3 で詰めた実装境界 6 点**（3 回目の査読）: (1) **提示の判定を「実行可能な登録」の
単位にした** — rev2 は「登録件数」と「`cwd` が決まるか」を別の列にしており、
**1 件でも `cwd` が決まればゲートが開き、決まらない登録まで列挙されていた**
（提示で防ぐはずのものが実行時の拒否に落ちる）。列挙も実行可能なものだけにし、
残りは件数 1 行 (2) **`args` → `extraArgs` へ改名**。登録簿の欄も `args` なので、
同じ名前だと「置き換えるのか足すのか」が読めない。実際に走るのは
`registered.args ++ extraArgs` (3) **`allowExtraArgs: false` への追加引数は
未登録とは別の文面**にし、登録要求には積まない — 同じ文面だとモデルは「登録待ち」と
読むが、実際は**引数を外せばその周で通る**（RepeatGuard が止めるのは 3 回目なので
2 回目までに直せる形にする） (4) **登録要求の欄は `attemptedExtraArgs`**、上限 50 件は
`firstRequestedAtMs` の古い順、**書き込みは temp + rename**（壊れたものをどう読むかより
壊れないように書くほうが効く） (5) **検証失敗は「無効化」であって「削除」ではない** —
ファイルを書き換えると、**配布した村を別の端末で一度開いて閉じただけで登録が消える**
(6) **fail closed が成立するのは `program` 自体の存在まで** — 子が呼ぶ孫は `PATH` で
引き直されるので、推移的な依存は閉じていない。

**再査読で詰めた未定義 4 点**: (1) 出力の分配は **stderr 4,000 / stdout 8,000 の
固定枠で融通しない** — 融通すると stderr の長さで stdout の打ち切り位置が変わり、
**RepeatGuard の完全一致が壊れる** (2) 登録要求は
**`{workspace}/command_requests.json` の別ファイル**（`commands.json` と同居させると、
不正 JSON の「空として起動 WARN」で**登録要求まで一緒に消える**）。`(name, args)` で
畳み、上限 50 件、時刻は引数で受ける (3) 提示は **2 段ゲート**（`enabledTools` に
`run` があるか × 登録 1 件以上かつ `cwd` が決まるか）。**設定でオンにしたのに
出てこない状態が起こりうる**ので、`AgentSettingsDialog` に理由を出す
（`noWorkDirWarn` と同じ形） (4) `program` の検証は**実在まで。実行可能性は見ない**
（判定の意味が OS で違い、確実に知る方法は実行してみるしかない。
**確かめられないことを確かめたふりの検査で覆わない** — 検査があると通ったものは
安全だと読まれる）。起動時と実行時の両方で見る（起動時だけだと、開いたまま環境を
入れ替えたときに素通りする）。

**査読で 1 件だけ反証した。** 村と検証役の両方が「`failures.md #41 の一般化 3` は
該当趣旨が無い。指すべきは処方 1」と報告したが、**実在する**（`failures.md:1319`、
「失敗を戻り値の型に載せていない層では、失敗を数える機構は必ず本文の一致で代用する
ことになる」）。`#41` の一般化は **1 → 2 → 4 → 5 → 3 の順**で 3 だけが末尾にあり、
行順の走査だと見出し（1225 行）の手前にある **#40 の一般化 3**（1221 行）を拾って
「範囲に無い」と結論する。**5 号も検証役も同じ走査をした。**
一般化は `failures.md` #57 — **独立検証の独立性は、人ではなく方法で数える。**
条例の Verify 段は「束ねに参加していない**サーヴァント**」＝*別の人*しか要求しておらず、
**別の人が同じ方法を使えば誤りは相関する**。しかも検証を通ったぶん確信だけが増え、
正しい 11 件が誤った 1 件に信用を貸していた。**条例へ 1 行足す候補**:
「同じ調べ方で確かめない。番号や行で引いた主張は、本文の語で引き直す」
（今回なら `grep "一般化 3"` でなく `grep "本文の一致で代用"` で一発だった）。

## 同梱ツールの機能追加（村のアンケート由来・2026-08-04）

ザリが 6 名へ `plan` で照会し集計した要望を、契約と突き合わせて絞った。
**票の多さでは決まらない** — 上位 2 件のうち 1 件は凍結済みの不変条件と衝突した。

- **grep の件数集計 — 実装済み**（`323e83a`）。新機能ではなく**契約違反の修復**
  だった。詳細は failures.md #55
- **`file read` の行範囲 — 撤回。** 私が足した案だったが、`data_contract` が
  「**read に offset / range は持たない**」を理由付きで凍結済み（「grep で既に
  書けるうえ『全部読む』を助長して入力トークンを増やす側に効く」）。
  **私の論拠と正面から逆**で、しかも grep の件数修正は「grep で既に書ける」側を
  強くしたので契約はより正しくなった
- **batch-sd（最多票 4 名）— 形を変えないと通らない。** `write_tools_contract` の
  「**1 呼び出し 1 ファイル**: インジェクション 1 発での改竄範囲を 1 ファイルに
  閉じる」と衝突する。**理由は利便性ではなくセキュリティなので票数では動かない。**
  逃げ道は**preview だけ複数ファイル対応にし、apply は 1 ファイルのまま** —
  不変条件が縛るのは書き込みの範囲なので破らない。10 ファイルで 20 呼び出しが
  11 呼び出しになり、「書き込み前に全差分を見る」という要求も満たす。
  **2026-08-05 に利用者判断で通り、[Spec 17](specs/17_batch-sd-preview.md) として
  起票 → rev2 承認 → P0〜P3 完了（残るは実機確認 3 件）。** 要望の 2 要素のうち
  **プレビューは Spec 01 から入っていた**（既定が preview）ので、欠けていたのは
  複数ファイルだけだった。実装は `sd` に `paths`（最大 20 件・**preview 専用**）を
  加算し、`run_multi` / `preview_one` / `render_multi` を新設。振り分けは `call` の
  1 箇所だけで**`run_multi` から `run_sd` を呼ばない**（単数を 1 要素の配列として
  通すと単数の出力書式まで動く）。テスト 469 → 483 本（`tools::edit` は 29 → 43）・
  既存 29 本は無改変・clippy 新規警告ゼロ。
  **査読が矛盾・曖昧 7 点を出し、うち 1 点は査読の処方のほうが契約違反だった** —
  `out.truncate()` で出力を文字数で切る案は「**diff 上限は切り詰めでなく拒否**」の
  凍結行を破る（diff が行の途中で終わり、モデルは「ここまでが変更点」と読む）。
  **rev1 の私の記述「diff 総量は打ち切り」も同じ違反だった。** 訂正は
  「**切るのは一覧であって diff ではない**」＝ファイル境界でだけ止め、各ファイルの
  diff は丸ごと出すか 1 字も出さないか。**一般化: 同梱ツールの既定の作法
  （上限に達したら打ち切って件数を書く）は読み取り系の作法で、書き換え系には
  別の凍結がある。** `grep` の規律を `sd` へ持ち込むときは、**どちらの契約の節に
  書かれた規律か**を先に見る（`failures.md` #58 と同型）。
  もう 1 点、**rev1 の「境界の外は全体を拒否・実在しないはスキップ」は実装できなかった** —
  `resolve_in_work_dir` は canonicalize 成功後に前方一致で外を判定するので
  **実在しない外側パスは「無い」として返り、戻りの `Result<_, String>` では
  区別できない**。処方は列挙型の新設ではなく、**`resolve` 段の失敗をまとめて
  全体拒否**（あの関数は 5 本が共有しており、1 本の新機能で呼び出し規約を変えない）。
  **P1 の実装でさらに 2 つ出た**: (a) **束ねの見出しに `---` は使えない** —
  unified diff のヘッダ行が `--- {ファイル名}` なので前置が衝突し、
  **束ねの見出しと diff のヘッダが区別できなくなる**（`===` へ変更。
  Spec 16 D2「一致行と文脈行を書式で区別する」と同じ形が別のツールで出た）
  (b) **凍結した「表示は初出の表記」は観測できない規則だった** —
  `./a.md` も `a.md` も同じ `a.md` へ正規化されるので初出かどうかで差が出ない。
  契約を「表示も正規化された相対パス」へ訂正した。**一般化: 規則を凍結する前に
  「その差が観測できるか」を確かめる**（Spec 14 D5 =「未決を立てるときは
  その形が実際にそうなっているかを先に見る」の凍結版）
- **`yq` に `op: "check"`（構文検証）** — 衝突なし（二段階実行は書き込み op のみで
  読み取り op は対象外と明記されている）。新ツールにしないのは、ツールを 1 本足すと
  スキーマ分のトークンが全員の毎ターンに乗るため
- **採らなかったもの**: 黒板の同時書き込みガード（条例の「1 人 1 ファイル」で衝突が
  構造的に起きない）/ 実行上限の緩和（#41 と衝突。**そもそも 12 は周回数であって
  ツール呼び出し数ではない** — 1 周に複数本を並列で呼べる）/ 検索結果キャッシュ
  （同じ呼び出しが違う結果を返すと RepeatGuard の判定が壊れる）/ 検索メタデータの
  自動付加（Spec 05 で `sources` が空と実測済み。存在しないデータは付けられない）/
  Langfuse・Helicone・Ragas（計器は `fuseforks.log` に既にあり、外部 SaaS は
  村の会話を外へ出す）

**アンケートの読み方で 1 つ**: ザリは「上限 12 回では最初から足りない」を
要望の構造的理由としたが、`max_tool_iterations` は **LLM の周回数**であって
ツール呼び出しの本数ではない（`orchestrator` の `executed_in_round`。
1 周に 2〜3 本を並列で呼ぶ）。**結論（batch-sd は要る）は正しく、理由が誤っていた。**
正しい理由はトークン — 周が増えるたびにプロンプト全体が再送され、diff が 1 本ずつ
履歴に積まれる。**6 名全員が「回数」の枠で考えており、誰も読み取りのトークン効率を
挙げなかった** — 検証役が居れば出た可能性のある欠落（条例の 4 点点検を自分で
やった回だった）。

## ライセンス（**MPL-2.0。2026-08-05 に relicense 完了**）

現在は **MPL-2.0**。**MIT → AGPL-3.0-or-later（2026-08-02）→ MPL-2.0（2026-08-05）**
の 3 つ目。差し替えたのは 4 箇所 — `LICENSE`（mozilla.org の原文 373 行）/
ルート `Cargo.toml` の `license`（下位 2 crate は `license.workspace = true` なので
自動追従）/ `apps/gui-tauri/package.json` / README 日英の「ライセンス」節。
当時は `package-lock.json` の自己エントリも `npm install --package-lock-only` で
追従させたが、**そのファイルは 2026-08-05 に削除した**（上記「リリースビルド」の節）。

**降ろした理由は利用者の言葉が正**（2026-08-05）—「AGPL はちょっと厳しすぎる。
厳密に管理したいのではなく、**ただ誰かがいい修正をしたら僕も使いたいというだけ**」。
AGPL はネットワーク越しの提供でも義務が発火し、組み込む側にも全体の公開を求めるので、
**道具として使ってもらうには強すぎた**。求めていた一点はファイル単位のコピーレフトで足りる。

**public にする前に差し替えたのが要点。** 公開後の relicense は取り消しが効かない —
公開した時点の版は AGPL のまま誰でも fork・複製でき、後から変えてもその複製は
AGPL のまま残る。**単独の著作権者でいられるうちに済ませる必要があった**
（v0.1.0 で public、v0.2.0 で公表、という段取りの手前）。

**Exhibit A のファイル冒頭注記は入れていない**（未決）。MPL は各ファイルへ
注記を置くことを推奨するが、`LICENSE` を同梱していれば「同じディレクトリの
LICENSE で代替してよい」と Exhibit A 自身が書いている。数百ファイルへ 3 行を
足す変更は、この村の doc コメントの書き方（ファイル冒頭は「何をする層か」）と
場所を争うので、**入れるなら別途決める**。

**relicense 前の実測（2026-08-04）。阻む依存は無かった**:

- `cargo license`（build / dev 依存込み・535 crate）で**第三者の GPL / AGPL /
  LGPL-only はゼロ**。`AGPL-3.0-or-later` に出るのは `fuseforks-core` と `concordia`
  （自分の 2 crate）だけ。`r-efi` の `Apache-2.0 OR LGPL-2.1-or-later OR MIT` は
  選択なので Apache / MIT を取れる。MPL-2.0 の依存が 5 本あるが同一ライセンスで無害
- フロント（`apps/gui-tauri` production）も MIT 65 / ISC 9 / BSD 4 / Apache 2 /
  Python-2.0 1 で全て permissive。`UNLICENSED` 1 件は**自分のパッケージ**
  `concordia-gui@0.1.0`（当時は `license` フィールドが無かった。その後 AGPL が入り、
  2026-08-05 に MPL-2.0 へ差し替え済み）
- 著作権者は単独（`git shortlog -sne --all` = K.Takahashi の 3 表記のみ）

**プラグイン DLL は却下（2026-08-04 利用者判断）。** 「同梱ツールをプラグイン化して
ライセンス外にする」という当初の動機は**成立しない** — AGPL のままなら動的リンクは
FSF の立場では派生物で逃げられず、MPL 2.0 にすれば**ファイル単位のコピーレフト**
なので利用者が新規に書いたファイルは最初から対象外（§3.3 が Larger Work を
自分の条件で配ることを明示的に許す）。**MPL 化はプラグイン化の動機を消す。**
加えて Rust に安定 ABI が無く、`AgentTool` は `async_trait` +
`serde_json::Value` + `CoreResult` の 3 つとも ABI 境界を渡せない。
**プラグイン機構は既にある — それが MCP**（`tools/mod.rs` の doc が
「外部の能力は MCP サーバー経由で足す」と最初から書いている）。

## Spec の状態

- [Spec 46](specs/46_acceptance-probe.md)（予定の後判定 — 検収の probe と、
  通るまでの再依頼）: **rev2 承認 → P0〜P2 完了**（2026-08-30。P0 =
  `data_contract` の `Acceptance` ブロック。P1 = コア + 結合 5 本・
  ミューテーション 3 回（D2 の直列は防御が二重で、1 層だけの変異は緑のまま —
  P1 実装記録の一般化）。P2 = 投影 2 欄 + **承認 3 箇所の拡張**
  （`retain_for` の生存集合に後判定を数え忘れると、掃除が検収の承認を落とし
  「人が押したのに unapproved」になる — 単体で凍結）+ ScheduleDialog の
  後判定の節 + 辞書 ja/en（検収の結末は `acceptanceOutcome` の別枝 —
  `probeOutcome.match`「一致（依頼しました）」は検収では嘘になる）。
  vitest 453・lib 616・clippy 0。**P3 台帳も完了** — README 3 言語の表へ
  「後判定」の行（165 / 166 / 161 行 — 上限 160 の意図の側で許容）/
  DETAIL 日英へ検収の節とディレクトリ木の追従。**grep 網の外**: ランディング
  ページは前判定までで嘘にならず（次のタグで更新）、Qiita 記事は
  スケジュールに触れていない。**残は P4 実機 6 件**）。査読で骨格が 2 つ精緻化された —
  「新しい根にしない」は不正確で**「新しい封筒・同じ財布」**（cancel /
  participants は試行ごとに新品、`Arc<BudgetPool>` だけ継承 = 封筒相乗りの
  既存形でフラグ不要）/ **前判定は発火時のみで再依頼は通らない**（凍結 6）。
  `expect` が承認ハッシュの外にあることを実装で確認し「承認の対象は実行で
  あって判定ではない」を明記（前判定も元から同じ性質）。起点は利用者
  「ワークの質を上げるのが Fuseforks の課題」+ Prime Agent の autonomous mode
  実読。骨格 = `ScheduledTask` へ `acceptance`（`ScheduleProbe` 再利用）+
  `maxAttempts`（既定 2）。因果の完了（`summarizeAfter` と同じ検知）で検収
  probe を走らせ、**`no_match` だけが再依頼を生む**（判定不能 3 値は
  「何もしない + 記録」= fail-closed。eigent 採点器の反面教師）。再依頼は
  発火時の**予算プールを継承**（Prime Agent の「予算 + 終了条件」の形 —
  `tokenBudget` が「通るまで」の天井）・**セッションは continue 固定**。
  範囲はスケジュールのみ（5 配送経路の他 4 つは別 Spec）。計器 `acceptance:` が
  完遂の軸の 3 本目（機械で決まる唯一の「成果物の検証」列）
- [Spec 42](specs/42_stats-period.md)（統計の「全会話」を月の集計にする — 締め日と、
  前の月への遡り）: **Done**（2026-08-19。**起票から Done まで同日**。P5 は利用者が
  実機で表示を確認）。詳細は上の「現在地（2026-08-19 更新）」が正
- [Spec 41](specs/41_model-pricing.md)（モデルごとの単価と、おおよその金額表示）:
  **Done**（2026-08-18。**起票から Done まで 1 日**。検収 9 件中 8 件を観測）。
  **取得先は `https://betyourluck.github.io/prices.json`**（GitHub Pages。出典
  `BerriAI/litellm` = MIT で、著作権表示は JSON 自身の `_notice`）。
  **2026-08-20 に Perplexity の 42 件を手で足した**（`perplexity/…` / `openai/…` の
  **プレフィックス付きキー**。それまでの LiteLLM 由来は 1 件も `/` を含まなかった）。
  村が送る `ModelTemplate.model` と直に突き合うのが照合規則なので、Perplexity 経由の
  モデルはプレフィックスごと鍵にする。**同じモデルでも経由で値段が違う** —
  `google/gemini-3.7-flash` は素の `gemini-3.7-flash` の**ちょうど半額**で、
  別エントリであること自体に意味がある。**総数は書かない**（#67 — 足すたびに腐る。
  「1,686 モデル」と書いた 2 日後にこの追加で嘘になった）。
  **鍵は素のモデル名**で、村が送る `ModelTemplate::model` と直に突き合う。
  **これがこの村で 1 本目の非 LLM 外向き通信**なので `PRIVACY.md` 日英に節を足した
  （押したときだけ / 変更・空にできる / GET のみ / **配信元に要求の事実と IP が残る**）。
  **「起動時に外部へ問い合わせません」は守っている** — 自動取得を作らないことを
  凍結し、`spawn` / `interval` / `HEAD` を持たないことと、起動経路が `fetch_table` を
  呼ばないことを**ソース走査で留めた**（ミューテーションで赤を確認）。
  **実機の 2 軸の被覆率が設計の裏付けになった** — `5/7 体・トークン 99.9%` が出て、
  **体数だけなら「3 割欠けている」、トークンだけなら「揃っている」と読める**。
  **片方だけでは嘘になる。**
  **次に触る人が要る 3 点** — (a) **`v-model.number` を使わない**（空文字を `0` に
  するので「未設定」が「無料」に化ける）。**ただし `type="number"` なら修飾子が無くても
  Vue は数値を渡す** — 文字列前提の setter が黙って死んで手入力が保存されない実バグが
  2026-08-19 に出た（`failures.md` #111。処方は `parsePriceInput(string | number)` の 1 実装） (b) **未設定は無視ではなく 1 段上へ落とす**
  （`cache_write_1h → cache_write → input`。落ちたトークンは素の入力に残るので
  **表が不完全でも合計は保存される**） (c) **`pricing.rs` は `budget.rs` を呼ばない**
  （実効は歯止めの単位、金額は請求の推定。繋ぐと片方を直すともう片方が動く）。
  以下は起票時の記録。起点は利用者 —「**おおよその合計コスト
  (金額)みたいなのは出したいね**」。**[Spec 39](specs/39_stats-view.md) D5
  「通貨に換算しない」を覆す** — 覆す根拠は**3 つの理由のうち 2 つが 3 日で消えた**こと
  （追従は「固定して取り込む」で選択になり、キャッシュの帯は Spec 40 の分解で
  **実測の内訳が帯の結果を含む**ようになった）。**残る 1 つ（通貨は「合っている」と
  読まれる）は撤回していない** — 「出さない」ではなく **`≈` と被覆率で近似だと
  分かる形にする**ことで受ける（D4）。**凍結を覆すときは凍結の側にも書く**ので
  Spec 39 D5 の見出しにも追記した。
  **利用者裁定 3 件** — 金額を出す / **欄は 4 + 1**（入力 / 出力 / キャッシュ読み /
  **キャッシュ書き込み** + `1h` 任意。**3 欄では今日測った 1.52 倍のずれが製品に入る**）/
  **取得は設定可能な URL（案 B）**。**これがこの村で 1 本目の非 LLM 外向き通信**に
  なるので、**`PRIVACY.md` の 2 文が嘘になる**（「開発者が運営する送信先はありません」/
  「利用者が自分で設定した接続先に対してだけ」）— P4 で 1 文足す。
  **「起動時に外部へ問い合わせません」は守る**（自動取得を作らないことを凍結）。
  **取得 URL は `world.json` に置かず `{app_data_dir}` の棚**（Spec 25 と同じ線引き —
  村を配ったとき、受け取った人の村が知らない URL へ取りに行ける状態を作らない）。
  **単価そのものは `ModelTemplate` に住み、村と一緒に配られる**（公開情報でテンプレートの属性）
- [Spec 40](specs/40_cache-write-accounting.md)（キャッシュ書き込みを数える —
  `cache_creation_input_tokens` が `prompt` へ畳まれている）: **Done**
  （2026-08-18。**起票から Done まで 1 日**。rev2 = 査読 4 点 / rev3 = otari の実読 /
  rev4 = 「未測定」が誤りだった / rev5 = D3 確定 / rev6 = 検収 7 / rev7 = P3。
  **検収 7 件のうち 6 件を観測し、1 件は実測が前提を覆した**）
  （2026-08-18）。起点は利用者 —「API のトークン単価のサイトを作り、そこから JSON で
  だいたいの使用料を算出させたい」。**`anthropic.rs` の `usage` の写しが 3 つの量を
  2 つの欄へ落としている** — `prompt = input + cache_read + cache_creation` と足した上で
  `cache_read` だけを別に持つので、下流の「未キャッシュ」は **1.0× と 2.0× の混合**になる。
  **倍率が 2.0× なのは `CACHE_TTL = "1h"` を送っているため**（5 分なら 1.25×）。
  **rev1 の「6 ワイヤのうち書き込みを持つのは Anthropic だけ」は誤りだった** —
  根拠が「*こちらの型に欄が無い*」だけで、**相手が返さないことを 1 度も測っていない**。
  **`deny_unknown_fields` は 6 ワイヤのどこにも無い**ので、返っていても黙って捨てている。
  **一般化: 「相手が返さない」を、こちらの型に欄が無いことで結論しない** —
  未知の欄を落とす層では、その 2 つは観測上まったく区別が付かない。
  査読は「GPT-5.6 以降の OpenAI は `cache_write_tokens` を返し 1.25× で課金」と報告した
  （**一次資料には当てていない**）が、**この村は `gpt-5.6-terra` を実際に走らせている**ので、
  欄を足して 1 ターン回せば文書を読まずに真偽が出る（rev2 の検収 7）。
  **`cache_creation` はどのログ行にもレコードにも出ていない**ので**既存のログでは
  大きさが測れず**、そのため **P2 を「測る」だけの Phase として置いた**（#47 の
  「頻度を見てから」を*量*へ当てた形）。**最も重い決定は D3 = 予算の重みを変えるか** —
  変えると既存の村の `tokenBudget` の意味が黙って変わる（同じ 1M で早く尽きる）ので、
  **本 Spec では変えず、契約に「実効トークンは書き込みを 1.0× で数えている」と明記する**
  案を推している（rev2 で文面を引用ブロックとして固定 — 書かないと P2 で数字が出た
  時点で議論が蒸し返される）。**書き込みは初回だけではない** — `place_message_breakpoint`
  が毎ターン境界を打つので、`cache_creation` はターンごとに立つ。
  **rev2 で D6（5m / 1h の内訳）も反転した** — rev1 は「`CACHE_TTL = "1h"` 固定なので
  倍率は 2.0× で一意」として見送ったが、これは**「こちらが要求した TTL」と「相手が実際に
  作った TTL」を同一視していた**。**要求が通っている保証は結果の側にしか無い** —
  この村は `EXTENDED_CACHE_BETA` で同じ形を既に踏んでいる（ベータ名を送らないと `ttl` が
  黙って無視されて 5 分に戻る）。内訳を持つコストは `u64` 2 つ、持たないコストは
  **後から遡れない**（畳んだ値は復元できない = この Spec が扱っている失敗そのもの）。
  **査読の「ストリーミングの二重計上」は反証した** — `stream` / `message_start` /
  `message_delta` が 6 ワイヤで 0 件で、この村は非ストリーミングのみ（Spec 37 の実測以来）。
  **入れた瞬間に発火する**指摘なので、Spec 40 の Notes 4 に検収項目の種として残した。
  **見落としの機序は #62 / Spec 32 P0 と同型で、向きが逆** — あちらは「捨てている行」で
  数えて*足し込んでいる* Gemini を落とし、こちらは`prompt` へ*足し込んでいる*ので
  「捨てている行」を探す走査にも `dropped` の計器にも出ない。**一般化: 数え落としは
  「捨てている場所」ではなく「畳んでいる場所」に住む。畳みは情報を消すが、消したことを
  何も記録しない**ので、捨てた回数を数える計器（#72 の処方）を置いても捕まらない
- Spec 39（統計画面 — ターンの記録を `sessions.redb` の 4 種別目に留め、全画面で読む）:
  **Done**（2026-08-16。**起票から Done まで 1 日**。実機の検収 6 件すべて観測。
  P0 は main 直、P1〜P4 はブランチ `20260816_stats` で積んで合流、rev3 で実機修正）。
  **rev4（2026-08-18）で `by_agent` の鍵を `agentId` 単独から `(agentId, model)` へ変えた** —
  旧実装は `model` を最後のターンで上書きしており、**モデルを切り替えた個体の全ターンが
  最後のモデルの行に畳まれていた**。外部の価格表を当てると金額が狂う（単価はモデルごとに
  違う）。**器は最初からあり、潰していたのは集計だけ**なので `Record::Turn.model` から
  遡って割れる。帰結として **`agentId` は `by_agent` の中で一意でなくなる**（`:key` に
  使えない）。**「いま何で払っているか」は `AgentSnapshot.model` = AgentCard が持つので
  失われない** — 統計は履歴、カードは現在で層が違う。詳細は Spec 39 の
  「D3 改訂（2026-08-18・rev4）」。**次に触る人が要る 3 点** —
  (a) **`TurnContext.started` がターンの開始**で、`TurnHandle.requested_at` は割り込みの
  要求時刻（起票時に取り違えていた。P1 実装記録）(b) **`settle_turn` が 4 出口の
  1 実装**（累計 / `Record::Turn` / `TurnRecorded` / `turn:` の 2 出口の行）。累計の
  加算を 4 出口から移したので、出口を足すならここを呼ぶ (c) **`budget_stop_reason` が
  `budget stop:` 行と `TurnStop` の 2 値を同じ判定から返す**（`budget_reserve_wiring` は
  「書式 1 実装 + 呼び出し 2 箇所」で数える）。フロントは `App.vue` の `view` 1 つで
  3 ペインを `v-show`、`StatsView` を `v-if`。**入口は StatusBar（時計の左の
  グラフアイコン。緑で発光）** — TitleBar は「ダイアログの入口だけ」の列なので、
  面ごと差し替える 1 つを混ぜない（2026-08-16 実機裁定）。統計中は TitleBar の
  ダイアログ入口 6 つが `disabled`（窓操作は残る）。**`failures.md` #107 を派生** —
  `v-show` のグリッドの中に全画面の層（トースト・確認・ダイアログ）が居たので
  統計画面で描画されず、**確認が出ない = 窓が閉じられない**状態が出来ていた。
  **利用者査読 9 点 → 採用 5 /
  訂正して採用 3 / 反証 1。**反証したのは前提の側** — 「新版が旧データを読めなくなる」「`sessions.redb`
  は会話ごとに 1 ファイル」「`model` は `Option`」の 3 つはコードで否定し、
  指摘の趣旨（互換の向きを契約に書く / `Scope` を独立ブロックへ /
  `completion` の引き算を書く側で 1 箇所に）は採った。**rev1 で私が外したのは
  更新性** — 「全ターンでイベントを撒くのは払いすぎ」と書いて 10 秒 pull にしたが、
  `AgentStatsUpdated` は稼働中の個体ごとに毎秒飛んでおり、ターン末の 1 通は
  その 1/数十以下。`TurnRecorded { agentId, sessionId }` を新設して統計を開いている
  間だけ取り直す形へ反転した）。裁定済み 3 点を取り込んだ —
  **3 ペインを丸ごと差し替える全画面**（ダイアログでもタブでもない。TitleBar と
  StatusBar は残す）/ **通貨に換算しない**（実効トークンは `budget.rs` の 1 実装）/
  **器は `Record::Turn`**（`turn:` 行と同じ構造体から書く 1 実装 `settle_turn`。
  `stop` は閉じた列挙 7 値、`model` 欄を持ち **`turn:` 行にも `model=` を末尾へ足す**）。
  **費用の層だけ**（otari 側）— pass rate / LLM-judge（promptfoo 側）は
  合格条件を持たないこの村では空の欄になるので**別 Spec の材料**として Notes に置いた。
  **過去の会話は遡れない**（数字が保存されていない）ので、記録の無い会話は 0 の表を
  出さず「記録はこの版から」の 1 行にする（D6）。D0 に「作らない = `turn:` 行を
  grep する」を生きた選択肢として残してある
- Spec 38（予算の予約 — `try_reserve` を load から CAS 予約へ）:
  **rev3 → P0〜P5 完了 = Done**（2026-08-15。**実機の検収 4 件すべて観測**）。
  起点は実機ではなく **TLC の反証**（`failures.md` #105）。査読 2 系統
  （採用 14 / 訂正して採用 3 / **反証 2**）+ 承認査読の磨き 5 点。

  **`reason=reserve_short` の 1 行が本命**（`ceiling=13000 remaining=1566`）—
  **旧実装には原理的に存在しない行**なので、出たこと自体が予約経路の証明になり、
  同時に **D3 で System 行の文言を変えた理由の実物**（旧文言「使い切った」は
  残額 1,566 のこの瞬間に嘘）。**同じ因果で 2 値が並んだ**のも観測できた
  （波の中で `reserve_short` と `exhausted` が別々の個体に出た）。

  **P5 で天井を 4 回外した。** 毎回「1 呼び出しの実効」を**単一の数字で
  代表させた**誤りで、実効を動かす軸は 3 つ（**個体**のシステムプロンプト /
  **キャッシュ率** / **ラウンド数**）。同一個体でもキャッシュの有無で 2 倍、
  個体が違えば 3 倍動く。**天井は「1 呼び出し × 何回」ではなく
  「その個体のターン 1 本 × 何ターン」で考える** — DETAIL の推奨値
  （6 体で 1,000,000）はターン建てで書かれており、そちらが正しい単位。

  **代償は波の欠け**（過大予約で落ちた個体は再依頼されない）。
  **初回床の限界に数字が付いた** — 床 1,000 実効に対し実測の 1 呼び出しは
  個体により 3,000〜20,900 で、**起動後 1 回目だけ 3〜20 倍の過小予約**。
  床を上げるかは P0 契約の変更なので本 Spec では触っていない。

- Spec 37（Meta の Responses ワイヤ — web 検索と 4 種別の添付）:
  **rev2 承認 → P0〜P5 完了 = Done**（2026-08-13。**実機の検収 5 件すべて観測**）。
  **probe 24 発を先に撃ってから起票した**（文書が 4 回続けて実装と食い違っている）。
  査読 8 点 → 採用 6 / 訂正して採用 2 / 反証 0。詳細は上の「現在地」と Spec が正。

  **P5 が凍結を 1 つ反証した** — 「受理集合は列挙されない（第 3 の様式）」は
  **型の層でしか成り立たない**。`reasoning.effort: max` の 400 が
  6 値を全列挙し、Spec 34 の手筋がそのまま効いた。**部分集合で観測した性質を
  集合全体の性質として書いていた**（#91 と同族）。同じ誤りが**コード doc /
  CLAUDE.md / data_contract / Spec の 4 層に写っていた**ので 4 箇所とも直した。

  **実バグ 1 件を実機だけが出した** — 「最大」を選んだ Meta の個体が
  400 で**個体ごと落ちる**（`max` の段が Meta に無い）。`meta_effort` で
  `xhigh` へ写した。**丸めではなく同義への写像**で、落とす先はプロバイダの天井。

  **検収の書き方の誤りが 4 例目**（#68 / #80 / #85 に続く）— 検収 5 に
  「本文が空になる」と書いたが、**アプリは切り詰めを `LLM_OUTPUT_TRUNCATED` へ
  名前を付けて返す**ので空にならない。**probe で見た症状を、アプリの層を通した
  後でも同じ形で出るか数えずに書いた。**

  **D2 の分割線が最も重かった** — Spec 34 D2 の「共有するのは要素の型が同じだと
  実測しているから」という前提が、Meta の `input_audio` / `input_video` で崩れた。
  着地は**関数ポインタを adapter が渡す形**（`encode(messages, part_for)`）。
  `part_for` は **`carries` を読まない**ので、`adapters_match_the_carries_table` が
  同語反復にならず網が生きたまま残る。

  **ミューテーションで網 2 本の役割が実証できた**（予測を 1 つ外した）—
  adapter だけを変えると一致テストが赤、**adapter と表を同時に変えると一致テストは
  緑で逐語凍結だけが赤**。「両方を同時に変えると通ってしまう」を塞ぐために
  逐語凍結を別に置いた理由が、そのまま出た。
- Spec 36（多モーダルの添付 — 音声・動画・PDF）: **rev2 承認 → P0〜P4 + P6 完了。
  残は P5 の README 日英 / DETAIL 日英**（この CLAUDE.md と `data_contract` は書いた）。
  起票から実機まで 2 日。**査読 11 点 → 採用 8 / 訂正して採用 3 / 反証 0**。
  詳細は上の「現在地」と Spec の側が正。

  **訂正して採用した 3 点が設計を決めた** — (a) D4 はワイヤ別テーブルではなく
  **種別 1 門を最狭基準で下げる**（宛先で通ったり落ちたりすると規則が読めない）
  (b) D5 は `Option` 化ではなく**入口保証 + テスト凍結**（redb の既存レコードと
  非互換になる）(c) D11 はクォータではなく**許容の明記 + 計器の種別内訳**
  （頻度が未知なので観測が先 — Spec 34 D8 の「頻度 1.0 に頻度ゲートを当てた誤り」の逆側）。

  **機械の網は 3 つ**: `tests/carries_table.rs`（**全 20 マスで述語と adapter が
  一致** + **表を逐語で凍結** — 一致だけ見ると両方同時に変えたとき通る）/
  `carriesTable.test.ts`（**TS の表が Rust の凍結表と一致**。鎖は
  TS ← 凍結表 → `carries` → adapter）/ `attachment.test.ts` の `routeAttachment`
  （P4 の退行の回帰）。**すべてミューテーションで赤を確かめた。**
- Spec 34（OpenAI の Responses ワイヤ — 思考の要約・web 検索・#77 の解消）:
  **rev6 → P0〜P4 完了。残は P5 の実機**（2026-08-11〜12）。
  **査読 2 系統 19 点**（利用者 15 = 論理 9 + 技術 6 / 村 4）→
  **採用 15 / 訂正して採用 3 / 反証 1**。**P0a の probe 11 発**で未測定 11 点を
  すべて閉じた（当たり 6 / 外れ 2）。

  **査読で最も効いたのは probe の順序**（技術 T3）— `reasoning.summary` は
  未検証の組織で 400 になるので、rev1 のように 2 番目へ置くと
  **関数往復まで巻き添えで未測定**になる。最後へ回した。

  **反証 1 件は「D6 が欠落している」** — 実在した（写しが途中で切れていた）。
  **正しい観察から誤った結論**が出た形で、処方は「Spec を査読へ出すときは
  渡した写しの行数か見出しの一覧を添える」。

  **実測が文書にも査読にも無い量を 3 つ持ち帰った**: 検索ツールは提示するだけで
  input が 4,434 増える（2 回目以降はキャッシュで約 1/10）/ `web_search` と
  `web_search_preview` は**別名**（input が 1 バイトも違わない）/ `mode: pro` にも
  固定費 +1,538。

  **実機（2026-08-12 00:23）で Goal 3 が対で決着** — 同じ個体・110 秒差で
  Responses が `rounds=5 reasoning=131`（ツールを 6 回呼びながら思考）、
  互換が `rounds=1 reasoning=0`。**要約もツール併用回で 3 件 1,308 字**出ており、
  **D4 の「ツール併用回は要約 0 字が大半」は Anthropic の非対称を写した誤り**だった。

  **開いた観察 1 件**: 思考 131 トークンに対し要約 1,308 字（英文 300 トークン超）。
  **Spec 33 の「忠実度」は要約が思考の部分集合であることを前提にしている**が、
  OpenAI では別途生成されている可能性がある。1 観測なので機構は足さない。
- Spec 33（思考の要約を受け取って表示する — ネイティブ 3 ワイヤ）:
  **rev3 → P0〜P5 完了 = Done**（2026-08-11。**起票から Done まで 1 セッション**。
  査読 8 点 → 採用 5 / 訂正して採用 3 / 反証 0 → rev2 → 範囲拡張で rev3）。
  **範囲はネイティブ 3 ワイヤ。OpenAI 互換だけが Spec 34**（数と本文は別の口 —
  `/v1/chat/completions` で数は取れるが本文は来ない。Spec 32 の
  「4 社で数える」と矛盾しない）。

  | 社 | 送信側 | 既定で本文が |
  |---|---|---|
  | **xAI** | 変更なし | **来る**（`summary:"auto"` は効果ゼロを 4 対 4 で実測） |
  | **Anthropic** | `thinking: {"type":"adaptive","display":"summarized"}` | 来ない（5 世代の既定は `omitted`。`signature` は 368〜16,328 字あるのに本文 0 字） |
  | **Gemini** | `generationConfig.thinkingConfig.includeThoughts: true` | 来ない（`thought` part がそもそも返らない） |

  **3 社とも「要求しなくても思考ぶんは払っている」**（Gemini で実測 3,773 トークン）
  ので、**トグルにせず常に送る**方針 — 「払っているのに見えない」設定を
  利用者が作れてしまう。**要約は 3 社とも英語で返る**（問いが日本語でも）。
  **「要求しても返らない回」がある** — xAI の短形 / Anthropic のツール併用回
  （`thinking_tokens=34` で要約 0 字）で、**実運用の大半は後者**。

  **rev2 の要点は「同じ語で 2 つの物を指していた」3 件の分離** —
  `履歴に積まない`（表示物 / ワイヤの往復物）/ `runtime 計測は要らない`
  （証明 / 傍証）/ `空`（0 字 / 内容が薄い）。**語を分けると決定も分かれた。**

  **rev3 は Gemini の数え落としの回収で、Spec 32 rev2 と同じ機序を
  同じセッションで 2 回踏んだ** — どちらも**「新しい作業が要る社」で列挙し**、
  どちらの形にも見えなかった Gemini が網の外に落ちた。
  **「対象で数える」（ネイティブ 3 ワイヤ）なら両方とも防げていた。**

  **P1（xAI）は probe を打たずに決着**した — `encode` が `reasoning` item を
  1 度も送り返しておらず、実機に `rounds=6/14` のターンが在る
  （6 周とも `kinds=reasoning:1` を落として完走）。**本番の実績が probe より強い**
- Spec 32（思考トークンを数える — `reasoning` を `completion` から分離）:
  **rev3 → P0〜P3 完了 = Done**（2026-08-10。**起票から Done まで 1 セッション**。
  査読 8 点 → 採用 6 / 訂正して採用 1 / 反証 1）。**範囲は「数える」まで**で、
  要約本文は Spec 33、OpenAI の Responses ワイヤは Spec 34 へ決定として引き渡した。
  **`failures.md` #92 を派生。**

  **P4 の実機で決定的な対が取れた** — 同じ個体・同じプロバイダで、
  P4 前が `dropped kinds=thinking … output_tokens=1743` + `reasoning=0`、
  P4 後が `dropped kinds=thinking … output_tokens=1832` + `reasoning=515`。
  **両方が思考しているのに前は 0 を出していた** = その 0 が観測ではなく
  定数だったことを、同じログの 2 行が証明している。**#72 は例外的な事故ではなく、
  常時起きていた**（Anthropic の思考ぶんをこの村はずっと払い続けていた）。

  **この Spec で私は 4 回外している。4 つとも「実装ではなく、数え方・書き方」**:
  (1) rev2 が **Gemini を数え落とした**（「捨てている社」で数えたので、
  **足し込んでいる**社が網の外。欄名 `thoughts_token_count` は
  `Thinking` / `reasoning` の grep にも出ない）(2) 検収 2 の対照に
  `gemini-3.5-flash-lite` を置いていた（Gemini なので 0 の保証が無い = #68）
  (3) **検収 1 の「差 3〜5」が probe のプロンプトの産物だった**（#92）
  (4) **「Anthropic は欄が無い」を、相手のワイヤではなく自分の型を見て判定した**
  （#93。同じコミットで 3 社は先にワイヤを確かめており、**1 社だけ手順が逆**だった）

  **検収の対照は 3 度失効した**（Gemini → OpenAI 互換の推論モデル → Anthropic）。
  いずれも「0 が出る」の根拠が**測る前の思い込み**で、**実測で 0 だったのは
  `gpt-5.6-terra` だけ**（#77 でツールを送る周は思考が止まる）。
  **「対照になる」と思ったものは、対照になることを先に測る。**

  **実機で 1 つ裏返った** — rev3 の「OpenAI 互換は 0 が正しい可能性が高い」は
  誤りで、**本文は来ないが数は来る**（`muse-spark-1.2` で `reasoning=1072`）。
  **「受け取れない」と「数えられない」は独立**だった。同じ走行の
  `gpt-5.6-terra` は 0 で、**互換かどうかではなく接続先とモデル次第**
- Spec 31（Grok の Live Search — `/v1/responses` ワイヤと「固有スキル」）:
  **rev2 承認 → P0〜P5 完了 = Done**（2026-08-10。**起票から Done まで 1 日**。
  査読 7 点のうち 3 点を**対の再 probe で決着**させ、`no_inline_citations` の
  常送で「本文の無汚染」と「出典の網羅」を同じ 1 手で取った）。
  **`failures.md` #91 を派生**（既知の値の集合へ 1 つ足したら、その集合を読む
  述語が 2 つとも意味を変えた）
- Spec 28（スケジュールの前判定と前後処理 — probe / sessionMode /
  summarizeAfter）: **rev3 承認 → P0〜P5 完了 = Done**
  （実機確認 8 件すべて観測。2026-08-08。起票から
  ここまで 1 日・査読 2 巡。コミット `9ff8dc6` / `d32fbbb` / `76401ca` / `73564d6` /
  `a51c8d4` / `8aab9f4` / `764801a` / `3ce4ae6` / `fb20c5e` / `4353f89`。
  workspace 673 全緑・clippy 警告ゼロ）。
  要点は上の「公開までのマイルストーン」表の 2 + 4 の行が正。

  **P2a で設計が 1 つ変わった** — **前判定つきの予定だけ経路が別になる。**
  `schedule_tick` は**ティッカーの `select!` の中で await されている**ので、
  前判定をそこで待つと `timeoutSecs`（上限 3600 秒）のあいだティッカーごと
  止まり、同じループが担う `AgentTyping` の排出まで止まる（`in_flight` が
  掃除されず受信が lag して fail open を誘発する）。**1 件の監視スクリプトが
  村中の予定を止める形**。着地は「前判定なし = tick の中で束ねて配送・配送
  できたときだけ消化」「前判定あり = 決めた時点で消化して切り離す・結末が
  出た順に独立して配送」。**D6 の「同一 tick は 1 セッション」は前判定なしの
  側でだけ成立する** — 契約の該当行へ条件を足すのは P2b の宿題。

  **既存テストがバグを捕まえた**（`fires_once_with_origin_prefix`）—
  `deliver_batch` が消化する予定時刻を `Local::now()` から引き直していた。
  tick の `now` は引数で受け取っており実時計とは限らない。**一般化: 判定で
  決まった値は判定から運ぶ。後段で計算し直せる形にすると、入力が変わった
  ときだけ食い違う。**

  **P2b も同日に着地**（`73564d6`）— `summarizeAfter` の対象「待って完了した
  個体」を、**因果の参加者集合を封筒で運ぶ**形にした（`Envelope` へ 1 欄 +
  `deliver_and_wait` が**答えを受け取った瞬間だけ**書き込む）。
  **予算プールに相乗りさせない** — `handoff` も同一の予算 Arc を継承するので、
  予算の伝播で代用すると `handoff` 先まで対象に入る。完了の検知は
  `AgentTyping { active: false }` に相乗り（別の完了検知を作ると片方だけが
  取りこぼす形が生まれる）。`summarize_session` の本体も `Shared` へ下ろして
  `summarize_agents(only)` へ割った。

  **P2b でテスト側の誤りを 1 つ踏んだ** — `drain_until_quiet` の静穏窓を
  1200 ms にしたが、**`stats_interval` が 1 秒**なので統計イベントで
  **窓が原理的に閉じない**。**一般化: 「静かになるまで待つ」テストの窓は、
  その系で最も短い定期イベントの周期より短くする。** 長くすると「失敗」では
  なく「永久に返らない」として現れ、**実装のデッドロックと区別が付かない**
  （実際まず実装側を疑った）。

  **P2c も同日に着地**（`a51c8d4`）— `probe_approvals.rs`（GUI 層・単体 7 本）+
  `AppState` への保持 + IPC 2 本（`create_schedule` の `options` /
  `approve_schedule_probe`）+ フロントの型とワイヤ。
  **`only_hashes_are_written_to_disk` が守っているものが本体** —
  原文を書くとこのファイルが「この端末で実行できるコマンドの一覧」になる。
  Windows で ACL を組まない判断（`mcp_server.rs` と同じ）は、
  **守る対象そのものを消してある**ことが前提。
  掃除は `retain_for`（**時計で切らない** — 判定に壁時計を持ち込むと
  テストが時刻に依存する）。保存の並びは固定（`HashSet` の反復順のまま書くと
  同じ内容で保存し直すたびにファイルが変わり、**差分が嘘をつく**）。

  **P3 も同日に着地**（`8aab9f4`）— `ScheduleDialog.vue` へ前判定の 5 欄 +
  新しい会話 + 完了後の要約 + 直近の判定 + 承認導線、辞書 ja/en（`schedule.*`
  が 29 → 61 鍵）。**表示規則は `lib/scheduleProbe.ts` の純関数**（単体 11 本）。
  併せて**コア側に直近の判定の器を足した**（`ScheduleRuntime.last_probe` /
  `probe_reports()` / `ScheduleView.lastProbe`）— D8 が `error` / `timeout` を
  「直せる沈黙」と書いている以上、どこにも出さないと「動かないが理由が
  分からない」になる。**プロセス寿命**で `schedules.json` へは書かない。

  **P3 でミューテーションが自分のテストの穴を出した**（Spec 25 P3 の再発）—
  `probeDisplay` の理由の分岐を外す変異に**予測「1 本」対 実測 2 本**。
  辞書キーを検査するテストが `toEqual` で `reason` まで固定しており、
  **理由の規則に寄りかかっていた**。分離して入れ直し、予測どおり 1 本を確認。
  **`toEqual` は全欄を固定するので、この罠を最も作りやすい書き方。**

  **P3 で辞書のエスケープを 1 度壊した** — ヒアドキュメント経由の `
` が
  実改行へ化けて JSON が壊れた。処方は `json` モジュールで組み立てること
  （**往復がバイト等価であることを先に確かめてから**書き換えたので、
  触った鍵以外に差分は出ていない）。**一般化: 生成した文字列で構造化ファイルを
  書き換えるときは、パーサへ通すまで「書けた」と言わない。**

  **P4 完了**（2026-08-08）。README 日英（「何ができるか」表 / 画面構成表 /
  ディレクトリ木 3 箇所 / workspace 木の `village_id` / `{app_data_dir}` の棚 /
  前判定と前後処理の節）+ `data_contract` の回収 2 件。
  **回収したうち 1 件は P2a が「P2b で書く」と自分で宿題に書いて落としていたもの**
  （同一 tick の束ねが**前判定なしの側でだけ**成立する条件）。
  **一般化の再確認: 「次の Phase で書く」と決めた契約の追記は、その Phase の
  Tasks に書かないと落ちる** — 実装記録に書いても、次の Phase は別の節を読む。

  **P5 は検収 1・2 を観測（2026-08-08 07:56〜08:00）。残 6 件。**
  一致は `outcome=match` の **19 ms 後**に `turn start: … chars=25`（計算と
  1 文字も違わない）。不一致は `outcome=no_match` が 2 回出て**その間に
  `turn start:` が 1 本も無い** — しかも **2 回目が 61 秒後**なので、
  1 分ごとの予定が**不一致でも消化している**ことまで同じ 2 行から読める。

  **実機が設計判断を 2 つ裏付けた。どちらも単体テストでは「通ることの確認」
  止まりだった:** (1) **CRLF の除去が主経路だった** — `stdout_chars=8` に対し
  `expect='GoMolt'` は 6 字で、Python の `print` が出した `GoMolt

`。
  **`
` を落とさなければ Windows では永久に一致しない** (2) **承認鍵に
  `expect` を入れない判断が働いた** — 同じ `python dummy.py` を使う予定 2 件に
  対し承認の鍵は **1 件だけ**で、両方走った。

  **`fresh` の切り替えはログに 1 行も出ない**（`open_session` は `note!` を
  持たず、`session:` の行は起動時だけ）。画面では分かるので実害は無いが、
  **ログだけでは読めない**。

  **同日 08:06〜08:11 に検収 3・7・8 も観測（残 3 件）。**
  **検収 3**: `outcome=match exit=1` の 10 ms 後に `turn start:` —
  **rev1 で書いて査読で撤回した「exit 非 0 は不発火」の逆が実機で通った**。
  `stdout_chars=9` は**観測前に書いた予測**（`CHANGED

`）と 1 文字も違わず。
  **検収 7**: `schedule summarize: root=agent_2 agents=1` — **9 体稼働中なのに
  畳んだのは因果の根 1 体だけ**。参加者集合を封筒で運んだ判断（予算プールに
  相乗りさせない）が実機の負の対照として読める。
  **検収 8**: 利用者が `echo CHANGED` を置いて `reason=not_found` を踏み、
  **その 1 語が診断を解いた**（真因 = Windows の `echo` は `cmd.exe` の組み込みで
  単体の実行ファイルが無い）。**D8 の「直せる沈黙を作らない」が書いた当日に効いた。**

  **躓きの形（次に同じ質問が来る）**: 前判定はシェルを介さないので、
  **シェルの組み込み（`echo` / `dir`）は指定できず、`;` / `&&` / `|` /
  `$(...)` も効かない**。Windows で最も確実なのは `python` + `-c` + 1 行のコード。
  **UI / README への追記は今は入れない**（躓きが 1 回。頻度を見てから決める）。

  **同日 16:41〜17:01 に検収 4・4' も観測（残 2 件）。** 手で置いた
  `schedules.json` に対し、**同じ 1 本の予定がコマンド行を 1 文字も変えないまま
  3 回状態を変えた** — 未承認 `unapproved`×2 → 承認 `no_match`×5 →
  **`village_id` だけ差し替え** `unapproved`×4 → 戻すと `no_match`×2。
  **決め手は 2 つ**: (a) **前判定コマンド本人にマーカーを書かせた** ——
  `schedule probe:` の行だけでは「実行してから捨てた」実装と区別が付かないが、
  マーカーが無いことは走っていないことそのもの（`resolved=-` も同じ向き）
  (b) **4' で `probe_approvals.json` が 1 バイトも変わらない** ——
  `ab21d14e…` は残ったまま新 salt の鍵 `39897bf5…` が無いだけで、
  **承認は消えていない・届かなくなっただけ**。差し替え防御が「記録の消去」ではなく
  「鍵の付け替え」で成立していることが読める。**再起動だけでは失効しない対照**も
  同じ走行で取れているので、4' を「再起動したから」と読む別解釈が消える。
  **承認鍵は観測前に外から計算して当てた**（canonical 形の裏取り）。

  **踏んだ落とし穴**: **前判定は宛先が稼働していないと到達しない。**
  `Tick::Fire` は probe より先に `mailboxes` を見て、停止中なら
  **`shared.record`（会話ペイン）で消化して `continue`** する — `note!` ではないので
  **`fuseforks.log` は完全に沈黙し、`lastConsumedDueMs` だけが動く**。
  **ログの沈黙には理由が 2 つある**（未承認で落ちた / probe に到達していない）。

  **後始末で穴が 1 件出た** — 予定を削除しても承認が落ちなかった。
  `retain_for` の呼び出し元が `create_schedule` だけで、doc の
  「保存のたびに呼ぶ」「予定を消せば承認も消える」が**2 文とも嘘**だった。
  **重いのは肥大化ではなく、承認を取り消す経路が他に無いこと** —
  「取り消したつもり」の状態が残る。処方は `delete_schedule` へ掃除を足し
  （**順序は create と逆** = 予定を先に消す）、配線を
  `tests/probe_approval_pruning.rs` が**ソース走査**で留める
  （`AppState` を組む結合は Tauri の `State` が要って重い。
  `defaultEnabledTools.test.ts` と同じ形）。`failures.md` #88。

  **同日 18:11〜18:16 に検収 5・6 も観測して Done。どちらも 1 回の観測では
  判定にならず、2 点目を取って初めて別解釈が消えた。**
  **検収 5** は 100 秒眠る前判定に**始まりと終わりの両方を刻ませた** —
  重なれば `start / start` が並ぶので**順序そのものが答え**になり、
  「覗いた瞬間にたまたま 1 本」と「常に 1 本」を取り違えない。
  実測は厳密に交互で**周期 120 秒**（間隔は 60 秒）= 走行中の due が畳まれた。
  **検収 6** は `fresh` の予定を 2 回発火させ、**2 回とも `history_msgs=0/16`・
  `prompt=3145` が同一**。1 回目で履歴は 2 になっているので `continue` なら伸びる。
  **`prompt` が同一なのが鋭い読み** — 「新しい会話が起きた」ではなく
  「2 回目が 1 回目と完全に同じ文脈を見た」まで言える。
  **「手動の新規チャットと区別が付かない」は構造的に消えた** — 予定自身が履歴を
  作り予定自身が畳んだので、人が触った瞬間が 1 つも挟まっていない。

  **同じ走行から漏れが 1 件出た**（`failures.md` #89）。利用者の観察 —
  **広場ログを切ってある個体が、自分宛でない予定の依頼文を読んでいた**。
  `compose_presence_notices` の抽出が **`from == System` しか見ておらず**、
  予定の配送（`from: System, to: Agent`）まで `## 入退室` として全員のプロンプトへ
  入っていた。**この節は `hearsRoomLog` の gate の外**なので、宛先付きの発話が
  混ざると**オプトアウトを構造的に迂回する**。**Spec 28 の退行ではなく
  Spec 07（2026-07-30）から 9 日間在った** — 気づく契機はテストではなく
  **モデルの答えの違和感**（届いていないはずの語を使った）。
  処方は `&& to == Endpoint::User`。**既存の `PromptProbeBackend` では
  この漏れは見えない** — 可変文脈は #45 の規律で最終 user 発話へ畳まれるので、
  `Role::System` だけを集める probe には載らない（`FullPromptProbe` を新設）。

  **`failures.md` へ 2 件起票した**（#86 / #87。どちらも Spec 28 の外で効く）—
  **#86 = 「静かになるまで待つ」テストの窓が系の定期イベントより長いと、
  失敗ではなく「永久に返らない」として現れ、実装のデッドロックと区別が付かない**
  （切り分けで効いたのは「変えていない経路を 1 本走らせる」。候補を 4 つ挙げて
  読むより速い）/ **#87 = `toEqual` で構造体を丸ごと固定すると、1 つの規則を
  検査するつもりのテストが他の規則にも寄りかかる** — Spec 25 P3 の再発なので、
  処方を注意から構造へ移した（**規則ごとのテストでは欄を選ぶ**。丸ごと固定して
  よいのは golden とワイヤ形の凍結だけ）。
  **查読の前提を実測で訂正した点が 3 つ** — 飛行中チェックは宛先個体単位
  （`orchestrator:2754`）/ handoff は予算の同一 Arc を継承
  （`tests/orchestrator.rs:6415`）/ workspace はパス固定なのでパス salt は
  村を区別しない（`state.rs:56`）。3 つ目は査読側も「配置モデルの前提を
  取り違えていた」と追認し、`village_id` での束縛に置き換えて承認された
- Spec 01（sd / yq 書き換え系）〜 03（新規チャット + 広場ログ）: **Done**
- Spec 27（ツール呼び出しの理由表示）: **rev3 承認 → P0〜P3 完了**（2026-08-07。
  lib 455 / 結合 124 / vitest 196 全緑・clippy 警告ゼロ・build 緑。
  **残は P4 実機 6 件中 2 件 = 検収 3・6**（2026-08-08 利用者判断で保留）—
  **理由を書かない個体でツールが動く** / **`ok=true` だが本文がエラー文の行で
  理由が「成功した」と読めない**。**どちらも正常系ではないので狙わないと出ない**。
  **画面の目視は 2026-08-07 に済んでいる**（`room_log` に理由の行が出ないことを
  実機で確認 — 利用者の言葉「広場ログを読むっていう理由しかない。書くのも冗長」で、
  **D2 の除外基準が使う側の言葉で裏返しに確かめられた**）。
  **実機で計器の穴を 1 つ塞いだ** — `reason_chars` だけでは
  「書かなかった / 外部で尋ねていない / 対象外」の 3 状態が全部 0 に畳まれる。
  **私自身が分布の平均を 20.8 → 14.3 へ誤って引き下げた**ので、
  `reason=written|omitted|unsupported|excluded` を併記した（#72 と同型）。
  **実測: Written のみで中央 19・平均 20.8・切り詰めゼロ**（起票時の仮定は 30 字）。
  契約は `data_contract.yaml` の
  `tool_reason_contract`（`command_tool_contract` の隣）。
  実装は `crates/fuseforks-core/src/tool_reason.rs`（純機構）+ `wants_reason` +
  `ToolRegistry::specs_for` での注入。**次に触る人が要る判断は Spec の P1 実装記録**。
  **`ReasonState` は実装で 4 値になった**（凍結の 3 値では足りず、契約も訂正）—
  **合成側も `ToolInvoked` を発行する**ので、3 値だと `ask_agent_3` に
  「外部ツール」と出て**画面のラベルが嘘になる**。
  **ミューテーション 2 回で赤を確かめ、1 回目で自分のテストの穴が出た** —
  `Excluded` を主張するテストが 1 本も無く、**提示側の検査は発行側を
  1 ミリも守っていなかった**。**一般化: 「A が生えない」を確かめるテストは、
  「B が入る」を確かめたことにならない**（同じ機構の提示側と発行側は別経路で、
  片方だけ留めるともう片方は既定値のまま何にでも化ける）。
  **`event.rs` の doc への追記は P0 から P1 へ送った** — `ToolInvoked` に欄が
  生える前に書くと**存在しない欄を説明する doc になる**。doc は型の隣にあり、
  **型と一緒に動くのが正しい単位**（P0 は実装より先に凍結するもの、doc は実装の一部）。
  **査読 7 点 → 5 点採用 / 1 点は結論を逆にして採用 / 1 点は前提を実測で訂正 /
  処方 1 点は却下**。最も重かったのは **D2 の判定基準が割れていた**こと —
  「引数が自然言語か」ではなく**「引数が既に別の形で画面へ出ているか」**へ純化した。
  **表は動かず根拠だけ入れ替わり、`remember` は境界例ではなく対象の中心**
  （`AgentMessage` を生まないので画面に出ず、書いた内容は以後の全ターンに残る）。
  **査読の「呼び出し比率 3% だから外しても変わらない」はカバレッジを
  呼び出し回数で測っている誤り** — `fd` 136 回と `remember` 26 回では
  1 回あたりの取り返しのつかなさが違う。
  **却下したのは「結果本文に `Error:` を含むなら `ok` を偽に倒す正規化層」**
  （Spec 08 の「分類は文言 parse でなく型で運ぶ」凍結 + `grep` は一致行を
  返すのが仕事なので偽陽性が壊滅的。呼び出しの 34%）。以下は旧 rev2 の記録。
  公開までのマイルストーン 6 件の 1 件目。**起票前に `concordia.log` の
  969 呼び出し / 355 ターンを実測し、コストを Goal に数字で埋めた**
  （ツールを呼んだターンで実効 +1.5〜2.2%・換算の仮定に頑健）。
  **rev2 で D8 = 理由が届くのは利用者だけ**（利用者裁定）。
  **コードは 1 行も要らない** — 広場ログ・履歴・`session_store` の 3 経路とも
  既に届いていない。**要るのは凍結とテスト**。そこから **D9** が派生
  （説明文で読み手を明かさない。「利用者が読みます」と書くと
  **安心させるための文章を誘発しうる**ので、求めるのは行為の記述）。
  **未決 3 件**（`remember` を外すか / 理由を書かせる文言の置き場 /
  `ok=false` の行の読まれ方）
- Spec 26（送り手封筒の健全性）: **Done**（2026-08-07。起票から Done まで 1 日。
  Spec 25 P5 の実機検証中に D6 の前提が崩れて派生）。
  **査読 7 点 → rev2 → 実測を受けて rev3 → P4 全件観測で rev4。**
  詳細は下の節が正
- Spec 25（Fuseforks を MCP サーバーにする）: **Done**
  （2026-08-07。起票 → rev3 承認 → 実装 → P5 実機 6/6 まで 1 日）。上の節が正。
  **P5 の検証中に D6 の前提が崩れ、Spec 26 が派生した**
- Spec 23（画像の添付）: **Done**（2026-08-06。起票から Done まで 1 日）。
  検収 6 件のうち 5 件を実機で観測し、**実機で踏める接続先が存在しない
  JPEG フォールバック**だけをループバック HTTP スタブの結合テストで覆った
- Spec 04（plan — 並列委譲と合流）: **Done**（2026-08-04 実機確認まで完了）。
  Phase 0〜2 は 2026-07-29、rev3 査読承認済み。
  **束ねて返る完走を `concordia.log` で観測**（残っていた最後の 1 本）—
  `plan wave: tasks=6 to=[...]` → `plan bundle: tasks=6 chars=3630
  elapsed_ms=17360` → `tool: name=plan ok=true body_chars=3630` →
  `turn: agent=agent rounds=5/36 waves=1 stop=-` で、**配送 → 合流 → 進行役が
  束ねを受け取って正常終了**まで揃っている。同じ形が 8/3 の 20:47（5 体・
  4,201 字・`rounds=10/36 stop=-`）と 22:35（3 体）にもあり、**計 3 回**。
  進行役のターンは `prompt=106379 cached=78825` で入力の 74% がキャッシュ。
  **観測が遅れたのは、探していた側が古い記述を信じて見に行かなかったから** —
  実機のログには 8/3 の時点で既に写っていた。**「未観測」と書いた台帳は、
  観測されたかどうかを自動では知らない。**
  査読で `to` の自由文字列・壁時計テスト・失敗分類の穴が出て rev3 で塞いだ。
  併せて `ask` の既存バグ（相手が転送すると「答えが返らなかった」と嘘を返す）を
  同経路なので同時修正（failures.md #35）
- Spec 05（Gemini ネイティブ経路と Google 検索による接地）: **Done**（2026-07-29）。
  実機確認まで完了し、`sources` は空＝**参照元 URL は返ってこない**が実測で確定。
  検索語と発表元だけを言う告知（Phase 2）が唯一の処方として残った。
  本 Spec は例外的に実装先行（Gemini の実挙動が実測でしか決まらなかったため）
- Spec 07（スケジュール実行 — 時刻で発火する依頼）: **Done**（2026-07-30
  実機確認まで完了）。`ScheduledTask` / `Recurrence` / `Weekday` と発火の規則は
  `data_contract.yaml` で**凍結済み**。Phase 0〜5
  （多重起動の排他=実機確認済み / 発火規則の純関数=テスト 18 本 /
  配線=結合テスト 8 本 / UI=`ScheduleDialog.vue` + IPC 4 本 / README 整合）+
  実機確認 2 本（`interval 1 分` の発火で会話ペインに応答が出る /
  停止中スキップの 1 行が 1 回だけ出る）。
  Phase 3 で契約に 2 点追記 — 予定の所有者は in-memory でファイルは投影・
  読めないファイルは書き込み拒否（`SCHEDULE_STORE_BLOCKED`）。
  トレイのマーク・チップス通知は利用者判断で別 UI 作業に分離。
  起点は利用者要望（毎週木曜 17 時に鐘／定期的なサイト監視）。
  rev1 で表に出した制約 2 つ — (1) 今はデーモンではなく起動していない間は
  動かない（**製品の行き先はタスクトレイ常駐**なので限界は将来消える。
  発火規則は常駐後も正しいまま）、(2) 「鐘を鳴らす」は同梱ツールに音も
  コマンド実行も無いため本 Spec では完結しない（P4）。
  rev2 で査読が **rev1 の実バグ**を出した — 猶予を全種別に掛けていたため
  `interval` の「再開時に 1 回だけ」が成立せず 0 回になっていた。修正は
  「due を最遅点にする」と「猶予を壁時計系だけに限る」の 2 つが揃って初めて成立。
  併せて多重起動の排他を P0 として追加（予定が入ると二重発火は正しさの問題）。
  `chrono` 採用は査読で承認済み。rev2 の査読で入った差分は 2 つだけ —
  `lastFiredMs` → `lastConsumedDueMs` の改名（飛ばした予定も書く欄なので
  「fired」は嘘）と、猶予超過で飛ばした予定を会話ログでなく debug ログへ倒す判断
- Spec 09（file — ファイル・フォルダ操作の同梱ツール）: **Done**（2026-08-01
  実機確認まで完了）。ザリが `docs/spec_coverage.md` を書いた — `docs/` は
  存在しなかったので**親フォルダの自動作成も同時に確認**できた。
  1 ツール + op 閉じた列挙（read / write / mkdir / move / copy / remove）で
  `data_contract.yaml` の `file_tool_contract` に**凍結済み**。
  起点は実機の空転（failures.md #39 — 新規作成が無く sd で 12 ラウンド試み続けた）。
  **境界が 2 種類あることが実装の核** — `resolve_in_work_dir`（実在パス専用。
  sd / yq の「新規作成なし」担保はこの canonicalize が根拠なので**触らない**）と
  新設 `resolve_creatable`（実在しない宛先用の 3 段検査）。削除は `trash` crate で
  ごみ箱のみ・完全削除の経路なし。依存追加は trash 1 つ（本体は std::fs）
- Spec 08（波ペイン — plan 実行の可視化、Airflow Grid 相当。GUI 表示名は
  2026-08-02 から「作業状況」で黒板とタブ切り替え、契約は不変）: **Done**
  （2026-08-04 実機確認まで完了）。Phase 0〜3 は 2026-07-30。
  **実機確認 2 本とも観測**: (1) 波が列として現れセルが answered / interrupted の
  混在で**個別に**確定（2026-08-02、Spec 10 と同じ走行）(2) **GUI を再読み込み
  しても波が残る**（2026-08-04）— アプリを落とさず画面だけ作り直し、
  `init()` の `list_plan_waves` 再投影が効くことを確認。`波 1・17.4s` と 6 セルが
  同じ走行の `concordia.log`（`plan bundle: tasks=6 elapsed_ms=17360`）と一致。
  **記録はコアの in-memory リング、画面はその投影**という設計が、画面を捨てて
  組み直しても成立している。rev2 査読（重大 7 / 明確化 6 / 改善 3）を全反映して
  凍結。`PlanTaskState` / `Reply { text, kind }` / `PlanWaveRecord` /
  `CoreEvent` 3 種 / `list_plan_waves` は `data_contract.yaml` で**凍結済み**。
  分類は文言 parse でなく型で運ぶ（刻み手は handle_message の 1 箇所）。
  `plan_id` は 1 始まり 0 予約・モデル非公開。リング上限 50・in-memory・
  配送ゼロの plan は記録しない（stderr と数え方一致は検証済み）。
  Spec 04 Notes 12 の発火条件 (b) の成立が起点（利用者要望 2026-07-30）
- Spec 11（トークン予算 — 依頼の因果に掛ける自動の天井）: **rev2 承認 →
  P0〜P1 完了**（2026-08-02 起票 → 同日査読で実装指摘 7 点 + D1/D2 裁定 →
  rev2 → LGTM → data_contract の token_budget ブロック凍結 +
  PlanTaskState へ budget_exhausted 加算 → budget.rs の純機構
  単体 9 本。欠落見積もりの分母は UTF-8 バイト数 s.len() で確定・
  tokenBudget は camelCase 自動対応で rename 不要）→ **P2〜P4 の機械側も
  同日完了**。P2 配線 = 根 2 箇所（ユーザー発話の宛先ごと / 予定発火）で
  new_root_budget、Envelope.budget は cancel と独立の Option<Arc<BudgetPool>>、
  周回境界で try_reserve → LLM → debit（usage 欠落はバイト見積もり）、
  deliver_and_wait の事前検査（尽きたら配送を始めない）、
  finish_budget_exhausted（System 行は note_exhausted の CAS で因果 1 本）。
  P3 投影 = 「予算切れ」ラベル・失敗色・加算的変更の回帰。P4 = README 日英
  （村規模別の推奨値ガイド込み）。結合 5 本（S1 打ち切り / 新予算で再走 /
  波の配送前停止 / 転送の同一 Arc / 既定値は新規 world.json だけ）。
  **実機確認 2 本とも完了**（小天井で止まる = 2026-08-02 `ceiling=50000
  spent=50000` / 既定天井で健全依頼が完走 = 2026-08-04 実効 110,157 で天井の
  11.0%）。**Done。** 実効トークン = 未キャッシュ ×1 + キャッシュ済み ×0.1 +
  出力 ×4、**内部は milli 建て AtomicU64・切り上げ**（Atomic に浮動小数は
  無い）。try_reserve / consume 分離で**オーバーシュートを許容**
  （check→LLM→減算は一体の atomic にできない）。~~1 呼び出し分~~
  **取り消し（2026-08-14。TLC で反証 — `specs/tla/BudgetOvershootBound`）**:
  `try_reserve` は load のみで予約しないので、**波が N 体へ撒くと残額 1 でも
  N 体が同時に通る。超過の上限は「同時に飛ぶ本数 × 1 呼び出しの実費」**
  （「1 呼び出し分」は因果が 1 本のときだけ）。
  **→ [Spec 38](specs/38_budget-reserve.md) で塞いだ**（2026-08-15。
  `try_reserve(estimate)` を CAS 化し、`ReservationGuard` の move セマンティクスで
  **二重返金を型で消した**。見積もりは個体ごとの直前実測・初回は床 1,000 実効で、
  **超過の上限は「飛行中の正の見積もり誤差の総和」へ縮んだ**）。
  **代償は波の欠け** — 残額はあるのに次の 1 呼び出しぶんを確保できない個体が
  budget_exhausted で確定し、**自動では頼み直さない**ので束ねに欠けが出る。
  打ち切りの理由は `budget stop: … reason=exhausted|reserve_short` で分かれる。
  `Envelope.budget` は
  cancel と独立の `Option<Arc<BudgetPool>>`、天井は world.json の
  `Option<u64>`（0 のマジック値なし・新規のみ Some(1M)・既存 None は
  起動 WARN で次の道）。歯止めの優先順位 cancel > budget_exhausted >
  RepeatGuard > hop を凍結。重みは P0 で code constant 凍結
  （モデル別設定を作らない）。根拠実測: 健全 6 体依頼 = 素 387K
  （実効 ≈ 250K）vs #41 病理 = 1 ターン素 2M。先行実装
  arXiv:2606.04056（実在確認済み）の delegation-fanout race 11 件が
  転送同一 Arc テストの根拠
- Spec 06（顔ぶれの提示と入退室の通知）: **Done**（2026-07-31）。実機確認まで
  完了 — ザリは停止中の相手へ一度も投げず、稼働中 3 体だけへ plan を撒いた
  （点呼のトークンが消えた）。実機の村は id↔表示名の番号が全部ずれており、
  id 併記が初回から効いた。起点は実機のザリ本人の要望。P0（表示名の一意性）は
  利用者指示で先行実装。rev4 査読で stable_len の定義矛盾（Skill と Memory が
  隣接していた間だけ 2 つの記述が同じ点を指せていた）が出て境界を再定義した

## 外部研究の受領（2026-08-04・**未読。次に読む候補**）

Grok と村民が収集し、**ザリが arXiv の abstract で一次確認した 5 本**。
**私は独立に確かめていない** — 出典はザリの照合で、ここに写しているのは
その報告（#57 の規律で、確かめていないものを確かめたと書かない）。

| ID | 題（副題まで） | 分類 | 投稿 |
|---|---|---|---|
| 2607.26760 | Metis: Memory Foundation Model | cs.CL | 2026-07-29 |
| 2607.26637 | Filesystem-Based Memory for LLM Agents: Organization, Evolution, and Sustainability | cs.CL | 2026-07-29 |
| 2607.29167 | Memory Provenance Laundering in LLM Agents: **A Non-Amplification Firewall for Persistent Memory** | cs.CR | 2026-07-31 |
| 2607.28802 | Model or Harness? An Interaction-Centric Taxonomy for Localizing Agent Failures | cs.AI | 2026-07-30 |
| 2607.08716 | Remember When It Matters: **Proactive Memory Agent for Long-Horizon Agents** | cs.AI | 2026-07-09 |

**この村の未決に直接当たるのは 3 本**（優先順）:

- **2607.26637 は黒板そのもの**。markdown のディレクトリツリーを管理／検索／実行の
  3 エージェントで回す構成。要旨の主張が 2 つあり、**どちらもこの村の判断に効く** —
  (a)「整理は検索コストを**材料が大きいとき**ほぼ半減させる」= **広場ログの pull 化
  （課題 2・3）の裏付け**。参照渡しはまさにこの「整理」 (b)「**測定した全エージェントで
  一件も**、整理そのものを良い答えへ変換できていない」= **黒板の機構化を急がない**
  判断の裏付け。条例の「書いてよいのは自分の付箋だけ」で整理の崩れを防いでいる形も
  論文の知見と一致する
- **2607.29167 は該当する脅威が実在する**。永続記憶への注入で ASR 最大 1.000。
  この村には `Memory.md`（`remember`）と黒板があり、**エージェントが書いた内容が
  他のエージェントのプロンプトへ入る経路**が既にある。`api_key_env` の事故と同じ形で、
  「注意書きは制御ではない」が当てはまる領域。**未検査**
- **2607.28802 は failures.md がやってきたことの分類学**。失敗を「モデル側か
  ハーネス側か」へ割り当てる taxonomy（41 failure modes、judge 一致 κ=0.76）。
  #41〜#61 の大半はハーネス側で、**外部の語彙が手に入る可能性**がある

**ザリが裏を取れなかったと明示した 2 点**（憶測で埋めない）: 全論文の著者所属
（abstract ページに所属欄が出ない）/「alphaXiv で 671 いいね」「DAIR.AI が紹介」の数値。

**ザリの自己申告が読む価値の本体**（2026-08-04）: 第一声で「arXiv ID が 1 年ずれて
いる」と断定し、**それを撤回した**。ずれていたのは ID ではなく年号の側で、
`2507.26637` を実際に叩いて 404 を確認して裁定している。
**手元の知識のカットオフを「世界の側の誤り」として処理しかけた**、という失敗の
申告付き — #57 の「同じ調べ方で確かめない」の実践例。

## 外部研究からの採用候補（2026-07-31 接地・雑談由来）

arXiv の 3 本を実在確認して読んだ（ジェミーが発見、私が ID から独立に接地）。
**3 本は互いに逆を向いており、境界はタスクの既知性**にある —
手順が既知なら外部制御は害、未知なら分解と検証に価値がある。

- arXiv:2604.27891 In-Context Prompting Obsoletes Agent Orchestration for
  Procedural Tasks（Melbourne / i14、5 著者。条件あたり 200 会話）
- arXiv:2602.16873 AdaptOrch（単著・査読venue なし）
- arXiv:2603.11445 VMAO（ICLR 2026 Workshop on MALGAI。25 問・LLM 判定）

### 採る: plan の束ねの前に検証を挟む（VMAO の Verify 段）

`plan` は**答えを検証していない**。ワーカーの応答が空でも、矛盾していても、
依頼を取り違えていても、そのまま束ねて進行役へ返す。VMAO はここに検証器を置いて
completeness 3.1 → 4.2（1〜5、LLM 判定、25 問）を報告している。

**ただし機構を足す前に、既存の機構で書けるか確かめること。** 進行役が束ねを
受け取ったあと、検証役のサーヴァントへ `ask` で投げれば同じ形になる —
コード変更ゼロで、条例か SKILL.md に手順として書ける。まずそちらを試し、
「毎回書くのが面倒」が実測で確認できてから機構化を検討する。
（設定を増やさないという軸から、この順序は動かさない）
**→ 条例へ書いて着地**（2026-08-02。文言は上の運用図対応の節）。解説記事
（deepsquare.jp 2026-05-03、宮原太陽）で Verify 段の設計を追加接地 —
検証器は実行側と**別の**モデル（自己評価バイアス対策。条例では「束ねに
参加していないサーヴァント」として反映）、検査 6 項目（status / coverage /
confidence / missing_aspects / contradictions / recommendation。条例では
空・取り違え・矛盾・欠落の 4 点 + 三択の結論に簡約）。

再計画（Replan）は**採らない**。波を自動で回し直す機構は、失敗ループの
トークン燃焼（#41 / #42）と正面から衝突する。止める側の作業が終わるまで、
自動で仕事を増やす機構は入れない。**不採用の根拠は解説記事の実測で強まった** —
VMAO 全体はトークン ×8.5・実行時間 ×5.5 で、クエリの 75% 以上が品質基準では
なく**リソース制約で打ち切られている**。検証 1 回の ask に留め、回し直しの
判断は進行役と人に残す（条例の「聞き直しは 1 巡まで」はこの数字が根拠）。

### 測る: 毎ターンの可変注入が品質を落としていないか（論文 1 の含意）

論文 1 の批判対象は「毎ターン指示を注入する外部層」で、Fuseforks は routing を
注入していないが、**顔ぶれ・入退室通知・広場ログ**を毎ターン注入している。
routing ではなく文脈だが、「毎ターン形が変わるものを差し込む」形は同じ。

**キャッシュの理由で静的にしたい部分と、品質の理由で静的にしたい部分が
同じ可能性がある**（README のプロンプトキャッシュ節「提示は静的、状態は動的」に、
論文 1 は品質側の根拠を与えうる）。これは未測定の仮説。

実験の口は既にある — `hearsRoomLog` のオプトアウトで、広場ログ有り／無しの
個体を並べれば失敗率が比べられる。#42 の実装で入力コストが下がったあとに測ると、
コスト差と品質差を分けて読める。

### 採らない: トポロジーの自動切り替え（AdaptOrch）

「モデル選択よりトポロジーが効く」という主張自体は村の地図の存在意義を補強する。
だが**依存グラフを推定して構造を勝手に変える機構は入れない**。利用者から見て
何が起きるか予測できない方向に倒れ、「設定が少なくて分かりやすい」と衝突する。
**線は人が引く。** 単著・査読なし・LLM ベンチで 12〜23% という証拠の強さも、
設計を変える根拠には足りない。

### 運用として既に採れているもの（論文 1）

手順が既知のタスクは、外部 routing ではなく**手順を丸ごと SKILL.md に書いて
自走させる**のが論文 1 の結論で、Fuseforks は機構追加ゼロでこれができる
（SKILL.md がそのまま system prompt の安定部分に入る）。
村の運用として書き方を推奨する余地がある。
