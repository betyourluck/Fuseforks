# CLAUDE.md

正式名は **Outcasts Concordia**（2026-07-30 決定）。アプリ内表示・System の
送り手名は「Concordia」のまま。狙いは**エンジニアのホビー + Outcasts への導線** —
核（agent-core / 契約 / 発火規則）は業務品質、殻（村・キャラ）はホビーの体験、
両方を中途半端にやるのが唯一の失敗形（README「思想」の節が正）。

## 台帳の構成（真実の記録先）

**README.md と CLAUDE.md は用途が違う**（2026-07-31 利用者指示）。README.md は
**アプリの紹介ドキュメント**で、読み手は利用者・見に来た人。CLAUDE.md は
**私と開発者のためのメモ**で、読み手は作業する側。測定値・実装手順・次の一手の
優先順位は memo 側に書き、README には「何が出来て、どこに穴があるか」を
読み手の言葉で書く。迷ったら「これは読み物か、作業の材料か」で分ける。

- 全体像・設計判断: [README.md](README.md)
- ドメイン契約（実装より先にここが正）: [data_contract.yaml](data_contract.yaml)
- 踏んだ罠（症状 → 真因 → 処方 → 一般化）: [failures.md](failures.md)
- 仕様: `specs/NN_*.md`（起票 → 査読 → rev 改訂 → Phase 分割で main 直コミット）

機構・enum・フィールド・API を変えたら、その名前で全台帳を grep して
追従漏れを回収するまで完了としない。

## 台帳の書き方

**台帳は grep されるための文章であって、読まれるための文章ではない。**
上の grep 規律が成立するのは、書く側が引く側の語彙で書いているときだけ。

- **具体名詞を 3 つ以上入れる**（それが検索語になる）
- **実際に起きた動作を述語にする**（「〜が落ちる」「〜を拒否する」）
- **比喩を使わない**
- **6 ヶ月後に自分がどんなクエリで引くかを想像し、その語を入れる**

```text
✗ 境界の防御は、開かれた列挙ではなく閉じた許容によってのみ成立する
✓ 外部が書いたデータの転送層では、除外リストは必ずもう一度落ちる
```

✗ の側は読めるが引けない。**教訓を書けと言われたときに最も滑り込みやすい形**が
これで、書いた本人には良く見えるのが厄介なところ。`failures.md` の一般化・
`data_contract.yaml` の invariant・Spec の Notes すべてに適用する。

**会話の文体と台帳の文体は分ける。** 対話では比喩も詩的な表現も使ってよいが、
記録には持ち込まない。混ぜると、どちらの要求も満たさない文章になる。

**用語の境界（2026-08-01 利用者判断）**: 外に見える文言 — システムプロンプト・
UI ラベル・README — は「**グラウンディング**」、内部の台帳・コメント・設計語は
「接地」のまま。「接地」は内部用語すぎて外部に通じないため。英語 README は
元から Grounding なので、この統一で日英も揃った。外向け文言を書くときは
「接地」を使わない。

**同型の二例目（2026-08-03 利用者判断）**: **システム設定ダイアログの語彙は
Concordia のテイストから降りてシステム用語にする**。UI ラベルは
「トークン制限」「制限あり / 制限なし」、内部の台帳・コード・ログ・設計語は
**「天井 / ceiling」のまま**。起点は実機の指摘 —「あまりにも砕けすぎていて
逆にわかりづらい」。`CoreError::InvalidTokenBudget` の `#[error]` は利用者に
見えるので制限側で書く。**ページ本文の「村」「サーヴァント」は残す** —
それは世界の説明であって分類名ではない。境界は「分類・ラベルか、説明か」。

## 次の作業（Spec 未起票・2026-07-31 合意）

失敗ループのトークン燃焼（failures.md #41）を **1 → 2 → 3 の順**で潰す。
1 だけが病（繰り返し失敗）を直し、2・3 は症状（燃焼）を抑える側。

1. **同一失敗の検出** — **完了**（2026-07-31）。ツール名 + 引数 + **結果本文**の
   完全一致が 2 回返ったら 3 回目は実行せず短い通知だけ返し、`ToolRepeatBlocked`
   を発行する（`orchestrator.rs` の `RepeatGuard`。単体 7 本 + 結合 2 本）。
   判定材料が「エラー文言」でなく結果本文なのは、同梱ツールが失敗を `Err` では
   なく `Ok(<エラー文の本文>)` で返すため（failures.md #41 の一般化 3）。
   数えは **(ツール名 + 引数) ごとで隣接を要求しない** — 隣接判定にしていた
   初版は実機で 1 件も発火しなかった（モデルは 1 周に 2〜3 本を並列で呼ぶため
   数えが切れる。#41 の (c) と一般化 4）。**止めるのはその 1 本でループではない**。
   ループを切るのはその周のツールが全部止まったときだけ（一般化 5）。
   **実機で初発火した**（2026-08-01）— 61,891 字の `data_contract.yaml` を
   `file read` させたら止まった。ただしその周のツールがその 1 本だけだったので
   一般化 5 の打ち切り条件を満たし、ターンごと落ちて本文が空になった。
   **機構は設計通りで、真因は歯止めの先に道が無かったこと**（failures.md #44、
   `file read` の打ち切り文だけが次の手を書いていなかった。修正済み）。
   残は実機確認（**複数本を並列で呼ぶ周で 1 本だけ止まり、ターンは続くこと**）
2. **割り込み停止** — ターンループの周回境界で見る協調的な打ち切り。
   `stop_agent` は飛行中ターンの完了を待つ設計なので別機構が要る。
   plan の波の途中で切ったときの伝播が設計の主戦場。
   **Spec 10 として完遂（起票 → rev2 査読承認 → Phase 0〜4 → 実機確認まで
   2026-08-01〜02 の 2 日で Done）**。機構: `Envelope.cancel` の child_token
   連鎖・周回境界の検査・出口 2a/2b/2c・`run_plan` select 化・
   「■ 停止」「全ターン停止」UI・`interrupt_all`・`stop_agent` 高速化。
   コア結合 8 本 + フロント投影 3 本 + 加算的変更回帰。実機で「要求から
   0.0 秒」（波の join 待ちの即検知 = select 化の効き）と「依頼元の打ち切りに
   連鎖」と「打ち切り前に答え終わったセルは answered のまま」を観測。
   実装中の改善 1 つ: 打ち切りターンは sent_user_turn を送った形のまま積む =
   キャッシュはリセットされない —
   伝播の単位はエージェントでなくターンの因果（波の巻き添え範囲 =
   その波が配送した封筒だけ）。rev1 査読が矛盾 4 件（不変条件 1 の射程 /
   出口 3 点セットの経路別分割 / stop_agent 但し書き / 畳みの Reply 必須）と
   未定義 3 件（親トークンの畳み込み / run_plan join の select 化 /
   interrupt_all の冪等）を出し、rev2 で全反映。API は
   `interrupt_turn(agent_id)`、`PlanTaskState` へ `Interrupted` 追加、
   依存は tokio-util 直接化（間接依存として既に Cargo.lock に居るので
   コストゼロ。CancellationToken の tokio 本体昇格説は実ソースで否定確認）
3. **トークン予算** — 歯止めを 1 つコスト建てにする。Task Budgets は
   claude-sonnet-5 で使えるがモデルへの申告であって強制ではなく、
   Gemini 系には無い。村全体の天井はハーネス側に要る。
   **優先度が上がった**（2026-08-02）— 利用者の村の運用図（8 段フロー、
   人が関与するのは最初と最後だけ）の自律度は、自動の天井があって初めて安全。
   燃焼対策 3 点のうち 1（RepeatGuard）と 2（Spec 10）は Done、これが最後。
   **Spec 11 として同日に起票 → rev2 承認 → P0〜P4 の機械側まで完了**
   （2026-08-02）— 因果（ユーザー発話の宛先ごと / 予定の発火ごと）に
   実効トークン建ての天井、検査は周回境界、`budget_exhausted` を加算追加、
   既定 1M は新規 world.json のみ・既存は起動 WARN・設定 UI なし。
   実機確認 (1) 小天井で止まる = **観測済み**（2026-08-02）。
   残は (2) 既定天井で健全依頼が完走、のみ（通常運用の 1 依頼で確認できる）。
   これが通れば燃焼対策 3 点は全て Done

## 次の大物: 会話の永続化とセッション管理（Spec 12・rev2 承認 → P0〜P4 完了）

**Spec 12 は rev2 承認 → P0 完了**（2026-08-02。契約は data_contract の
`session_store` + `reset_rule` の Spec 12 改訂節 + `sessionSwitched` の加算。
Spec 03 のヘッダにも改訂を記録）**→ P1 完了**（2026-08-03。
`crates/agent-core/src/session_store.rs`。`SessionStore::open` /
`create_session` / `append` / `session_meta` / `list_sessions` /
`latest_session` / `records` / `tail_messages` / `restore_histories` /
`fork_session` / `delete_session` / `export_session`。単体 16 本・
workspace 全緑・clippy 新規警告ゼロ）**→ P2 完了**（2026-08-03。
書き込み点 2 種・起動時の復元・セッション API・`reset_conversation` の委譲。
結合 8 本）**→ P3 完了**（2026-08-03。IPC 7 本・`SessionDialog.vue`・
`sessionSwitched` の投影。フロント 2 本 + ワイヤ形の凍結 1 本）
**→ P4 完了**（2026-08-03。手動要約 `summarize_session` + 可変文脈への注入。
結合 3 本）。**残 Phase: P5 実機確認のみ**。起点は利用者の判断 —
「再起動して前回の続きから始められないと、道具として使えない」。
利用者が Claude Code / Copilot / Cursor / LangGraph / CrewAI を調査した
資料を提供し、Spec の参考節へ接地済み。

**設計の核は、この村には履歴が 2 層あること**（他ツールに無い事情）:
`Shared.log`（村の会話ログ・`AgentMessage`・リング 5,000）と
`AgentRecord.history`（エージェント個別のプロンプト履歴・`ChatMessage`・
滑る窓 8 往復）。**片方から他方を復元できない** — history には #45 の規律で
「送った文字列そのもの」（畳んだ可変文脈込み）が入り、log にその文字列は無い。
log には System 行や他人宛の発話があり、どの history にも入らない。
ゆえに**会話ログだけ保存して再開すると、画面は正しいのに全エージェントが
健忘症で始まり、その 2 つは画面上区別が付かない**。

**rev2 で保存先を redb 1 ファイルへ変更**（2026-08-02 査読）。rev1 の
`JSONL + index.json` は**真実が 2 つ**になっており、原子性の矛盾も fork の孤児も
その帰結だった。redb は機構を**減らす**（index.json・temp+rename の自前実装・
壊れた行の切り捨て・fork の 2 段階が全部消える）。ハイブリッド
（redb = 索引 / JSONL = 本文）は採らない。

`{workspace}/sessions.redb` にテーブル 2 つ（`sessions` / `records`、
records のキーは `(session_id, seq)`）。`Record` は **message / exchange /
summary の 3 種別**。復元しないものは波・予算・MCP 接続・稼働状態（作業の寿命）。
索引は作らない・要約は手動のみ。**`export_session`（JSONL 書き出し）は P1 の
完了条件** — 読めない保存先を作るなら出口は機構の一部（この企画の診断は
grep に依存している）。

**P1 の実装で決めた 3 点**（2026-08-03）:

- **`fork` は seq を振り直さない**。振り直すと `summary.coversUpToSeq` の指す
  境界がずれ、複製した側だけ要約の覆う範囲が変わる
- **1 往復 → `ChatMessage` 対の変換を `world::exchange_pair` の 1 実装に統一した**。
  押し込む側（`push_exchange`）と読み戻す側で規律が分かれると、**復元した履歴
  だけが空メッセージを持ち、次のターンで 400 になる**（failures.md #29 の再演）
- **`restore_histories` は `RestoredHistories { histories, summaries }` を返す**。
  要約が履歴の席を持たないことを型で保つ（注入は可変文脈の畳みへ相乗り = #45）

**P2 の実装で決めた 3 点**（2026-08-03）:

- **`start_agent` が履歴をクリアするのをやめた**（既存挙動の変更としては最大）。
  旧い規律「起動は新しい会話の開始として扱う」は、会話を始め直す手段が他に
  無かった時期の代用だった。**起動時に自動起動しない契約と組み合わさると、
  再起動して開いた会話の履歴を、エージェントを起動した瞬間に捨てる** — S1
  （続きから始められる）が原理的に成立しない。始め直しは「新規チャット」が担う
- **飛行中ターンの拒否は「既存の会話を開くとき」だけ**。凍結済みの 2 節が
  衝突して見えた（`reset_rule` の案 A = 完了書き込みは許容 / 不変条件 11 =
  切り替えは idle のときだけ）。解は**着地先で分ける** — 新規チャットの着地先は
  新しい空の会話で既存の記録を汚さないので案 A が成立する。resume / fork /
  continue_latest / 開いている会話の delete は `SESSION_SWITCH_BLOCKED` で拒む。
  **自動で `interrupt_all` してから切り替えることはしない**（線は人が引く）
- **保存の失敗は WARN 1 行で続行**し、保存先が開けない起動も普通に通す
  （その村では会話が再起動で消え、`reset_conversation` は改訂前の挙動へ落ちる）

**P3 の実装で決めた 3 点**（2026-08-03）:

- **分岐は `ForkPoint` の一覧から地点を選ぶ 2 段階**。S4 は「ある地点までは
  同じで」が本体なので、会話まるごとの複製しかできない fork では満たさない。
  候補は**ユーザー発話だけ**（全レコードを並べると入退室の System 行や往復の
  記録まで候補に出て、「どこで切ったのか」が読めなくなる）。
  **切るのは選んだ依頼の「直前」で、依頼の文面は入力欄へ差し戻す**
  （実機の観察で直した。下記）
- **`conversationCleared` では通知を出さない**。この 1 本は新規チャット・
  開き直し・分岐のどれでも同じように飛ぶので、既存の「新規チャットを
  開始しました」は開き直しのときに嘘になる。通知は操作した側が出す
- **書き出し先は `{workspace}/exports/{session_id}.jsonl` に固定**。ファイル選択
  ダイアログのために plugin を足すより、置き場所を 1 つ決めてパスを通知に出す
  ほうが依存も操作も少ない

**P3 の実機確認**（2026-08-03、利用者）: 会話一覧・開き直し・新規チャット・
分岐すべて確認済み。**分岐で 1 件、設計の誤りが実機でだけ見えた**:

- 症状: 分岐先に**返事の付かない依頼が宙に浮いたまま残る**（`合言葉はブラボー`
  が画面に出ているのに、`合言葉は？` に `アルファです` と答える）
- 真因: 選んだ依頼**を含めて**複製していた。返事と `exchange` はその発話より
  後に書かれるので、`at_seq` の外へ落ちる。**これは契約の「半端なターン」の
  規律どおりで、機構は正しく動いていた** — 正しく動いた結果が使いにくかった形
- 処方（利用者の案）: **依頼は複製先に残さず、文面を入力欄へ差し戻して
  書き換えられる状態にする**。S4「そこから別の頼み方を試したい」がそのまま
  操作になる。差分は `ForkPoint`（`seq` → `at_seq` = seq − 1、`text` と `to` を追加）と
  `ChatInput.fill()` の公開だけで、`fork_session` の inclusive 契約は触っていない
- **一般化**: テストが全部通る機構でも、「その結果を人がどう使うか」は実機でしか
  出ない。単体・結合は `at_seq` の意味を固定しただけで、**その `at_seq` が
  使う人にとって正しい位置かは検証していなかった**（failures.md #49）

**この Spec で failures.md へ 3 件残した**（2026-08-03）: #48 復元の配線を入れたのに
起動ボタンが履歴を捨てていた（代用機構は本来の機構が来た時点で害へ反転する）/
#49 境界値の「意味の正しさ」と「位置の適切さ」は別で、後者は実機でしか出ない /
#50 ターンループの外へ出した LLM 呼び出しが、どの計器にも載っていなかった。

**P4 の実装で決めた 4 点**（2026-08-03）:

- **対象は稼働中のサーヴァントだけ**（2026-08-03 の実機指摘で修正。初版は停止中にも
  呼んでいた）。要約の目的は以後のプロンプトを短くすることで、停止中の個体には
  以後のターンが無い。それでも呼べば**参加していない個体のぶんまで押した人が
  トークンを払う**。履歴は停止しても消えないので、起動してから押せばそのとき
  要約される。飛ばした相手が居たら System 行に必ず書く（#44 の規律）
- **要約を作るのは本人**（そのエージェント自身のモデル）。別のモデルに書かせると
  要約の文体と本人の文体が食い違い、次のターンで「自分が書いた覚え書き」として
  読めなくなる。要約の呼び出しにツールは提示しない（呼びに行く個体が出て、
  1 回で終わる呼び出しがループになる）
- **境界は書く前に取る**（`SessionStore::last_seq`）。書いた後だと自分自身を覆う
  要約になり、`coversUpToSeq < 自身の seq` が破れる
- **畳むのは保存が済んでから**。空の要約・失敗した要約では畳まない —
  要約に失敗した代償が履歴の喪失になるのは最悪の交換
- **使ったトークンは統計へ積み、`summarize:` の 1 行を出す**（2026-08-03 の実機
  ログで塞いだ穴）。要約はターンループの外の LLM 呼び出しなので、積まないと
  **押した人が払った分がどの数字にも現れない**（カードの累計・村の集計・`turn:` 行の
  どれにも出ない）。実機の 1 回目はログに一切痕跡が無く、`history_msgs` が
  4 → 0 に落ちたことからしか実行を確認できなかった
- **要約の長さはモデルで桁が違う**（2026-08-03 実機、4 体同時）。同じ 1 往復に対し
  `claude-opus-5` が 1,163 字、`gemini-3.5-flash-lite` が 234 字。opus は畳んだ
  履歴より長い要約を書いた。**要約は滑る窓と違って落ちない**ので、膨らみは以後の
  全ターンに乗り続ける固定費になる。処方は計器のみ — `summarize:` 行へ
  `folded_chars` を足し、`summary_chars >= folded_chars` で WARN。
  **機構では止めない**（止めると「要約したのに畳まれない」が画面から読めない）
- **`/compact` のようなスラッシュコマンドは採らない**（利用者と相談して決定）。
  入力欄は「特定のサーヴァント 1 体への発話」の場で、要約は村全体の操作なので
  射程が誤解される。入力欄に構文解析を持ち込むと、取りこぼしはそのまま相手へ飛び、
  `/` で始まる正当な発話を飲む。GUI にはボタンの置き場所がある

**実測で決めた**（2026-08-02）: redb 4.1.0 = 2 依存 10 秒ビルド、
「後ろから 16 件」0.03 ms、20,000 件の全走査 11 ms、fork 55 ms。
候補比較は tantivy+lindera = **451 依存**（しかも zstd-sys で C が戻る）、
rusqlite bundled = 10 依存。**SQLite FTS5 の既定 unicode61 は日本語で 0 件**、
trigram なら全ヒット — 日本語検索に形態素解析は要らない、が lindera を
外した根拠。D1〜D9 は全決着、D10（redb のファイル形式更新時の移行）が残る。

## 観測中（機構を作らず計器だけ置いてある）

**本文へ漏れたツール呼び出し**（2026-08-02、failures.md #47）。モデルが
ネイティブの `tool_use` ではなく本文へツール呼び出しの XML を書くことがあり、
ハーネスは「ツールを呼ばなかった = 最終出力」と読んで生の XML を配信する。
実機の初発は claude-opus-5 + MCP 27 本で **1 回だけ**（21:39）。同じ依頼を
再実行したら `MCP_DOCKER__fetch` はネイティブで成功したので、プロンプト起因では
なく一過性の揺らぎと読んでいる。

- **L0 計器**（Done）— `text tool call leaked:` を 1 行。検出は
  `</invoke>` / `<parameter name=` / `<invoke ` の 3 つ。**`<invoke` だけを
  探すと実機で一度も発火しない**（届く本文は先頭の `<invoke name="` が
  食われて `MCP_DOCKER__fetch">` から始まる。削ったのは API 側で、
  `anthropic.rs` の decode は XML を解釈しない）。単体 2 本 +
  **in-situ 1 本**（実際にターンを走らせログへ 1 行出るまで見る）
- **L3 提示外呼び出しの返却**（Done）— フィルタが捨てていた呼び出しを
  `tool_result` で返す。判定は RepeatGuard の後ろ。data_contract の
  `tool_execution` 不変条件に凍結
- **L1 自動修復 / L2 教示リトライ は作らない**（2026-08-02 利用者判断）。
  1 回しか観測できていない事象に機構を足すと、効いているか分からない機構が
  増える。計器があるので次に起きれば必ずログに残る — **頻度を見てから
  Spec 起票**。実装時の要点は data_contract の `leaked_tool_call` に書いてある
  （L1 の正規表現は開始タグが食われた形を主に据えること）

## 村の黒板（共有作業メモ）— 条例で運用開始（2026-08-02）、Spec 未起票

利用者の要望は「BBS ではなく、みんなが共有している記憶 = 作業メモ」。
正体は blackboard（共有の作業記憶）で、長期記憶（Memory.md / Memoria）とは
寿命が違う — 黒板は作業の寿命、Memory.md は個体の寿命。混ぜない。

**機構は作らず運用で始める**（「機構を足す前に既存の機構で書けるか確かめる」）:

- `file` ツール（Spec 09）+ 共通 work_dir で既に書ける。**条例へ 1 節書いた**
  （2026-08-02、`{workspace}/Ordinance.md` の「村の黒板」節）。採ったのは
  下の**付箋形** — 黒板は `黒板/` フォルダ、着手前に fd + file read、
  書くのは自分の `黒板/<表示名>.md` だけ、進行役だけが `黒板/まとめ.md` を
  束ねる、仕事の答えは黒板でなく返信で返す。初案の「単一ファイル +
  書くのは進行役だけ」は付箋形で置き換えた（file write は全文上書きなので
  並行書き込みは後勝ちで消えるが、1 人 1 ファイルなら衝突が構造的に起きない）
- **push 注入はしない**（pull = 読みたいときに file read）。毎ターンの可変注入は
  論文 1 の品質仮説の疑いの筋で、広場ログに続く 2 本目を増やさない。
  実測で「読み忘れ」が出たら、広場ログと同型のコスト設計
  （オプトアウト・上限・System に積まず可変尾部 = #45 の規律）で機構化を検討
- 膨張したら要約が要る（Memoria の昇華と同型）。急がない
- **GUI に黒板タブを追加**（2026-08-02 利用者要望）— 中央下段をタブ化し
  「黒板 / 作業状況」を切り替え（波ペインの表示名を「作業状況」へ改名）。
  `blackboard.rs` + IPC `list_blackboard` + `BlackboardPane.vue` /
  `BottomPaneTabs.vue`。**読み取り専用**（書き込み IPC 不在 = 条例の
  「1 人 1 ファイル」を GUI が迂回しない）、pull のみ（表示中 10 秒の再読 +
  手動更新。push 注入なしの原則は GUI 側も同じ）。契約は
  data_contract の blackboard_contract
- **実機確認済み**（2026-08-02）— ザリが条例だけを頼りに
  `黒板/ザリ・ロブステル.md` を書いた（依頼待ちの状態・タスク分解方針・
  実行上限のメモ。機構の追加指示なしで付箋形が成立）。GUI の黒板タブ表示も
  利用者が確認。work_dir がこのリポジトリを指す村では黒板がリポジトリ直下に
  生まれるため、`黒板/` を .gitignore へ追加した（黒板は作業の寿命の共有記憶で
  あってリポジトリの内容物ではない。コミットすると公開リポジトリへ漏れる）

**利用者の運用図（8 段: 相談→方針→調査依頼→グラウンディング/MCP→
DAG 分業→実行→フィードバック→完了報告）**との対応: 1〜6 は既存部品
（進行役 / ask・plan / Spec 05・02 / 波）。7 のフィードバック = VMAO の
Verify 段は**条例へ書いた**（2026-08-02、「束ねの検証」節。コード変更ゼロ）:
配るとき合格基準を 1 行添える / 束ねに参加していないサーヴァントへ ask で
検証（自己採点しない）/ 見るのは 空・取り違え・矛盾・欠落 の 4 点 /
聞き直しは該当の相手へだけ・1 巡まで / 揃わなければ揃わない事実を添えて報告。
自動の再計画は引き続き入れない。残は実機確認（ザリが束ねの後に検証の ask を
実際に挟むか）。**初回走行（2026-08-02 17:47）では観測できず** — ザリが
6 体全員へ撒いた（to=[agent_2..agent_7]）ため「束ねに参加していない
サーヴァント」が存在せず、条例のフォールバック（自分で 4 点確認）の経路に
落ちた。自己検査はログに映らない。本命経路の観測には独立検証役の居る構図が
要る — 依頼文で 1 体を波から外すか、検証専任を 1 体足す（線は人が引く）。

図は 2 枚（2026-08-02）。2 枚目（AI Crossing）は黒板が構図の中心で、
追加の読みが 3 つ:

- **並行書き込みは付箋の形で解ける** — 「1 人 1 ファイル（`黒板/<名>.md`）を
  自分だけが書く。読みは全員がフォルダごと（fd + file read）。進行役が
  `黒板/まとめ.md` へ束ねる」。上書きの衝突が構造的に起きず、機構ゼロのまま
- **承認ループ（Goal→Plan→Execute→Report→Approve→Repeat）は今の会話の
  形そのもの**。承認ゲートの機構は不要 — 要るのは報告の書式（条例）
- **村長 = Concordia は人格設計であって機構ではない**。進行役はトポロジーの
  帰結（plan は接続 2 体以上で生える）のまま。「進行役フラグ」は作らない —
  線は人が引く、の原則を維持

**この 3 つとは別に出ていたより大きい梃子（failures.md #42）は完了**（2026-07-31）。
`cache_control` が **system ブロックにしか打たれておらず**、ツールループが毎周
`messages` 全体を素の値段で送り直していた（1 ターン 2,052,314 トークンの入力のうち
1,826,109 が素の値段）。`anthropic.rs` の `place_message_breakpoint` で
**最後の発話の最後のブロック**へ境界を追加した（種別は問わない — `tool_result` に
打てないとツールループの周回だけ境界が抜ける）。単体 4 本追加、workspace 全緑。
見積もりは**同じ仕事が約 1/3 の値段**。2・3 が「燃焼を止める」側なのに対し、
これは**燃焼を止めずに安くする**側なので、仕事を減らさない。
**実機確認まで完了**（2026-08-01）— claude-sonnet-5 のザリで 8 ツール呼び出し
（`file` / `fd` / `grep`×5 / `file`）158.4K トークンの turn が
**入力の 87% をキャッシュ**。修正前の実測は 11% と 16%。安定プレフィックスは
6,463 トークン固定なので、5〜6 周で 87% には構造的に届かない = 履歴が載っている
（#42 の一般化 1 を逆向きに使った判定）。
**ただし 87% は「RAG 無し・村に他に誰も居ない」最良ケースの数字だった**
（failures.md #45）。可変な文脈を `Role::System` で積んでおり、adapter が位置に
関係なく先頭へ畳むため、他人が喋ると前方一致が切れていた。**2026-08-01 に 2 段で
修正** — (1) 可変文脈を最終 user 発話へ畳む (2) **送った文字列をそのまま履歴へ積む**
（(1) だけでは送信と保存が食い違い、cached が tools+system の 6,465 で頭打ちになった）。
修正後の実測は**ターンをまたいで 99%**（`cached(N)` が `prompt(N-1)` と 2 トークン差で
一致 = 前のターンのプロンプト全体が載っている）、ツールループ内で 84〜99%。

実装時に Anthropic の仕様へ接地して未決 2 点を潰した。**`tool_choice` の変更は
履歴層のキャッシュを落とす**（tools 層と system 層は保つ）ので、まとめ呼び出しの
1 周だけは今も書き込みになる。**境界の後方探索は 20 ブロックまで**で、1 周の追記が
超えるとその周だけ書き直しになる（実機の 2〜3 本並列は 1 周 7 ブロック前後）。
どちらも仕様上の限界なので打ち消す仕掛けは置かない（data_contract の
`prompt_cache.bounds`）。

## 次の Spec: システム設定ダイアログ（Spec 13・**rev3 承認 → P0〜P5 台帳完了・残は実機確認のみ**）

**P5 の台帳側完了**（2026-08-03）— README 日英の回収。**射程が見積もりより広かった**:
起票時の「⚙ 8 箇所」に対し、実機のアイコンと突き合わせたら ⏰ / 📜 / 📡 / 📁 も同じ原因
（絵文字 → SVG の置換に記述が追従していない）で腐っていた。**▶ / ■ は正**
（`batchStart.ts` は今も文字グリフ）、「何ができるか」表の絵文字は README の装飾なので据え置き。
**置換先を絵文字にせず、ボタンのラベルと置き場所で指す形へ統一**し、その規律自体も
README 本文へ 1 段落書いた（次にアイコンを変えても台帳が腐らない）。
同時に**⚙ とは別原因の追従漏れ 3 件**を回収 — README に Spec 13 の節が 1 つも無かった
（`grep "システム設定" README.md` が 0 件）/ `tokenBudget` の説明が「world.json の 1 つだけ」の
まま（P1 で画面から触れる事実が抜けていた）/ ディレクトリ木と workspace 木に直近 2 Spec の
産物（`SettingsDialog.vue` / `SessionDialog.vue` / `ConfirmHost.vue` / `locales/` /
`useUiSettings.ts` / `sessions.redb` / `exports/`）が無い。**一般化は
`failures.md` #51 へ**（記号 1 文字の追従漏れは識別子と違ってコンパイラにも lint にも
引っかからない）。

**実機の指摘で分類を改稿**（2026-08-03、P4 の後）— 左メニューを
**保存先ではなく主題で切り直した**: 全般（言語）/ コスト管理（トークン制限）/
ユーザーインターフェース（メッセージ表示/非表示）。初版の「村の設定 /
この画面の設定」は保存先での分類で、**実装の都合がそのまま目録に出ていた**のが
「砕けすぎて分かりづらい」の真因。保存先は分類から降ろし、各ページ末尾の注記へ
（村に保存＝配ると付いて回る / 端末に保存）。「メッセージ表示/非表示」は節を
足せば増える器にしたが、**機構は今も線の削除 1 つだけ**。用語境界を 1 つ追加
（上の「同型の二例目」）。

**P4 完了**（2026-08-03）— コアのコード変更ゼロ。UI 側 `errors.{code}` 辞書 31 本 +
`formatError`（ja = message そのまま / 他言語 = 訳語 + 原文併記 / 未知 code =
原文へ）。表示 10 箇所を集約。**System 行は訳さないと決定**（会話ログの一部で
あり UI 文言ではない。表示時に訳すには kind+params の構造化 = ワイヤ +
session_store の加算が要り、やるなら別 Spec。利用者の異論があれば差し戻し可）。

**P3b 完了**（2026-08-03）— 残り全コンポーネント + useOrchestrator + index.html を
辞書化（並列 4 分担、辞書はフラグメント統合で競合ゼロ）。機構の変更 3 つ:
`STATUS_LABELS`/`WEEKDAY_LABELS` → `*_LABEL_KEYS`（辞書キーの表）、`batchLabel` は
`titleKey`+`titleParams` を返す（純関数は i18n を知らない）、`guard`/`mutate` の
`${label}に失敗しました` 連結を `opFailed` + `op.*` の 2 段へ（語順が言語を
またげない）。意図的な残り: 言語名ラベル・実ファイル名・console ログ・コア産
文字列（P4）。既知の残: toLocaleString("ja-JP") の日時整形は言語に追従しない。
検収は機械 2 段（テンプレート走査 + リテラル走査）で完了。

**P3a 完了**（2026-08-03）— 村民 5 体 + ジェミーの i18n 調査と突き合わせ、
「判断次第」3 点は凍結済み契約で決着（A-4 訳さない / rust-i18n 不採用 = 案 A /
タイトルはブランド名）。コア `world::Language`（生 String で受けて未知は
未確定へ・sys-locale で初回確定・**再起動で再判定しない**）+ IPC 2 本 +
vue-i18n 11 基盤（辞書 `locales/{ja,en}.json`・**鍵集合一致テスト**）+
言語設定ページ（保存 → 成功 → setLocale の順）+ 代表 3 ファイル差し替え
（TitleBar / App / SettingsDialog）。結合 2 本 = 確定保存・再判定なし /
**言語切り替え前後で system ブロック不変**（PromptProbeBackend）。
**P3b 残**: 残り 18 コンポーネント + useOrchestrator トースト + index.html
`<title>`（一覧は Spec の P3 設計記録）。将来言語 zh-Hans / de / fr は
辞書追加 + 契約の選択肢改訂で足せる形にしてある。

**P2 完了**（2026-08-03）— `useUiSettings.ts`（`concordia.settings.v1`、書き込みは
watch = 値が変わったときだけなので「触らず閉じたら不変」が構造で成立。boolean
以外は既定へ落とす）+ `TopologyMap.removeEdge` の先頭に `askConfirm`（既定 ON・
id 併記・双方向は「両方向とも切れます」）+ SettingsDialog「この画面の設定 >
線削除の確認」（即保存を明記）。vitest 4 本追加（63→67）。

**P1 完了**（2026-08-03）— コア `token_budget` / `set_token_budget`（persist 経路は
`upsert_template` と同じ、`Some(0)` は新設 `INVALID_TOKEN_BUDGET` で拒否）、
IPC 2 本、`SettingsDialog.vue`（2 ペイン + 左メニュー。**未実装ページは目録に
並べない**）、TitleBar に COG + 「システム設定」。結合 3 本（保存 → 依頼 →
新天井で打ち切り = 再起動なしで効く / なしへ戻すとキーごと消える / 0 拒否は
メモリもファイルも触らない）。⚙ 文言の回収は予定 3 箇所 + 実行時 4 箇所
（error.rs / client.rs ×2 / useOrchestrator.ts）— **COG を入れた時点で
「⚙ の画面」は間違った行き先を指すようになる**ため全て
「「モデルテンプレートを管理」の画面」へ統一。README 日英の ⚙ 8 箇所は P5 で回収。

**P0 完了**（2026-08-03）— `data_contract.yaml` の `settings_contract`
（ConfigFileKind 配下、`session_store` / `blackboard_contract` と同じ棚）。
凍結 5 点: 置き場の 2 分割・OrchestratorConfig を出さない線・言語の 3 層・
アイコン規約（SVG）・即時反映の定義。P0 で具体名を 2 つ確定 —
言語は `world.json` の `language`（"ja" | "en"）、画面設定は `localStorage` の
`concordia.settings.v1`（`confirmEdgeDelete`、既定 true。命名は
`concordia.layout.v1` に揃えた）。**data_contract.yaml は既存の `- **` 箇条書き
66 箇所が YAML alias と誤読される既知の件（別セッション task_62bfa37a）で
strict パーサでは元から通らない — settings_contract はリテラルブロックなので
無関係**（前半だけの切り出しパースで検証済み）。次は P1: 天井の読み書き
IPC 2 本 + `SettingsDialog.vue` の骨格（2 ペイン + 左メニュー + COG 入口）。

起点は利用者の言葉（2026-08-03）— 「**僕もなにが設定できるのか分かりやすい**」。
主目的は**既にある設定の可視化**。トークンの天井（Spec 11）は `world.json` の
`tokenBudget` として既に存在するのに、**手編集以外に触る方法が無く、天井の
無い村は起動のたびに WARN が出るのに直す場所が画面に無い**。

**新規の設定は 2 つ足す（言語・線削除の確認）**。rev1 は「S4 は設定の追加では
ない」と書いていたが虚偽で、査読で潰した。**`OrchestratorConfig` は出さない**
（履歴の往復数・hop 上限・広場ログの窓 ほか 12 個）— ここが「増やさない」の本体。

- **入口はタイトルバーの「スケジュール」の右**、COG + 「システム設定」。画面は
  **2 ペイン**（左メニュー・右ページ、Kataribe と同型）。**左メニューが設定できる
  ものの目録そのもの**になるのが Goal への効き筋（利用者指示 2026-08-03）
- **アイコンは SVG。絵文字を恒久要素に使わない**（rev3 で機構へ）— 絵文字は
  フォント依存で字形と大きさが環境ごとに変わり、`currentColor` を継承しないので
  テーマの配色に追従しない。**システム設定は COG を使ってよい**（カードの設定は
  利用者が鉛筆の SVG へ変更済みで、歯車は空いた）
- **天井の表示は実効トークンの整数**（rev3 の D1）。素の値は参考でも出さない —
  打ち切り判定に使われるのは実効値で、素の値を並べると「まだ余裕があるのに
  切られた」という誤った読みを作る。入力の内訳が画面に無いので逆算もできない
- **線削除の確認は既定 ON**（rev3 の D6）。他 6 種の破壊的操作はすべて確認あり、
  線だけ無いのは整合性の破れ。OFF は 1 回切れば済むが、ON を知らずに失うと戻せない
- **言語は `world.json` に置く**（rev2 で `localStorage` から変更）。System 行は
  **会話ログに保存される**ので、言語は村の共有物に触る。ペイン幅と同じ棚に置けない
- **多言語化は 3 層** — (1) UI 文言（引用符付き 324 種 + 地の文 81 箇所）
  (2) コアの文言（`#[error]` 31 本 + System 行）(3) システムプロンプト・ツール説明。
  **(3) は訳さない**（訳すのは翻訳ではなくプロンプトの変更で、Spec 08 の
  「束ねの文言は 1 字も変えない」と衝突する）。コアは言語を知らない（案 A 固定）
- **地図の線の削除だけが、確認なしで消える唯一の破壊的操作**（実測で棚卸し済み。
  地図の破壊的ハンドラは `@edge-click` の 1 つだけ）。機構は Spec 12 P3 の
  `askConfirm` が既にある
- **査読の前提誤り 2 件をコードで反証**: (a)「天井の即時反映にホットリロード IPC が
  要る」— `world.json` は投影で、天井は**依頼ごと**にメモリの `World` から読まれる
  （`new_root_budget`）。IPC 1 本で次の依頼から効く (b)「×0.1 があるから `u64` に
  入らない」— 重みは**内部の milli 表現**に閉じており、天井は実効トークンの整数。
  `Some(0)` の正規化もコアに既にある

## フッターのステータスバー（2026-08-03 着地。Spec 不要と判断）

利用者要望 —「**管理ツールなのでスクショするときに時刻があったほうがいい**」。
`StatusBar.vue`（22px の帯・最下段）+ `lib/clock.ts`（純関数・単体 5 本）。
`App.vue` の**グリッドの外**（flex 列の末尾）に置く = 3 ペインの分割規則に
巻き込まない。契約面はゼロ（新しい型・enum・ワイヤ・IPC が 1 つも無い）ので
data_contract は触っていない。**Spec を切らなかったのはそのため** — 決める
ことが無い変更に査読の窓は要らない。

**時計は言語設定に追従させない。これは追従漏れではない。**
形式を `YYYY-MM-DD HH:MM:SS` に固定し ja / en で同一にしてある。理由は用途 —
`diag.rs:132` の `%Y-%m-%d %H:%M:%S%.3f` と揃えることで、**撮った画面の時刻から
`concordia.log` の該当行を目で引ける**。en のロケール整形
（`8/3/2026, 10:15:03 PM`）にすると、月日の順が読み手の国で変わる・12 時間制の
判別が要る・ログと突き合わせられない、が同時に起きる。

**Spec 13 P3b の「既知の残: `toLocaleString("ja-JP")` の日時整形は言語に追従
しない」とは別件。** あちらは追従させるべきなのに漏れている箇所、こちらは
**意図して固定している**箇所。同じ「言語に追従しない」でも処方が逆なので、
まとめて直そうとしないこと。

現在時刻は**引数で受け取る**（`formatClock(now: Date)`）— `schedule.rs:5` の
「内部で `Local::now()` を呼ばない」と同じ規律。内部で `new Date()` を読むと、
テストが壁時計に依存して特定の時刻でだけ落ちるものになる。

**左側は意図的に空けてある。** 「常に見えている細い帯」なので、常駐させる価値の
ある情報しか置かない。増やすときは**常に見る必要があるか**を毎回問う。

## システム設定への追加候補（2026-08-03 利用者。小出しに来る前提）

Spec 13 の器（左メニュー = 目録 / メッセージ表示/非表示のページ）へ後から
足していく分。**まだ起票していない。** 出てきた順に置く:

- **テーマ** — ユーザーインターフェース。配色は `style.css` の `@theme` 1 箇所で
  決まっている状態を既に保っているので（生のパレット禁止の規律）、切り替えの
  受け皿はある。保存先は端末側（`localStorage`）が素直
- **ツール実行ログを会話で非表示にする** — **「メッセージ表示/非表示」の 2 件目**。
  P2 で器を作ったのはこのため。ただし会話ペインのツール行は
  「**エージェントが何をしたかは会話ログに現れない**」を埋めるために置いた計器
  （`chatRows` の設計理由）なので、既定 ON は動かさない
- **起動時に一括起動 ON のサーヴァントを立ち上げる** — **既存の凍結された
  不変条件を変える**（`batch_start_invariant`: 「**自動起動ではない。**
  アプリを開いた時点では誰も走らない — 開いただけで課金が始まる作りにしない」。
  data_contract:583 / model.rs:201 / README.md:186 / failures.md #1701 に接地）。
  **既定 OFF の明示的なオプトインなら不変条件の理由（開いただけで課金しない）は
  保てる** — 起票時はそこを軸に書くこと。既定 ON にすると理由ごと壊れる

## 次の大物候補: 役職（Role）のラベル（2026-08-03 利用者。未起票）

入口は**タイトルバーの「条例」の次くらい**。Role は 2 役を兼ねる:

1. **サーヴァントを作るときのテンプレート**（雛形としての Role）
2. **サーヴァント自身の判断基準** —「このサーヴァントはこういう役割なんだ」を
   本人が読む。つまり**プロンプト素材**になる

起票前に確かめる点が 3 つ（設計はまだしない）:

- **(2) はプロンプトへ入る** = 安定プレフィックスの一部になり、
  `ordinance_contract` の連結順（条例 → 自分について → … → 顔ぶれ → Memory）の
  どこへ挿すかで、キャッシュの安定境界が動く
- **他人の役職が見えるべきか**は Spec 06（顔ぶれ）の拡張になる。見せるなら
  「誰に何を頼めるか」の判断材料が増える一方、毎ターンの可変注入は増やさない
  規律（#45 / 論文 1 の品質仮説）と衝突しないか要確認
- **Role を「進行役フラグ」にしない。** 既存の判断は
  「進行役はトポロジーの帰結（plan は接続 2 体以上で生える）」「線は人が引く」。
  役職ラベルは**人格・語彙の設計**であって、配送や権限の機構ではない —
  ここが混ざると AdaptOrch を採らなかった根拠ごと崩れる

### 起票前調査は完了 → **草案 [specs/draft_role-label.md](specs/draft_role-label.md)**

**起票は保留**（2026-08-03 利用者判断・Claude のリセットまで待つ）。3 点は全部
コードで接地済みで、詳細・行番号・反証の口は草案に書いた。ここには索引だけ置く。

- **主論点が 3 点より上に出た: Role は「コピー」か「参照」か。** 作成時に本文を
  `Construct.md` へ流し込むだけなら**コア変更ゼロ・契約変更ゼロ**で 2 役とも満たせる。
  代償は後から Role を直しても既存の個体に伝わらないこと。**線は「後から直したとき
  反映されてほしいか」の 1 問**で、条例が既にその役を担っているので参照方式は
  同じ性質の層を 2 つにする。**草案の推しはコピー方式**
- (1) 挿入点 — `config_store.rs:383` が安定境界。**編集頻度を先に決めてから位置を決める**
- (2) 他人の役職 — 顔ぶれは境界の**外**なのでキャッシュは割れない。増えるのは**量**。
  #45 が問題にしたのは揺れであって量ではないので、同じ規律では自動的に却下できない
- (3) 進行役フラグ — `orchestrator.rs:5025` の doc コメントが既に明記。
  起票時にやるのは新しい判断ではなく**凍結**（data_contract へ 1 行）

## Spec の状態

- Spec 01（sd / yq 書き換え系）〜 03（新規チャット + 広場ログ）: **Done**
- Spec 04（plan — 並列委譲と合流）: **rev3 査読承認 → Phase 0〜2 完了**（2026-07-29）。
  残は実機確認のみ（ザリに「1〜3 号へそれぞれ調べさせてまとめて」と頼む）。
  **2026-08-02 の Spec 10 実機確認で配送側は観測済み** — ザリが plan で 3 体へ
  撒き、並列に grep が走った。ただしその波は打ち切りの検証で切ったため、
  **束ねて返る完走は未観測のまま**（残はそこだけ）。
  査読で `to` の自由文字列・壁時計テスト・失敗分類の穴が出て rev3 で塞いだ。
  併せて `ask` の既存バグ（相手が転送すると「答えが返らなかった」と嘘を返す）を
  同経路なので同時修正（failures.md #35）
- Spec 05（Gemini ネイティブ経路と Google 検索による接地）: **Done**（2026-07-29）。
  実機確認まで完了し、`sources` は空＝**参照元 URL は返ってこない**が実測で確定。
  検索語と発表元だけを言う告知（Phase 2）が唯一の処方として残った。
  本 Spec は例外的に実装先行（Gemini の実挙動が実測でしか決まらなかったため）
- Spec 07（スケジュール実行 — 時刻で発火する依頼）: **Done**（2026-07-30
  実機確認まで完了）。`ScheduledTask` / `Recurrence` / `Weekday` と発火の規則は
  `data_contract.yaml` で**凍結済み**。Phase 0〜5
  （多重起動の排他=実機確認済み / 発火規則の純関数=テスト 18 本 /
  配線=結合テスト 8 本 / UI=`ScheduleDialog.vue` + IPC 4 本 / README 整合）+
  実機確認 2 本（`interval 1 分` の発火で会話ペインに応答が出る /
  停止中スキップの 1 行が 1 回だけ出る）。
  Phase 3 で契約に 2 点追記 — 予定の所有者は in-memory でファイルは投影・
  読めないファイルは書き込み拒否（`SCHEDULE_STORE_BLOCKED`）。
  トレイのマーク・チップス通知は利用者判断で別 UI 作業に分離。
  起点は利用者要望（毎週木曜 17 時に鐘／定期的なサイト監視）。
  rev1 で表に出した制約 2 つ — (1) 今はデーモンではなく起動していない間は
  動かない（**製品の行き先はタスクトレイ常駐**なので限界は将来消える。
  発火規則は常駐後も正しいまま）、(2) 「鐘を鳴らす」は同梱ツールに音も
  コマンド実行も無いため本 Spec では完結しない（P4）。
  rev2 で査読が **rev1 の実バグ**を出した — 猶予を全種別に掛けていたため
  `interval` の「再開時に 1 回だけ」が成立せず 0 回になっていた。修正は
  「due を最遅点にする」と「猶予を壁時計系だけに限る」の 2 つが揃って初めて成立。
  併せて多重起動の排他を P0 として追加（予定が入ると二重発火は正しさの問題）。
  `chrono` 採用は査読で承認済み。rev2 の査読で入った差分は 2 つだけ —
  `lastFiredMs` → `lastConsumedDueMs` の改名（飛ばした予定も書く欄なので
  「fired」は嘘）と、猶予超過で飛ばした予定を会話ログでなく debug ログへ倒す判断
- Spec 09（file — ファイル・フォルダ操作の同梱ツール）: **Done**（2026-08-01
  実機確認まで完了）。ザリが `docs/spec_coverage.md` を書いた — `docs/` は
  存在しなかったので**親フォルダの自動作成も同時に確認**できた。
  1 ツール + op 閉じた列挙（read / write / mkdir / move / copy / remove）で
  `data_contract.yaml` の `file_tool_contract` に**凍結済み**。
  起点は実機の空転（failures.md #39 — 新規作成が無く sd で 12 ラウンド試み続けた）。
  **境界が 2 種類あることが実装の核** — `resolve_in_work_dir`（実在パス専用。
  sd / yq の「新規作成なし」担保はこの canonicalize が根拠なので**触らない**）と
  新設 `resolve_creatable`（実在しない宛先用の 3 段検査）。削除は `trash` crate で
  ごみ箱のみ・完全削除の経路なし。依存追加は trash 1 つ（本体は std::fs）
- Spec 08（波ペイン — plan 実行の可視化、Airflow Grid 相当。GUI 表示名は
  2026-08-02 から「作業状況」で黒板とタブ切り替え、契約は不変）: **Phase 0〜3 完了**
  （2026-07-30）。実機確認 2 本のうち **1 本目は 2026-08-02 に観測済み**
  （波が列として現れ、セルが answered / interrupted の混在で**個別に**確定 —
  Spec 10 の実機確認と同じ走行）。残は「GUI 再読み込みで波が残る」の 1 本。rev2 査読（重大 7 / 明確化 6 / 改善 3）を全反映して
  凍結。`PlanTaskState` / `Reply { text, kind }` / `PlanWaveRecord` /
  `CoreEvent` 3 種 / `list_plan_waves` は `data_contract.yaml` で**凍結済み**。
  分類は文言 parse でなく型で運ぶ（刻み手は handle_message の 1 箇所）。
  `plan_id` は 1 始まり 0 予約・モデル非公開。リング上限 50・in-memory・
  配送ゼロの plan は記録しない（stderr と数え方一致は検証済み）。
  Spec 04 Notes 12 の発火条件 (b) の成立が起点（利用者要望 2026-07-30）
- Spec 11（トークン予算 — 依頼の因果に掛ける自動の天井）: **rev2 承認 →
  P0〜P1 完了**（2026-08-02 起票 → 同日査読で実装指摘 7 点 + D1/D2 裁定 →
  rev2 → LGTM → data_contract の token_budget ブロック凍結 +
  PlanTaskState へ budget_exhausted 加算 → budget.rs の純機構
  単体 9 本。欠落見積もりの分母は UTF-8 バイト数 s.len() で確定・
  tokenBudget は camelCase 自動対応で rename 不要）→ **P2〜P4 の機械側も
  同日完了**。P2 配線 = 根 2 箇所（ユーザー発話の宛先ごと / 予定発火）で
  new_root_budget、Envelope.budget は cancel と独立の Option<Arc<BudgetPool>>、
  周回境界で try_reserve → LLM → debit（usage 欠落はバイト見積もり）、
  deliver_and_wait の事前検査（尽きたら配送を始めない）、
  finish_budget_exhausted（System 行は note_exhausted の CAS で因果 1 本）。
  P3 投影 = 「予算切れ」ラベル・失敗色・加算的変更の回帰。P4 = README 日英
  （村規模別の推奨値ガイド込み）。結合 5 本（S1 打ち切り / 新予算で再走 /
  波の配送前停止 / 転送の同一 Arc / 既定値は新規 world.json だけ）。
  **残は実機確認 2 本のみ**（小天井で止まる / 既定天井で健全依頼が完走）。実効トークン = 未キャッシュ ×1 + キャッシュ済み ×0.1 +
  出力 ×4、**内部は milli 建て AtomicU64・切り上げ**（Atomic に浮動小数は
  無い）。try_reserve / consume 分離で**オーバーシュート 1 呼び出し分許容**
  （check→LLM→減算は一体の atomic にできない）。`Envelope.budget` は
  cancel と独立の `Option<Arc<BudgetPool>>`、天井は world.json の
  `Option<u64>`（0 のマジック値なし・新規のみ Some(1M)・既存 None は
  起動 WARN で次の道）。歯止めの優先順位 cancel > budget_exhausted >
  RepeatGuard > hop を凍結。重みは P0 で code constant 凍結
  （モデル別設定を作らない）。根拠実測: 健全 6 体依頼 = 素 387K
  （実効 ≈ 250K）vs #41 病理 = 1 ターン素 2M。先行実装
  arXiv:2606.04056（実在確認済み）の delegation-fanout race 11 件が
  転送同一 Arc テストの根拠
- Spec 06（顔ぶれの提示と入退室の通知）: **Done**（2026-07-31）。実機確認まで
  完了 — ザリは停止中の相手へ一度も投げず、稼働中 3 体だけへ plan を撒いた
  （点呼のトークンが消えた）。実機の村は id↔表示名の番号が全部ずれており、
  id 併記が初回から効いた。起点は実機のザリ本人の要望。P0（表示名の一意性）は
  利用者指示で先行実装。rev4 査読で stable_len の定義矛盾（Skill と Memory が
  隣接していた間だけ 2 つの記述が同じ点を指せていた）が出て境界を再定義した

## 外部研究からの採用候補（2026-07-31 接地・雑談由来）

arXiv の 3 本を実在確認して読んだ（ジェミーが発見、私が ID から独立に接地）。
**3 本は互いに逆を向いており、境界はタスクの既知性**にある —
手順が既知なら外部制御は害、未知なら分解と検証に価値がある。

- arXiv:2604.27891 In-Context Prompting Obsoletes Agent Orchestration for
  Procedural Tasks（Melbourne / i14、5 著者。条件あたり 200 会話）
- arXiv:2602.16873 AdaptOrch（単著・査読venue なし）
- arXiv:2603.11445 VMAO（ICLR 2026 Workshop on MALGAI。25 問・LLM 判定）

### 採る: plan の束ねの前に検証を挟む（VMAO の Verify 段）

`plan` は**答えを検証していない**。ワーカーの応答が空でも、矛盾していても、
依頼を取り違えていても、そのまま束ねて進行役へ返す。VMAO はここに検証器を置いて
completeness 3.1 → 4.2（1〜5、LLM 判定、25 問）を報告している。

**ただし機構を足す前に、既存の機構で書けるか確かめること。** 進行役が束ねを
受け取ったあと、検証役のサーヴァントへ `ask` で投げれば同じ形になる —
コード変更ゼロで、条例か SKILL.md に手順として書ける。まずそちらを試し、
「毎回書くのが面倒」が実測で確認できてから機構化を検討する。
（設定を増やさないという軸から、この順序は動かさない）
**→ 条例へ書いて着地**（2026-08-02。文言は上の運用図対応の節）。解説記事
（deepsquare.jp 2026-05-03、宮原太陽）で Verify 段の設計を追加接地 —
検証器は実行側と**別の**モデル（自己評価バイアス対策。条例では「束ねに
参加していないサーヴァント」として反映）、検査 6 項目（status / coverage /
confidence / missing_aspects / contradictions / recommendation。条例では
空・取り違え・矛盾・欠落の 4 点 + 三択の結論に簡約）。

再計画（Replan）は**採らない**。波を自動で回し直す機構は、失敗ループの
トークン燃焼（#41 / #42）と正面から衝突する。止める側の作業が終わるまで、
自動で仕事を増やす機構は入れない。**不採用の根拠は解説記事の実測で強まった** —
VMAO 全体はトークン ×8.5・実行時間 ×5.5 で、クエリの 75% 以上が品質基準では
なく**リソース制約で打ち切られている**。検証 1 回の ask に留め、回し直しの
判断は進行役と人に残す（条例の「聞き直しは 1 巡まで」はこの数字が根拠）。

### 測る: 毎ターンの可変注入が品質を落としていないか（論文 1 の含意）

論文 1 の批判対象は「毎ターン指示を注入する外部層」で、Concordia は routing を
注入していないが、**顔ぶれ・入退室通知・広場ログ**を毎ターン注入している。
routing ではなく文脈だが、「毎ターン形が変わるものを差し込む」形は同じ。

**キャッシュの理由で静的にしたい部分と、品質の理由で静的にしたい部分が
同じ可能性がある**（README のプロンプトキャッシュ節「提示は静的、状態は動的」に、
論文 1 は品質側の根拠を与えうる）。これは未測定の仮説。

実験の口は既にある — `hearsRoomLog` のオプトアウトで、広場ログ有り／無しの
個体を並べれば失敗率が比べられる。#42 の実装で入力コストが下がったあとに測ると、
コスト差と品質差を分けて読める。

### 採らない: トポロジーの自動切り替え（AdaptOrch）

「モデル選択よりトポロジーが効く」という主張自体は村の地図の存在意義を補強する。
だが**依存グラフを推定して構造を勝手に変える機構は入れない**。利用者から見て
何が起きるか予測できない方向に倒れ、「設定が少なくて分かりやすい」と衝突する。
**線は人が引く。** 単著・査読なし・LLM ベンチで 12〜23% という証拠の強さも、
設計を変える根拠には足りない。

### 運用として既に採れているもの（論文 1）

手順が既知のタスクは、外部 routing ではなく**手順を丸ごと SKILL.md に書いて
自走させる**のが論文 1 の結論で、Concordia は機構追加ゼロでこれができる
（SKILL.md がそのまま system prompt の安定部分に入る）。
村の運用として書き方を推奨する余地がある。
