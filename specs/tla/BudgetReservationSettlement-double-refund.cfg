\* 壊れた版 1: commit が清算したうえで Drop も全額返す（committed フラグ設計の事故）。
\* 期待: AccountingHolds が VIOLATED。**Spec 38 承認査読 (a) の裏づけ**。
\*
\* 実測（2026-08-15・TLC 2.19・JBR 21）:
\*   Error: Invariant AccountingHolds is violated.
\*   4 states generated, 4 distinct, depth 3（予約 → 二重返金で即破れる）
\*   **move セマンティクスを選んだ判断（承認査読 a）の機械の裏づけ。**
\*   committed フラグ設計はこの経路を型で消せない。
\*
\* **違反しなくなったら直すのは .cfg ではなく .tla のほう。**
SPECIFICATION SpecDoubleRefund
CONSTANTS
    Agents = {a1, a2}
    Ceiling = 4
    Estimate = 2
    Actuals = {1, 3}
    MaxCalls = 2
INVARIANT TypeOK
INVARIANT AccountingHolds
INVARIANT RemainingNeverExceedsCeiling
INVARIANT OvershootWithinEstimateError
CHECK_DEADLOCK FALSE
