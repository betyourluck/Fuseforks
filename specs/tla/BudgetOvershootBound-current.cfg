\* 出荷済みの形（try_reserve = load のみ）。
\* 期待: OvershootAtMostOneCall が VIOLATED
\*       （`run_turn` 旧注釈の主張が並列の波では成り立たない、の機械証明）
\*
\* 実測（2026-08-14・TLC 2.19・JBR 21）:
\*   Invariant OvershootAtMostOneCall is violated.
\*   State 2-4: ReserveByLoad x3（remaining=1 のまま 3 体とも通る）
\*   State 5-7: DebitByLoad x3 → spent=3（天井 1・主張の上限 2 を超える）
\*   35 states generated, 20 distinct, depth 7
\*   **OvershootAtMostConcurrency は保たれた** = 本当の上限は
\*   天井 + (同時に飛ぶ本数 x 1 呼び出しの実費)。
SPECIFICATION SpecCurrent
CONSTANTS
    Agents = {a1, a2, a3}
    Ceiling = 1
    CallCost = 1
INVARIANT TypeOK
INVARIANT OvershootAtMostConcurrency
INVARIANT OvershootAtMostOneCall
CHECK_DEADLOCK FALSE
