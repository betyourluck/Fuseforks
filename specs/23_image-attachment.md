# Spec: 画像の添付 — 入力欄からサーヴァントへ画像を送る

**ID**: 23
**Date**: 2026-08-06
**Status**: **rev3 承認 → P0〜P3 完了（2026-08-06）→ 次は P4（フロント）**。
未決はゼロ — **D1 は利用者裁定で「1 ターン限り」に確定**、**D3 の既定は
P0 の実測で WebP に確定**（5 系統 × 2 形式 = 10 通りすべて通過。下記「P0 実測結果」。
JPEG フォールバックは保険として維持 — xAI は公式文書が jpg/png のみと明記しており、
文書どおりに締められる日が来うる。ローカル互換 llama.cpp / vLLM は未実測）。
2 巡目の査読は rev1 に対するもので、8 点中 5 点は rev2 で反映済みだった。
新規 2 点（Goal の語の切り分け・`validate_icon` の扱い）を rev3 で反映。
トークンの数字は 2 巡とも同じ指摘だったが一次資料で据え置き（Notes 7・10）
**Branch**: なし（main へ Phase 単位で直接コミット）

## Goal

会話ペインの入力欄に画像を添付し、宛先のサーヴァントへ送れるようにする。

起点は利用者の言葉（2026-08-06）—「**おそらく僕はコストの面で使いたくない機能だが、
無ければ『どうして無いの？』と聞かれそうな機能だ。なので機能は入れておきたい**」。

**この Goal は「画像を活用する」ではない。「目立つ欠落を埋める」ことと
「使わない人に払わせない」ことの両立が本体である。** 設計の判断はすべてここから出る:

- **使わない村の固定費はゼロ** — ツールを 1 本も増やさないので、**モデルへ提示する
  スキーマもシステムプロンプトも 1 字も伸びない**。**添付が無い発話のシリアライズ
  結果は今日とバイト等価**（P2 のテストで凍結）。
  **Rust の型は増える**（`Attachment` / 各層の加算欄 / `AnthropicRequestBlock::Image`）—
  「1 字も伸びない」のは**モデルが読む面とワイヤの出力**であって、コードではない
  （rev3: 査読 1。両者を同じ語で書くと、実装時に「型を増やしたから Goal 違反だ」と
  読まれるか、逆に「バイト等価も緩めてよい」と読まれる）
- **払うのは添付した人だけ**、しかも**その 1 ターンだけ**（D1）
- **既定は「使える」で OFF ではない** — `run` のような明示オプトインにしない。
  オプトインが要るのは*実行能力*が増えるからで、画像は能力ではなく入力の種類

## 現況（実測 2026-08-06）

**LLM 層に画像・base64 という語は 1 箇所も無い。** 型は 3 層とも素の文字列:

| 層 | 型 | 位置 |
|---|---|---|
| canonical | `ChatMessage.content: String` | `canonical.rs:40` |
| OpenAI 互換ワイヤ | `OaiMessage.content: Option<String>` | `wire.rs:46` |
| Anthropic ワイヤ | `AnthropicRequestBlock` = `Text` / `ToolUse` / `ToolResult` の 3 種 | `wire.rs:335` |
| Gemini ネイティブワイヤ | `GeminiPart` は `text` だけ | `wire.rs:508` |
| 会話ログ | `AgentMessage.content: String` | `model.rs:656` |
| 永続化 | **`Record::Exchange { sent: String, replied: String }`** | `session_store.rs:115` |

**`Record::Exchange` が `String` であることが D1 を決める**（後述）。

### プロバイダの画像仕様（Anthropic は一次資料で確認・2026-08-06）

**Anthropic の公式ドキュメント（vision.md）で確定した事実**:

- **トークンは 28×28px のパッチ数**。`⌈幅/28⌉ × ⌈高さ/28⌉` が視覚トークン数。
  **`(w*h)/750` ではない**（あれは旧版の近似。28²=784 なので値は近いが、
  契約へ書くなら現行の式で書く）
- **ティアは 2 つ** — 高解像度（Claude 4.7 以降）は長辺 2576px・**上限 4,784 トークン**、
  標準は長辺 1568px・**上限 1,568 トークン**。超える画像は自動縮小される
- 形式は **JPEG / PNG / GIF / WebP**。**アニメーションは非対応で先頭フレームだけ**
  （アニメーション WebP を弾く根拠 — D3）
- source は **base64 / url / file_id の 3 種**（Bedrock と Vertex は base64 のみ）
- 上限は **10MB/枚**（Bedrock・Vertex は 5MB）。**20 枚を超えるリクエストでは
  1 枚 2000px の制限**が別に掛かる（D5 が 1 枚なので当たらない）
- **画像はテキストより前に置くのが推奨**（P2 の組み立て順に効く）

**互換層は保証されない**（査読の指摘。**未実測** — P0 で叩く）。xAI/Grok の公式は
`jpg/jpeg` と `png` のみと明記しており、自前の OpenAI 互換サーバ（llama.cpp /
vLLM 系）も WebP を拒むことがある。`Provider::detect` は判定できないホストを
互換へ倒す設計（`client.rs:51`）なので、**rev1 が「仕様上は通る」とした所が最初に落ちる**。

**Spec 05 で Gemini のネイティブ経路を作った事情は当てはまらない** — あれは
`google_search` が互換層に `400 Invalid tool type` で拒否されたからで、
**ツールの制約**だった。画像は `content` に乗るので互換層が知らない道具ではない。

### P0 実測結果（2026-08-06。WebP / JPEG × 5 系統 = 10 通り全部通過）

256×256 の図形画像（赤い円 + 青い矩形）を base64 で送り、応答が図形を
正しく読めたかまで見た（HTTP 200 だけでは「受理して無視」と区別できない）。
互換系は `image_url` の data URL、Anthropic は `source: {type: base64}`。
モデルは村のテンプレートに実在するものを使った:

| 接続先 | モデル | WebP | JPEG |
|---|---|---|---|
| Anthropic 公式（`/v1/messages`） | claude-haiku-4-5-20251001 | ✓ | ✓ |
| OpenAI 公式 | gpt-5.6-luna | ✓ | ✓ |
| Gemini 互換（`/v1beta/openai/`） | gemini-3.5-flash-lite | ✓ | ✓ |
| xAI | grok-4.3 | ✓ | ✓ |
| さくら互換 | preview/Qwen3-VL-30B-A3B-Instruct | ✓ | ✓ |

読み方 3 点:

- **D3 の既定は WebP で確定。** rev1 が恐れた「互換層が軒並み 400」は起きなかった
- **xAI は公式文書（jpg/jpeg・png のみ）と実装が食い違っている。** WebP は
  実測では通るが**文書上は非対応** — 文書どおりに締められる日が来うるので、
  JPEG フォールバック（D3）は保険として維持する。**「文書が実測と食い違う」は
  「実測が常に勝つ」ではない** — 文書は将来の実装の予告でもある
- **ローカル互換（llama.cpp / vLLM）は未実測**（実測時にサーバ不在）。
  llama.cpp の vision 対応はモデルと mmproj 次第で、WebP デコードを持たない
  ビルドがありうる。ここが D3 フォールバックの残る受益者

**入口の部品は揃っている**: `dialog:allow-open` は scope 無しで許可済み
（`capabilities/default.json`）。Spec 19 の画像機構（**WebP 1 形式へ固定** /
`validate_icon` / `write_icon_at` / `read_icon_at` / 「UI が WebP へ変換して送る」規約）も
そのまま延長できる。

## 設計の核

**`AgentMessage` へ加算的な欄を 1 つ足し、画像の実体はファイルへ置く。**
canonical と各ワイヤは**送信の瞬間だけ**ブロック列へ組み立てる。

```
入力欄（貼り付け / 参照…）
  → フロントで縮小・WebP へ変換（WebWorker。D3）
  → send_user_message(… , attachments)
  → {workspace}/attachments/{uuid}.webp へ保存（バックエンドで再検証）
  → AgentMessage.attachments = [参照]        ← 会話ログ・再開後の表示はここ
  → そのターンのプロンプト組み立て時だけ画像ブロックへ展開（画像はテキストより前）
  → 履歴（Record::Exchange.sent）へは入れない  ← D1
```

- **`AgentMessage.attachments: Vec<Attachment>` は加算**（`#[serde(default,
  skip_serializing_if = "Vec::is_empty")]`）。**`grounding` と同じ形**で、
  添付が無い発話のシリアライズ結果は今日と 1 バイトも変わらない
- **本体は base64 ではなくファイル参照**。`{workspace}/attachments/{uuid}.webp`
  へ置き、`AgentMessage` は id・寸法・元ファイル名だけ持つ
- **`ChatMessage.content: String` は変えない**（D2）。添付は
  `ChatMessage.attachments`（加算）で運び、adapter が組み立てる
- **ログが 2 つあることを曖昧にしない**（rev3: 査読 2）。この村は元から
  履歴が 2 層で（Spec 12 の起点そのもの）、**添付についてはこう分かれる**:

  | 問い | 答える層 | 添付を持つか |
  |---|---|---|
  | 人が画面で何を見るか | `Shared.log` / `AgentMessage`（redb の `Record::Message`） | **持つ**（ファイル参照） |
  | 次のターンのプロンプトに何が載るか | `AgentRecord.history` / `Record::Exchange` | **持たない**（D1） |

  **`restore_histories` は後者だけを読む**ので、再起動しても
  「画面には残るがモデルには渡らない」が保たれる。**2 つのログのどちらが正本かを
  問うと答えられない** — 問いが 2 つあり、それぞれに正本が 1 つずつある
  （Spec 12 の「片方から他方を復元できない」の同型）

## 決めること

- **D1（最重要）画像は 1 ターン限り。履歴に残さない。** 根拠 3 つ:
  1. **`Record::Exchange.sent` が `String`** — 残すなら永続化の形が変わる
     （redb の既存レコードと非互換）
  2. **滑る窓は 8 往復**。残すと同じ画像が最大 8 ターン再送される。
     実額は下表（rev2 で一次資料の式に基づき計算し直した）:

     | 送る画像 | 1 ターン | 8 ターン再送 |
     |---|---|---|
     | 縮小なし・高解像度ティア上限 | 4,784 | **38,272** |
     | D3 の 1568px 制限・16:9（1568×882） | 1,792 | **14,336** |
     | D3 の 1568px 制限・正方形（1568×1568） | 3,136 | 25,088 |

  3. Goal が「コスト面で使いたくない」— 残す設計は Goal と正面から反する

  **代償を正直に書く**: 2 ターン目に「さっきの画像の左側は？」と訊いても
  モデルには見えていない。**ただし完全に失われるわけではない** — モデルが
  1 ターン目に書いた説明は履歴に残るので、そこを足がかりにできる。

  **決着（2026-08-06 利用者裁定）: 1 ターン限りのまま P0 へ進む。**
  理由は Goal への差し戻し —「払う量と人の操作の 1 対 1 対応」が本 Spec の背骨で、
  履歴に残すと 1 回の添付が 8 ターンぶんの支払いに化けてその対応が切れる。
  **足りなければ「もう一度添付する」という人の操作をもう 1 回挟めばよく、
  自動で残すより因果が明確**（利用者の言葉）。上表の 14,336〜38,272 は払わない
- **D2 `ChatMessage.content: String` は変えない**。型を丸ごとブロック列へ変えると
  `push_exchange` / `exchange_pair` / `restore_histories` / `session_store` の
  4 箇所が同時に動き、#45 と #29 の規律を全部書き直すことになる
- **D3 形式は WebP を既定とし、互換層が拒んだら JPEG へ 1 度だけ再エンコードして
  再送する**（rev2。査読 1）。フロントは常に WebP を作る。`Provider::OpenAiCompat`
  かつ画像付きで **400 が返った 1 回だけ** JPEG へ落として再試行し、それも落ちたら
  「この接続先は画像を受け付けません」と**画面へ出す**（黙って落とさない）。
  **アニメーション WebP は拒否**（先頭フレームしか渡らず、利用者の意図と食い違う）。
  **長辺の上限は 1568px** — 高解像度ティアの 2576px を使わないのは Goal のため
  （上表で 4,784 対 1,792）。**縮小したことを画面に出す**
- **D4 入口は「貼り付け」と「参照…」の 2 つ。ドラッグ&ドロップは入れない。**
  Spec 21 P0 の実測で **Tauri v2 の `dragDropEnabled` 既定 `true` が
  ページ内の `drop` を横取りする**ことが分かっている。**スクリーンショットの
  主経路は Ctrl+V なので実害は小さい**
- **D5 上限は 1 発話あたり 1 枚。元ファイル 10MB で拒否、変換後 2MB で拒否**
  （rev2: 元ファイルの門を追加。査読 6 — 20MB の HEIC がフロントへ届くと
  デコードで落ちる）
- **D6 サーヴァント同士の転送では画像を渡さない。ただし落としたことを System 行で
  必ず知らせる**（rev2。査読 8）。`ask` / `plan` / `transfer_to_*` の経路で
  添付を落とすとき、転送先の本文へ `（画像 1 枚は転送されません）` を入れる。
  **黙って落とすと、B がなぜ画像を見られないのか誰にも診断できない** —
  #44 の「歯止めの先に道を書く」と、Spec 12 P4 の「飛ばした相手が居たら
  System 行に必ず書く」の同型
- **D7 広場ログには添付の存在だけを書く**（`（画像 1 枚）`）。本文には出さない
- **D8（新設）Gemini は互換経路で通す。ネイティブ経路には画像を実装しない**
  （rev2。査読 5）。この村の Gemini ネイティブは `GeminiPart { text }`
  （`wire.rs:508`）で、画像を足すなら `inline_data` を新設することになる。
  **ネイティブ経路が要るのは Google 検索の接地だけ**（Spec 05 の凍結）なので、
  **画像と接地を同時に使う経路は作らない**。ネイティブ選択時に画像を添付したら
  D6 と同じく画面で断る
- **D9（新設）`{workspace}/attachments/` は GC する**（rev2。査読 2）。
  **起動時に 30 日より古いファイルを削除**し、合計 500MB を超えたら古い順に削る。
  **会話ログから参照が消えても実体は残る**（redb と file の 2 つの真実を
  同期させない — Spec 12 が index.json を捨てた判断と同じ）。GC で消えた画像の
  表示は「画像は保持期間を過ぎて削除されました」の枠にする

## Stories

- S1 利用者が入力欄へ画像を貼り付け（または「参照…」で選び）、宛先のサーヴァントが
  その画像を見て答える
- S2 画像を添付しない村・しないターンでは、送るワイヤも払うトークンも今日と同じ
- S3 添付した画像が会話ペインに残り、再起動して開き直しても（保持期間内なら）消えない
- S4 「この画像は今回のみモデルへ渡ります。2 ターン目以降はモデルからは見えません
  （画面には残ります）」が画面から読め、利用者が驚かない（rev2: 文言を具体化。査読 4）
- S5 画像を受け付けない接続先では、その旨が画面に出る（黙って落ちない）

## Tasks

- **P0 契約 + 実測**（rev2: 実測を P0 へ前倒し。査読 9）— **完了（2026-08-06）**:
  - ~~**先に `curl` で叩く**~~ **実測済み** — Anthropic 公式 / OpenAI 公式 /
    Gemini 互換 / xAI / さくら互換（Qwen3-VL）の 5 つに WebP と JPEG を投げ、
    10 通り全部通過（上記「P0 実測結果」）。**D3 の既定は WebP で確定**。
    ローカル互換（llama.cpp / vLLM）だけ未実測（サーバ不在。
    5 本目は実在の互換層であるさくらで代替した）
  - ~~`data_contract.yaml` へ `attachment_contract`~~ **凍結済み** —
    凍結 9 本: 加算欄の形 / 置き場 / **1 ターン限り（D1）** /
    形式とフォールバック（D3）/ 転送時の System 行（D6）/ 広場ログは存在のみ（D7）/
    ネイティブ Gemini 非対応（D8）/ **保持期間と容量上限（D9）** / 入口 2 つ（D4）。
    `prompt_cache` の invariants へ**画像は安定プレフィックスに入らない**ことを追記済み
- **P1 コア（純機構）** — **完了（2026-08-06。`attachment.rs` 新設・単体 16 本・
  workspace 全緑・clippy 警告ゼロ）**: `Attachment` 型 + `validate_attachment`（形式マジック・
  **アニメーション WebP の拒否**・サイズ・寸法）+ `{workspace}/attachments/` の
  読み書き + **GC**（D9）。

  **P1 実装記録（実装で決めた 5 点）**:
  1. **置き場は `attachment.rs` 新設で、`ConfigStore` には足さない** — 添付は
     設定ではない（`sessions.redb` が `SessionStore` を別に持つのと同じ分業）。
     `is_webp` は `attachment.rs` 側に置き、`validate_icon` が参照する向き
  2. **`validate_attachment` は検証と同時に寸法 `(w, h)` を返す** — 別々の関数に
     すると 2 回パースするか、検証を通さず寸法だけ読む経路が生える
  3. **GC は `gc_plan`（純関数）+ I/O の 2 層。現在時刻は引数で受け取る**
     （`schedule.rs` / `clock.ts` と同じ規律）。期限切れのテストは mtime を
     細工せず **now を 31 日進める**ことで再現した — 時刻の注入は
     テストの道具を 1 つ減らす
  4. **アニメーション判定は VP8X フラグと ANIM / ANMF チャンクの両方を見る** —
     フラグだけだと、フラグを 0 にした細工ファイルが素通りする
  5. **VP8X のキャンバス寸法がビットストリーム寸法に勝つ** — 食い違う細工
     ファイルで大きい方を見逃さない。エラーは `InvalidAttachment`（`InvalidIcon` と
     **別変種** — 上限も文言も違うものを畳むと、画面の辞書がどちらの上限の話かを
     区別できない）
  **`validate_icon` はパラメータ化しない**（rev3: 査読 5 を**採るが方法を変える**）—
  あの関数の doc は「**2 種類のアイコンが同じ述語を通る。2 箇所に書くと、片方の
  上限を変えたときにもう片方が古い値のまま通し続ける**」と書いており、
  **`ICON_MAX_BYTES` が 1 つであること自体が不変条件**。上限を引数にすると
  その不変条件が消える。**共有するのはマジックバイト判定だけ**を
  `fn is_webp(bytes) -> bool` として切り出し、`validate_icon` は現状のまま、
  添付は自分の上限を持つ別の述語にする。
  **アニメーション判定は magic では足りない** — WebP の `RIFF....WEBP` の先は
  `VP8X` チャンクのフラグを見る必要があり、これは新規実装（無料ではない）。
  単体: 非対応形式を拒否 / アニメーション WebP を拒否 / 上限超を拒否 / 往復 /
  GC が期限内を消さない・期限切れを消す / **`validate_icon` の挙動が変わっていない**
- **P2 ワイヤ** — **完了（2026-08-06。単体 6 本追加 = agent-core 417 → 423・
  workspace 580 全緑・clippy 警告ゼロ）**: `ChatMessage.attachments`（加算）+ 3 adapter。
  **`OaiMessage.content` は `enum OaiContent { Text(String), Blocks(Vec<..>) }`
  にして、添付が無ければ必ず `Text` を構築する**（rev2。査読 4 — ただし
  機序は査読の説明と違う。`#[serde(untagged)]` は*シリアライズ*では variant が
  そのまま決まるので勝手に配列化はしない。**危ないのは構築側**で、
  「常に `Blocks` を作る」実装にすると添付ゼロでも配列が出る）。
  `AnthropicRequestBlock` へ `Image` variant。**画像はテキストより前に置く**。
  単体: **添付なしのエンコード結果が現行とバイト等価** × 3 adapter /
  添付ありは provider 別の golden file（rev2。査読 3）

  **P2 実装記録（実装で決めた 5 点）**:
  1. **canonical に `ImageMediaType`（webp / jpeg の閉じた列挙）+
     `ImageAttachment { media_type, data: base64 済み文字列 }`** — adapter は
     純関数なので、ファイル読みと base64 化はプロンプトを組む側（P3）の責務
  2. **decode は無傷** — 応答側は `OaiResponseMessage` という別型で、
     `content` の enum 化はリクエスト側だけに閉じた
  3. **#29 の空発話フィルタに「添付があるか」を足した** — 本文なしの画像だけの
     発話は「空」ではない。空テキストのフォールバック（発言なし）も入らない
     ことをテストで凍結
  4. **`message_tokens` は Image を 0 と数える** — 視覚トークンは寸法（28px
     パッチ数）からしか出ず adapter は base64 しか持たない。「少なめに見積もる
     側へ倒す」というこの関数の既定の向きに一致。`place_message_breakpoint` には
     Image の腕を足した（「種別は問わない」— 画像だけの発話では末尾ブロックが
     Image になる）
  5. **gemini は添付を組み立てず、添付あり = 添付なしのバイト等価をテストで凍結**
     （D8。`inline_data` を生やす退行はテストが先に落ちる）。バイト等価の golden は
     文字列リテラルで固定（openai / anthropic）
- **P3 配線** — **完了（2026-08-06。結合 3 + 単体 2 = agent-core 423 → 425・
  結合 119 → 122・workspace 585 全緑・clippy 警告ゼロ）**:
  `send_user_message` へ `attachments`（加算・省略可）+
  `AgentMessage.attachments` + 保存 + そのターンだけプロンプトへ展開 +
  **履歴へは入れない**（D1）+ **転送時の System 行**（D6）+ 400 時の JPEG 再試行（D3）。
  結合: 画像が 1 ターン目に届く / 2 ターン目のリクエストに画像ブロックが無い /
  転送先へ渡らず System 行が出る / WebP 400 で JPEG へ落ちる

  **P3 実装記録（実装で決めた 6 点）**:
  1. **保存は発話の記録より前**。検証に落ちたら発話ごと拒否 —
     「画像なしで送信されました」は送った人の意図と黙って食い違う。
     D5 の 2 枚拒否も同じ入口（`send_user_message_with_attachments`）
  2. **D1 は構造で成立** — 展開（`load_turn_attachments`）が読むのは
     `incoming.attachments` だけで、履歴の発話は `String` なので画像を持てない。
     読めなかった参照（GC 済み）は**黙って抜かず本文で断る**（「画像は見えて
     いない前提で答えてください」）+ `attachment missing:` の 1 行
  3. **D6 は `note_dropped_attachment` の 1 実装を 3 経路が共有**
     （handoff の配送構築 / `ask_agent` の question / `run_plan` の各 task）。
     経路ごとに書くと文言がずれて grep が割れる
  4. **D3 の再試行は `LlmBackend::chat` の最外周**（バックオフの外）。発火条件は
     「OpenAiCompat × 400 × WebP 添付あり」の 3 条件 — 画像と無関係な 400 で
     もう 1 回課金しないことをテストで凍結。両形式拒否は
     「この接続先は画像を受け付けません」を 400 本文へ前置して返す。
     `attachment fallback:` の 1 行が出る。**HTTP 経路ごと踏む結合は書いていない**
     （スタブサーバが要る）— 変換（`with_jpeg_attachments`）と発火条件を単体で
     凍結し、実機は P6 の 5 が担う
  5. **GC は bootstrap で 1 回**（D9）。`attachment gc: removed=… remaining=…
     bytes=…` の 1 行。失敗しても起動は止めない
  6. **依存の実測**: 増えたのは 9 crate（image / image-webp / zune-jpeg /
     zune-core / moxcms / pxfm / bytemuck / byteorder-lite / quick-error）で
     **全部純 Rust・C 依存なし**。base64 は既にツリーに居た（282 → 291）
- **P4 フロント**: `ChatInput.vue` の貼り付け + 「参照…」+ **WebWorker で縮小・
  WebP 変換**（rev2。査読 6 — メインスレッドを止めない）+ **元ファイル 10MB の
  事前拒否** + 添付チップ（×で外す）+ S4 の注記 + `ChatPanel` の表示 + 辞書 ja/en
- **P5 台帳**: README 日英 + CLAUDE.md + ディレクトリ木（`attachments/`）
- **P6 実機確認**（画面・ログで観測できる項目だけ — #68）:
  1. 貼り付けた画像をサーヴァントが説明できる
  2. 2 ターン目に画像が渡っていない（`turn:` 行の `prompt` が 1 ターン目より小さい）
  3. 転送したときに System 行が出る（D6）
  4. GC が古いファイルを消す（D9）

## Notes

1. **「使わない人に払わせない」の実装上の意味**は、**モデルへ提示するスキーマを
   増やさない**ことと、**添付なしのワイヤがバイト等価**であること（rev3: 査読 1 で
   「コードのスキーマは増える」と指摘され、語を分けた）。後者を P2 のテストで
   凍結する — ここが崩れると、画像を一度も使わない村のキャッシュが割れる
   （`stable_len` と同じ性質の事故で、**画面には何も出ない**）
2. **D1 は「画像を軽く扱う」判断ではなく「トークンの因果を人の目に見える所へ置く」判断。**
   Spec 11 が天井を依頼の因果へ掛けたのと同じ形で、**払う量が添付という
   人の操作と 1 対 1 で対応する**
3. **Files API は誘惑だが D1 の代わりにはならない**（rev2 の新発見）。Anthropic の
   vision ドキュメントは「多ターンでは base64 が毎回再送されるので `file_id` を
   使え」と明示的に勧めている。**しかしあれが小さくするのはペイロードであって
   トークンではない** — 視覚トークンは毎ターン数えられる。**「payload を減らす」と
   「トークンを減らす」を取り違えると、D1 を捨てて Files API を採る誤った判断へ
   まっすぐ進む。** ここは将来の改訂で最も踏みやすい罠なので明記しておく
4. **キャッシュヒット率は画像を添付したターンだけ下がる。これは仕様**（rev2。査読 10）。
   D1 により 2 ターン目以降はプレフィックスが伸びて率は戻る。毎ターン添付する
   使い方では毎回割れるが、それは Goal（払うのは添付した人だけ）と一致している
5. **Spec 05 の「ネイティブ経路が要る」は今回に適用しない**。あれはツールの制約で、
   画像は content の話。**同じ「プロバイダ差」でも、道具か中身かで結論が変わる**
6. **ドラッグ&ドロップを外した根拠は Spec 21 P0 の実測**。**別 Spec の実測が
   設計判断に効いた 2 例目**（1 例目は Spec 20 の `prune_settled` が `decide` を
   使い回した件）
7. **査読の 1 点を一次資料で訂正して採用した**（rev2）。査読は「`4,784` は
   OpenAI のタイル方式の数字で、Anthropic は `(w*h)/750` 方式」と指摘したが、
   Anthropic の vision ドキュメントで**両方とも誤り**と確認した —
   **4,784 は Anthropic 高解像度ティアの視覚トークン上限そのもの**であり、
   式は `(w*h)/750` ではなく **28×28px パッチの `⌈幅/28⌉ × ⌈高さ/28⌉`**
   （28²=784 なので旧式の `/750` は近似）。
   **ただし指摘の効き目は本物だった** — 数字をプロバイダと式に紐付けずに
   書いていたため、rev1 の「38,000 トークン」は*縮小しない場合*の値なのに
   D3 で 1568px へ縮小する前提と並べて書いており、**自分の 2 つの決定が
   噛み合っていなかった**。rev2 で 3 行の表にして、どの前提でいくつかを固定した。
   **一般化: 数字は出典と前提とセットでしか書けない。片方だけ書くと、
   同じ Spec の中の別の決定と矛盾しても気づけない**。
   **rev3 で同じ指摘が再度来たので据え置きを明記する** — 2 巡目も
   「`4,784` は OpenAI の数字」「Anthropic は `(w*h)/750`」だった。
   一次資料（`vision.md` の「Resolution and token cost」表）は
   **高解像度ティア = 長辺 2576px・最大 4,784 視覚トークン / 標準ティア =
   長辺 1568px・最大 1,568** と明記しており、式は 28×28px パッチ。
   **繰り返しは裏付けではない**（#57 —「独立検証の独立性は、人ではなく方法で
   数える」。同じ方法で 2 度引けば同じ誤りが 2 度出るだけ）。
   **この数字を動かすなら、一次資料の別の記述を引いてくること**
8. **査読の点 7（Anthropic は base64 のみ・URL 不可）も一次資料で反証した** —
   `type: url` と `type: file` は公式に対応している（Bedrock と Vertex は
   base64 のみ）。ただし**この Spec は base64 を選ぶ** — URL は外部から到達できる
   場所に画像を置くことになり、村の画像を外へ出さない方が筋が通る
9. ~~**未確認のまま残すもの**: 互換層（xAI / ローカル）の WebP 可否~~
   **P0 で実測済み（2026-08-06）** — xAI は WebP が通った（ただし公式文書は
   jpg/png のみ。文書との食い違いは「P0 実測結果」の読み方 2 点目）。
   未実測で残るのは**ローカル互換（llama.cpp / vLLM）のみ**。
   D3 のフォールバックがその保険を兼ねる

10. **2 巡目の査読で分かったこと**（rev3）: 8 点のうち **5 点は rev2 で既に
    反映済み**だった（GC / Gemini のネイティブ非対応 / JPEG フォールバック /
    untagged の機序 / 2MB の対象）。**査読が rev1 を見ていたため**で、
    査読の質の問題ではない。**残り 3 点のうち 2 点が新規に効き**（Goal の語の
    切り分け・`validate_icon` の扱い）、1 点は据え置き（Notes 7）。
    **`validate_icon` の件は、査読の結論を採って方法を変えた例** —
    「パラメータ化」を素直に採ると、あの関数が doc で守っている不変条件
    （2 種類のアイコンが 1 つの上限を共有する）が消える。
    **共有すべきものと分けるべきものが同じ関数の中に同居していた。**
    一般化: **既存関数を「一般化して共有」と書くときは、その関数が何を
    不変に保っているかを doc から読む。共有の単位は関数ではなく述語**

## 改訂履歴

- **P3 完了**（2026-08-06）: `send_user_message_with_attachments` +
  `AgentMessage.attachments`（参照）+ `Shared.attachments` + 起動時 GC +
  D1（展開は受信の参照だけ・履歴は文字列）+ D6（`note_dropped_attachment` の
  1 実装 × 3 経路）+ D3（client の JPEG 再試行）+ IPC `attachments` 加算。
  依存 +9 crate（全部純 Rust）。結合 3 + 単体 2。実装で決めた 6 点は
  Tasks の P3 実装記録
- **P2 完了**（2026-08-06）: `ChatMessage.attachments` + `ImageMediaType` /
  `ImageAttachment`（canonical）+ `OaiContent` enum 化 +
  `AnthropicRequestBlock::Image` + gemini は素通り（D8）。バイト等価 golden ×
  2 + 添付あり golden × 2 + gemini 等価 + 画像だけの発話の 3 点をテストで凍結。
  単体 6 本（agent-core 417 → 423）。実装で決めた 5 点は Tasks の P2 実装記録
- **P1 完了**（2026-08-06）: `attachment.rs` 新設（`Attachment` / `validate_attachment` /
  `AttachmentStore` / `gc_plan` + `GcReport`）+ `CoreError::InvalidAttachment` +
  `validate_icon` の magic 判定を `is_webp` へ共有。単体 16 本
  （agent-core 401 → 417）。実装で決めた 5 点は Tasks の P1 実装記録
- **P0 完了**（2026-08-06）: 5 系統 × WebP / JPEG の実測 10 通り全部通過 —
  **D3 の既定は WebP で確定**（表と読み方 3 点は「P0 実測結果」）。
  実測 5 本目はローカル互換の代わりに実在の互換層（さくら / Qwen3-VL）で取った。
  `data_contract.yaml` へ `attachment_contract`（凍結 9 本）+ `prompt_cache`
  invariants へ「画像は安定プレフィックスに入らない」を追記
- **rev3**（2026-08-06）: 2 巡目査読のうち新規 2 点を反映 —
  Goal と Notes 1 の「1 字も伸びない」を**モデルが読む面とワイヤの出力**に限定し、
  Rust の型は増えると明記 / `validate_icon` は**パラメータ化せず**
  マジックバイト判定だけを共有（不変条件を壊さないため）。
  あわせて**2 つのログのどちらが添付を持つか**を表で固定（査読 2 の後半）。
  トークンの数字は 2 巡目も同じ指摘だったが、一次資料を再掲して据え置き
- **rev2**（2026-08-06）: 査読 10 点を反映。D3 を JPEG フォールバック付きへ /
  D5 に元ファイルの門 / D6 に System 行 / **D8（Gemini は互換経路）と
  D9（GC）を新設** / P0 へ `curl` 実測を前倒し / P2 のテストを 2 種へ /
  P4 を WebWorker 化。**トークンの数字を一次資料の式で計算し直し、
  査読の前提 2 点（`4,784` の出典・`(w*h)/750` の式・URL 不可）を訂正して採用**
  （Notes 7・8）
- **rev1**（2026-08-06）: 起票
