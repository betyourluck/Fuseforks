# Spec: 会話の永続化とセッション管理（再起動で消えない・複数持てる・再開できる）

**ID**: 12
**Date**: 2026-08-02
**Status**: **rev2 査読承認（2026-08-02 LGTM）→ P0〜P5 完了 = Done**
（P1〜P4 は 2026-08-03、Done は 2026-08-08）。
**P5 の実機確認は 2026-08-03 に P3・P4 の記録として観測済み**（会話一覧・開き直し・
新規チャット・分岐 / 要約が作られ再起動をまたいで復元され `history_msgs` が
4 → 0 に落ちる）。**残っていた「要約して続ける」も、その後の運用で問題が出ていない
ことを利用者が確認して Done とした**（2026-08-08）。
**狙って撮った 1 回の観測ではなく、使い続けて壊れなかったという別種の根拠**。
data_contract へ `session_store` ブロックを凍結（backend = redb 4.1.0 /
テーブル 2 つ / `Record` 3 種別（**続報: 2026-08-16 に [Spec 39](39_stats-view.md) P0 で
`turn` を加算して 4 種別**。既存 3 種別のシリアライズは不変）/ `seq` の意味と fork の inclusive /
summary の `coversUpToSeq < 自身の seq` / 単一ライターは await を跨がない /
権限は unix のみ / 上限 / API 一覧 / 実測値）＋ `reset_rule` へ Spec 03 の
改訂節を加算 ＋ `sessionSwitched` イベントを加算。
LGTM 時の明記 3 点も凍結済み — `seq` は 0 から単調増加の u64・fork は
`at_seq` **inclusive** / `coversUpToSeq < summary 自身の seq` /
`SessionMeta { title, createdAt, updatedAt, parentId?, forkedAtSeq?,
recordCount }`。実装は P1 から。
**Branch**: なし（main へ Phase 単位で直接コミット — Spec 01〜11 と同じプロセス）

rev2 で入った差分（査読 2026-08-02）:

- **R0 保存先を redb 1 ファイルへ変更**（最大の差分）。rev1 は
  `JSONL + index.json` だったが、**index.json を置いた時点で真実が 2 つになり**、
  査読指摘 2（原子性の自己矛盾）と 6（fork の孤児）はその帰結だった。
  redb は機構を足すのではなく**減らす** — index.json が消え、temp+rename の
  自前実装が消え、壊れた行の切り捨て実装が消え、fork が 1 トランザクションになる。
  ハイブリッド（redb = 索引 / JSONL = 本文）は**採らない**（永続化を 2 種類
  管理する最悪の形）
- **R1 レコード種別を 3 つで凍結** — `message | exchange | summary`。
  rev1 は要点 3 で「2 種別」、要点 7 と P0 で「3 種別」と書いており矛盾していた
- **R2 原子性の矛盾は R0 で消滅** — 「append+flush か temp+rename か」の使い分け
  自体が不要になった（1 トランザクション）
- **R3 上限を決めた**（D5）。P0 で凍結する
- **R4 Spec 03 の改訂差分を P0 に含めた** — イベントは**加算**（`conversationCleared`
  は「会話ペインを空にせよ」の意味で残し、`sessionSwitched` を足す）
- **R5 API に `create_session` を追加**。`reset_conversation` は
  `create_session` へ委譲する内部実装になる
- **R6 fork を定義** — `fork_session(id, at_seq)`、`parentId` / `forkedAtSeq` を
  `SessionMeta` に持つ
- **R7 summary の `covers` と注入位置を定義**（査読で最も鋭い指摘。#45 との衝突を
  解いた。下記「要約の設計」）
- **R8 飛行中に閉じた場合を定義** — 「message はあるが exchange が無い」は**許容**し、
  画面には出すが履歴には入れない
- **R9 秘密の置き場を決めた**（D8）。ただし査読の `700/600` は**そのままでは
  採れない**（下記の訂正 2）
- **R10 単一ライターを不変条件へ** — redb の書き込みトランザクションは 1 本。
  **await を跨いでトランザクションを持たない**ことを明記

**査読への訂正 2 点**:

1. **redb は 2.x ではなく 4.1.0**（2026-08-02 実測）。API も
   `ReadableDatabase` trait の import が要るなど 2.x とは違う。凍結する形は
   実際にビルドして測った（下記「実測」）
2. **`700 / 600` のパーミッションは Windows では効かない**。`{workspace}` は
   `%APPDATA%\jp.outcasts.fuseforks\` で、Windows では既にユーザー私有の ACL 下に
   ある。POSIX 権限の設定は **unix でのみ**適用し、Windows では「既定の ACL に
   依存する」と書くのが正直。また `sessions.redb` は `{workspace}` 配下で
   **work_dir（リポジトリ）ではない**ので、黒板（`黒板/`）のように
   リポジトリへ紛れ込む経路は無い — `.gitignore` は保険として書くが、
   必要性の理由は黒板とは違う

## Goal

村の会話を再起動をまたいで保つ。複数の会話を名前付きで持ち、切り替えて再開できる。
長く続いた会話は要約して続けられる。

起点は利用者の判断（2026-08-02）: **「再起動して前回の続きから始められないと、
道具として使えない」**。現状は会話ログもエージェントの履歴も in-memory で、
アプリを閉じた瞬間に消える。

### 立場の宣言

- **保存するのは「会話」であって「実行状態」ではない。** plan の波・トークン予算・
  MCP 接続・飛行中ターンは作業の寿命で、プロセスと共に消えてよい（Spec 08 が
  波を in-memory と凍結したのと同じ線）
- **要約は自動で走らせない（v1）。** 要約は LLM 呼び出し = トークンで、
  Spec 11 の天井と競合する。「自動で仕事を増やす機構は入れない」（Replan 不採用と
  同じ規律）に従い、**人が押したときだけ**要約する
- **索引は作らない（v1）。** 実測で 20,000 レコードの全走査が 11 ms。
  索引が要るのはその 1 桁上からで、いま作ると「効いているか分からない機構」が増える
- **人が読める出口を必ず持つ。** redb はバイナリなので、`export_session` で
  JSONL へ書き出せることを **P1 の完了条件**にする。この企画の診断は
  grep に依存しており（failures.md #47 は `concordia.log` の grep で解けた）、
  読めない保存先を作るなら出口は機構の一部
- **秘密は書かれうる前提で置き場を決める。** 会話ログをディスクへ書くのは
  この Spec が初めて

## 現状の構造（接地・2026-08-02 起票時）

**履歴は 2 層ある。これが他ツール（Claude Code / Copilot / Cursor）と決定的に違う。**

| | 何を持つか | 形 | 上限 | 誰が読むか |
|---|---|---|---|---|
| `Shared.log` | 村の会話ログ | `Vec<AgentMessage>` | リング 5,000 件 | 会話ペイン・広場ログ・トークン集計 |
| `AgentRecord.history` | エージェント個別のプロンプト履歴 | `Vec<ChatMessage>` | 滑る窓 8 往復 | **LLM へ送るリクエスト本体** |

- 単一エージェントの道具では「会話ログ = モデルの文脈」なので、ログを保存すれば
  再開できる。**Fuseforks では一致しない**
- **`log` から `history` は復元できない** — `push_exchange` は #45 の規律で
  「**送った文字列をそのまま**」積む。そこには畳んだ可変文脈（入退室通知・
  広場ログ・顔ぶれ・送り手名）が含まれ、`log` のどこにも無い
- **`history` から `log` も復元できない** — `log` には System 行・他人宛の発話・
  どの history にも入らない発話がある

→ **会話ログだけ保存して再開すると、画面は正しく見えるのに全エージェントが
健忘症で始まる。しかも画面上その 2 つは区別が付かない。**

その他の接地:

- `reset_conversation`（Spec 03 の「新規チャット」）は log と全 history を
  **捨てる**。契約で処理順まで凍結済み（log → history → `conversationCleared`）
- `ChatMessage` / `AgentMessage` はどちらも serde 可能
- `PersistedWorld` は履歴を**意図的に持たない**
- 依存は現在 17 個で、それぞれ `Cargo.toml` に理由が書いてある

## Stories

### S1: 閉じて開いたら続きから

村を使い、アプリを閉じ、開き直す。会話ペインには前回の会話がそのまま出ている。
**そのまま次の依頼を出すと、エージェントは再開前の話を踏まえて答える**
（S1 の本丸はここ — 画面ではなく履歴）。

### S2: 会話を複数持てる

「Spec 11 の実装」の会話と「黒板の運用」の会話を別々に持ち、一覧から選んで
切り替える。切り替えても、もう一方は消えない。

### S3: 新規チャットは捨てるのではなく閉じる

「新規チャット」は今の会話を**捨てる**のではなく、**閉じて新しい会話を始める**。
前の会話は一覧に残り、いつでも戻れる（Spec 03 の意味を変更する）。

### S4: 枝分かれ

ある地点までは同じで、そこから別の頼み方を試したい。**fork** で
`at_seq` までを複製し、元は不変のまま残す。

### S5: 長い会話を要約して続ける

会話が長引いたとき、人が「要約して続ける」を押す。要約はエージェントごとに作られ、
以後のプロンプトへ注入される。**元のレコードは消さない**（要約の品質が悪かった
ときに戻れる）。

### S6: 中身を人が読める

`export_session(id)` で JSONL へ書き出せる。デバッグ・移行・他ツール
（Claude Code 形式）との突き合わせに使う。**redb を選んだ以上、これは必須機能**。

## 機構の要点（Phase 0 で凍結する不変条件）

1. **セッションの単位は村**（エージェント別ではない）。1 セッション =
   「村の会話ログ + 全エージェントの履歴 + 要約」。エージェント別に切ると、
   同じ波に参加した 2 体が別々の会話に属することになり、束ねの因果が切れる
2. **保存先は `{workspace}/sessions.redb` の 1 ファイル**。`world.json` へ
   入れない理由は `schedules.json` と同じ — エージェントの定義ではない。
   **`index.json` は作らない**（真実を 2 つにしない）
3. **テーブルは 2 つ**:
   ```
   sessions: TableDefinition<&str, &[u8]>          // key = session_id, value = SessionMeta(JSON)
   records:  TableDefinition<(&str, u64), &[u8]>   // key = (session_id, seq), value = Record(JSON)
   ```
   `seq` はセッション内で単調増加。**タプルキーの範囲走査**が「後ろから N 件」を
   O(log n + k) で返すことを実測済み（16 件で 0.03 ms / 20,000 件のテーブル）
4. **`Record` は 3 種別で閉じる**（`kind` の閉じた列挙）:
   - `message` — 会話ログ 1 件（`AgentMessage` 丸ごと。`grounding` /
     `co_recipients` も含む — 再開後の表示に要る）
   - `exchange` — 履歴 1 往復（`agentId` / `sent` / `replied`）。
     `sent` は**実際に送った文字列そのもの**（#45 の規律を保存側へ延長）
   - `summary` — 要約 1 本（`agentId` / `text` / `coversUpToSeq`）
   **2 種別ではない**（rev1 の記述矛盾を解消）
5. **1 トランザクション = 1 つの原子的な変更**。メタデータ更新とレコード追加は
   同じトランザクションで行う。temp+rename は使わない（redb が保証する）
6. **書き込みトランザクションを `await` を跨いで持たない。** redb は同期 API で
   書き込みは 1 本に直列化される。トランザクションを持ったまま LLM 呼び出しや
   ファイル I/O を待つと村全体が固まる。**書くのは決まった 2 点だけ**
   （`record` の中 / `push_exchange` と同じ点）で、どちらも短いトランザクションで閉じる
7. **滑る窓はロード時に再適用する。** `records` には全 `exchange` が残り、
   復元時に `history_turns` の窓で切る。窓の値を変えたら次回の復元から効く
   （保存時に切ると、設定を広げても過去は戻らない）
8. **復元しないもの**: plan の波（Spec 08 が in-memory と凍結）/ `BudgetPool`
   （因果ごと・Spec 11）/ MCP 接続 / `TurnHandle` / 稼働状態
   （bootstrap は自動起動しない、既存契約）/ 累積統計（プロセス寿命）
9. **`Shared.log` の 5,000 件はメモリ上の上限であって、保存の上限ではない。**
   ファイルには全量が残り、起動時に読むのは末尾 5,000 件（リングと同じ形で
   画面へ復元する）
10. **半端なターンを許容する。** ユーザー発話の `message` は投入時に、
    `exchange` はターン完了時に書かれるので、飛行中にアプリが落ちると
    「`message` はあるが `exchange` が無い」状態が残る。これは**正しい記録**
    （発話は起きた事実）として扱い、**画面には出すが履歴には入れない**。
    再開後は「頼んだが答えが返らなかった」ように見える
11. **セッションの切り替えは飛行中ターンが無いときだけ**（`interrupt_all` の
    後か、全員 idle のとき）。飛行中に切り替えると、答えが別のセッションへ着地する
12. **新規チャットは破棄しない**（Spec 03 の改訂）。今のセッションを閉じ、
    新しい `session_id` を開く。イベントは**加算**:
    `conversationCleared` は「会話ペインを空にせよ」の意味で残し、
    `sessionSwitched { sessionId }` を足す（旧イベントの意味を変えると
    既存 UI が誤動作する）
13. **`session_id` は時刻順にソートできる形**にする: `{epoch_ms}-{短い乱数}`。
    「最新」を辞書順に依存させない — 判定は `SessionMeta.updatedAt` で行い、
    ID の順序は**人が一覧を読むときの都合**に留める
14. **上限**（R3 で確定）:
    - セッション数: 無制限。**200 を超えたら起動時に WARN 1 行**（削除はしない）
    - 1 セッションのレコード数: 無制限。**50,000 を超えたら WARN**（要約を促す）
    - ファイルは**自動で縮まない**（redb は copy-on-write）。`compact()` は
      持つが v1 では呼ばない — 呼ぶなら明示操作
15. **`sessions.redb` の権限**: unix では `0o600`（親ディレクトリ `0o700`）。
    **Windows では設定しない** — `%APPDATA%` は既にユーザー私有の ACL 下にあり、
    POSIX 権限の API は無い。`.gitignore` へ `sessions.redb` を保険として足す
    （`{workspace}` は既定で `%APPDATA%` 配下なのでリポジトリには入らないが、
    workspace をリポジトリ内へ置く運用を将来する場合の備え）

### 要約の設計（査読指摘 7 — #45 との衝突を解く）

**`coversUpToSeq` は「レコードの seq 境界」であって件数ではない。**

- 復元時、あるエージェントの履歴は
  「**最新の `summary`** ＋ `coversUpToSeq` より後の `exchange`（滑る窓を適用）」で組む
- **注入は既存の可変文脈の畳みに相乗りさせる。** #45 の規律は
  「可変文脈は最終 user 発話へ畳み、**送った文字列をそのまま履歴へ積む**」。
  要約も同じ経路に乗せれば、送信と保存が食い違わない —
  つまり **`history` の中に summary 専用の席を作らない**。次のターンの
  `context`（入退室通知・広場ログと同じ場所）へ要約を差し、
  その結果できた `sent_user_turn` がそのまま `exchange` として保存される
- **要約の要約は許す。** 新しい `summary` は前の `summary` の本文を入力に取れる。
  `coversUpToSeq` は単調増加するので、参照が循環しない（査読が指摘した
  無限ループはこの単調性で塞ぐ）
- **注入されるのは最新の 1 本だけ。** 古い `summary` はレコードとして残るが、
  復元には使わない（痕跡であって現役ではない）

## 検証の設計（実装の前に決める）

- **本丸**: 再起動後の最初のターンで、エージェントが再開前の文脈を参照できること。
  「画面に出ている」だけでは通さない — **LLM へ渡ったリクエストの `messages` に
  復元した履歴が入っていること**を結合テストで見る
- 送った文字列と保存された文字列が一致すること（#45 の回帰の保存側版）
- 飛行中に落ちた形（`message` だけ・`exchange` 無し）を読んでも、
  画面に出て履歴に入らないこと
- 新規チャットで前のセッションが消えないこと / `fork(at_seq)` で元が不変であること
- 要約の後、履歴が「最新 summary + それ以降の exchange」で組まれること /
  要約を 2 回しても循環しないこと
- `export_session` の JSONL を読んで、同じ会話が再構成できること
- 上限（200 セッション / 50,000 レコード）で WARN が 1 回出ること
- 実機: アプリを閉じて開き、前の会話が出て、続きの依頼が文脈込みで通ること

## Phases

- **P0 契約凍結**: data_contract へ `session_store` ブロック
  （backend = redb / テーブル 2 つ / `Record` 3 種別 / seq の意味 /
  滑る窓の再適用 / 復元しないものの列挙 / 上限 / 権限 / イベントの加算 /
  要約の注入位置）。**Spec 03 の改訂差分もここに含める**
- **P1 純機構**: `session_store.rs`（redb の読み書き・`seq` 採番・
  滑る窓の再適用・`fork` のコピー・`export_session`）。単体テスト。
  **`export_session` は P1 の完了条件**（読めない保存先を先に作らない）
  → **完了**（2026-08-03）。単体 16 本・workspace 全緑（278 + 結合）・
  clippy 新規警告ゼロ。実装で決めた 3 点:
  (a) **`fork` は seq を振り直さない** — 振り直すと `summary.coversUpToSeq` の
      指す境界がずれ、複製した側だけ要約の覆う範囲が変わる
  (b) **1 往復 → `ChatMessage` 対の変換を `world::exchange_pair` へ切り出した** —
      押し込む側（`push_exchange`）と読み戻す側で規律が分かれると、**復元した
      履歴だけが空メッセージを持って次のターンで 400 になる**（failures.md #29）
  (c) **`restore_histories` は `RestoredHistories { histories, summaries }` を返す** —
      要約が履歴の席を持たないことを型で保つ（注入は可変文脈の畳みに相乗り）
  エラーは `SESSION_NOT_FOUND` / `SESSION_STORE_FAILED` を追加（data_contract の
  `ErrorPayload.codes` へ加算済み）。`.gitignore` へ `sessions.redb` を保険として追加
- **P2 配線**: `record` / `push_exchange` の書き込み点、bootstrap での復元、
  `reset_conversation` の意味変更、セッション API
  （`create_session` / `resume_session` / `continue_latest` /
  `fork_session` / `list_sessions` / `delete_session` / `export_session`）
  → **完了**（2026-08-03）。結合 8 本・workspace 全緑・clippy 新規警告ゼロ。
  実装で決めた 3 点:
  (a) **`start_agent` が履歴をクリアするのをやめた**（この Spec で最も大きい
      既存挙動の変更）。旧い規律「起動は新しい会話の開始として扱う」は、会話を
      始め直す手段が他に無かった時期の代用だった。**起動時に自動起動しない契約と
      組み合わさると、再起動して開いた会話の履歴を、エージェントを起動した瞬間に
      捨てることになり、S1 が原理的に成立しない**。始め直しは「新規チャット」
      （= 新しいセッション）が担う
  (b) **飛行中ターンの拒否は「既存の会話を開くとき」だけ**。査読で凍結した
      2 つの節が衝突して見えた — `reset_rule` の案 A（飛行中ターンの完了書き込みは
      許容）と不変条件 11（切り替えは飛行中ターンが無いときだけ）。解は
      **着地先で分ける**: 新規チャットの着地先は新しい空の会話なので既存の記録を
      汚さず案 A が成立する。resume / fork / continue_latest / 開いている会話の
      delete は前に保存された会話へ無関係な答えが混ざるので `SESSION_SWITCH_BLOCKED`
      で拒む。**自動で `interrupt_all` してから切り替えることはしない**（線は人が引く）
  (c) **保存の失敗は WARN 1 行で続行し、保存先が開けない起動も普通に通す**。
      保存できないことは会話を止める理由にならない（D1 の「起動が止まる経路は
      作らない」を書き込み側へ延長）。その村では `reset_conversation` が
      改訂前の挙動（メモリ白紙化 + `conversationCleared` のみ）へ落ちる
  書き込み点は **`Shared::record`（message）と `Shared::push_exchange`（exchange）
  の 2 つだけ**で、後者は履歴へ積む処理と保存を 1 つの関数に閉じた —
  呼び出し側 4 箇所（成功 / 失敗 / 割り込み / 予算切れ）が別々に保存を書くと、
  片方だけ更新されて再開後の履歴が実行中の履歴と食い違う（P1 の (b) と同じ形）
- **P3 投影**: 会話ペインのセッション一覧・切り替え・fork・削除の UI、IPC、
  `sessionSwitched` の投影
  → **完了**（2026-08-03）。IPC 7 本（`list_sessions` / `current_session` /
  `resume_session` / `list_fork_points` / `fork_session` / `delete_session` /
  `export_session`）+ `SessionDialog.vue` + 会話ペインの「会話一覧」ボタン。
  フロント単体 2 本 + ワイヤ形の凍結 1 本（`session_wire_fields_are_frozen`）。
  実装で決めた 3 点:
  (a) **分岐は `ForkPoint` の一覧から地点を選ぶ 2 段階にした**。S4 は
      「ある地点までは同じで」が本体なので、会話まるごとの複製しかできない
      fork は S4 を満たさない。候補を**ユーザー発話だけ**に絞ったのは、全
      レコードを並べると入退室の System 行や往復の記録まで候補に出て、
      「どこで切ったのか」が読めなくなるため（`SessionStore::fork_points` を
      P3 で追加）。**切る位置は実機の観察で直した**（下記の改訂）
  (b) **`conversationCleared` では通知を出さないことにした**。この 1 本は
      新規チャット・開き直し・分岐のどれでも同じように飛ぶので、既存の
      「新規チャットを開始しました」は開き直しのときに嘘になる。何が起きたかは
      操作した側が知っているので、通知は操作側が出す
  (c) **書き出し先は `{workspace}/exports/{session_id}.jsonl` に固定した**。
      ファイル選択ダイアログのために plugin を足すより、置き場所を 1 つ決めて
      パスを通知に出すほうが依存も操作も少ない
  `create_session` の IPC は**作っていない** — 新規チャットは
  `reset_conversation` が担うので、同じ操作の入口を 2 つ作らない。

  **P3 の改訂: 分岐で切る位置を「選んだ依頼の直前」へ変えた**（2026-08-03、
  実機の観察 → 利用者の判断）。初版は選んだ依頼**を含めて**複製していた。
  実機で見えたのは、**返事の付かない依頼が宙に浮いたまま残る**状態
  （返事と `exchange` はその発話より後に書かれるので、`at_seq` の外へ落ちる）。
  これ自体は契約の「半端なターン」の規律どおりで、機構としては正しく動いていた —
  **正しく動いた結果が使いにくかった**という形。
  再実行の機構は無いので、宙に浮いた依頼はどのみち打ち直すことになる。
  処方は利用者の案: **依頼は複製先に残さず、文面を入力欄へ差し戻して
  書き換えられる状態にする**。S4「そこから別の頼み方を試したい」がそのまま
  操作になる。差分は `ForkPoint`（`seq` → `at_seq` = seq − 1、`text` と `to` を追加）と
  `ChatInput.fill()` の公開だけで、`fork_session` の inclusive 契約は**触っていない**
  （P0 凍結のまま — 変えたのは境界の選び方であって、複製の意味ではない）。
  **先頭の発話は候補から外した** — その手前まで複製すると空の会話で、
  それは「新規チャット」と同じ操作になる。
  **実機確認（2026-08-03、利用者）: 会話一覧・開き直し・新規チャット・分岐すべて
  確認済み。** 分岐の実機で切る位置の設計ミスが 1 件出て、その場で直した（下記）
- **P4 要約**: 手動のみ。エージェントごとに要約し `summary` を追加、
  注入は可変文脈の畳みへ相乗り
  → **完了**（2026-08-03）。`Orchestrator::summarize_session()` + IPC +
  会話ペインの「要約して続ける」ボタン（確認つき）。結合 3 本。
  実装で決めた 5 点:
  (0) **対象は稼働中のサーヴァントだけ**（2026-08-03 の実機指摘で修正 —
      初版は停止中にも呼んでいた）。要約の目的は以後のプロンプトを短くすることで、
      停止中の個体には以後のターンが無い。それでも呼べば**参加していない個体の
      ぶんまで押した人がトークンを払う**。履歴は停止しても消えない（P2 で
      `start_agent` のクリアをやめた）ので、起動してから押せばそのとき要約される。
      飛ばした相手が居たら System 行に必ず書く（#44 の「歯止めの先に道を書く」）
  (a) **要約を作るのは本人**（そのエージェント自身のモデル）。別のモデルに
      書かせると要約の文体と本人の文体が食い違い、次のターンで「自分が書いた
      覚え書き」として読めなくなる。ツールは提示しない（提示すると呼びに行く
      個体が出て、1 回で終わる呼び出しがループになる）
  (b) **境界は書く前に取る**（`SessionStore::last_seq`）。書いた後だと自分自身を
      覆う要約になり、`coversUpToSeq < 自身の seq` が破れる
  (c) **畳むのは保存が済んでから**。先に畳むと、保存に失敗した瞬間に履歴も要約も
      無い状態になる。**空の要約・失敗した要約では畳まない** —
      要約に失敗した代償が履歴の喪失になるのは最悪の交換
  (d) **飛行中ターンがあっても拒まない**。そのターンは `sent_user_turn` を組み
      終えており、完了時の `exchange` は `coversUpToSeq` より後の seq を取る。
      畳んだ履歴の上に 1 往復が乗るだけで、復元しても同じ形になる
  **P4 の実機確認**（2026-08-03、利用者）: 要約が作られ、再起動をまたいで復元され、
  次のターンの履歴が畳まれることをログで確認した（`履歴 1 体 / 要約 1 体` の復元と、
  同じエージェントの `history_msgs` が 4 → 0 に落ちる `cache:` 行）。
  **穴が 1 つ出た** — 要約の LLM 呼び出しがログにも統計にも一切現れず、
  `history_msgs` が落ちたことからしか実行を推定できなかった。`response.usage` を
  捨てていたため、**押した人が払ったトークンがどの数字にも載らない**。
  統計への加算と `summarize:` の 1 行を追加して塞いだ（結合 1 本追加）。
  併せて実測: **短い会話で要約するとプロンプトはむしろ伸びる**
  （4 往復・各 10〜30 字の会話で `prompt` 10,292 → 10,413）。要約が得になるのは
  畳む履歴が注入する要約より大きくなってから、という当たり前の性質が実機で出た形。

  **2 回目の実機確認**（2026-08-03 14:24、4 体同時）: `summarize:` 行が
  エージェントごとに 1 本出て、`covers_up_to=45` が 4 体とも一致
  （境界を書く前に 1 度だけ取る規律が実機で確認できた）。要約 4 体の合計は
  素 10,301 トークン（agent_3 の 1 ターンが 13,309 なので、村 1 回の要約 ≒ 1 ターン弱）。
  **モデルによる要約の長さの差が大きい** — 同じ 1 往復（`folded_msgs=2`）に対し
  `claude-opus-5` が 1,163 字、`gemini-3.5-flash-lite` が 234 字。opus は
  畳んだ履歴より長い要約を書いており、**要約が滑る窓と違って落ちない**ぶん、
  膨らみは以後の全ターンに乗り続ける。処方は計器のみ — `folded_chars` を
  `summarize:` 行へ足し、`summary_chars >= folded_chars` で WARN を 1 行出す。
  **機構では止めない**（止めると「要約したのに畳まれない」状態が画面から読めない）。
  指示に字数上限を入れるかは頻度を見てから決める。
  要約呼び出しの `cached` は 4 体とも 0 で、これは構造的（要約用のプレフィックスは
  通常ターンと共有しない）。

  **`/compact` のようなスラッシュコマンドは採らない**（利用者との相談、
  2026-08-03）。入力欄は「特定のサーヴァント 1 体への発話」の場で、要約は村全体の
  操作なので射程が誤解される。加えて入力欄に構文解析を持ち込むと、
  取りこぼしはそのまま相手へ飛び、`/` で始まる正当な発話を飲む。
  GUI にはボタンの置き場所がある（「新規チャット」「会話一覧」「全ターン停止」
  と同じ列）ので、CLI の語彙を持ち込む必要が無い
- **P5 台帳と実機確認**: README 日英・CLAUDE.md・Spec 03 の改訂・
  依存追加の理由を `Cargo.toml` へ

## Notes（rev2 — 査読で決着した項目と、残る未決）

- **D1 起動時の既定 — 決定: 最新セッションを開く**（`SessionMeta.updatedAt` で判定）。
  **フォールバックを定義**（査読指摘）: 最新が読めない・0 件なら、
  警告を 1 行出して**新規セッションを作る**。起動が止まる経路は作らない
- **D2 保存の粒度 — 決定: 3 種別のレコード**（R1）
- **D3 セッションの単位 — 決定: 村全体**（不変条件 1）
- **D4 要約は手動のみ — 決定**（自動は Spec 11 の天井と競合する）
- **D5 上限 — 決定**（不変条件 14）
- **D6 backend — 決定: redb 4.1.0**（rev1 の「SQLite を入れない」は維持しつつ、
  **入れるなら redb**へ倒した）。判断材料は下の実測。
  **索引は作らない**（全走査が 11 ms / 20,000 件）。
  純 Rust の全文検索（tantivy + lindera = 451 依存）も**採らない**
- **D7 保存するフィールド — 決定: `AgentMessage` 丸ごと**（`grounding` /
  `co_recipients` は再開後の表示に要る）
- **D8 秘密の扱い — 決定: (a) 何もしない + 置き場の明示**（不変条件 15）。
  伏せ字保存は会話の再現性を壊し、検出警告は誤検出が続けば無視される。
  ただし **README へ「会話はディスクに平文で残る」と明記**し、
  `export_session` で中身を確認できる経路を保つ
- **D9 Spec 03 の改訂範囲 — 決定**: 確認ダイアログは**残すが文言を変える**
  （「破棄しますか」→「この会話を閉じて、新しい会話を始めますか」）。
  捨てないので警告色は外す
- **D10（新規・未決）**: redb のファイル形式はメジャー版更新で変わりうる
  （README は「形式は安定、変更時は移行経路を用意する努力をする」と書いている）。
  **移行が必要になったときの手当ては `export_session` → 新形式で取り込み**で
  足りるか。v1 はこれで進めるが、実際に更新が来たときに検証が要る

## 実測（2026-08-02、この機械で計測）

**候補の比較**:

| 候補 | 依存数 | ビルド | C 依存 | 日本語検索 |
|---|---:|---:|---|---|
| **redb 4.1.0** | **2** | 10 秒 | なし | 全走査（索引なし） |
| tantivy 0.26（既定） | 189 | 1分21秒 | **zstd-sys** | 別途トークナイザ |
| tantivy + lindera | **451** | — | zstd-sys | 形態素解析 |
| rusqlite（bundled） | 10 | 29 秒 | SQLite 同梱 | FTS5 trigram |

**SQLite FTS5 の日本語**（rusqlite 経由・SQLite 3.53.2）:
`unicode61`（既定）は「周回境界」「予算切れ」「ロブステル」すべて **0 件**、
`tokenize='trigram'` は全ヒット。**日本語検索に形態素解析は要らない**
（この事実が lindera の 451 依存を外す根拠になった）。

**redb で凍結する形の実測**（40,000 レコード・1 件約 600 バイト）:

| 操作 | 所要 |
|---|---:|
| 書き込み 40,000 件（1 トランザクション） | 271 ms |
| **後ろから 16 件**（滑る窓の復元） | **0.03 ms** |
| 1 セッション 20,000 件の全走査（v1 の検索） | 11 ms |
| `fork`（10,000 件のコピー・1 トランザクション） | 55 ms |
| ファイルサイズ | 64.3 MB |

ファイルサイズは実データの約 2 倍（copy-on-write の頁が残るため）。
自動では縮まないので、気になる規模になったら `compact()` を明示操作で出す。

**全走査の比較対象**（JSONL 案で測った値）: 25.9 MB で 30 ms（暖まった状態）、
415 MB で 732 ms。redb でも桁は同じで、**索引の要否は保存先の選択に依存しない**。

## 参考（外部調査・2026-08-02 利用者提供）

収束点は 4 つ — 追記型永続化・プロジェクト紐付け・continue / resume / fork の
再開モデル・フルログ温存 + 要約レイヤ。

- **Claude Code**: `~/.claude/projects/<encoded-cwd>/<session-id>.jsonl`。
  continue / resume / fork の 3 プリミティブ。**会話は永続化するが
  ファイルシステムは永続化しない**という分離 — 本 Spec の不変条件 8 の下敷き
- **Copilot CLI/SDK**: 索引 DB と本体ファイルの二層。`disconnect()`（メモリ解放）と
  `deleteSession()`（削除）の区別
- **Cursor**: workspace DB と global DB の二重 SQLite。ワークスペースのハッシュ
  紐付けが切れると「履歴が消えた」ように見える — **紐付けをパスのハッシュに
  依存させない**理由になる（本 Spec は workspace 直下に置く）
- **VS Code Copilot**: 自動要約はトークン 95%、手動 `/compact` は**要約の焦点を
  指示できる**。D4 で自動化を検討する際の型
- **LangGraph**: checkpointer を `thread_id` 単位で抽象化。本 Spec は村 1 つなので
  抽象化しない
- **CrewAI Flows**: `kickoff(inputs={"id"})` = resume、
  `kickoff(restore_from_state_id=)` = fork
- **Turso**（旧 Limbo、2026-08 時点で beta）: SQLite を純 Rust で書き直した実装。
  ファイル形式に互換がある。**SQLite を選んでも C への永久の賭けにはならない**
  ことの根拠だが、beta で FTS5 の対応も未確認のため v1 では採らない
