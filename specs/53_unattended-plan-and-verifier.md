# Spec: 無人の計画と検証役 — 予定から始まった計画の確認を飛ばす / ステータスバーのスイッチ / 既定の検証役

- 起票: 2026-09-15
- 状態: **rev2 承認（2026-09-15。査読 2 系統 = 指摘 15 点 + 決めどころ 3 点（Notes 5 の表は計 16 行）。
  指摘は採用 8 / 前提を訂正して採用 4 / 反証 3。決めどころ 3 点は査読 2 系統と利用者が
  草案の推しを承認 — スイッチは保存しない / 既定の検証役は作業状況タブの見出し / 条例は改訂案を渡す）
  → P0 完了**（同日。`data_contract.yaml` の 5 箇所 — `plan_edit_window` の凍結 4 改訂・凍結 11 新設・
  計器 `plan review skipped:` / 新ブロック `plan_verifier_contract`（凍結 8 本）/ `ScheduledTask` の
  `autoApprovePlans` / `settings_contract` の `world.json` の列に `defaultVerifier` と「置き場ではないもの」
  にスイッチ。IPC と `CoreEvent` は契約に列挙が無いので P3 で `tests/ipc_contract.rs` 側を追従する）
  → P1 完了**（同日。記録は「P1 実装記録」）**→ P2 完了**（同日。記録は「P2 実装記録」— 契約を 3 点訂正）
  **→ P3 完了**（同日。記録は「P3 実装記録」— 実機は未確認）**→ P4 完了**（同日。記録は「P4 台帳記録」）
- 起点: 利用者 —「スケジュールで回しているとき作業を割り振って承認待ちで止まっちゃう。
  それは自律化にならない。承認を自動で OK させるようにしたい。あと検証役もデフォルトの
  検証役になるようにしたい」（2026-09-15）。形は同日の裁定 2 点:
  - 「**予定ごとに選べる**のと、**下のステータスバーに計画の確認を飛ばせるスイッチ**が欲しい。
    スイッチが ON だと確認を飛ばせる。ただ予定で自動で通すになっているとスイッチが OFF でも飛ばす」
  - 「**既定の検証役を指定できる。ただ『なし』も選択できる**ようにして欲しい。それは現在の確認でも
    そうで、検証役なしも選べるようにしたい」

## Goal

1. **予定から始まった作業は、計画の確認（Spec 43）で止まらずに配られる**（予定ごとのオプトイン）
2. **ステータスバーのスイッチで、計画の確認をまとめて飛ばせる**（1 プロセス 1 村なので村全体）
3. **束ねの検証を仕組みにする** — 既定の検証役を 1 体（または「なし」）指定でき、計画の確認の
   パネルでは波ごとに選び直せる（既定を初期選択・「なし」可）

**既存の村は 1 ミリも変わらない。** 予定の欄の既定は偽・スイッチの既定は OFF・既定の検証役の
既定は「なし」で、3 つとも既定なら今日の挙動とバイト等価（検収 7 と P0 の凍結で担保）。

**やらないこと（範囲外）**: 窓に時間切れを付ける（承認待ちを N 分で自動実行する）/ 検証の結論で
自動的に聞き直す（Replan は 2026-08-02 に ×8.5 の実測で退けた）/ 検証役を複数にする /
波ペインに検証役の列を出す（計器で読む）/ 条例の書き換え（利用者の資産 — Notes 3）。

## 起票時の実測（2026-09-15。コードと条例を読んだ）

**窓を開く判定は 1 箇所**（`orchestrator/turn.rs:2062`）:
`spec.plan_review && !self.awaiting_reply`。後半は凍結 10（委譲ターンでは開けない）。

**封筒に「起点が予定か」の欄は無い**（`orchestrator/mod.rs:181` の `Envelope`）。予定の発火は
`Envelope::plain` の通常配送（`schedules.rs` の `deliver_scheduled`）で、送り手 `Endpoint::System`
は束ねの配送（Spec 43 凍結 6）と検収の再依頼（Spec 46、`schedules.rs:700`）でも使うので、
**送り手では見分けられない**。予算（`budget`）と参加者（`participants`）は根で生まれて因果を
運ばれる前例があり、同じ経路に 1 欄足せる。経路は `handle_message`（`turn.rs:810` で封筒を分解）→
`run_turn`（`:1183`）→ `CallRunner`（`:1921`）→ `run_plan` / `execute_wave` / `deliver_and_wait`。

**検証役は機構ではなく条例の文言だけ**（`{workspace}/Ordinance.md` の「束ねの検証」節 —
「plan で束ねた結果を依頼元へ報告する前に、**束ねに参加していないサーヴァント**へ ask で検証を
頼む」「検証を頼める相手が居ないときは、報告の前に自分で同じ 4 点を確かめる」）。コード・契約・
UI に「検証役」は 0 件。誰に頼むかは毎回進行役のモデルが選び、実機ではフォールバック（自己点検）
だけが観測されている（CLAUDE.md「村の黒板」の節）。

**束ねの組み立ては 1 実装**（`delegation.rs:524` の `execute_wave` — 673〜712 行で `bundle` を
組み、`Some(bundle)` を返す）。ターンの中の `run_plan`（`:299`）と、承認後の `run_dispatched_wave`
（`:727`、凍結 8 の実行形 2 つ目）の両方がこれを通る。**検証を差し込む点はここ 1 箇所で足りる。**
`execute_wave` は今は検証役の引数を持たない。承認後の波の財布は、承認時に切った新しい根の財布
（`mod.rs:1164` の `new_root_budget`）。

**村はプロセスに 1 つ**（`apps/gui-tauri/src-tauri/src/state.rs:70` — workspace は
`{app_data_dir}/workspace` 固定。Spec 44 凍結 5 の「このアプリは 1 プロセス 1 村」と同じ事実）。

**検収待ち（`AcceptancePending`、`schedules.rs:618`）はメモリだけに住む**（`Serialize` を持たない）。
再起動すると検収待ちごと消えるので、「再起動後の再依頼」は存在しない。

## Design

### D1. 予定起点の印 — `Envelope.auto_approve_plans: bool`

- **真を立てられるのはコードの 2 箇所だけ**: `deliver_scheduled` で `task.auto_approve_plans` が
  真のとき / その予定の検収の再依頼 `redeliver_for_acceptance`（`AcceptancePending` に
  `auto_approve_plans: bool` を足し、発火時に写し、再配送の封筒へ積む）。**封筒はコアの内部型で、
  モデルや外部クライアントが組む経路は無い** — 他の配送はすべて受信封筒の値を**写すだけ**
- **引き継ぐ**: `ask` / `plan` / 転送の配送は、受信封筒の値をそのまま積む（予算と同じ経路。
  委譲先のワーカーが進行役で、さらに `plan` を撒く場合も止まらない）
- **新しい根は偽**: 利用者の発話 / `dispatch_plan_wave` / 束ねの配送 / MCP の外部依頼
- **予定の欄**: `ScheduledTask.autoApprovePlans: bool`（`serde(default)`・偽なら省略）。
  `ScheduleOptions` / `ScheduleView` / `scheduleDraft.ts` の往復 / `update_schedule` で保つ
- **運ぶ引数の数**: `run_turn` / `CallRunner` / `run_plan` / `execute_wave` / `deliver_and_wait` に
  `bool` が 1 本ずつ増える。因果の欄（予算・参加者・待ちの連鎖・この印）を 1 つの構造体へ畳むのは
  **本 Spec ではやらない**（差分が移動と混ざる — 巨大ファイル分割の 6 箇条の 3）

### D2. 窓の判定を 1 箇所で拡張する

```rust
spec.plan_review
    && !self.awaiting_reply          // 凍結 10
    && !self.auto_approve_plans      // D1（予定の欄）
    && !shared.plan_review_bypass()  // D3（ステータスバーのスイッチ）
```

- 利用者の裁定「予定で自動で通すになっているとスイッチが OFF でも飛ばす」は、この OR で成立する
- **飛ばしたことを計器に出す**: `plan review skipped: agent=… reason=schedule|bypass`
  （**設定がオンの個体で窓が開かなかったときだけ**。`awaiting_reply` の飛ばしは既存どおり出さない）。
  出さないと「計画の確認をオンにしたのに止まらなかった」がログから読めない
- **両方が真なら `reason=schedule`**（予定の印を優先）。スイッチを戻しても同じ予定は飛ばし続けるので、
  持続する側の理由を出すほうが、後から読んだ人の次の手（予定を直す）に繋がる

### D3. ステータスバーのスイッチ — プロセス寿命で保存しない

- 状態は `Shared` のメモリだけ（`AtomicBool`）。**起動時は必ず OFF。** 1 プロセス 1 村なので、
  スコープは「プロセス全体 = その村」。IPC 2 本（`get_plan_review_bypass` /
  `set_plan_review_bypass`、村の識別子は持たない）と、変更を画面へ伝える
  `CoreEvent::PlanReviewBypassChanged`
- **保存しない理由**: スイッチは人の確認をまとめて外す操作で、**付けっぱなしのまま再起動すると、
  確認が要る村で黙って確認が消え続ける**（MCP の扉を「誰もいないのにつけっぱなしにしない」ために
  ステータスバーへ出したのと同じ懸念）。**再起動をまたいで無人で回したいものは、予定の欄（D1）が担う**
- ステータスバーでは統計アイコンの左に置き、オンの間は発光させる（統計と同じ作法）。
  **ステータスバーの doc の「この帯で唯一の操作」を覆す** — 2 つ目の操作になる

### D4. 検証役の決まり方

| 経路 | 使う検証役 | 解決する時点 |
|---|---|---|
| ターンの中で撒く `plan`（窓なし・窓を飛ばした場合を含む） | 村の既定（`world.json` の `defaultVerifier`） | `run_plan` が撒く時点で `World` から読む（「検証は今」の規律） |
| 計画の確認で承認した波 | **パネルで選んだもの**（初期選択は村の既定。「なし」を選べる） | `dispatch_plan_wave` の引数 |

- `World.default_verifier: Option<AgentId>`（`None` = なし）。保存は既存の `set_ask_timeout` と
  同じ形（IPC 2 本・`world.json` へ書き戻し・次の波から効く）。指定した個体が削除されたら
  読むときに `None` 扱い（`reception` と同じく掃除はしない — 設定し直すのと初めて設定するのとで
  次の手が同じなので、ここは画面で「（削除済み）」と出す）
- `dispatch_plan_wave(planId, tasks, verifier?)` — **凍結 4 の IPC の形を覆す**（3 つ目の引数）。
  **3 つ目は省略可で、省略は `None`（なし）と解釈する**（Tauri のコマンド引数の `Option` の既定）。
  TS 側は `verifier?: AgentId | null`。提示時の値は持たず、押したときの値が真実（凍結 4 と同じ規律）
- `execute_wave` に `verifier: Option<AgentId>` を足す。`run_plan` は村の既定を、
  `run_dispatched_wave` は承認時の引数を渡す（**`execute_wave` の中では解決しない** — 解決の場所が
  2 つあると、窓を通った波と通らない波で既定の読み方がずれる）
- 検証役として選べるのは登録済みのサーヴァント。**絆（接続）は要らない** — 人が明示に選んだ宛先で、
  予定の `to` と同じ扱い。これは「線は人が引く」の例外として `plan_verifier_contract` に明記する
  （**選ぶのが人である**ことで原則の意図は保つ）
- **設定の置き場は作業状況タブの見出し**（「検証役: [選択]」）。検証役は村の内容物で
  （#52 の境界 — 保存先ではなく「村の内容物か、アプリの設定か」）、計画を見る場所の隣にある

### D5. 検証の機構 — 束ねの後・返す前に、システムが検証役へ頼む

**検証役が `None` なら何もしない** — 検証の処理に入らず、束ねには見出しも理由も一切足さない
（既存とバイト等価。検収 7）。以下は検証役が指定されているときだけ:

1. **システムが検証役へ `deliver_and_wait` で頼む**（`execute_wave` の `Some(bundle)` の手前）。
   本文 = 各タスクの依頼文 + 束ね + 見る 4 点（条例と同じ: 空の答え / 依頼の取り違え /
   答え同士の矛盾 / 欠けている論点）+ 結論の 3 択（このまま使える / この相手にだけ聞き直す /
   依頼元に確認する）
2. **検証役の答えを束ねの末尾に付ける**: `## 検証: agent_x（表示名）` の見出しで 1 節
3. **検証役が指定されているのに検証しなかった場合は、理由を束ねに 1 行書く**（黙って飛ばさない — #44）:
   検証役が波の宛先に含まれる（自己採点しない）/ 検証役が進行役自身 / 停止中 /
   待ちの輪（Spec 44 の拒否）/ 時間切れ / 予算切れ。**どれも束ねは返す**（検証の失敗で仕事の
   結果を捨てない）
4. **波が打ち切られた場合は検証しない**（束ねを作らない既存の経路のまま）

- **財布**: 検証役の配送は、その波を配った財布を使う。ターンの中の `run_plan` = 進行役の因果の財布、
  承認後の波 = 承認時に切った根の財布（`mod.rs:1164`）。**波のワーカーで尽きていたら 3 の
  「予算切れ」**（`deliver_and_wait` の事前検査がそのまま返す）
- **待ちの連鎖**: ターンの中では進行役を積む（検証役が進行役へ `ask` し返すと Spec 44 で拒否される）。
  承認後の波は根なので空から始まる
- **本文の区切り**: 依頼文と各答えは既存の束ねと同じ `## agent_id（表示名）` の見出しで分け、
  `---` は使わない（答えは Markdown で `---` が普通に現れる。Spec 17 で diff の見出しと衝突した前例）。
  答えの中に封筒構文があれば、配送の `wrap` が既存どおり無害化する（Spec 26）
- **LLM に「検証するか」を選ばせない**（凍結 1 以来の規律 — 選べる形にすると #84 で飛ばされる）
- **計器**: `plan verify: agent=… plan_id=… verifier=… outcome=ok|skipped:<理由> chars=… elapsed_ms=…`
  （検証役が `None` の波では出さない）

## Tasks

- **P0 契約**: `data_contract.yaml` の `plan_edit_window` に凍結 11（窓を飛ばす 2 条件・優先・計器。
  既存の番号つき文章の形で書く）/ 凍結 4 の IPC 形の改訂（3 つ目は省略可）/ 新ブロック
  `plan_verifier_contract`（D4・D5・絆の例外・`None` はバイト等価）/ `ScheduledTask` の欄 /
  `settings_contract` にスイッチの置き場（メモリのみ・プロセス全体）と `defaultVerifier`
- **P1 コア（窓）**: `Envelope.auto_approve_plans` と引き継ぎ（`AcceptancePending` を含む）/
  予定の欄 / `Shared` のスイッチ / 判定の拡張と計器。結合テスト: 予定起点で窓が開かない /
  **利用者起点では開く**（負の対照）/ スイッチで開かない / 両方真で `reason=schedule` /
  委譲先の進行役にも印が届く / 再依頼にも届く / **利用者・MCP・dispatch の根では偽**
- **P2 コア（検証役）**: `World.default_verifier` / `execute_wave` の引数と検証 /
  `dispatch_plan_wave` の引数 / `None` でバイト等価 / スキップ理由 6 つの結合テスト
  （ミューテーションで赤を確認）
- **P3 IPC と画面**: IPC（スイッチ 2 本・既定の検証役 2 本・dispatch の引数）/ `CoreEvent` /
  ステータスバーのスイッチ / 予定のダイアログのチェック / 計画の確認パネルの検証役の選択 /
  作業状況タブ見出しの既定の検証役 / 辞書 ja・en
- **P4 台帳**: DETAIL 日英（計画の確認・予定・ステータスバー）/ CLAUDE.md / README 3 言語の該当行 /
  **条例の「束ねの検証」節の改訂案を利用者へ渡す**（Notes 3）
- **P5 実機**

## P1 実装記録（2026-09-15）

- **印の運び方を D1 から 1 点変えた — `run_turn` / `run_turn_inner` の署名には足さず、既存の
  `HandoffGates` に 1 欄足した。** `HandoffGates` は `awaiting_reply`（凍結 10 の材料）を運ぶ構造体で、
  `handle_message` が封筒を分解した直後に組み、`run_turn_inner` で分解して `CallRunner` へ入れる。
  凍結 10 と 11 の材料が同じ器に並ぶ。`present_tools` と `build_prompt` は `..` で受ける
  （**プロンプトには印を出さない** — モデルに「窓が無い」を知らせる理由が無い）
- **引数が増えた関数**: `deliver` / `Envelope::plain` / `deliver_and_wait` / `ask_agent` / `run_plan` /
  `execute_wave` / `dispatch_outcome`。**真を立てるのは 2 箇所だけ**（`deliver_scheduled` の
  `task.auto_approve_plans` / `redeliver_for_acceptance` の `state.auto_approve_plans`）で、
  `false` を明示する根は 4 箇所（利用者の発話 `runtime.rs` / 外部依頼 `runtime.rs` /
  承認後の波 `run_dispatched_wave` / 束ねの配送）。他は受信封筒の値を写すだけ
- **窓の判定は `CallRunner::opens_plan_review` の 1 メソッド**（凍結 10 と 11 を 1 箇所に集めた。
  旧 `spec.plan_review && !self.awaiting_reply` の式はこのメソッドへ移動）。計器
  `plan review skipped: agent=… reason=schedule|bypass` もここで出す
- **スイッチ**: `Shared.plan_review_bypass: AtomicBool`（`bootstrap` で `false`）と
  `Orchestrator::plan_review_bypass()` / `set_plan_review_bypass(bool)`（`persist` を呼ばない）。
  **IPC と `CoreEvent::PlanReviewBypassChanged` は P3**
- **欄**: `ScheduledTask.auto_approve_plans`（`serde(default, skip_serializing_if = "is_false")`）/
  `ScheduleOptions.auto_approve_plans` / `AcceptancePending.auto_approve_plans`。
  リテラルの追従は `schedule.rs` 3・`schedules.rs` 2・`probe_approvals.rs` 1・テスト 4。
  **GUI 層の `ScheduleView`（`commands.rs:958`）への写しは P3**
- **結合テスト `tests/unattended_plan.rs`（5 本・一発で緑）**: 理由の計器と優先（ログを読む唯一の 1 本 —
  予定の印とスイッチが両方真で `reason=schedule`、利用者の依頼でスイッチだけ真で `reason=bypass`、
  `plan pending:` は出ない）/ 印の無い予定と利用者の依頼では窓が開く（負の対照）/ スイッチは既定 OFF・
  ON で開かず OFF に戻すと開く / 予定 → 中継役の転送 → 進行役でも開かない（印の無い予定の対照つき）/
  検収の再依頼でも開かない（常に不一致の検収・総試行 2 回で波が 2 つとも実行）
- **ミューテーション 4 回とも予測どおりの赤**: 判定で予定の印を無視 → 転送と再依頼の 2 本 /
  再依頼の印を `false` → 再依頼の 1 本 / 転送の印を `false` → 転送の 1 本 / 理由の優先を逆 → ログの 1 本。
  **テストが 1 本ずつ別の経路を守っている**ことが分かる（予測を先に書いてから撃った）
- **ワークスペース全件 953 本が緑・clippy 警告 0**。**1 回目の全件は出力が空だった** — バックグラウンドで
  `grep` を通した出力に `test result` が 1 行も無く、終了コード 0 は末尾の `echo` のものだった。
  ログファイルへ書き出して流し直して 40 スイートを確認した（原因は特定していない。**終了コードは
  パイプの最後のコマンドのもので、テストの成否を語らない**）
- **確かめていないこと**: 外部依頼（MCP）の根で印が偽になること — 外部依頼は必ず答えを待つ委譲なので、
  凍結 10 で窓がそもそも開かず、観測で区別できない。承認後の波・束ねの配送で偽になることも同じく
  窓の開閉では観測できない（コードで `false` を明示している箇所の列挙で担保）

## P2 実装記録（2026-09-15）

- **欄と入口**: `World.default_verifier` / `PersistedWorld.defaultVerifier`（`serde(default, skip_serializing_if)`）。
  **読み出し `World::default_verifier()` は指す個体が削除されていれば `None`** を返す（値は掃除しない —
  凍結 6）。`set_default_verifier` は未登録を `AgentNotFound` で拒み、拒否したときは何も変えない
  （`set_reception` と同じ形）。`Orchestrator::default_verifier` / `set_default_verifier`（`persist` する）。
  `dispatch_plan_wave(plan_id, tasks, verifier: Option<AgentId>)` は検証役の登録を確かめてから
  `run_dispatched_wave` へ渡す。GUI 層の Tauri コマンドにも省略可の引数を足した（**TS 側は P3**）
- **検証は `execute_wave` の束ねの後の 1 箇所**（`verify_bundle`）。`run_plan` は撒く時点で村の既定を読み、
  `run_dispatched_wave` は承認時の引数を渡す（凍結 2 — 解決は呼び手）。**検証役が `None` なら
  `verify_bundle` を呼ばない**ので、束ねも計器もバイト等価。`plan bundle:` の字数は検証の前の束ね
- **契約を 3 点訂正した**（`plan_verifier_contract`。D5 の本文は起票時の記述のまま残す）:
  1. **送り手は進行役**（凍結 3）。D5 は「システムが頼む」とだけ書き送り手を決めていなかった。
     波のタスクと同じ送り手にしたので、待ちの連鎖の規則も会話ペインの見え方も波と揃う。
     本文に「この依頼はシステムが自動で送っています」と書き、検証役が進行役の頼みと読まないようにした
  2. **理由は閉じた 7 つ**（凍結 4）。起票時の 6 つに「答えを返さなかった」を足した — 検証役が転送した
     （`HandedOff`）・答えずに終えた（`NoAnswer`）が 6 つのどれにも当たらなかった。
     結末 → 理由の写しは純関数 `verification_of` の 1 実装
  3. **承認後の波でも待ちの連鎖に進行役が入る**（凍結 5）。D5 の「承認後の波は根なので空から始まる」は
     誤り — 波のタスクの `deliver_and_wait` が既に進行役を積んでおり、検証だけ規則を割る理由が無い
- **待ちは打ち切りと競わせる** — `deliver_and_wait` 自身は時間切れしか見ないので、`parent.cancelled()` と
  `select!` した（波の待ちと同じ）。打ち切られたら `None` を返し、束ねを作らない既存の経路へ合流する
- **検収 6 の文言と実物の差**: 実物は「（検証役「X」が束ねに参加したため、検証していません）」。
  理由 7 つを「〜ため、検証していません」の 1 つの枠へ揃えたので、検収 6 の文字列そのままではない（P5 で
  読むときはこの形で読む）
- **結合テスト `tests/plan_verifier.rs`（5 本・一発で緑）**: `None` で束ねがバイト等価・検証役へ何も届かない /
  既定の検証役の結論が末尾に付き計器が出る（ログを読む唯一の 1 本）/ 承認時の選択が真実（既定が居ても
  「なし」で承認すれば付かず、指名すれば付く）/ 理由 4 つ（参加者・進行役自身・停止中・待ちの輪）/
  削除した検証役は「なし」に読め、未登録の指定は拒否される。**時間切れ・予算切れ・無応答・打ち切りは
  単体テスト**（`verification_of_maps_every_task_state` — 結合で作ると時間切れだけで 30 秒以上待つ）。
  既存 `plan_edit_window.rs` の `dispatch_plan_wave` 8 箇所に `None` を足した（8 本とも緑）
- **ミューテーション 6 回とも予測どおりの赤**: 指定されていても検証しない → 既定の結論・承認時の選択・
  理由の 3 本 / ターンの中で既定を読まない → 既定の結論・理由の 2 本（承認経路は緑）/ 承認時の「なし」を
  既定へ落とす → 承認時の選択の 1 本 / 参加者の判定を外す → 理由の 1 本 / 削除済みを「なし」に読まない →
  削除の 1 本 / 打ち切りを「答えなし」へ写す → 単体の 1 本
- **ワークスペース全件 960 本が緑（41 スイート）・clippy 警告 0**（どちらもログファイルへ書き出して数えた — P1 の教訓）

## P3 実装記録（2026-09-15）

- **IPC 4 本**（`get_plan_review_bypass` / `set_plan_review_bypass` / `get_default_verifier` /
  `set_default_verifier`）と `dispatch_plan_wave` の `verifier` 引数（TS は `verifier?: AgentId | null`、
  省略は `null` で送る）。**スイッチの変化は `CoreEvent::PlanReviewBypassChanged { on }`** —
  `Orchestrator::set_plan_review_bypass` が発行する。ワイヤ形は `ipc_contract.rs` の
  `plan_review_bypass_event_wire_is_frozen` で凍結（`{"type":"planReviewBypassChanged","on":true}`）
- **投影**: `useOrchestrator` の `planReviewBypass` / `defaultVerifier`。**起動時に両方を読む** —
  起動直後ならスイッチは必ず OFF だが、**画面の再読み込みではコアが生きていて ON のまま戻る**。
  以後スイッチはイベントで追う。操作は `setPlanReviewBypass` / `setDefaultVerifier`（成功したら投影を
  先に書く）と `dispatchPlanWave(planId, tasks, verifier = null)`
- **画面 4 箇所**:
  - **ステータスバー**: 統計の左にスイッチ。オンの間は注意色で発光し「確認なし」の字も出す
    （色だけに頼らない・`aria-pressed` も出す）。doc の「この帯で唯一の操作」を「操作は 2 つ」へ直した
  - **予定のダイアログ**: 「計画の確認を自動で通す」のチェック（要約のチェックの下）。一覧のバッジは
    付けていない — **既存の要約・新規会話のバッジの鍵（`summarizeBadge` / `freshBadge`）はどの画面からも
    使われていない死んだ鍵**だったので、同じ形の鍵を足さなかった
  - **作業状況タブの見出し**: 「検証役: [なし / 全サーヴァント]」。消しゴムの `ml-auto` をこの選択へ移した
  - **確認待ちパネル**: 「破棄」の左に検証役の選択。**初期値は村の既定で、提案が切り替わるたびに既定へ
    戻す**（前の提案の選択を持ち越さない）。承認で渡すのはこの選択
- **D4 と違えた点**: 「指定した個体が削除されたら画面で（削除済み）と出す」は**出せない**。コアの
  `default_verifier` が削除済みを `None` で返すので、画面は保存値を知らない（区別する欄をワイヤに
  持たない判断。画面は一覧に居ない個体も「なし」として扱う）
- **テスト**: `useOrchestrator.planReview.test.ts`（3 本 — 起動時の読みとイベントの追従 / 操作で投影が
  変わる / 承認時の検証役を押したときの値で渡し、省略は `null`）/ `scheduleDraft.test.ts` の往復に
  `autoApprovePlans` / 起動テストのモックに IPC 2 本。`bun run build`（vue-tsc）緑
- **ミューテーション 3 つ**: イベントの受け口を消す → 1 本赤 / 変換で `autoApprovePlans` を落とす →
  往復の 2 本赤 / **承認時の「なし」に村の既定を差し込む → 2 回続けて緑のまま通った**。
  **穴は別々に 2 つ**あった — (1) 状態はモジュールで 1 つ・`init` は 2 回目以降は何もしないので、前の
  テストが既定を `null` にしていて差し込みが `null` のままだった (2) それを直そうとテストで
  `orchestrator.state.defaultVerifier = "agent_v"` と代入したが、**公開される状態は `readonly(state)` で、
  代入は黙って捨てられた**。操作（`setDefaultVerifier`）で置く形に直して赤を確認した。
  **一般化: 変異が緑のとき、まずテストの前提（その値が本当にその状態になっているか）を確かめる**
- **Rust 全件の 1 回目はテストの前にビルドが落ちた**（`only metadata stub found for rlib dependency core` /
  `can't find crate for std`）。流し直すと 41 スイート 961 本が緑。直前の `cargo check`（rmeta だけを作る）
  の成果物との食い違いと見ているが、**原因は特定していない**。clippy 警告 0
- **実機は未確認**（P5）

## P4 台帳記録（2026-09-15）

- **DETAIL 日英**: 画面の構成表とディレクトリ木の `StatusBar.vue` 行にスイッチ /「計画の確認」の節へ
  窓を飛ばす 2 経路と計器 / 新節「束ねの検証役」（置き場・4 点・波ごとの選び直し・絆の例外・
  理由 7 つ・「なし」でバイト等価・費用・条例との二重化・削除時の扱い）/ 予定の節へ
  「計画の確認を自動で通す」
- **README 3 言語**: 「委譲と合流」の行へ 1 文ずつ（行数は増やしていない）
- **CLAUDE.md**: 「Spec の状態」の先頭と「現在地（2026-09-14 更新）」の 2026-09-15 追加
- **追従の grep**: `dispatch_plan_wave` / `defaultVerifier` / `autoApprovePlans` / 「唯一の操作」を
  台帳 7 ファイル（CLAUDE / DETAIL 日英 / README 3 言語 / failures）で数えた。**台帳には 1 件も無かった**
  （DETAIL の「唯一の操作」は会話の切れ目の話で別件）。コードの doc の「唯一の操作」は P3 で直し済み
- **grep 網の外**: ランディングページは計画の確認に触れておらず嘘にならない。Qiita 記事も同じ
- **条例の改訂案を利用者へ渡した**（Notes 3）。村の条例は `{workspace}/Ordinance.md` の
  「束ねの検証（依頼を配った者の手順）」節（実物を読んで案を作った）。適用するかは P5 の前に利用者が決める

## 検収

1. 「自動で通す」予定から始まった作業で、進行役（計画の確認オン）が止まらず撒く。
   `plan review skipped: … reason=schedule` が出て、`plan pending:` が出ない
2. 同じ進行役に利用者が頼むと、今までどおり止まる（負の対照）
3. スイッチをオンにすると利用者の依頼でも止まらない（`reason=bypass`）。アプリを再起動すると OFF に戻る
4. 既定の検証役を指定した村で、束ねの末尾に `## 検証:` の節が付き、`plan verify: outcome=ok` が出る
5. 計画の確認パネルで「なし」を選んで実行すると、検証の節も理由の行も付かない
6. 検証役を波の宛先に含めると、束ねに「検証役が束ねに参加したため検証していません」が出る
7. 3 つの設定を既定のままにした村で、`plan` の束ねの本文と計器が変わらない

## Notes

1. **決めどころ 3 点**（査読 2 系統とも草案の推しを支持。利用者の承認待ち）: D3 スイッチを
   保存しない / D4 既定の検証役は作業状況タブの見出し / Notes 3 条例は Spec で書き換えず改訂案を渡す
2. **凍結 8（ターンの外の実行形は 2 つまで）は覆さない。** 検証は既存の実行形（`run_plan` と
   `run_dispatched_wave`）の中の配送で、新しい実行形は作らない。検証役のターンは普通のターンとして
   `TurnSpend` で精算される
3. **条例との二重化**: 既定の検証役を入れた村で条例の「束ねの検証」節が残っていると、進行役が
   さらに別の個体へ検証を頼む（二重の検証・二重の払い）。条例は利用者の資産なので Spec では
   書き換えない。**P4 で改訂案（節の削除、または「検証役が指定されている場合は機構に委ねる」への
   緩和）を渡し、P5 の前に利用者が適用するかを決める**
4. **費用**: 検証役を指定すると `plan` 1 回ごとに検証役のターンが 1 本増え、束ねの全文が検証役の
   入力になる（束ねの大きさは実測で 244〜4,201 字 — CLAUDE.md の Spec 04 の節）
5. **rev1 査読の記録（2 系統・16 点）**

   | # | 指摘 | 判定 | 反映 |
   |---|---|---|---|
   | 1-1 | `reason` が両方真のとき未定義 | 採用 | D2 に「両方真なら schedule」と理由 |
   | 1-2 | `AcceptancePending` に欄が無いと再起動後に印が消える | **前提を訂正して採用** | 構造体はメモリだけで再起動後の再依頼は存在しない（`serde(default)` は不要）。欄を足すこと自体は必要なので D1 に明記 |
   | 1-3 | 誰でも印を立てられると窓を飛ばせる | **前提を訂正して採用** | 封筒はコアの内部型でモデルが組む経路は無い。「真を立てるのはコード 2 箇所・他は写すだけ」を D1 に明記し、負のテストは「利用者・MCP・dispatch の根で偽」に置き換え |
   | 1-4 | 旧クライアントが 2 引数で呼ぶと落ちる | **前提を訂正して採用** | 画面とコアは同じバイナリで古いタブは無い。ただし 3 つ目を省略可（`None`）にするのは費用ゼロなので D4 に明記 |
   | 1-5 | 絆不要は原則の例外として明記を | 採用 | `plan_verifier_contract` に明記（`settings_contract` ではない — 原則は設定の契約に書かれていない） |
   | 1-6 | スイッチは村ごとに・IPC に `world_id` を | **反証** | 1 プロセス 1 村（`state.rs:70` の workspace 固定・Spec 44 凍結 5）。スコープの書き方だけ 2-1 に揃えた |
   | 1-補1 | 判定の場所で封筒が手元にあるか | 採用 | 経路（`handle_message` → `run_turn` → `CallRunner`）を実測の節と D1 に書いた |
   | 1-補2 | 検証の本文の区切りを `---` で固定 | **反証** | 答えは Markdown で `---` が普通に出る（Spec 17 の前例）。既存の見出しと Spec 26 の無害化で受ける（D5） |
   | 1-補3 | P1 に「利用者起点で開く」正例が抜けている | **反証** | rev1 の P1 に在った（「利用者起点では開く」）。rev2 で負の対照と明記 |
   | 1-P0 | 凍結 11 を `{bypass_conditions: …}` の形で | 訂正して採用 | 契約は番号つきの文章で書いているので既存の形に揃える（中身は同じ） |
   | 1-P4 | 条例の改訂案の文面 | 採用 | P4 の成果物に入れた（Notes 3） |
   | 2-1 | スイッチのスコープが「村全体」と「プロセス全体」で割れている | 採用 | 「プロセス全体 = その村（1 プロセス 1 村）」に統一 |
   | 2-2 | 窓なし・飛ばした場合の検証役の伝達経路が無い | 採用 | `execute_wave` に引数を足し、解決は呼び手 2 箇所で行う（D4 の表） |
   | 2-3 | 検証役 `None` と「検証スキップ」の境界が曖昧 | 採用 | D5 の冒頭に「`None` なら何も足さない」、3 は「指定されているのに」に限定 |
   | 2-4 | 承認後の波で検証役の財布が曖昧 | 採用 | 承認時に切った根の財布（`mod.rs:1164`）と明記、尽きていたら「予算切れ」 |
   | 決定 3 点 | スイッチ保存 / 置き場 / 条例 | 両系統とも推しを支持 | Notes 1 |
